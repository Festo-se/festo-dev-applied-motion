{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Festo Applied Motion","text":"<p><code>festo-dev-applied-motion</code> is a Python library for controlling Festo electric motion systems with a consistent gantry API.</p> <p>It supports two production backends:</p> Backend Best for Transport Modbus / festo-edcon (<code>EdconAxis</code>) Direct per-drive control (CMMT/CMMT-ST) Modbus TCP per axis FPosBAPI (<code>FPosBAxis</code>) CECC-X PLC controlled multi-axis gantries Single shared TCP socket"},{"location":"#install","title":"Install","text":"<pre><code>uv add festo-dev-applied-motion\n</code></pre> <p>Or from source:</p> <pre><code>git clone https://github.com/Festo-se/festo-dev-applied-motion.git\ncd festo-dev-applied-motion\nuv pip install -e .\n</code></pre>"},{"location":"#quick-links","title":"Quick links","text":"<ul> <li>Getting Started</li> <li>FPosBAPI User Guide</li> <li>festo-edcon User Guide</li> <li>FPosBAPI Examples</li> <li>festo-edcon Examples</li> <li>API reference pages (auto-generated during docs build; available in left navigation)</li> </ul>"},{"location":"#notes","title":"Notes","text":"<ul> <li>Position units are millimetres (mm) throughout the public API.</li> <li><code>Gantry.from_config(...)</code> is convenience wrapper over <code>Gantry(config=...)</code>.</li> </ul>"},{"location":"#festo-resources","title":"Festo resources","text":"<ul> <li>Repository</li> <li>Issues Tracker</li> </ul>"},{"location":"autoapi/summary/","title":"Summary","text":"<ul> <li>applied_motion<ul> <li>applied_motion</li> <li>backends<ul> <li>axis_protocol</li> <li>edcon_axis</li> <li>fposbapi_axis</li> <li>fposbapi_client</li> <li>gantry_backend</li> </ul> </li> <li>cli<ul> <li>cli</li> <li>compose<ul> <li>core</li> <li>repl</li> </ul> </li> <li>formatting</li> <li>logging_utils</li> <li>session</li> <li>theme</li> </ul> </li> <li>config</li> <li>gantry_factory</li> </ul> </li> </ul>"},{"location":"autoapi/applied_motion/","title":"applied_motion","text":""},{"location":"autoapi/applied_motion/#applied_motion","title":"applied_motion","text":""},{"location":"autoapi/applied_motion/#applied_motion.Gantry","title":"Gantry","text":"<pre><code>Gantry(\n    axes: AxisMap | None = None,\n    concurrent_axes: AxisMap | None = None,\n    *,\n    config: dict | str | Path | None = None,\n    name: str = \"gantry_1\",\n    _backend: GantryBackend | None = None,\n)\n</code></pre> <p>Coordinate multiple axis objects as a single gantry.</p> <p>Dispatches sequential or concurrent move commands to the individual axes and provides convenience methods for homing, status queries, and position readback.</p> <p>Attributes:</p> Name Type Description <code>axes</code> <p>Mapping of axis name \u2192 axis instance for all registered axes.</p> <code>concurrent_axes</code> <p>Optional mapping of axes that may move simultaneously. When <code>None</code>, no concurrent grouping is applied.</p> <p>Initialise the gantry with the provided axis mapping.</p> <p>When <code>config</code> is provided, this constructor selects the correct backend (Modbus or FPosBAPI) and creates axes automatically from a JSON configuration dict or file.</p> <p>Parameters:</p> Name Type Description Default <code>axes</code> <code>AxisMap | None</code> <p>Dict mapping axis names to axis instances.  Accepts both <code>EdconAxis</code> (Modbus backend) and <code>FPosBAxis</code> (FPosBAPI backend) \u2014 any object satisfying <code>Axis</code>.</p> <code>None</code> <code>concurrent_axes</code> <code>AxisMap | None</code> <p>Optional dict of axes that are allowed to move simultaneously.  Pass <code>None</code> (default) to disable concurrent grouping.</p> <code>None</code> <code>config</code> <code>dict | str | Path | None</code> <p>Optional configuration dict or JSON file path. When provided, the gantry builds axes and backend from config and ignores <code>axes</code> / <code>concurrent_axes</code>.</p> <code>None</code> <code>name</code> <code>str</code> <p>Gantry component name to load from <code>config</code>.</p> <code>'gantry_1'</code> <code>_backend</code> <code>GantryBackend | None</code> <p>Internal backend strategy object that owns backend- specific gantry behavior.</p> <code>None</code> Source code in <code>src/applied_motion/applied_motion.py</code> <pre><code>def __init__(\n    self,\n    axes: AxisMap | None = None,\n    concurrent_axes: AxisMap | None = None,\n    *,\n    config: dict | str | Path | None = None,\n    name: str = \"gantry_1\",\n    _backend: GantryBackend | None = None,\n) -&gt; None:\n    \"\"\"Initialise the gantry with the provided axis mapping.\n\n    When ``config`` is provided, this constructor selects the correct\n    backend (Modbus or FPosBAPI) and creates axes automatically from a JSON\n    configuration dict or file.\n\n    Args:\n        axes: Dict mapping axis names to axis instances.  Accepts both\n            [`EdconAxis`][applied_motion.backends.edcon_axis.EdconAxis] (Modbus backend) and\n            [`FPosBAxis`][applied_motion.backends.fposbapi_axis.FPosBAxis]\n            (FPosBAPI backend) \u2014 any object satisfying\n            [`Axis`][applied_motion.backends.axis_protocol.Axis].\n        concurrent_axes: Optional dict of axes that are allowed to move\n            simultaneously.  Pass ``None`` (default) to disable concurrent\n            grouping.\n        config: Optional configuration dict or JSON file path. When\n            provided, the gantry builds axes and backend from config and\n            ignores ``axes`` / ``concurrent_axes``.\n        name: Gantry component name to load from ``config``.\n        _backend: Internal backend strategy object that owns backend-\n            specific gantry behavior.\n    \"\"\"\n    if config is not None:\n        if axes is not None or concurrent_axes is not None or _backend is not None:\n            raise ValueError(\"Pass either config or axes/concurrent_axes, not both\")\n        build = build_gantry_from_config(config, name)\n        axes = build.axes\n        concurrent_axes = build.concurrent_axes\n        _backend = build.backend\n\n    if axes is None:\n        raise ValueError(\"axes must be provided when config is not supplied\")\n\n    self.axes = axes\n    self.concurrent_axes = concurrent_axes\n    self._backend = _backend\n    logger.info(\"Gantry: initialized with axes=%s\", list(axes.keys()))\n    if concurrent_axes:\n        logger.debug(\"Gantry: concurrent axes=%s\", list(concurrent_axes.keys()))\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.Gantry.from_config","title":"from_config  <code>classmethod</code>","text":"<pre><code>from_config(\n    config: dict | str | Path, name: str = \"gantry_1\"\n) -&gt; Gantry\n</code></pre> <p>Instantiate a <code>Gantry</code> from a configuration dict or JSON file.</p> <p>Parameters:</p> Name Type Description Default <code>config</code> <code>dict | str | Path</code> <p>Parsed configuration mapping or JSON file path.</p> required <code>name</code> <code>str</code> <p>Component name to load from config.</p> <code>'gantry_1'</code> <p>Returns:</p> Type Description <code>Gantry</code> <p>Initialised <code>Gantry</code> instance.</p> Source code in <code>src/applied_motion/applied_motion.py</code> <pre><code>@classmethod\ndef from_config(cls, config: dict | str | Path, name: str = \"gantry_1\") -&gt; \"Gantry\":\n    \"\"\"Instantiate a [`Gantry`][applied_motion.applied_motion.Gantry] from a configuration dict or JSON file.\n\n    Args:\n        config: Parsed configuration mapping or JSON file path.\n        name: Component name to load from config.\n\n    Returns:\n        Initialised [`Gantry`][applied_motion.applied_motion.Gantry] instance.\n    \"\"\"\n    return cls(config=config, name=name)\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.Gantry.move_to","title":"move_to","text":"<pre><code>move_to(\n    movements: MovementBatch,\n    timeout: int | None = None,\n    concurrent: bool = False,\n) -&gt; None\n</code></pre> <p>Dispatch a queue of movements to the gantry axes.</p> <p>Processes each movement dict in movements, dispatching them either sequentially or concurrently according to concurrent and the gantry's <code>concurrent_axes</code> configuration.</p> <p>Parameters:</p> Name Type Description Default <code>movements</code> <code>MovementBatch</code> <p>A <code>deque</code> of movement dicts.  Each dict maps a single axis name to its kinematic parameters, e.g. <code>{\"X\": {\"position\": 100.0, \"velocity\": 50.0}}</code>.</p> required <code>timeout</code> <code>int | None</code> <p>Optional per-move time limit in seconds.  Passed through to each <code>EdconAxis.move</code> call.</p> <code>None</code> <code>concurrent</code> <code>bool</code> <p>When <code>True</code>, all movements in the current batch are dispatched in parallel threads.  When <code>False</code> (default), movements are grouped using <code>concurrent_axes</code> and dispatched sequentially.</p> <code>False</code> Notes <p>Axis references are validated before dispatch. Malformed movement entries or entries for unknown axes are logged and skipped.</p> Source code in <code>src/applied_motion/applied_motion.py</code> <pre><code>def move_to(\n    self,\n    movements: MovementBatch,\n    timeout: int | None = None,\n    concurrent: bool = False,\n) -&gt; None:\n    \"\"\"Dispatch a queue of movements to the gantry axes.\n\n    Processes each movement dict in *movements*, dispatching them either\n    sequentially or concurrently according to *concurrent* and the\n    gantry's ``concurrent_axes`` configuration.\n\n    Args:\n        movements: A `deque` of movement dicts.  Each\n            dict maps a single axis name to its kinematic parameters, e.g.\n            ``{\"X\": {\"position\": 100.0, \"velocity\": 50.0}}``.\n        timeout: Optional per-move time limit in seconds.  Passed through\n            to each [`EdconAxis.move`][applied_motion.backends.edcon_axis.EdconAxis.move] call.\n        concurrent: When ``True``, all movements in the current batch are\n            dispatched in parallel threads.  When ``False`` (default),\n            movements are grouped using ``concurrent_axes`` and dispatched\n            sequentially.\n\n    Notes:\n        Axis references are validated before dispatch. Malformed movement\n        entries or entries for unknown axes are logged and skipped.\n    \"\"\"\n    logger.info(\"Gantry.move_to: queued=%d concurrent=%s timeout=%s\", len(movements), concurrent, timeout)\n\n    valid_movements, validated_axis_names = self._collect_valid_movements(movements)\n    if not valid_movements:\n        logger.warning(\"Gantry.move_to: no valid movement entries to dispatch\")\n        return\n\n    skipped_count = len(movements) - len(valid_movements)\n    if skipped_count &gt; 0:\n        logger.warning(\"Gantry.move_to: skipped %d invalid movement entrie(s)\", skipped_count)\n\n    if concurrent:\n        results = self._move_dispatch(valid_movements, concurrent=concurrent, timeout=timeout)\n        self._log_move_results(validated_axis_names, results, concurrent=True)\n        return\n    else:\n        concurrent_axes = self.concurrent_axes or {}\n        while valid_movements:\n            next_moves = self._get_next_moves(valid_movements, concurrent_axes)\n            axis_names = tuple(next(iter(movement)) for movement in next_moves)\n            results = self._move_dispatch(next_moves, concurrent=True, timeout=timeout)\n            self._log_move_results(axis_names, results, concurrent=False)\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.Gantry.home","title":"home","text":"<pre><code>home() -&gt; None\n</code></pre> <p>Home all registered axes.</p> <p>For the FPosBAPI backend, sends a single <code>HOME</code> command to the CECC-X PLC which homes all axes in a coordinated sequence.</p> <p>For the Modbus backend, iterates over every axis in insertion order and calls <code>Axis.home</code> on each one sequentially.</p> Source code in <code>src/applied_motion/applied_motion.py</code> <pre><code>def home(self) -&gt; None:\n    \"\"\"Home all registered axes.\n\n    For the **FPosBAPI backend**, sends a single ``HOME`` command to the\n    CECC-X PLC which homes all axes in a coordinated sequence.\n\n    For the **Modbus backend**, iterates over every axis in insertion\n    order and calls [`Axis.home`][applied_motion.backends.axis_protocol.Axis.home] on each one sequentially.\n    \"\"\"\n    if self._backend is None:\n        for axis in self.axes.values():\n            axis.home()\n    else:\n        self._backend.home(self.axes)\n    logger.info(\"Gantry.home: complete\")\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.Gantry.close","title":"close","text":"<pre><code>close() -&gt; None\n</code></pre> <p>Close backend-owned resources.</p> <p>Safe to call multiple times.</p> Source code in <code>src/applied_motion/applied_motion.py</code> <pre><code>def close(self) -&gt; None:\n    \"\"\"Close backend-owned resources.\n\n    Safe to call multiple times.\n    \"\"\"\n    if self._backend is not None:\n        self._backend.close()\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.Gantry.supports_teach","title":"supports_teach","text":"<pre><code>supports_teach() -&gt; bool\n</code></pre> <p>Return whether PLC teaching commands are supported by this backend.</p> Source code in <code>src/applied_motion/applied_motion.py</code> <pre><code>def supports_teach(self) -&gt; bool:\n    \"\"\"Return whether PLC teaching commands are supported by this backend.\"\"\"\n    if self._backend is None:\n        return False\n    return self._backend.supports_teach()\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.Gantry.teach_pos","title":"teach_pos","text":"<pre><code>teach_pos(pos_id: int) -&gt; None\n</code></pre> <p>Teach current location into PLC position slot.</p> <p>Parameters:</p> Name Type Description Default <code>pos_id</code> <code>int</code> <p>PLC position slot ID.</p> required <p>Raises:</p> Type Description <code>NotImplementedError</code> <p>If backend does not support PLC teaching.</p> Source code in <code>src/applied_motion/applied_motion.py</code> <pre><code>def teach_pos(self, pos_id: int) -&gt; None:\n    \"\"\"Teach current location into PLC position slot.\n\n    Args:\n        pos_id: PLC position slot ID.\n\n    Raises:\n        NotImplementedError: If backend does not support PLC teaching.\n    \"\"\"\n    if self._backend is None:\n        raise NotImplementedError(\"teach_pos is only available for FPosBAPI backend\")\n    self._backend.teach_pos(pos_id)\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.Gantry.teach_tray","title":"teach_tray","text":"<pre><code>teach_tray(tray_id: int, tray_pos: int) -&gt; None\n</code></pre> <p>Teach current location into PLC tray slot.</p> <p>Parameters:</p> Name Type Description Default <code>tray_id</code> <code>int</code> <p>Tray ID.</p> required <code>tray_pos</code> <code>int</code> <p>Position index within tray.</p> required <p>Raises:</p> Type Description <code>NotImplementedError</code> <p>If backend does not support PLC teaching.</p> Source code in <code>src/applied_motion/applied_motion.py</code> <pre><code>def teach_tray(self, tray_id: int, tray_pos: int) -&gt; None:\n    \"\"\"Teach current location into PLC tray slot.\n\n    Args:\n        tray_id: Tray ID.\n        tray_pos: Position index within tray.\n\n    Raises:\n        NotImplementedError: If backend does not support PLC teaching.\n    \"\"\"\n    if self._backend is None:\n        raise NotImplementedError(\"teach_tray is only available for FPosBAPI backend\")\n    self._backend.teach_tray(tray_id, tray_pos)\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.Gantry.list_commands","title":"list_commands","text":"<pre><code>list_commands() -&gt; list[str]\n</code></pre> <p>Return backend command list when available.</p> Source code in <code>src/applied_motion/applied_motion.py</code> <pre><code>def list_commands(self) -&gt; list[str]:\n    \"\"\"Return backend command list when available.\"\"\"\n    if self._backend is None:\n        return []\n    return self._backend.list_commands()\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.Gantry.get_status","title":"get_status","text":"<pre><code>get_status() -&gt; GantryStatus\n</code></pre> <p>Return a comprehensive status snapshot for the gantry.</p> <p>The returned mapping combines per-axis health, aggregate summary booleans, and (when available) controller-level diagnostics exposed by the backend's shared client.</p> <p>Returns:</p> Type Description <code>GantryStatus</code> <p>A dict with keys:</p> <code>GantryStatus</code> <ul> <li><code>backend</code>: Backend class name.</li> </ul> <code>GantryStatus</code> <ul> <li><code>supports_teach</code>: Whether backend supports TEACH commands.</li> </ul> <code>GantryStatus</code> <ul> <li><code>axes</code>: Mapping of axis name to status details.</li> </ul> <code>GantryStatus</code> <ul> <li><code>summary</code>: Aggregate booleans and counts.</li> </ul> <code>GantryStatus</code> <ul> <li><code>controller</code>: PLC/controller diagnostics (for FPosBAPI).</li> </ul> Source code in <code>src/applied_motion/applied_motion.py</code> <pre><code>def get_status(self) -&gt; GantryStatus:\n    \"\"\"Return a comprehensive status snapshot for the gantry.\n\n    The returned mapping combines per-axis health, aggregate summary\n    booleans, and (when available) controller-level diagnostics exposed\n    by the backend's shared client.\n\n    Returns:\n        A dict with keys:\n\n        - ``backend``: Backend class name.\n        - ``supports_teach``: Whether backend supports TEACH commands.\n        - ``axes``: Mapping of axis name to status details.\n        - ``summary``: Aggregate booleans and counts.\n        - ``controller``: PLC/controller diagnostics (for FPosBAPI).\n    \"\"\"\n    axis_statuses = {axis_name: self._collect_axis_status(axis_name, axis) for axis_name, axis in self.axes.items()}\n    controller_status = self._collect_controller_status()\n    summary = self._build_status_summary(axis_statuses, controller_status)\n\n    status: GantryStatus = {\n        \"backend\": \"ModbusGantryBackend\" if self._backend is None else type(self._backend).__name__,\n        \"supports_teach\": self.supports_teach(),\n        \"axes\": axis_statuses,\n        \"summary\": summary,\n        \"controller\": controller_status,\n    }\n    logger.debug(\"Gantry.get_status: %s\", status)\n    return status\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.Gantry.get_location","title":"get_location","text":"<pre><code>get_location() -&gt; dict[str, float]\n</code></pre> <p>Return the current position of every axis in millimetres.</p> <p>Calls <code>EdconAxis.get_current_axis_position</code> for each registered axis and returns the results as a dict keyed by axis name.</p> <p>Returns:</p> Type Description <code>dict[str, float]</code> <p>Mapping of axis name \u2192 current position in mm.</p> Source code in <code>src/applied_motion/applied_motion.py</code> <pre><code>def get_location(self) -&gt; dict[str, float]:\n    \"\"\"Return the current position of every axis in millimetres.\n\n    Calls [`EdconAxis.get_current_axis_position`][applied_motion.backends.edcon_axis.EdconAxis.get_current_axis_position] for each registered\n    axis and returns the results as a dict keyed by axis name.\n\n    Returns:\n        Mapping of axis name \u2192 current position in mm.\n    \"\"\"\n    coordinates = {axis: self.axes[axis].get_current_axis_position() for axis in self.axes}\n    logger.debug(\"Gantry.get_location: %s\", coordinates)\n    return coordinates\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.Gantry.is_stopped","title":"is_stopped","text":"<pre><code>is_stopped() -&gt; bool\n</code></pre> <p>Return True when every registered axis has stopped moving.</p> <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> if <code>stopped</code> returns <code>True</code> for all axes; <code>False</code> if any axis is still in motion.</p> Source code in <code>src/applied_motion/applied_motion.py</code> <pre><code>def is_stopped(self) -&gt; bool:\n    \"\"\"Return True when every registered axis has stopped moving.\n\n    Returns:\n        ``True`` if `stopped`\n            returns ``True`` for all axes; ``False`` if any axis is still in motion.\n    \"\"\"\n    return all(axis.stopped() for axis in self.axes.values())\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.Gantry.is_ready_for_motion","title":"is_ready_for_motion","text":"<pre><code>is_ready_for_motion() -&gt; bool\n</code></pre> <p>Return True when every registered axis is ready to accept a move command.</p> <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> if <code>ready_for_motion</code> returns <code>True</code> for all axes; <code>False</code> otherwise.</p> Source code in <code>src/applied_motion/applied_motion.py</code> <pre><code>def is_ready_for_motion(self) -&gt; bool:\n    \"\"\"Return True when every registered axis is ready to accept a move command.\n\n    Returns:\n        ``True`` if `ready_for_motion` returns ``True`` for all axes; ``False`` otherwise.\n    \"\"\"\n    return all(axis.ready_for_motion() for axis in self.axes.values())\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.MovementError","title":"MovementError","text":"<p>               Bases: <code>Exception</code></p> <p>Raised when a gantry or axis movement fails.</p>"},{"location":"autoapi/applied_motion/#applied_motion.AxisNotFoundError","title":"AxisNotFoundError","text":"<p>               Bases: <code>MovementError</code></p> <p>Raised when an axis name referenced in a movement command does not exist.</p>"},{"location":"autoapi/applied_motion/#applied_motion.Axis","title":"Axis","text":"<p>               Bases: <code>Protocol</code></p> <p>Structural interface for a single controllable axis.</p> <p>Both <code>EdconAxis</code> (Modbus/festo-edcon backend) and <code>FPosBAxis</code> (FPosBAPI backend) satisfy this protocol structurally \u2014 no explicit inheritance required.</p> <p>All position values are in millimetres (mm). All velocity values are in mm/s.</p>"},{"location":"autoapi/applied_motion/#applied_motion.Axis.move","title":"move","text":"<pre><code>move(position: float, velocity: float, **kwargs) -&gt; bool\n</code></pre> <p>Move the axis to position at velocity.</p> <p>Parameters:</p> Name Type Description Default <code>position</code> <code>float</code> <p>Target position in mm (absolute or relative depending on <code>position_type</code> kwarg).</p> required <code>velocity</code> <code>float</code> <p>Move speed in mm/s.</p> required <code>**kwargs</code> <p>Backend-specific options (e.g. <code>position_type</code>, <code>timeout</code>).</p> <code>{}</code> <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> if the move completed successfully.</p> Source code in <code>src/applied_motion/backends/axis_protocol.py</code> <pre><code>def move(self, position: float, velocity: float, **kwargs) -&gt; bool:\n    \"\"\"Move the axis to *position* at *velocity*.\n\n    Args:\n        position: Target position in mm (absolute or relative depending on\n            ``position_type`` kwarg).\n        velocity: Move speed in mm/s.\n        **kwargs: Backend-specific options (e.g. ``position_type``,\n            ``timeout``).\n\n    Returns:\n        ``True`` if the move completed successfully.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.Axis.home","title":"home","text":"<pre><code>home() -&gt; bool\n</code></pre> <p>Run the axis homing/referencing sequence.</p> Source code in <code>src/applied_motion/backends/axis_protocol.py</code> <pre><code>def home(self) -&gt; bool:\n    \"\"\"Run the axis homing/referencing sequence.\"\"\"\n    ...\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.Axis.get_current_axis_position","title":"get_current_axis_position","text":"<pre><code>get_current_axis_position() -&gt; float\n</code></pre> <p>Return the current axis position in mm.</p> <p>Returns:</p> Type Description <code>float</code> <p>Current position in mm.</p> Source code in <code>src/applied_motion/backends/axis_protocol.py</code> <pre><code>def get_current_axis_position(self) -&gt; float:\n    \"\"\"Return the current axis position in mm.\n\n    Returns:\n        Current position in mm.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.Axis.is_homed","title":"is_homed","text":"<pre><code>is_homed() -&gt; bool\n</code></pre> <p>Return whether this axis reports a valid homed/reference state.</p> <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> if this axis is homed/referenced; <code>False</code> otherwise.</p> Source code in <code>src/applied_motion/backends/axis_protocol.py</code> <pre><code>def is_homed(self) -&gt; bool:\n    \"\"\"Return whether this axis reports a valid homed/reference state.\n\n    Returns:\n        ``True`` if this axis is homed/referenced; ``False`` otherwise.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.Axis.stopped","title":"stopped","text":"<pre><code>stopped() -&gt; bool\n</code></pre> <p>Return <code>True</code> when the axis is not currently in motion.</p> <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> if the axis has stopped; <code>False</code> if motion is ongoing.</p> Source code in <code>src/applied_motion/backends/axis_protocol.py</code> <pre><code>def stopped(self) -&gt; bool:\n    \"\"\"Return ``True`` when the axis is not currently in motion.\n\n    Returns:\n        ``True`` if the axis has stopped; ``False`` if motion is ongoing.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.Axis.acknowledge_faults","title":"acknowledge_faults","text":"<pre><code>acknowledge_faults()\n</code></pre> <p>Clear errors.</p> Source code in <code>src/applied_motion/backends/axis_protocol.py</code> <pre><code>def acknowledge_faults(self):\n    \"\"\"Clear errors.\"\"\"\n    # TODO: This shouldn't be exposed to end-users. This is done automatically for almost every motion task to proceed and is part of an internal interface.\n    ...\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.Axis.enable_powerstage","title":"enable_powerstage","text":"<pre><code>enable_powerstage()\n</code></pre> <p>Turn on torque to motor controlling this axis.</p> Source code in <code>src/applied_motion/backends/axis_protocol.py</code> <pre><code>def enable_powerstage(self):\n    \"\"\"Turn on torque to motor controlling this axis.\"\"\"\n    # TODO: This shouldn't be exposed to end-users. This is done automatically for almost every motion task to proceed and is part of an internal interface.\n    ...\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.Axis.disable_powerstage","title":"disable_powerstage","text":"<pre><code>disable_powerstage()\n</code></pre> <p>Turn off torque to motor controlling this axis.</p> Source code in <code>src/applied_motion/backends/axis_protocol.py</code> <pre><code>def disable_powerstage(self):\n    \"\"\"Turn off torque to motor controlling this axis.\"\"\"\n    ...\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.Axis.current_position","title":"current_position","text":"<pre><code>current_position()\n</code></pre> <p>Return current position.</p> Source code in <code>src/applied_motion/backends/axis_protocol.py</code> <pre><code>def current_position(self):\n    \"\"\"Return current position.\"\"\"\n    ...\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.Axis.current_velocity","title":"current_velocity","text":"<pre><code>current_velocity()\n</code></pre> <p>Return current velocity.</p> Source code in <code>src/applied_motion/backends/axis_protocol.py</code> <pre><code>def current_velocity(self):\n    \"\"\"Return current velocity.\"\"\"\n    ...\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.Axis.jog_task","title":"jog_task","text":"<pre><code>jog_task(\n    jog_positive: bool = True,\n    jog_negative: bool = False,\n    incremental: bool = False,\n    duration: float = 0.0,\n) -&gt; bool\n</code></pre> <p>Jog the axis according to backend-specific jog semantics.</p> <p>Parameters:</p> Name Type Description Default <code>jog_positive</code> <code>bool</code> <p>When <code>True</code>, request jog in the positive direction.</p> <code>True</code> <code>jog_negative</code> <code>bool</code> <p>When <code>True</code>, request jog in the negative direction.</p> <code>False</code> <code>incremental</code> <code>bool</code> <p>Backend-specific incremental jog mode flag.</p> <code>False</code> <code>duration</code> <code>float</code> <p>Optional jog duration in seconds. A value of <code>0</code> starts the jog and returns immediately.</p> <code>0.0</code> <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> if the jog command succeeds; <code>False</code> otherwise.</p> Source code in <code>src/applied_motion/backends/axis_protocol.py</code> <pre><code>def jog_task(\n    self,\n    jog_positive: bool = True,\n    jog_negative: bool = False,\n    incremental: bool = False,\n    duration: float = 0.0,\n) -&gt; bool:\n    \"\"\"Jog the axis according to backend-specific jog semantics.\n\n    Args:\n        jog_positive: When ``True``, request jog in the positive direction.\n        jog_negative: When ``True``, request jog in the negative direction.\n        incremental: Backend-specific incremental jog mode flag.\n        duration: Optional jog duration in seconds. A value of ``0``\n            starts the jog and returns immediately.\n\n    Returns:\n        ``True`` if the jog command succeeds; ``False`` otherwise.\n    \"\"\"\n    # TODO: Implement this functionality using corresponding FPosBAPI command\n\n    ...\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.Axis.ready_for_motion","title":"ready_for_motion","text":"<pre><code>ready_for_motion() -&gt; bool\n</code></pre> <p>Return <code>True</code> when the axis is ready to accept a move command.</p> <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> if the axis is enabled, homed, and fault-free.</p> Source code in <code>src/applied_motion/backends/axis_protocol.py</code> <pre><code>def ready_for_motion(self) -&gt; bool:\n    \"\"\"Return ``True`` when the axis is ready to accept a move command.\n\n    Returns:\n        ``True`` if the axis is enabled, homed, and fault-free.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.FPosBAPIClient","title":"FPosBAPIClient","text":"<pre><code>FPosBAPIClient(\n    ip: str, port: int = 1234, timeout: float | None = None\n)\n</code></pre> <p>Thread-safe TCP socket client for the Festo FPosBAPI ASCII protocol.</p> <p>Connects to the CECC-X PLC on ip:port and wraps the request/response cycle in <code>send_command</code>.  A <code>threading.Lock</code> serialises all socket I/O so multiple <code>FPosBAxis</code> objects sharing the same client do not interleave their frames.</p> <p>Attributes:</p> Name Type Description <code>ip</code> <p>IPv4 address of the CECC-X TCP server.</p> <code>port</code> <p>TCP port of the FPosBAPI server (default <code>1234</code>).</p> <p>Connect to the FPosBAPI server.</p> <p>Parameters:</p> Name Type Description Default <code>ip</code> <code>str</code> <p>IPv4 address of the CECC-X PLC.</p> required <code>port</code> <code>int</code> <p>TCP port the FPosBAPI server is listening on.  Defaults to <code>1234</code>.</p> <code>1234</code> <code>timeout</code> <code>float | None</code> <p>Socket timeout in seconds.  Applied to both <code>connect</code> and <code>recv</code> operations.  <code>None</code> (default) means blocking with no timeout.</p> <code>None</code> <p>Raises:</p> Type Description <code>OSError</code> <p>If the TCP connection cannot be established within timeout seconds.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def __init__(self, ip: str, port: int = 1234, timeout: float | None = None) -&gt; None:\n    \"\"\"Connect to the FPosBAPI server.\n\n    Args:\n        ip: IPv4 address of the CECC-X PLC.\n        port: TCP port the FPosBAPI server is listening on.  Defaults to\n            ``1234``.\n        timeout: Socket timeout in seconds.  Applied to both ``connect``\n            and ``recv`` operations.  ``None`` (default) means blocking\n            with no timeout.\n\n    Raises:\n        OSError: If the TCP connection cannot be established within\n            *timeout* seconds.\n    \"\"\"\n    self.ip = ip\n    self.port = port\n    self._timeout = timeout\n    self._lock = threading.Lock()\n    self._msg_id = 0\n    self._sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)\n\n    logger.info(\"FPosBAPIClient attempting to connect to %s:%d\", ip, port)\n    self._connect()\n    logger.info(\"FPosBAPIClient connected to %s:%d\", ip, port)\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.FPosBAPIClient.send_command","title":"send_command","text":"<pre><code>send_command(\n    command: str,\n    *params,\n    timeout: float | None | _Unset = _UNSET,\n) -&gt; list[str]\n</code></pre> <p>Send a command to the FPosBAPI server and return all response lines.</p> <p>Acquires the internal lock, increments the message ID, formats the ASCII request frame, sends it, then reads lines via <code>_collect_response</code> until the terminal (non-<code>ACK</code>) line arrives.  The terminal (last) line is validated for correct MSG_ID echo, CMD echo, and <code>SUCCESS</code> status before returning.</p> <p>Parameters:</p> Name Type Description Default <code>command</code> <code>str</code> <p>FPosBAPI command string, e.g. <code>\"ENABLE\"</code>, <code>\"MOVE_AXIS\"</code>.</p> required <code>*params</code> <p>Zero or more positional parameters appended to the frame.</p> <code>()</code> <code>timeout</code> <code>float | None | _Unset</code> <p>Per-call socket timeout in seconds.  Pass <code>None</code> for blocking mode (no timeout), e.g. for long-running motion commands.  Omit to use the client's configured defaults.</p> <code>_UNSET</code> <p>Returns:</p> Type Description <code>list[str]</code> <p>All response lines received before the empty terminator, in arrival</p> <code>list[str]</code> <p>order.  The last element is always the terminal status line echoing</p> <code>list[str]</code> <p>the command and carrying <code>SUCCESS</code>.</p> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error status, the echoed MSG_ID or command name does not match what was sent, the response is empty, or the connection is closed.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def send_command(self, command: str, *params, timeout: float | None | _Unset = _UNSET) -&gt; list[str]:\n    \"\"\"Send a command to the FPosBAPI server and return all response lines.\n\n    Acquires the internal lock, increments the message ID, formats the\n    ASCII request frame, sends it, then reads lines via\n    `_collect_response` until the terminal (non-``ACK``) line\n    arrives.  The terminal (last) line is validated for correct MSG_ID\n    echo, CMD echo, and ``SUCCESS`` status before returning.\n\n    Args:\n        command: FPosBAPI command string, e.g. ``\"ENABLE\"``, ``\"MOVE_AXIS\"``.\n        *params: Zero or more positional parameters appended to the frame.\n        timeout: Per-call socket timeout in seconds.  Pass ``None`` for\n            blocking mode (no timeout), e.g. for long-running motion\n            commands.  Omit to use the client's configured defaults.\n\n    Returns:\n        All response lines received before the empty terminator, in arrival\n        order.  The last element is always the terminal status line echoing\n        the command and carrying ``SUCCESS``.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error status, the\n            echoed MSG_ID or command name does not match what was sent, the\n            response is empty, or the connection is closed.\n    \"\"\"\n    with self._lock:\n        if not isinstance(timeout, _Unset):\n            self._sock.settimeout(timeout)\n        msg_id = self._next_id()\n        raw = self._build_frame(msg_id, command, params)\n        logger.debug(\"FPosBAPIClient tx: %s\", raw.strip())\n        self._send_frame(raw, command, timeout)\n        try:\n            lines = self._collect_response()\n        finally:\n            if not isinstance(timeout, _Unset):\n                self._sock.settimeout(self._timeout)\n        logger.debug(\"FPosBAPIClient rx: %s\", \", \".join(lines))\n        self._validate_terminal(lines, msg_id, command)\n    return lines\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.FPosBAPIClient.try_command","title":"try_command","text":"<pre><code>try_command(\n    command: str,\n    *params,\n    timeout: float | None | _Unset = _UNSET,\n) -&gt; bool\n</code></pre> <p>Send a command; raise on communication failure, return bool for PLC result.</p> <p>Parameters:</p> Name Type Description Default <code>command</code> <code>str</code> <p>FPosBAPI command string, e.g. <code>\"HOME\"</code>, <code>\"MOVE_AXIS\"</code>.</p> required <code>*params</code> <p>Zero or more positional parameters.</p> <code>()</code> <code>timeout</code> <code>float | None | _Unset</code> <p>Per-call socket timeout forwarded to <code>send_command</code>. Pass <code>None</code> for blocking mode on long-running motion commands.</p> <code>_UNSET</code> <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> when the PLC reports <code>SUCCESS</code>; <code>False</code> when the PLC</p> <code>bool</code> <p>returns a valid but non-SUCCESS response.</p> <p>Raises:</p> Type Description <code>FPosBAPICommunicationError</code> <p>If a transport or protocol-level failure occurs.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def try_command(self, command: str, *params, timeout: float | None | _Unset = _UNSET) -&gt; bool:\n    \"\"\"Send a command; raise on communication failure, return bool for PLC result.\n\n    Args:\n        command: FPosBAPI command string, e.g. ``\"HOME\"``, ``\"MOVE_AXIS\"``.\n        *params: Zero or more positional parameters.\n        timeout: Per-call socket timeout forwarded to [`send_command`][applied_motion.backends.fposbapi_client.FPosBAPIClient.send_command].\n            Pass ``None`` for blocking mode on long-running motion commands.\n\n    Returns:\n        ``True`` when the PLC reports ``SUCCESS``; ``False`` when the PLC\n        returns a valid but non-SUCCESS response.\n\n    Raises:\n        FPosBAPICommunicationError: If a transport or protocol-level failure occurs.\n    \"\"\"\n    try:\n        self.send_command(command, *params, timeout=timeout)\n        return True\n    except FPosBAPICommandError:\n        logger.warning(\"FPosBAPIClient: command %r rejected by PLC\", command)\n        return False\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.FPosBAPIClient.get_veab","title":"get_veab","text":"<pre><code>get_veab() -&gt; int\n</code></pre> <p>Get the pressure set value of the VEAB.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def get_veab(self) -&gt; int:\n    \"\"\"Get the pressure set value of the VEAB.\"\"\"\n    # TODO: This __REALLY__ needs to be contingent on the config and how it is passed in\n    # TODO: The ideal would be a PressureControl() class that shares the intantiated gantry and can take it in as input parameter. All of these classes would benefit from that bc of necessary shared socket on CECC-X\n\n    lines = self.send_command(\"GET_VEAB\")\n    terminal = lines[-1]\n    fields = [f.strip() for f in terminal.split(\",\")]\n\n    return int(fields[2])\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.FPosBAPIClient.set_veab","title":"set_veab","text":"<pre><code>set_veab(set_value: int) -&gt; None\n</code></pre> <p>Set the pressure value of the VEAB.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def set_veab(self, set_value: int) -&gt; None:\n    \"\"\"Set the pressure value of the VEAB.\"\"\"\n    self.send_command(\"SET_VEAB\", set_value)\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.FPosBAPIClient.list_commands","title":"list_commands","text":"<pre><code>list_commands() -&gt; list[str]\n</code></pre> <p>Query the server for its supported command set via <code>CMD_LIST</code>.</p> <p>Sends <code>CMD_LIST</code> and parses the response to extract the command names the connected FPosBAPI server advertises.  The set varies between firmware versions, so callers can use this to feature-detect before issuing optional commands (e.g. <code>IS_HOME</code>, <code>SYS_STATUS</code>).</p> <p>Response layout assumed::</p> <pre><code>MSG_ID, CMD_LIST, CMD_1, CMD_2, ..., CMD_N, ERR_ID, ERR_TYPE, ERR_MSG\n</code></pre> <p>The first two fields (<code>MSG_ID</code> and <code>CMD_LIST</code>) and the last three (error fields ending with <code>SUCCESS</code>) are stripped; the remaining fields are returned as a list of command name strings.</p> <p>Returns:</p> Type Description <code>list[str]</code> <p>List of command name strings advertised by the server,</p> <code>list[str]</code> <p>e.g. <code>[\"ENABLE\", \"DISABLE\", \"HOME\", \"MOVE_AXIS\", ...]</code>.</p> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def list_commands(self) -&gt; list[str]:\n    \"\"\"Query the server for its supported command set via ``CMD_LIST``.\n\n    Sends ``CMD_LIST`` and parses the response to extract the command\n    names the connected FPosBAPI server advertises.  The set varies\n    between firmware versions, so callers can use this to feature-detect\n    before issuing optional commands (e.g. ``IS_HOME``, ``SYS_STATUS``).\n\n    Response layout assumed::\n\n        MSG_ID, CMD_LIST, CMD_1, CMD_2, ..., CMD_N, ERR_ID, ERR_TYPE, ERR_MSG\n\n    The first two fields (``MSG_ID`` and ``CMD_LIST``) and the last\n    three (error fields ending with ``SUCCESS``) are stripped; the\n    remaining fields are returned as a list of command name strings.\n\n    Returns:\n        List of command name strings advertised by the server,\n        e.g. ``[\"ENABLE\", \"DISABLE\", \"HOME\", \"MOVE_AXIS\", ...]``.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    lines = self.send_command(\"CMD_LIST\")\n    # The server sends one bare command name per intermediate line.\n    # lines[0] is the ACK, lines[1:-1] are the bare command names,\n    # lines[-1] is the terminal SUCCESS line.\n    commands = [line.strip() for line in lines[1:-1] if line.strip()]\n    logger.debug(\"FPosBAPIClient: CMD_LIST returned %d command(s): %s\", len(commands), commands)\n    return commands\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.FPosBAPIClient.enable","title":"enable","text":"<pre><code>enable() -&gt; None\n</code></pre> <p>Enable the gantry drives via <code>ENABLE</code>.</p> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def enable(self) -&gt; None:\n    \"\"\"Enable the gantry drives via ``ENABLE``.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    self.send_command(\"ENABLE\")\n    logger.info(\"FPosBAPIClient: drives enabled\")\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.FPosBAPIClient.disable","title":"disable","text":"<pre><code>disable() -&gt; None\n</code></pre> <p>Disable the gantry drives via <code>DISABLE</code>.</p> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def disable(self) -&gt; None:\n    \"\"\"Disable the gantry drives via ``DISABLE``.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    self.send_command(\"DISABLE\")\n    logger.info(\"FPosBAPIClient: drives disabled\")\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.FPosBAPIClient.home","title":"home","text":"<pre><code>home() -&gt; None\n</code></pre> <p>Home all axes via <code>HOME</code>.</p> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def home(self) -&gt; None:\n    \"\"\"Home all axes via ``HOME``.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    self.send_command(\"HOME\")\n    logger.info(\"FPosBAPIClient: homing complete\")\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.FPosBAPIClient.move_pos","title":"move_pos","text":"<pre><code>move_pos(\n    pos_id: int,\n    tool_id: int = 0,\n    retract_z: int = 0,\n    slow_app: int = 0,\n) -&gt; None\n</code></pre> <p>Execute a motion sequence to a stored position via <code>MOVE_POS</code>.</p> <p>Parameters:</p> Name Type Description Default <code>pos_id</code> <code>int</code> <p>Position record ID (1-100).</p> required <code>tool_id</code> <code>int</code> <p>Tool index to apply.  <code>0</code> selects no tool.</p> <code>0</code> <code>retract_z</code> <code>int</code> <p><code>1</code> to retract Z before moving, <code>0</code> to skip.</p> <code>0</code> <code>slow_app</code> <code>int</code> <p><code>1</code> to slow approach the target position, <code>0</code> for full speed.</p> <code>0</code> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def move_pos(\n    self,\n    pos_id: int,\n    tool_id: int = 0,\n    retract_z: int = 0,\n    slow_app: int = 0,\n) -&gt; None:\n    \"\"\"Execute a motion sequence to a stored position via ``MOVE_POS``.\n\n    Args:\n        pos_id: Position record ID (1-100).\n        tool_id: Tool index to apply.  ``0`` selects no tool.\n        retract_z: ``1`` to retract Z before moving, ``0`` to skip.\n        slow_app: ``1`` to slow approach the target position, ``0`` for\n            full speed.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    self.send_command(\"MOVE_POS\", pos_id, tool_id, retract_z, slow_app)\n    logger.info(\"FPosBAPIClient: MOVE_POS complete (pos_id=%d, tool_id=%d)\", pos_id, tool_id)\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.FPosBAPIClient.move_loc","title":"move_loc","text":"<pre><code>move_loc(\n    a1: float,\n    a2: float,\n    a3: float,\n    tool_id: int = 0,\n    move_typ: int = 0,\n    retract_z: int = 0,\n    slow_app: int = 0,\n) -&gt; None\n</code></pre> <p>Execute a coordinated 3-axis move to explicit coordinates via <code>MOVE_LOC</code>.</p> <p>Parameters:</p> Name Type Description Default <code>a1</code> <code>float</code> <p>Target X-axis position in mm.</p> required <code>a2</code> <code>float</code> <p>Target Y-axis position in mm.</p> required <code>a3</code> <code>float</code> <p>Target Z-axis position in mm.</p> required <code>tool_id</code> <code>int</code> <p>Tool offset to apply.  <code>0</code> selects no tool.</p> <code>0</code> <code>move_typ</code> <code>int</code> <p><code>0</code> for absolute, <code>1</code> for relative.</p> <code>0</code> <code>retract_z</code> <code>int</code> <p><code>1</code> to retract Z before moving, <code>0</code> to skip.</p> <code>0</code> <code>slow_app</code> <code>int</code> <p><code>1</code> to slow approach the target position, <code>0</code> for full speed.</p> <code>0</code> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def move_loc(\n    self,\n    a1: float,\n    a2: float,\n    a3: float,\n    tool_id: int = 0,\n    move_typ: int = 0,\n    retract_z: int = 0,\n    slow_app: int = 0,\n) -&gt; None:\n    \"\"\"Execute a coordinated 3-axis move to explicit coordinates via ``MOVE_LOC``.\n\n    Args:\n        a1: Target X-axis position in mm.\n        a2: Target Y-axis position in mm.\n        a3: Target Z-axis position in mm.\n        tool_id: Tool offset to apply.  ``0`` selects no tool.\n        move_typ: ``0`` for absolute, ``1`` for relative.\n        retract_z: ``1`` to retract Z before moving, ``0`` to skip.\n        slow_app: ``1`` to slow approach the target position, ``0`` for\n            full speed.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    self.send_command(\"MOVE_LOC\", a1, a2, a3, tool_id, move_typ, retract_z, slow_app)\n    logger.info(\"FPosBAPIClient: MOVE_LOC complete (a1=%s, a2=%s, a3=%s mm)\", a1, a2, a3)\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.FPosBAPIClient.move_path","title":"move_path","text":"<pre><code>move_path() -&gt; None\n</code></pre> <p>Execute the programmed motion path via <code>MOVE_PATH</code>.</p> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def move_path(self) -&gt; None:\n    \"\"\"Execute the programmed motion path via ``MOVE_PATH``.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    # TODO: Needs WRITE_PATH?\n    self.send_command(\"MOVE_PATH\")\n    logger.info(\"FPosBAPIClient: MOVE_PATH complete\")\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.FPosBAPIClient.halt","title":"halt","text":"<pre><code>halt() -&gt; None\n</code></pre> <p>Halt the current motion via <code>HALT</code>.</p> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def halt(self) -&gt; None:\n    \"\"\"Halt the current motion via ``HALT``.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    self.send_command(\"HALT\")\n    logger.info(\"FPosBAPIClient: HALT issued\")\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.FPosBAPIClient.resume","title":"resume","text":"<pre><code>resume() -&gt; None\n</code></pre> <p>Resume a halted motion via <code>RESUME</code>.</p> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def resume(self) -&gt; None:\n    \"\"\"Resume a halted motion via ``RESUME``.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    self.send_command(\"RESUME\")\n    logger.info(\"FPosBAPIClient: RESUME issued\")\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.FPosBAPIClient.abort","title":"abort","text":"<pre><code>abort() -&gt; None\n</code></pre> <p>Abort the current motion via <code>ABORT</code>.</p> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def abort(self) -&gt; None:\n    \"\"\"Abort the current motion via ``ABORT``.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    self.send_command(\"ABORT\")\n    logger.info(\"FPosBAPIClient: ABORT issued\")\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.FPosBAPIClient.reset_err","title":"reset_err","text":"<pre><code>reset_err() -&gt; None\n</code></pre> <p>Reset the active error via <code>RESET_ERR</code>.</p> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def reset_err(self) -&gt; None:\n    \"\"\"Reset the active error via ``RESET_ERR``.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    self.send_command(\"RESET_ERR\")\n    logger.info(\"FPosBAPIClient: error reset\")\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.FPosBAPIClient.open_valve","title":"open_valve","text":"<pre><code>open_valve(\n    v1_time: int = 0,\n    v2_time: int = 0,\n    v3_time: int = 0,\n    v4_time: int = 0,\n    v5_time: int = 0,\n    v6_time: int = 0,\n    v7_time: int = 0,\n    v8_time: int = 0,\n) -&gt; None\n</code></pre> <p>Open one or more valves for the specified durations via <code>OPEN_VALVE</code>.</p> <p>Parameters:</p> Name Type Description Default <code>v1_time</code> <code>int</code> <p>Valve 1 open duration in ms.  <code>0</code> keeps the valve closed.</p> <code>0</code> <code>v2_time</code> <code>int</code> <p>Valve 2 open duration in ms.</p> <code>0</code> <code>v3_time</code> <code>int</code> <p>Valve 3 open duration in ms.</p> <code>0</code> <code>v4_time</code> <code>int</code> <p>Valve 4 open duration in ms.</p> <code>0</code> <code>v5_time</code> <code>int</code> <p>Valve 5 open duration in ms.</p> <code>0</code> <code>v6_time</code> <code>int</code> <p>Valve 6 open duration in ms.</p> <code>0</code> <code>v7_time</code> <code>int</code> <p>Valve 7 open duration in ms.</p> <code>0</code> <code>v8_time</code> <code>int</code> <p>Valve 8 open duration in ms.</p> <code>0</code> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def open_valve(\n    self,\n    v1_time: int = 0,\n    v2_time: int = 0,\n    v3_time: int = 0,\n    v4_time: int = 0,\n    v5_time: int = 0,\n    v6_time: int = 0,\n    v7_time: int = 0,\n    v8_time: int = 0,\n) -&gt; None:\n    \"\"\"Open one or more valves for the specified durations via ``OPEN_VALVE``.\n\n    Args:\n        v1_time: Valve 1 open duration in ms.  ``0`` keeps the valve closed.\n        v2_time: Valve 2 open duration in ms.\n        v3_time: Valve 3 open duration in ms.\n        v4_time: Valve 4 open duration in ms.\n        v5_time: Valve 5 open duration in ms.\n        v6_time: Valve 6 open duration in ms.\n        v7_time: Valve 7 open duration in ms.\n        v8_time: Valve 8 open duration in ms.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    self.send_command(\n        \"OPEN_VALVE\",\n        v1_time,\n        v2_time,\n        v3_time,\n        v4_time,\n        v5_time,\n        v6_time,\n        v7_time,\n        v8_time,\n    )\n    logger.info(\"FPosBAPIClient: OPEN_VALVE complete\")\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.FPosBAPIClient.read_pos","title":"read_pos","text":"<pre><code>read_pos(pos_id: int) -&gt; tuple[float, float, float]\n</code></pre> <p>Read the stored absolute position for pos_id via <code>READ_POS</code>.</p> <p>Response layout::</p> <pre><code>MSG_ID, READ_POS, POS_ID, ABS_A1, ABS_A2, ABS_A3, 0, NULL, SUCCESS\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>pos_id</code> <code>int</code> <p>Position record ID (1-100).</p> required <p>Returns:</p> Type Description <code>tuple[float, float, float]</code> <p>Tuple of <code>(abs_a1, abs_a2, abs_a3)</code> in mm.</p> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> <code>RuntimeError</code> <p>If the response cannot be parsed.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def read_pos(self, pos_id: int) -&gt; tuple[float, float, float]:\n    \"\"\"Read the stored absolute position for *pos_id* via ``READ_POS``.\n\n    Response layout::\n\n        MSG_ID, READ_POS, POS_ID, ABS_A1, ABS_A2, ABS_A3, 0, NULL, SUCCESS\n\n    Args:\n        pos_id: Position record ID (1-100).\n\n    Returns:\n        Tuple of ``(abs_a1, abs_a2, abs_a3)`` in mm.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n        RuntimeError: If the response cannot be parsed.\n    \"\"\"\n    lines = self.send_command(\"READ_POS\", pos_id)\n    fields = [f.strip() for f in lines[-1].split(\",\")]\n    # fields: msg_id, READ_POS, pos_id, abs_a1, abs_a2, abs_a3, 0, NULL, SUCCESS\n    try:\n        result = float(fields[3]), float(fields[4]), float(fields[5])\n    except (IndexError, ValueError) as exc:\n        raise RuntimeError(f\"Failed to parse READ_POS response for pos_id={pos_id}: {lines!r}\") from exc\n    logger.debug(\"FPosBAPIClient: READ_POS pos_id=%d \u2192 %s\", pos_id, result)\n    return result\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.FPosBAPIClient.teach_pos","title":"teach_pos","text":"<pre><code>teach_pos(pos_id: int, tool_id: int = 1) -&gt; None\n</code></pre> <p>Save the current axis positions to pos_id via <code>TEACH_POS</code>.</p> <p>Parameters:</p> Name Type Description Default <code>pos_id</code> <code>int</code> <p>Position record ID to write (1-100).</p> required <code>tool_id</code> <code>int</code> <p>Tool offset to associate with this position.</p> <code>1</code> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def teach_pos(self, pos_id: int, tool_id: int = 1) -&gt; None:\n    \"\"\"Save the current axis positions to *pos_id* via ``TEACH_POS``.\n\n    Args:\n        pos_id: Position record ID to write (1-100).\n        tool_id: Tool offset to associate with this position.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    self.send_command(\"TEACH_POS\", pos_id, tool_id)\n    logger.info(\"FPosBAPIClient: TEACH_POS complete (pos_id=%d)\", pos_id)\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.FPosBAPIClient.write_pos","title":"write_pos","text":"<pre><code>write_pos(\n    pos_id: int, abs_a1: float, abs_a2: float, abs_a3: float\n) -&gt; None\n</code></pre> <p>Write absolute axis coordinates to pos_id via <code>WRITE_POS</code>.</p> <p>Parameters:</p> Name Type Description Default <code>pos_id</code> <code>int</code> <p>Position record ID to write (1-100).</p> required <code>abs_a1</code> <code>float</code> <p>Absolute X-axis position in mm.</p> required <code>abs_a2</code> <code>float</code> <p>Absolute Y-axis position in mm.</p> required <code>abs_a3</code> <code>float</code> <p>Absolute Z-axis position in mm.</p> required <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def write_pos(\n    self,\n    pos_id: int,\n    abs_a1: float,\n    abs_a2: float,\n    abs_a3: float,\n) -&gt; None:\n    \"\"\"Write absolute axis coordinates to *pos_id* via ``WRITE_POS``.\n\n    Args:\n        pos_id: Position record ID to write (1-100).\n        abs_a1: Absolute X-axis position in mm.\n        abs_a2: Absolute Y-axis position in mm.\n        abs_a3: Absolute Z-axis position in mm.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    self.send_command(\"WRITE_POS\", pos_id, abs_a1, abs_a2, abs_a3)\n    logger.info(\n        \"FPosBAPIClient: WRITE_POS complete (pos_id=%d, a1=%s, a2=%s, a3=%s mm)\",\n        pos_id,\n        abs_a1,\n        abs_a2,\n        abs_a3,\n    )\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.FPosBAPIClient.mod_pos","title":"mod_pos","text":"<pre><code>mod_pos(\n    pos_id: int, rel_a1: float, rel_a2: float, rel_a3: float\n) -&gt; None\n</code></pre> <p>Modify a stored position by relative offsets via <code>MOD_POS</code>.</p> <p>Parameters:</p> Name Type Description Default <code>pos_id</code> <code>int</code> <p>Position record ID to modify (1-100).</p> required <code>rel_a1</code> <code>float</code> <p>Relative X-axis offset in mm.</p> required <code>rel_a2</code> <code>float</code> <p>Relative Y-axis offset in mm.</p> required <code>rel_a3</code> <code>float</code> <p>Relative Z-axis offset in mm.</p> required <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def mod_pos(\n    self,\n    pos_id: int,\n    rel_a1: float,\n    rel_a2: float,\n    rel_a3: float,\n) -&gt; None:\n    \"\"\"Modify a stored position by relative offsets via ``MOD_POS``.\n\n    Args:\n        pos_id: Position record ID to modify (1-100).\n        rel_a1: Relative X-axis offset in mm.\n        rel_a2: Relative Y-axis offset in mm.\n        rel_a3: Relative Z-axis offset in mm.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    self.send_command(\"MOD_POS\", pos_id, rel_a1, rel_a2, rel_a3)\n    logger.info(\"FPosBAPIClient: MOD_POS complete (pos_id=%d)\", pos_id)\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.FPosBAPIClient.write_path","title":"write_path","text":"<pre><code>write_path(\n    pa_pos_id: int,\n    abs_a1: float,\n    abs_a2: float,\n    abs_a3: float,\n) -&gt; None\n</code></pre> <p>Write one waypoint along the motion path via <code>WRITE_PATH</code>.</p> <p>Parameters:</p> Name Type Description Default <code>pa_pos_id</code> <code>int</code> <p>Path position index (1-10).</p> required <code>abs_a1</code> <code>float</code> <p>Absolute X-axis position in mm.</p> required <code>abs_a2</code> <code>float</code> <p>Absolute Y-axis position in mm.</p> required <code>abs_a3</code> <code>float</code> <p>Absolute Z-axis position in mm.</p> required <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def write_path(\n    self,\n    pa_pos_id: int,\n    abs_a1: float,\n    abs_a2: float,\n    abs_a3: float,\n) -&gt; None:\n    \"\"\"Write one waypoint along the motion path via ``WRITE_PATH``.\n\n    Args:\n        pa_pos_id: Path position index (1-10).\n        abs_a1: Absolute X-axis position in mm.\n        abs_a2: Absolute Y-axis position in mm.\n        abs_a3: Absolute Z-axis position in mm.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    self.send_command(\"WRITE_PATH\", pa_pos_id, abs_a1, abs_a2, abs_a3)\n    logger.info(\"FPosBAPIClient: WRITE_PATH complete (pa_pos_id=%d)\", pa_pos_id)\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.FPosBAPIClient.read_path","title":"read_path","text":"<pre><code>read_path(pa_pos_id: int) -&gt; tuple[float, float, float]\n</code></pre> <p>Read one waypoint from the motion path via <code>READ_PATH</code>.</p> <p>Parameters:</p> Name Type Description Default <code>pa_pos_id</code> <code>int</code> <p>Path position index (1-10).</p> required <p>Returns:</p> Type Description <code>tuple[float, float, float]</code> <p>Tuple of <code>(abs_a1, abs_a2, abs_a3)</code> in mm.</p> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> <code>RuntimeError</code> <p>If the response cannot be parsed.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def read_path(self, pa_pos_id: int) -&gt; tuple[float, float, float]:\n    \"\"\"Read one waypoint from the motion path via ``READ_PATH``.\n\n    Args:\n        pa_pos_id: Path position index (1-10).\n\n    Returns:\n        Tuple of ``(abs_a1, abs_a2, abs_a3)`` in mm.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n        RuntimeError: If the response cannot be parsed.\n    \"\"\"\n    lines = self.send_command(\"READ_PATH\", pa_pos_id)\n    fields = [f.strip() for f in lines[-1].split(\",\")]\n    # fields: msg_id, READ_PATH, pa_pos_id, abs_a1, abs_a2, abs_a3, 0, NULL, SUCCESS\n    try:\n        result = float(fields[3]), float(fields[4]), float(fields[5])\n    except (IndexError, ValueError) as exc:\n        raise RuntimeError(f\"Failed to parse READ_PATH response for pa_pos_id={pa_pos_id}: {lines!r}\") from exc\n    logger.debug(\"FPosBAPIClient: READ_PATH pa_pos_id=%d \u2192 %s\", pa_pos_id, result)\n    return result\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.FPosBAPIClient.sys_status","title":"sys_status","text":"<pre><code>sys_status() -&gt; str\n</code></pre> <p>Return the system status string via <code>SYS_STATUS</code>.</p> <p>Response layout::</p> <pre><code>MSG_ID, SYS_STATUS, STATUS, 0, NULL, SUCCESS\n</code></pre> <p>Returns:</p> Type Description <code>str</code> <p>Status string reported by the PLC (e.g. <code>\"IDLE\"</code>).</p> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> <code>RuntimeError</code> <p>If the response cannot be parsed.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def sys_status(self) -&gt; str:\n    \"\"\"Return the system status string via ``SYS_STATUS``.\n\n    Response layout::\n\n        MSG_ID, SYS_STATUS, STATUS, 0, NULL, SUCCESS\n\n    Returns:\n        Status string reported by the PLC (e.g. ``\"IDLE\"``).\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n        RuntimeError: If the response cannot be parsed.\n    \"\"\"\n    lines = self.send_command(\"SYS_STATUS\")\n    fields = [f.strip() for f in lines[-1].split(\",\")]\n    # fields: msg_id, SYS_STATUS, status, 0, NULL, SUCCESS\n    try:\n        status = fields[2]\n    except IndexError as exc:\n        raise RuntimeError(f\"Failed to parse SYS_STATUS response: {lines!r}\") from exc\n    logger.debug(\"FPosBAPIClient: SYS_STATUS \u2192 %r\", status)\n    return status\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.FPosBAPIClient.is_error","title":"is_error","text":"<pre><code>is_error() -&gt; bool\n</code></pre> <p>Return whether the gantry is in an error state via <code>IS_ERROR</code>.</p> <p>Response layout::</p> <pre><code>MSG_ID, IS_ERROR, FLAG, 0, NULL, SUCCESS\n</code></pre> <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> if the PLC reports an active error; <code>False</code> otherwise.</p> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> <code>RuntimeError</code> <p>If the response cannot be parsed.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def is_error(self) -&gt; bool:\n    \"\"\"Return whether the gantry is in an error state via ``IS_ERROR``.\n\n    Response layout::\n\n        MSG_ID, IS_ERROR, FLAG, 0, NULL, SUCCESS\n\n    Returns:\n        ``True`` if the PLC reports an active error; ``False`` otherwise.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n        RuntimeError: If the response cannot be parsed.\n    \"\"\"\n    lines = self.send_command(\"IS_ERROR\")\n    fields = [f.strip() for f in lines[-1].split(\",\")]\n    # fields: msg_id, IS_ERROR, flag, 0, NULL, SUCCESS\n    try:\n        result = bool(int(fields[2]))\n    except (IndexError, ValueError) as exc:\n        raise RuntimeError(f\"Failed to parse IS_ERROR response: {lines!r}\") from exc\n    logger.debug(\"FPosBAPIClient: IS_ERROR \u2192 %s\", result)\n    return result\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.FPosBAPIClient.fpb_error","title":"fpb_error","text":"<pre><code>fpb_error() -&gt; str\n</code></pre> <p>Return the fieldbus error state via <code>FPB_ERROR</code>.</p> <p>Response layout::</p> <pre><code>MSG_ID, FPB_ERROR, STATUS, 0, NULL, SUCCESS\n</code></pre> <p>Returns:</p> Type Description <code>str</code> <p>Raw status string from the terminal response field.</p> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def fpb_error(self) -&gt; str:\n    \"\"\"Return the fieldbus error state via ``FPB_ERROR``.\n\n    Response layout::\n\n        MSG_ID, FPB_ERROR, STATUS, 0, NULL, SUCCESS\n\n    Returns:\n        Raw status string from the terminal response field.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    lines = self.send_command(\"FPB_ERROR\")\n    fields = [f.strip() for f in lines[-1].split(\",\")]\n    status = fields[2] if len(fields) &gt; 2 else \"\"\n    logger.debug(\"FPosBAPIClient: FPB_ERROR \u2192 %r\", status)\n    return status\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.FPosBAPIClient.read_err","title":"read_err","text":"<pre><code>read_err() -&gt; str\n</code></pre> <p>Return the current error description via <code>READ_ERR</code>.</p> <p>Returns:</p> Type Description <code>str</code> <p>The raw terminal response line from the server.</p> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def read_err(self) -&gt; str:\n    \"\"\"Return the current error description via ``READ_ERR``.\n\n    Returns:\n        The raw terminal response line from the server.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    lines = self.send_command(\"READ_ERR\")\n    logger.debug(\"FPosBAPIClient: READ_ERR \u2192 %r\", lines[-1])\n    return lines[-1]\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.FPosBAPIClient.err_log","title":"err_log","text":"<pre><code>err_log() -&gt; list[str]\n</code></pre> <p>Return the error log entries via <code>ERR_LOG</code>.</p> <p>The server may return multiple intermediate lines before the terminal SUCCESS line.  All non-terminal lines are returned as the log.</p> <p>Returns:</p> Type Description <code>list[str]</code> <p>List of error log strings (all lines except the terminal status).</p> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def err_log(self) -&gt; list[str]:\n    \"\"\"Return the error log entries via ``ERR_LOG``.\n\n    The server may return multiple intermediate lines before the terminal\n    SUCCESS line.  All non-terminal lines are returned as the log.\n\n    Returns:\n        List of error log strings (all lines except the terminal status).\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    lines = self.send_command(\"ERR_LOG\")\n    log = lines[:-1]  # strip terminal SUCCESS line\n    logger.debug(\"FPosBAPIClient: ERR_LOG returned %d line(s)\", len(log))\n    return log\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.FPosBAPIClient.com_log","title":"com_log","text":"<pre><code>com_log() -&gt; list[str]\n</code></pre> <p>Return the communication log entries via <code>COM_LOG</code>.</p> <p>The server may return multiple intermediate lines before the terminal SUCCESS line.  All non-terminal lines are returned as the log.</p> <p>Returns:</p> Type Description <code>list[str]</code> <p>List of communication log strings (all lines except the terminal status).</p> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def com_log(self) -&gt; list[str]:\n    \"\"\"Return the communication log entries via ``COM_LOG``.\n\n    The server may return multiple intermediate lines before the terminal\n    SUCCESS line.  All non-terminal lines are returned as the log.\n\n    Returns:\n        List of communication log strings (all lines except the terminal status).\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    lines = self.send_command(\"COM_LOG\")\n    log = lines[:-1]  # strip terminal SUCCESS line\n    logger.debug(\"FPosBAPIClient: COM_LOG returned %d line(s)\", len(log))\n    return log\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.FPosBAPIClient.err_desc","title":"err_desc","text":"<pre><code>err_desc() -&gt; str\n</code></pre> <p>Return the error description via <code>ERR_DESC</code>.</p> <p>Returns:</p> Type Description <code>str</code> <p>Raw terminal response line from the server.</p> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def err_desc(self) -&gt; str:\n    \"\"\"Return the error description via ``ERR_DESC``.\n\n    Returns:\n        Raw terminal response line from the server.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    lines = self.send_command(\"ERR_DESC\")\n    logger.debug(\"FPosBAPIClient: ERR_DESC \u2192 %r\", lines[-1])\n    return lines[-1]\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.FPosBAPIClient.get_io","title":"get_io","text":"<pre><code>get_io(io_id: int) -&gt; float\n</code></pre> <p>Return the value of an I/O channel via <code>GET_IO</code>.</p> <p>Response layout::</p> <pre><code>MSG_ID, GET_IO, IO_ID, VALUE, 0, NULL, SUCCESS\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>io_id</code> <code>int</code> <p>I/O channel ID.</p> required <p>Returns:</p> Type Description <code>float</code> <p>Current value of the I/O channel.</p> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> <code>RuntimeError</code> <p>If the response cannot be parsed.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def get_io(self, io_id: int) -&gt; float:\n    \"\"\"Return the value of an I/O channel via ``GET_IO``.\n\n    Response layout::\n\n        MSG_ID, GET_IO, IO_ID, VALUE, 0, NULL, SUCCESS\n\n    Args:\n        io_id: I/O channel ID.\n\n    Returns:\n        Current value of the I/O channel.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n        RuntimeError: If the response cannot be parsed.\n    \"\"\"\n    lines = self.send_command(\"GET_IO\", io_id)\n    fields = [f.strip() for f in lines[-1].split(\",\")]\n    # fields: msg_id, GET_IO, io_id, value, 0, NULL, SUCCESS\n    try:\n        value = float(fields[3])\n    except (IndexError, ValueError) as exc:\n        raise RuntimeError(f\"Failed to parse GET_IO response for io_id={io_id}: {lines!r}\") from exc\n    logger.debug(\"FPosBAPIClient: GET_IO io_id=%d \u2192 %s\", io_id, value)\n    return value\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.FPosBAPIClient.set_io","title":"set_io","text":"<pre><code>set_io(io_id: int, value: float) -&gt; None\n</code></pre> <p>Set the value of an I/O channel via <code>SET_IO</code>.</p> <p>Parameters:</p> Name Type Description Default <code>io_id</code> <code>int</code> <p>I/O channel ID.</p> required <code>value</code> <code>float</code> <p>Value to write to the I/O channel.</p> required <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def set_io(self, io_id: int, value: float) -&gt; None:\n    \"\"\"Set the value of an I/O channel via ``SET_IO``.\n\n    Args:\n        io_id: I/O channel ID.\n        value: Value to write to the I/O channel.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    self.send_command(\"SET_IO\", io_id, value)\n    logger.info(\"FPosBAPIClient: SET_IO io_id=%d value=%s\", io_id, value)\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.FPosBAPIClient.get_par","title":"get_par","text":"<pre><code>get_par(par_id: int) -&gt; list[float]\n</code></pre> <p>Return parameter values for par_id via <code>GET_PAR</code>.</p> <p>The FPosBAPI server returns up to four value fields per parameter (e.g. tray parameters include column count, column offset, row count, and row offset).  Fields that are absent or non-numeric are omitted from the returned list.</p> <p>Response layout::</p> <pre><code>MSG_ID, GET_PAR, PAR_ID, VALUE, VALUE, VALUE, VALUE, 0, NULL, SUCCESS\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>par_id</code> <code>int</code> <p>Parameter ID (see API Rev 5 Parameter ID sheet).</p> required <p>Returns:</p> Type Description <code>list[float]</code> <p>List of up to four float values for the requested parameter.</p> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def get_par(self, par_id: int) -&gt; list[float]:\n    \"\"\"Return parameter values for *par_id* via ``GET_PAR``.\n\n    The FPosBAPI server returns up to four value fields per parameter\n    (e.g. tray parameters include column count, column offset, row count,\n    and row offset).  Fields that are absent or non-numeric are omitted\n    from the returned list.\n\n    Response layout::\n\n        MSG_ID, GET_PAR, PAR_ID, VALUE, VALUE, VALUE, VALUE, 0, NULL, SUCCESS\n\n    Args:\n        par_id: Parameter ID (see API Rev 5 Parameter ID sheet).\n\n    Returns:\n        List of up to four float values for the requested parameter.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    lines = self.send_command(\"GET_PAR\", par_id)\n    fields = [f.strip() for f in lines[-1].split(\",\")]\n    # fields: msg_id, GET_PAR, par_id, v1, v2, v3, v4, 0, NULL, SUCCESS\n    # Value fields are at indices 3-6; trailing error triplet at -3 to -1.\n    value_fields = fields[3:-3]\n    values: list[float] = []\n    for f in value_fields:\n        try:\n            values.append(float(f))\n        except ValueError:\n            break\n    logger.debug(\"FPosBAPIClient: GET_PAR par_id=%d \u2192 %s\", par_id, values)\n    return values\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.FPosBAPIClient.set_par","title":"set_par","text":"<pre><code>set_par(par_id: int, *values: float) -&gt; None\n</code></pre> <p>Write one or more values to parameter par_id via <code>SET_PAR</code>.</p> <p>Parameters:</p> Name Type Description Default <code>par_id</code> <code>int</code> <p>Parameter ID (see API Rev 5 Parameter ID sheet).</p> required <code>*values</code> <code>float</code> <p>Up to four values to write to the parameter.</p> <code>()</code> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def set_par(self, par_id: int, *values: float) -&gt; None:\n    \"\"\"Write one or more values to parameter *par_id* via ``SET_PAR``.\n\n    Args:\n        par_id: Parameter ID (see API Rev 5 Parameter ID sheet).\n        *values: Up to four values to write to the parameter.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    self.send_command(\"SET_PAR\", par_id, *values)\n    logger.info(\"FPosBAPIClient: SET_PAR par_id=%d values=%s\", par_id, values)\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.FPosBAPIClient.teach_tray","title":"teach_tray","text":"<pre><code>teach_tray(\n    tray_id: int, tray_pos: int, tool_id: int = 0\n) -&gt; None\n</code></pre> <p>Save the current axis positions as a tray position via <code>TEACH_TRAY</code>.</p> <p>Parameters:</p> Name Type Description Default <code>tray_id</code> <code>int</code> <p>Tray ID (1-20).</p> required <code>tray_pos</code> <code>int</code> <p>Position within the tray to teach.</p> required <code>tool_id</code> <code>int</code> <p>Tool offset to associate with this position.</p> <code>0</code> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def teach_tray(self, tray_id: int, tray_pos: int, tool_id: int = 0) -&gt; None:\n    \"\"\"Save the current axis positions as a tray position via ``TEACH_TRAY``.\n\n    Args:\n        tray_id: Tray ID (1-20).\n        tray_pos: Position within the tray to teach.\n        tool_id: Tool offset to associate with this position.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    self.send_command(\"TEACH_TRAY\", tray_id, tray_pos, tool_id)\n    logger.info(\"FPosBAPIClient: TEACH_TRAY complete (tray_id=%d, tray_pos=%d)\", tray_id, tray_pos)\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.FPosBAPIClient.write_tray","title":"write_tray","text":"<pre><code>write_tray(\n    tray_id: int,\n    tray_pos: int,\n    abs_a1: float,\n    abs_a2: float,\n    abs_a3: float,\n) -&gt; None\n</code></pre> <p>Write absolute coordinates to a tray position via <code>WRITE_TRAY</code>.</p> <p>Parameters:</p> Name Type Description Default <code>tray_id</code> <code>int</code> <p>Tray ID (1-20).</p> required <code>tray_pos</code> <code>int</code> <p>Position index within the tray.</p> required <code>abs_a1</code> <code>float</code> <p>Absolute X-axis position in mm.</p> required <code>abs_a2</code> <code>float</code> <p>Absolute Y-axis position in mm.</p> required <code>abs_a3</code> <code>float</code> <p>Absolute Z-axis position in mm.</p> required <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def write_tray(\n    self,\n    tray_id: int,\n    tray_pos: int,\n    abs_a1: float,\n    abs_a2: float,\n    abs_a3: float,\n) -&gt; None:\n    \"\"\"Write absolute coordinates to a tray position via ``WRITE_TRAY``.\n\n    Args:\n        tray_id: Tray ID (1-20).\n        tray_pos: Position index within the tray.\n        abs_a1: Absolute X-axis position in mm.\n        abs_a2: Absolute Y-axis position in mm.\n        abs_a3: Absolute Z-axis position in mm.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    self.send_command(\"WRITE_TRAY\", tray_id, tray_pos, abs_a1, abs_a2, abs_a3)\n    logger.info(\"FPosBAPIClient: WRITE_TRAY complete (tray_id=%d, tray_pos=%d)\", tray_id, tray_pos)\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.FPosBAPIClient.read_tray","title":"read_tray","text":"<pre><code>read_tray(\n    tray_id: int, tray_pos: int\n) -&gt; tuple[float, float, float]\n</code></pre> <p>Read the stored coordinates of a tray position via <code>READ_TRAY</code>.</p> <p>Response layout::</p> <pre><code>MSG_ID, READ_TRAY, TRAY_ID, TRAY_POS, ABS_A1, ABS_A2, ABS_A3, 0, NULL, SUCCESS\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>tray_id</code> <code>int</code> <p>Tray ID (1-20).</p> required <code>tray_pos</code> <code>int</code> <p>Position index within the tray.</p> required <p>Returns:</p> Type Description <code>tuple[float, float, float]</code> <p>Tuple of <code>(abs_a1, abs_a2, abs_a3)</code> in mm.</p> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> <code>RuntimeError</code> <p>If the response cannot be parsed.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def read_tray(self, tray_id: int, tray_pos: int) -&gt; tuple[float, float, float]:\n    \"\"\"Read the stored coordinates of a tray position via ``READ_TRAY``.\n\n    Response layout::\n\n        MSG_ID, READ_TRAY, TRAY_ID, TRAY_POS, ABS_A1, ABS_A2, ABS_A3, 0, NULL, SUCCESS\n\n    Args:\n        tray_id: Tray ID (1-20).\n        tray_pos: Position index within the tray.\n\n    Returns:\n        Tuple of ``(abs_a1, abs_a2, abs_a3)`` in mm.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n        RuntimeError: If the response cannot be parsed.\n    \"\"\"\n    lines = self.send_command(\"READ_TRAY\", tray_id, tray_pos)\n    fields = [f.strip() for f in lines[-1].split(\",\")]\n    # fields: msg_id, READ_TRAY, tray_id, tray_pos, abs_a1, abs_a2, abs_a3, 0, NULL, SUCCESS\n    try:\n        result = float(fields[4]), float(fields[5]), float(fields[6])\n    except (IndexError, ValueError) as exc:\n        raise RuntimeError(\n            f\"Failed to parse READ_TRAY response for tray_id={tray_id}, tray_pos={tray_pos}: {lines!r}\"\n        ) from exc\n    logger.debug(\"FPosBAPIClient: READ_TRAY tray_id=%d tray_pos=%d \u2192 %s\", tray_id, tray_pos, result)\n    return result\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.FPosBAPIClient.mod_tray","title":"mod_tray","text":"<pre><code>mod_tray(\n    tray_id: int,\n    tray_pos: int,\n    rel_a1: float,\n    rel_a2: float,\n    rel_a3: float,\n) -&gt; None\n</code></pre> <p>Modify a stored tray position by relative offsets via <code>MOD_TRAY</code>.</p> <p>Parameters:</p> Name Type Description Default <code>tray_id</code> <code>int</code> <p>Tray ID (1-20).</p> required <code>tray_pos</code> <code>int</code> <p>Position index within the tray.</p> required <code>rel_a1</code> <code>float</code> <p>Relative X-axis offset in mm.</p> required <code>rel_a2</code> <code>float</code> <p>Relative Y-axis offset in mm.</p> required <code>rel_a3</code> <code>float</code> <p>Relative Z-axis offset in mm.</p> required <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def mod_tray(\n    self,\n    tray_id: int,\n    tray_pos: int,\n    rel_a1: float,\n    rel_a2: float,\n    rel_a3: float,\n) -&gt; None:\n    \"\"\"Modify a stored tray position by relative offsets via ``MOD_TRAY``.\n\n    Args:\n        tray_id: Tray ID (1-20).\n        tray_pos: Position index within the tray.\n        rel_a1: Relative X-axis offset in mm.\n        rel_a2: Relative Y-axis offset in mm.\n        rel_a3: Relative Z-axis offset in mm.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    self.send_command(\"MOD_TRAY\", tray_id, tray_pos, rel_a1, rel_a2, rel_a3)\n    logger.info(\"FPosBAPIClient: MOD_TRAY complete (tray_id=%d, tray_pos=%d)\", tray_id, tray_pos)\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.FPosBAPIClient.move_tray","title":"move_tray","text":"<pre><code>move_tray(\n    tray_id: int,\n    tray_col: int,\n    tray_row: int,\n    tool_id: int = 0,\n    retract_z: int = 0,\n    slow_app: int = 0,\n) -&gt; None\n</code></pre> <p>Move the gantry to a tray location via <code>MOVE_TRAY</code>.</p> <p>Parameters:</p> Name Type Description Default <code>tray_id</code> <code>int</code> <p>Tray ID (1-20).</p> required <code>tray_col</code> <code>int</code> <p>Target column within the tray.</p> required <code>tray_row</code> <code>int</code> <p>Target row within the tray.</p> required <code>tool_id</code> <code>int</code> <p>Tool offset to apply.  <code>0</code> selects no tool.</p> <code>0</code> <code>retract_z</code> <code>int</code> <p><code>1</code> to retract Z before moving, <code>0</code> to skip.</p> <code>0</code> <code>slow_app</code> <code>int</code> <p><code>1</code> to slow approach the target position, <code>0</code> for full speed.</p> <code>0</code> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def move_tray(\n    self,\n    tray_id: int,\n    tray_col: int,\n    tray_row: int,\n    tool_id: int = 0,\n    retract_z: int = 0,\n    slow_app: int = 0,\n) -&gt; None:\n    \"\"\"Move the gantry to a tray location via ``MOVE_TRAY``.\n\n    Args:\n        tray_id: Tray ID (1-20).\n        tray_col: Target column within the tray.\n        tray_row: Target row within the tray.\n        tool_id: Tool offset to apply.  ``0`` selects no tool.\n        retract_z: ``1`` to retract Z before moving, ``0`` to skip.\n        slow_app: ``1`` to slow approach the target position, ``0`` for\n            full speed.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    self.send_command(\"MOVE_TRAY\", tray_id, tray_col, tray_row, tool_id, retract_z, slow_app)\n    logger.info(\n        \"FPosBAPIClient: MOVE_TRAY complete (tray_id=%d, col=%d, row=%d)\",\n        tray_id,\n        tray_col,\n        tray_row,\n    )\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.FPosBAPIClient.init_sys","title":"init_sys","text":"<pre><code>init_sys() -&gt; None\n</code></pre> <p>Initialize system parameters via <code>INIT_SYS</code>.</p> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def init_sys(self) -&gt; None:\n    \"\"\"Initialize system parameters via ``INIT_SYS``.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    self.send_command(\"INIT_SYS\")\n    logger.info(\"FPosBAPIClient: system initialized\")\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.FPosBAPIClient.close","title":"close","text":"<pre><code>close() -&gt; None\n</code></pre> <p>Close the underlying TCP socket.</p> <p>Safe to call more than once; subsequent calls are no-ops because the OS will already have released the socket descriptor.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def close(self) -&gt; None:\n    \"\"\"Close the underlying TCP socket.\n\n    Safe to call more than once; subsequent calls are no-ops because the\n    OS will already have released the socket descriptor.\n    \"\"\"\n    self._sock.close()\n    logger.info(\"FPosBAPIClient disconnected from %s:%d\", self.ip, self.port)\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.FPosBAPIClientError","title":"FPosBAPIClientError","text":"<p>               Bases: <code>Exception</code></p> <p>Raised when the FPosBAPI server returns an error response or the connection is lost.</p>"},{"location":"autoapi/applied_motion/#applied_motion.FPosBAxis","title":"FPosBAxis","text":"<pre><code>FPosBAxis(name: str, index: int, client: FPosBAPIClient)\n</code></pre> <p>FPosBAPI-backed representation of a single CECC-X gantry axis.</p> <p>Wraps <code>MOVE_AXIS</code>, <code>HOME</code>, and <code>ROB_POS</code> FPosBAPI commands behind the same public interface as <code>EdconAxis</code>.  All socket communication is performed via the shared client instance which must be owned by the parent <code>Gantry</code>.</p> <p>Attributes:</p> Name Type Description <code>name</code> <p>Human-readable axis label, e.g. <code>\"X\"</code>.</p> <code>index</code> <p>1-based axis number sent to <code>MOV_AXIS</code> (<code>1</code>=X, <code>2</code>=Y, <code>3</code>=Z by CECC-X convention).</p> <p>Initialise the axis proxy.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Human-readable axis label used in log messages and equality checks.</p> required <code>index</code> <code>int</code> <p>1-based axis index for <code>MOVE_AXIS</code> commands.  Must match the axis numbering configured in the CECC-X CoDeSys program.</p> required <code>client</code> <code>FPosBAPIClient</code> <p>Shared <code>FPosBAPIClient</code> instance owned by the parent gantry.</p> required Source code in <code>src/applied_motion/backends/fposbapi_axis.py</code> <pre><code>def __init__(self, name: str, index: int, client: FPosBAPIClient) -&gt; None:\n    \"\"\"Initialise the axis proxy.\n\n    Args:\n        name: Human-readable axis label used in log messages and equality\n            checks.\n        index: 1-based axis index for ``MOVE_AXIS`` commands.  Must match\n            the axis numbering configured in the CECC-X CoDeSys program.\n        client: Shared [`FPosBAPIClient`][applied_motion.backends.fposbapi_client.FPosBAPIClient]\n            instance owned by the parent gantry.\n    \"\"\"\n    self.name = name\n    self.index = index\n    self._client = client\n    self.min_position: float = -inf\n    self.max_position: float = inf\n    logger.info(\"FPosBAxis '%s' (index=%d) created\", name, index)\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.FPosBAxis.move","title":"move","text":"<pre><code>move(\n    position: float,\n    velocity: float,\n    timeout: int | None = None,\n    **kwargs,\n) -&gt; bool\n</code></pre> <p>Move this axis to position at velocity.</p> <p>Sets the global gantry speed (parameter 103) to velocity before issuing the <code>MOVE_AXIS</code> command.  The call blocks until the PLC reports <code>SUCCESS</code>.</p> <p>Parameters:</p> Name Type Description Default <code>position</code> <code>float</code> <p>Target position in mm.  Interpreted as absolute by default; pass <code>position_type=\"relative\"</code> in kwargs for a relative displacement.</p> required <code>velocity</code> <code>float</code> <p>Move speed in mm/s.  Written to PLC parameter 103 (global speed) before the move.</p> required <code>timeout</code> <code>int | None</code> <p>Optional per-command socket timeout in seconds for the <code>MOVE_AXIS</code> request.  <code>None</code> means blocking mode for this request.</p> <code>None</code> <code>**kwargs</code> <p>Optional keyword overrides.  Recognised keys:</p> <ul> <li><code>position_type</code> (<code>str</code>): <code>\"absolute\"</code> (default) or   <code>\"relative\"</code>.</li> </ul> <code>{}</code> <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> when both the speed parameter and move commands succeed;</p> <code>bool</code> <p><code>False</code> if the PLC rejects either command.</p> <p>Raises:</p> Type Description <code>~FPosBAPICommunicationError</code> <p>If a communication or protocol failure occurs.</p> Source code in <code>src/applied_motion/backends/fposbapi_axis.py</code> <pre><code>def move(self, position: float, velocity: float, timeout: int | None = None, **kwargs) -&gt; bool:\n    \"\"\"Move this axis to *position* at *velocity*.\n\n    Sets the global gantry speed (parameter 103) to *velocity* before\n    issuing the ``MOVE_AXIS`` command.  The call blocks until the PLC\n    reports ``SUCCESS``.\n\n    Args:\n        position: Target position in mm.  Interpreted as absolute by\n            default; pass ``position_type=\"relative\"`` in *kwargs* for a\n            relative displacement.\n        velocity: Move speed in mm/s.  Written to PLC parameter 103\n            (global speed) before the move.\n        timeout: Optional per-command socket timeout in seconds for the\n            ``MOVE_AXIS`` request.  ``None`` means blocking mode for this\n            request.\n        **kwargs: Optional keyword overrides.  Recognised keys:\n\n            - ``position_type`` (``str``): ``\"absolute\"`` (default) or\n              ``\"relative\"``.\n\n    Returns:\n        ``True`` when both the speed parameter and move commands succeed;\n        ``False`` if the PLC rejects either command.\n\n    Raises:\n        ~applied_motion.backends.fposbapi_client.FPosBAPICommunicationError: If a\n            communication or protocol failure occurs.\n    \"\"\"\n    position_type = kwargs.get(\"position_type\", \"absolute\")\n    rel_flag = 0 if position_type == \"absolute\" else 1\n    logger.debug(\n        \"FPosBAxis '%s': move position=%s velocity=%s rel_flag=%d\",\n        self.name,\n        position,\n        velocity,\n        rel_flag,\n    )\n    if not self._client.try_command(\"SET_PAR\", 103, velocity):\n        return False\n    result = self._client.try_command(\"MOVE_AXIS\", self.index, rel_flag, position, timeout=timeout)\n    if result:\n        logger.info(\n            \"FPosBAxis '%s': move complete (position=%s mm, velocity=%s mm/s)\",\n            self.name,\n            position,\n            velocity,\n        )\n    return result\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.FPosBAxis.home","title":"home","text":"<pre><code>home() -&gt; bool\n</code></pre> <p>Issue the <code>HOME</code> command, homing all axes together.</p> <p>The FPosBAPI <code>HOME</code> command references every axis simultaneously \u2014 there is no per-axis home.  Calling this method on any individual proxy triggers a full multi-axis homing sequence.</p> <p>.. note::     <code>Gantry.home</code> sends a single     <code>HOME</code> command at the gantry level and does not call this method     on each proxy individually, avoiding duplicate <code>HOME</code> requests.</p> <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> when the PLC reports the HOME command succeeded; <code>False</code></p> <code>bool</code> <p>if the PLC rejects it.</p> <p>Raises:</p> Type Description <code>~FPosBAPICommunicationError</code> <p>If a communication or protocol failure occurs.</p> Source code in <code>src/applied_motion/backends/fposbapi_axis.py</code> <pre><code>def home(self) -&gt; bool:\n    \"\"\"Issue the ``HOME`` command, homing all axes together.\n\n    The FPosBAPI ``HOME`` command references every axis simultaneously \u2014\n    there is no per-axis home.  Calling this method on any individual\n    proxy triggers a full multi-axis homing sequence.\n\n    .. note::\n        [`Gantry.home`][applied_motion.applied_motion.Gantry.home] sends a single\n        ``HOME`` command at the gantry level and does not call this method\n        on each proxy individually, avoiding duplicate ``HOME`` requests.\n\n    Returns:\n        ``True`` when the PLC reports the HOME command succeeded; ``False``\n        if the PLC rejects it.\n\n    Raises:\n        ~applied_motion.backends.fposbapi_client.FPosBAPICommunicationError: If a\n            communication or protocol failure occurs.\n    \"\"\"\n    logger.info(\"FPosBAxis '%s': issuing HOME command (homes all axes)\", self.name)\n    return self._client.try_command(\"HOME\", timeout=None)\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.FPosBAxis.get_current_axis_position","title":"get_current_axis_position","text":"<pre><code>get_current_axis_position() -&gt; float\n</code></pre> <p>Return this axis's current position in mm via <code>ROB_POS</code>.</p> <p>Sends <code>ROB_POS</code> and extracts this axis's coordinate from the response.  Assumes the response layout::</p> <pre><code>msg_id, ROB_POS, x_mm, y_mm, z_mm, 0, NULL, SUCCESS\n</code></pre> <p>where the position field for axis n is at <code>fields[n + 1]</code> (1-based field numbering after stripping the leading <code>msg_id</code> and <code>ROB_POS</code> label fields).</p> <p>Returns:</p> Type Description <code>float</code> <p>Current axis position in mm.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>If the response cannot be parsed.</p> <code>~FPosBAPIClientError</code> <p>If the PLC returns an error response.</p> Source code in <code>src/applied_motion/backends/fposbapi_axis.py</code> <pre><code>def get_current_axis_position(self) -&gt; float:\n    \"\"\"Return this axis's current position in mm via ``ROB_POS``.\n\n    Sends ``ROB_POS`` and extracts this axis's coordinate from the\n    response.  Assumes the response layout::\n\n        msg_id, ROB_POS, x_mm, y_mm, z_mm, 0, NULL, SUCCESS\n\n    where the position field for axis *n* is at ``fields[n + 1]``\n    (1-based field numbering after stripping the leading ``msg_id``\n    and ``ROB_POS`` label fields).\n\n    Returns:\n        Current axis position in mm.\n\n    Raises:\n        RuntimeError: If the response cannot be parsed.\n        ~applied_motion.backends.fposbapi_client.FPosBAPIClientError: If the\n            PLC returns an error response.\n    \"\"\"\n    response = self._client.send_command(\"ROB_POS\")\n    fields = [\n        f.strip() for f in response[-1].split(\",\")\n    ]  # TODO: Make this a function in the client that is passed in here\n    # fields[0] = msg_id, fields[1] = \"ROB_POS\", fields[2] = X, fields[3] = Y, fields[4] = Z\n    position_field_index = self.index + 1\n    try:\n        value = float(fields[position_field_index])\n    except (IndexError, ValueError) as exc:\n        logger.exception(\n            \"FPosBAxis '%s': cannot parse ROB_POS response \u2014 %s\",\n            self.name,\n            response,\n        )\n        raise RuntimeError(\n            f\"Failed to parse ROB_POS position for axis '{self.name}' \"\n            f\"(field index {position_field_index}): {response!r}\"\n        ) from exc\n    logger.debug(\"FPosBAxis '%s': current position = %s mm\", self.name, value)\n    return value\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.FPosBAxis.is_homed","title":"is_homed","text":"<pre><code>is_homed() -&gt; bool\n</code></pre> <p>Return whether the gantry has been homed via <code>IS_HOME</code>.</p> <p>Sends <code>IS_HOME</code> and interprets the response field that follows the echoed command name.  The PLC returns <code>1</code> when homing is complete and <code>0</code> when it is not.</p> <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> if the PLC reports the gantry is homed; <code>False</code></p> <code>bool</code> <p>otherwise.</p> <p>Raises:</p> Type Description <code>~FPosBAPIClientError</code> <p>If the PLC returns an error response.</p> <code>RuntimeError</code> <p>If the response cannot be parsed.</p> Source code in <code>src/applied_motion/backends/fposbapi_axis.py</code> <pre><code>def is_homed(self) -&gt; bool:\n    \"\"\"Return whether the gantry has been homed via ``IS_HOME``.\n\n    Sends ``IS_HOME`` and interprets the response field that follows the\n    echoed command name.  The PLC returns ``1`` when homing is complete\n    and ``0`` when it is not.\n\n    Returns:\n        ``True`` if the PLC reports the gantry is homed; ``False``\n        otherwise.\n\n    Raises:\n        ~applied_motion.backends.fposbapi_client.FPosBAPIClientError: If the\n            PLC returns an error response.\n        RuntimeError: If the response cannot be parsed.\n    \"\"\"\n    response = self._client.send_command(\"IS_HOME\")\n    fields = [f.strip() for f in response[-1].split(\",\")]\n    # fields: msg_id, IS_HOME, value, ERR_ID, ERR_TYPE, SUCCESS\n    try:\n        homed = bool(int(fields[2]))\n    except (IndexError, ValueError) as exc:\n        raise RuntimeError(f\"Failed to parse IS_HOME response for axis '{self.name}': {response!r}\") from exc\n    logger.debug(\"FPosBAxis '%s': is_homed=%s\", self.name, homed)\n    return homed\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.FPosBAxis.stopped","title":"stopped","text":"<pre><code>stopped() -&gt; bool\n</code></pre> <p>Return <code>True</code>; FPosBAPI moves are blocking so motion is always complete on return.</p> <p><code>move</code> blocks until the PLC emits a <code>SUCCESS</code> response, meaning by the time Python regains control the axis has stopped. This method always returns <code>True</code> to satisfy the <code>Axis</code> interface.</p> <p>Returns:</p> Type Description <code>bool</code> <p>Always <code>True</code>.</p> Source code in <code>src/applied_motion/backends/fposbapi_axis.py</code> <pre><code>def stopped(self) -&gt; bool:\n    \"\"\"Return ``True``; FPosBAPI moves are blocking so motion is always complete on return.\n\n    [`move`][applied_motion.backends.fposbapi_axis.FPosBAxis.move] blocks until the PLC emits a ``SUCCESS`` response,\n    meaning by the time Python regains control the axis has stopped.\n    This method always returns ``True`` to satisfy the\n    [`Axis`][applied_motion.backends.axis_protocol.Axis] interface.\n\n    Returns:\n        Always ``True``.\n    \"\"\"\n    return True\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.FPosBAxis.acknowledge_faults","title":"acknowledge_faults","text":"<pre><code>acknowledge_faults()\n</code></pre> <p>Clear errors.</p> Source code in <code>src/applied_motion/backends/fposbapi_axis.py</code> <pre><code>def acknowledge_faults(self):\n    \"\"\"Clear errors.\"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.FPosBAxis.enable_powerstage","title":"enable_powerstage","text":"<pre><code>enable_powerstage()\n</code></pre> <p>Turn on torque to motor controlling this axis.</p> Source code in <code>src/applied_motion/backends/fposbapi_axis.py</code> <pre><code>def enable_powerstage(self):\n    \"\"\"Turn on torque to motor controlling this axis.\"\"\"\n    # TODO: Implement this functionality using corresponding FPosBAPI command\n    pass\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.FPosBAxis.disable_powerstage","title":"disable_powerstage","text":"<pre><code>disable_powerstage()\n</code></pre> <p>Turn off torque to motor controlling this axis.</p> Source code in <code>src/applied_motion/backends/fposbapi_axis.py</code> <pre><code>def disable_powerstage(self):\n    \"\"\"Turn off torque to motor controlling this axis.\"\"\"\n    # TODO: Implement this functionality using corresponding FPosBAPI command\n    pass\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.FPosBAxis.current_position","title":"current_position","text":"<pre><code>current_position()\n</code></pre> <p>Return current position.</p> Source code in <code>src/applied_motion/backends/fposbapi_axis.py</code> <pre><code>def current_position(self):\n    \"\"\"Return current position.\"\"\"\n    # TODO: Implement this functionality using corresponding FPosBAPI command\n    pass\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.FPosBAxis.current_velocity","title":"current_velocity","text":"<pre><code>current_velocity()\n</code></pre> <p>Return current velocity.</p> Source code in <code>src/applied_motion/backends/fposbapi_axis.py</code> <pre><code>def current_velocity(self):\n    \"\"\"Return current velocity.\"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.FPosBAxis.jog_task","title":"jog_task","text":"<pre><code>jog_task(\n    jog_positive: bool = True,\n    jog_negative: bool = False,\n    incremental: bool = False,\n    duration: float = 0.0,\n) -&gt; bool\n</code></pre> <p>Jog axis.</p> Source code in <code>src/applied_motion/backends/fposbapi_axis.py</code> <pre><code>def jog_task(\n    self,\n    jog_positive: bool = True,\n    jog_negative: bool = False,\n    incremental: bool = False,\n    duration: float = 0.0,\n) -&gt; bool:\n    \"\"\"Jog axis.\"\"\"\n    # TODO: Implement this functionality using corresponding FPosBAPI command\n    return True\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.FPosBAxis.ready_for_motion","title":"ready_for_motion","text":"<pre><code>ready_for_motion() -&gt; bool\n</code></pre> <p>Return whether the gantry drives are enabled via <code>IS_ENBL</code>.</p> <p>Sends <code>IS_ENBL</code> and interprets the response.  The PLC returns <code>1</code> when drives are enabled and ready to accept motion commands, <code>0</code> when they are disabled.</p> <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> if the PLC reports drives are enabled; <code>False</code></p> <code>bool</code> <p>otherwise.</p> <p>Raises:</p> Type Description <code>~FPosBAPIClientError</code> <p>If the PLC returns an error response.</p> <code>RuntimeError</code> <p>If the response cannot be parsed.</p> Source code in <code>src/applied_motion/backends/fposbapi_axis.py</code> <pre><code>def ready_for_motion(self) -&gt; bool:\n    \"\"\"Return whether the gantry drives are enabled via ``IS_ENBL``.\n\n    Sends ``IS_ENBL`` and interprets the response.  The PLC returns\n    ``1`` when drives are enabled and ready to accept motion commands,\n    ``0`` when they are disabled.\n\n    Returns:\n        ``True`` if the PLC reports drives are enabled; ``False``\n        otherwise.\n\n    Raises:\n        ~applied_motion.backends.fposbapi_client.FPosBAPIClientError: If the\n            PLC returns an error response.\n        RuntimeError: If the response cannot be parsed.\n    \"\"\"\n    response = self._client.send_command(\"IS_ENBL\")\n    fields = [f.strip() for f in response[-1].split(\",\")]\n    # fields: msg_id, IS_ENBL, value, ERR_ID, ERR_TYPE, SUCCESS\n    try:\n        enabled = bool(int(fields[2]))\n    except (IndexError, ValueError) as exc:\n        raise RuntimeError(f\"Failed to parse IS_ENBL response for axis '{self.name}': {response!r}\") from exc\n    logger.debug(\"FPosBAxis '%s': ready_for_motion=%s\", self.name, enabled)\n    return enabled\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.EdconAxis","title":"EdconAxis","text":"<pre><code>EdconAxis(\n    name: str,\n    ip: str,\n    run_referencing: bool = False,\n    max_position: float = inf,\n    min_position: float = -inf,\n)\n</code></pre> <p>               Bases: <code>MotionHandler</code></p> <p>Modbus TCP axis: direct festo-edcon <code>MotionHandler</code> connection to one drive.</p> <p>Wraps low-level Modbus communication and position/velocity unit conversion behind a millimetre-based public API.  All position values accepted and returned by the public methods use millimetres (mm) as their unit.</p> <p>Attributes:</p> Name Type Description <code>name</code> <p>Human-readable label for this axis (e.g. <code>\"X\"</code>).</p> <code>ip</code> <p>IPv4 address of the drive's Modbus TCP endpoint.</p> <code>com</code> <p>Active <code>ComModbus</code> connection.</p> <code>max_speed</code> <code>float</code> <p>Maximum achievable speed in drive velocity units.</p> <code>max_position</code> <code>float</code> <p>Maximum achievable position in mm.</p> <code>min_position</code> <code>float</code> <p>Minimum achievable position in mm.</p> <p>Initialise the axis and establish a Modbus connection.</p> <p>Creates the <code>ComModbus</code> connection, initialises the parent <code>MotionHandler</code>, reads the software-limit PNUs, and optionally triggers homing.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Human-readable axis label used in log messages and equality checks.  Axis labels are unique within a gantry.</p> required <code>ip</code> <code>str</code> <p>IPv4 address of the Festo drive's Modbus TCP interface.</p> required <code>run_referencing</code> <code>bool</code> <p>When <code>True</code>, perform a homing (referencing) sequence during construction.  Defaults to <code>False</code>.</p> <code>False</code> <code>max_position</code> <code>float</code> <p>Optional max position limitation, if the drive is restricted more than the internal SW limit position check. This avoids an issue where traversing toward the actual endstop is interrupted because the interia of the motion causes the drive to overshoot the limit position, throwing an error and interrupting the power stage on state.</p> <code>inf</code> <code>min_position</code> <code>float</code> <p>Optional min position limitation, if the drive is restricted more than the internal SW limit position check. This avoids an issue where traversing toward the actual endstop is interrupted because the interia of the motion causes the drive to overshoot the limit position, throwing an error and interrupting the power stage on state.</p> <code>-inf</code> Source code in <code>src/applied_motion/backends/edcon_axis.py</code> <pre><code>def __init__(\n    self,\n    name: str,\n    ip: str,\n    run_referencing: bool = False,\n    max_position: float = inf,\n    min_position: float = -inf,\n) -&gt; None:\n    \"\"\"Initialise the axis and establish a Modbus connection.\n\n    Creates the `ComModbus` connection,\n    initialises the parent `MotionHandler`,\n    reads the software-limit PNUs, and optionally triggers homing.\n\n    Args:\n        name: Human-readable axis label used in log messages and equality\n            checks.  Axis labels are unique within a gantry.\n        ip: IPv4 address of the Festo drive's Modbus TCP interface.\n        run_referencing: When ``True``, perform a homing (referencing)\n            sequence during construction.  Defaults to ``False``.\n        max_position: Optional max position limitation, if the drive is restricted\n            more than the internal SW limit position check. This avoids an issue where\n            traversing toward the actual endstop is interrupted because the interia of\n            the motion causes the drive to overshoot the limit position, throwing an error\n            and interrupting the power stage on state.\n        min_position: Optional min position limitation, if the drive is restricted\n            more than the internal SW limit position check. This avoids an issue where\n            traversing toward the actual endstop is interrupted because the interia of\n            the motion causes the drive to overshoot the limit position, throwing an error\n            and interrupting the power stage on state.\n    \"\"\"\n    self.name = name\n    self.ip = ip\n    self.com = ComModbus(self.ip)\n    super().__init__(self.com)\n    self.acknowledge_faults()\n\n    self.acknowledge_faults()\n    # self.home()\n    logger.info(\"Axis '%s': initialized\", self.name)\n    self.configure_software_limit_switch(True)\n    logger.info(\"Axis '%s': software limit switch configured\", self.name)\n\n    self._neg_sw_limit: int = self.com.read_pnu(834)\n    self._pos_sw_limit: int = self.com.read_pnu(835)\n\n    self._min_vel: int = self.com.read_pnu(11212)\n    self._max_vel: int = self.com.read_pnu(11213)\n    self._pos_scale_exp: int = self.com.read_pnu(11724)\n    self._vel_scale_exp: int = self.com.read_pnu(11725)\n    self.input_pos_unit = {\"distance\": {\"unit\": \"m\", \"power\": 1, \"power_of_ten\": -3}}\n    self.input_vel_unit = {\n        \"distance\": {\"unit\": \"m\", \"power\": 1, \"power_of_ten\": -3},\n        \"time\": {\"unit\": \"s\", \"power\": -1, \"power_of_ten\": 1},\n    }\n    self.max_position = -1 + min(\n        max_position,\n        self._valid_position(self._pos_sw_limit, self.input_pos_unit, invert=True),\n    )  # TODO: Fix overshooting following error in drive control\n    self.min_position = 1 + max(\n        min_position,\n        self._valid_position(self._neg_sw_limit, self.input_pos_unit, invert=True),\n    )  # TODO: Fix overshooting following error in drive control\n    self.max_velocity = self._valid_velocity(\n        self._max_vel, self.input_vel_unit, invert=True\n    )  # TODO: Get these from config, compare with SW limits and take most restrictive superset\n    self.min_velocity = self._valid_velocity(\n        self._min_vel, self.input_vel_unit, invert=True\n    )  # TODO: Get these from config, compare with SW limits and take most restrictive superset\n\n    logger.info(\n        \"Axis '%s': SW limits loaded \u2014 neg=%s, pos=%s (drive units)\",\n        self.name,\n        self._neg_sw_limit,\n        self._pos_sw_limit,\n    )\n    logger.info(\n        \"Axis '%s': SW limits loaded \u2014 neg=%s, pos=%s (standard units)\",\n        self.name,\n        self.min_position,\n        self.max_position,\n    )\n\n    if self.fault_present():\n        logger.warning(\"Axis '%s': fault present on init\", self.name)\n    logger.debug(\"Axis '%s' fault string: %s\", self.name, self.fault_string())\n    logger.debug(\"Axis '%s' current fault code: %s\", self.name, self.current_fault_code())\n    self.acknowledge_faults()\n    self.enable_powerstage()\n\n    if run_referencing:\n        logger.info(\"Axis '%s': referencing requested during initialization\", self.name)\n        self.home()\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.EdconAxis.max_speed","title":"max_speed  <code>property</code>","text":"<pre><code>max_speed: float\n</code></pre> <p>Return largest allowed speed magnitude for this axis in mm/s.</p> <p>This derives from configured signed velocity bounds and returns absolute maximum magnitude, independent of direction.</p> <p>Returns:</p> Type Description <code>float</code> <p>Maximum absolute velocity limit in mm/s.</p>"},{"location":"autoapi/applied_motion/#applied_motion.EdconAxis.home","title":"home","text":"<pre><code>home() -&gt; bool\n</code></pre> <p>Home the axis by running a referencing task.</p> <p>Acknowledges any faults, enables the power stage, and then executes a blocking referencing task to establish the axis zero point.</p> Source code in <code>src/applied_motion/backends/edcon_axis.py</code> <pre><code>def home(self) -&gt; bool:\n    \"\"\"Home the axis by running a referencing task.\n\n    Acknowledges any faults, enables the power stage, and then\n    executes a blocking referencing task to establish the axis zero\n    point.\n    \"\"\"\n    logger.info(\"Axis '%s': starting homing sequence\", self.name)\n    self.acknowledge_faults()\n    self.enable_powerstage()\n    res = self.referencing_task(nonblocking=False)\n\n    logger.info(\"Axis '%s': homing result=%s\", self.name, res)\n    logger.info(\"Axis '%s': homing complete\", self.name)\n    return res\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.EdconAxis.is_homed","title":"is_homed","text":"<pre><code>is_homed() -&gt; bool\n</code></pre> <p>Checks whether drive has been homed and has a valid frame of reference.</p> <p>Thin wrapper for interface consistency around <code>MotionHandler.referenced()</code>.</p> Source code in <code>src/applied_motion/backends/edcon_axis.py</code> <pre><code>def is_homed(self) -&gt; bool:\n    \"\"\"Checks whether drive has been homed and has a valid frame of reference.\n\n    Thin wrapper for interface consistency around `MotionHandler.referenced()`.\n\n    \"\"\"\n    return self.referenced()\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.EdconAxis.current_position","title":"current_position","text":"<pre><code>current_position()\n</code></pre> <p>Transparent wrapper around <code>MotionHandler.current_position</code>.</p>"},{"location":"autoapi/applied_motion/#applied_motion.EdconAxis.current_position--todo-is-this-necessary","title":"TODO: is this necessary?","text":"<p>Returns the raw position value in the drive's internal unit system (drive units, whose scale is determined by PNU 11724 and varies by firmware/configuration \u2014 typically 0.001 mm per unit).  Use <code>get_current_axis_position</code> when you need the position in a consistent, human-readable unit (mm).</p> Source code in <code>src/applied_motion/backends/edcon_axis.py</code> <pre><code>def current_position(self):\n    \"\"\"Transparent wrapper around ``MotionHandler.current_position``.\n\n    # TODO: is this necessary?\n\n    Returns the raw position value in the drive's **internal unit system**\n    (drive units, whose scale is determined by PNU 11724 and varies by\n    firmware/configuration \u2014 typically 0.001 mm per unit).  Use\n    [`get_current_axis_position`][applied_motion.backends.edcon_axis.EdconAxis.get_current_axis_position] when you need the position in a\n    consistent, human-readable unit (mm).\n    \"\"\"\n    logger.warning(\n        \"Axis '%s': current_position() returns a raw drive-unit value \u2014 \"\n        \"unit depends on drive configuration (PNU 11724). \"\n        \"Call get_current_axis_position() for a consistent mm value.\",\n        self.name,\n    )\n    return super().current_position()\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.EdconAxis.get_current_axis_position","title":"get_current_axis_position","text":"<pre><code>get_current_axis_position() -&gt; float\n</code></pre> <p>Return the current axis position in the library's canonical output unit (mm).</p> <p>Reads the raw drive-unit position from <code>MotionHandler.current_position</code>, then converts it to mm via <code>_valid_position</code> with <code>invert=True</code>. The drive's own PNU-reported position-unit scale is used for the conversion, so the result is always consistent regardless of how the drive was configured.</p> <p>Internal methods (<code>_check_overshoot</code>, unit converters) continue to use <code>super().current_position()</code> directly so their drive-unit arithmetic is unaffected.</p> Source code in <code>src/applied_motion/backends/edcon_axis.py</code> <pre><code>def get_current_axis_position(self) -&gt; float:\n    \"\"\"Return the current axis position in the library's canonical output unit (mm).\n\n    Reads the raw drive-unit position from ``MotionHandler.current_position``,\n    then converts it to mm via `_valid_position` with ``invert=True``.\n    The drive's own PNU-reported position-unit scale is used for the\n    conversion, so the result is always consistent regardless of how the\n    drive was configured.\n\n    Internal methods (`_check_overshoot`, unit converters) continue to\n    use ``super().current_position()`` directly so their drive-unit arithmetic\n    is unaffected.\n    \"\"\"\n    _mm_unit = {\"distance\": {\"unit\": \"m\", \"power\": 1, \"power_of_ten\": -3}}\n    return self._valid_position(super().current_position(), _mm_unit, invert=True)\n</code></pre>"},{"location":"autoapi/applied_motion/#applied_motion.EdconAxis.move","title":"move","text":"<pre><code>move(\n    position: int | float,\n    velocity: int | float,\n    timeout: int | None = None,\n    **kwargs,\n) -&gt; bool\n</code></pre> <p>Move the axis to a specified position with a given velocity.</p> <p>Parameters:</p> Name Type Description Default <code>position</code> <code>int | float</code> <p>Target position in millimetres (mm).  Converted internally to drive units before the motion command is issued.</p> required <code>velocity</code> <code>int | float</code> <p>Move velocity in mm/s.  Converted internally to drive units.</p> required <code>timeout</code> <code>int | None</code> <p>Optional time limit in seconds for the move.  When provided, the motion is launched in a background thread and a stop command is issued if the thread has not finished by the deadline.  When <code>None</code> (default), the call blocks until motion completes.</p> <code>None</code> <code>**kwargs</code> <p>Optional keyword overrides.  Recognised keys:</p> <ul> <li><code>position_type</code> (<code>str</code>): <code>\"absolute\"</code> for an absolute   target position or <code>\"relative\"</code> for a relative displacement.   When omitted the move is treated as absolute.</li> </ul> <code>{}</code> <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> if the motion task succeeded, <code>False</code> otherwise.</p> Source code in <code>src/applied_motion/backends/edcon_axis.py</code> <pre><code>def move(self, position: int | float, velocity: int | float, timeout: int | None = None, **kwargs) -&gt; bool:\n    \"\"\"Move the axis to a specified position with a given velocity.\n\n    Args:\n        position: Target position in millimetres (mm).  Converted internally\n            to drive units before the motion command is issued.\n        velocity: Move velocity in mm/s.  Converted internally to drive units.\n        timeout: Optional time limit in seconds for the move.  When provided,\n            the motion is launched in a background thread and a stop command\n            is issued if the thread has not finished by the deadline.  When\n            ``None`` (default), the call blocks until motion completes.\n        **kwargs: Optional keyword overrides.  Recognised keys:\n\n            - ``position_type`` (``str``): ``\"absolute\"`` for an absolute\n              target position or ``\"relative\"`` for a relative displacement.\n              When omitted the move is treated as **absolute**.\n\n    Returns:\n        ``True`` if the motion task succeeded, ``False`` otherwise.\n    \"\"\"\n    # TODO: check valid move parameters against internal set values\n    # e.g. Position limit, velocity limit, acceleration limit, etc.\n    # if not valid, raise exception\n    pos_type = kwargs.get(\"position_type\", \"absolute\")\n    logger.debug(\n        \"Axis '%s': entering move \u2014 raw position=%s  raw velocity=%s raw timeout=%s raw position_type=%s\",\n        self.name,\n        position,\n        velocity,\n        timeout,\n        pos_type,\n    )\n    positioning_type = pos_type == \"absolute\"\n\n    clamped_position = self._clamped_target(position, positioning_type)\n    logger.debug(\"Axis '%s': processing move \u2014 clamped_position=%s\", self.name, clamped_position)\n    try:\n        validated_position = int(self._valid_position(clamped_position, self.input_pos_unit))\n    except Exception:\n        logger.exception(\"Axis '%s': failed to validate position\", self.name)\n        validated_position = -5  # TODO: This seems bad and wrong\n\n    try:\n        validated_velocity = int(self._valid_velocity(velocity, self.input_vel_unit))\n    except Exception:\n        logger.exception(\"Axis '%s': failed to validate velocity\", self.name)\n        validated_velocity = 5  # TODO: This seems bad and wrong\n    logger.debug(\n        \"Axis '%s': processing move \u2014 validated_position=%s  validated_velocity=%s\",\n        self.name,\n        validated_position,\n        validated_velocity,\n    )\n    # self.configure_continuous_update(True) # continous update so that new position tasks can be started while still in motion\n    #\n    # TODO: Check faults and abort if critical fault encountered\n\n    # self.acknowledge_faults()\n\n    logger.debug(\n        \"Axis '%s': fault_string=%s  fault_code=%s\", self.name, self.fault_string(), self.current_fault_code()\n    )\n    validated_position = self._check_overshoot(validated_position, absolute=positioning_type)\n    logger.debug(\n        \"Axis '%s': clamped_position=%s  validated_velocity=%s\", self.name, validated_position, validated_velocity\n    )\n    # Skip if already at target (absolute) or zero delta (relative).\n    at_target = (\n        abs(validated_position - int(super().current_position())) == 0\n        if positioning_type\n        else abs(validated_position) == 0\n    )\n    if at_target:\n        logger.info(\"Axis '%s': already at target drive position %s, skipping move\", self.name, validated_position)\n        return True\n    result: bool = False\n    # TODO: Check powerstage enabled, attempt enable if not\n    # TODO:\n    # TODO: Check for CRITICAL error stages,\n    # TODO: ack faults,\n    # TODO: then move.\n    # TODO: Incorporate jog mode into this function?\n    self.acknowledge_faults()\n    self.enable_powerstage()\n    if timeout is None:\n        result = self.position_task(\n            validated_position,\n            validated_velocity,\n            absolute=positioning_type,\n            nonblocking=False,\n        )\n    else:\n        _result_box: list[bool] = []\n        move_thread = Thread(\n            target=lambda: _result_box.append(\n                self.position_task(\n                    validated_position,\n                    validated_velocity,\n                    absolute=positioning_type,\n                    nonblocking=False,\n                )\n            )\n        )\n        move_thread.start()\n        move_thread.join(timeout=timeout)\n        if move_thread.is_alive():\n            logger.warning(\"Axis '%s': move timed out after %ss \u2014 sending stop motion task\", self.name, timeout)\n            self.stop_motion_task()\n            # Wait for the thread to actually exit before returning; 50 ms is not enough.\n            move_thread.join(timeout=timeout)\n            if move_thread.is_alive():\n                logger.error(\n                    f\"Axis '%s': position_task thread still alive {timeout}s after stop command\", self.name\n                )\n        else:\n            result = _result_box[0] if _result_box else False  # TODO: AllTrue?\n\n    logger.info(\"Axis '%s': motion task complete, result=%s\", self.name, result)\n    return result\n</code></pre>"},{"location":"autoapi/applied_motion/applied_motion/","title":"applied_motion","text":""},{"location":"autoapi/applied_motion/applied_motion/#applied_motion.applied_motion","title":"applied_motion","text":"<p>Festo gantry axis and multi-axis gantry abstractions.</p> <p>This module provides <code>Gantry</code>, which coordinates one or more axes for sequential or concurrent positioning.</p> <p>Pass <code>config=...</code> to <code>Gantry</code> to instantiate the correct backend automatically from a JSON configuration dict or file. <code>Gantry.from_config</code> is a convenience wrapper around that constructor path.</p>"},{"location":"autoapi/applied_motion/applied_motion/#applied_motion.applied_motion.KinematicParams","title":"KinematicParams","text":"<p>               Bases: <code>_OptionalKinematicParams</code></p> <p>Kinematic parameters accepted by <code>Axis.move</code>.</p>"},{"location":"autoapi/applied_motion/applied_motion/#applied_motion.applied_motion.AxisStatus","title":"AxisStatus","text":"<p>               Bases: <code>TypedDict</code></p> <p>Per-axis status payload returned by <code>Gantry.get_status</code>.</p>"},{"location":"autoapi/applied_motion/applied_motion/#applied_motion.applied_motion.ControllerStatus","title":"ControllerStatus","text":"<p>               Bases: <code>TypedDict</code></p> <p>Controller diagnostics payload returned by <code>Gantry.get_status</code>.</p>"},{"location":"autoapi/applied_motion/applied_motion/#applied_motion.applied_motion.GantryStatusSummary","title":"GantryStatusSummary","text":"<p>               Bases: <code>TypedDict</code></p> <p>Aggregate gantry health values returned by <code>Gantry.get_status</code>.</p>"},{"location":"autoapi/applied_motion/applied_motion/#applied_motion.applied_motion.GantryStatus","title":"GantryStatus","text":"<p>               Bases: <code>TypedDict</code></p> <p>Top-level status payload returned by <code>Gantry.get_status</code>.</p>"},{"location":"autoapi/applied_motion/applied_motion/#applied_motion.applied_motion.MovementError","title":"MovementError","text":"<p>               Bases: <code>Exception</code></p> <p>Raised when a gantry or axis movement fails.</p>"},{"location":"autoapi/applied_motion/applied_motion/#applied_motion.applied_motion.AxisNotFoundError","title":"AxisNotFoundError","text":"<p>               Bases: <code>MovementError</code></p> <p>Raised when an axis name referenced in a movement command does not exist.</p>"},{"location":"autoapi/applied_motion/applied_motion/#applied_motion.applied_motion.Gantry","title":"Gantry","text":"<pre><code>Gantry(\n    axes: AxisMap | None = None,\n    concurrent_axes: AxisMap | None = None,\n    *,\n    config: dict | str | Path | None = None,\n    name: str = \"gantry_1\",\n    _backend: GantryBackend | None = None,\n)\n</code></pre> <p>Coordinate multiple axis objects as a single gantry.</p> <p>Dispatches sequential or concurrent move commands to the individual axes and provides convenience methods for homing, status queries, and position readback.</p> <p>Attributes:</p> Name Type Description <code>axes</code> <p>Mapping of axis name \u2192 axis instance for all registered axes.</p> <code>concurrent_axes</code> <p>Optional mapping of axes that may move simultaneously. When <code>None</code>, no concurrent grouping is applied.</p> <p>Initialise the gantry with the provided axis mapping.</p> <p>When <code>config</code> is provided, this constructor selects the correct backend (Modbus or FPosBAPI) and creates axes automatically from a JSON configuration dict or file.</p> <p>Parameters:</p> Name Type Description Default <code>axes</code> <code>AxisMap | None</code> <p>Dict mapping axis names to axis instances.  Accepts both <code>EdconAxis</code> (Modbus backend) and <code>FPosBAxis</code> (FPosBAPI backend) \u2014 any object satisfying <code>Axis</code>.</p> <code>None</code> <code>concurrent_axes</code> <code>AxisMap | None</code> <p>Optional dict of axes that are allowed to move simultaneously.  Pass <code>None</code> (default) to disable concurrent grouping.</p> <code>None</code> <code>config</code> <code>dict | str | Path | None</code> <p>Optional configuration dict or JSON file path. When provided, the gantry builds axes and backend from config and ignores <code>axes</code> / <code>concurrent_axes</code>.</p> <code>None</code> <code>name</code> <code>str</code> <p>Gantry component name to load from <code>config</code>.</p> <code>'gantry_1'</code> <code>_backend</code> <code>GantryBackend | None</code> <p>Internal backend strategy object that owns backend- specific gantry behavior.</p> <code>None</code> Source code in <code>src/applied_motion/applied_motion.py</code> <pre><code>def __init__(\n    self,\n    axes: AxisMap | None = None,\n    concurrent_axes: AxisMap | None = None,\n    *,\n    config: dict | str | Path | None = None,\n    name: str = \"gantry_1\",\n    _backend: GantryBackend | None = None,\n) -&gt; None:\n    \"\"\"Initialise the gantry with the provided axis mapping.\n\n    When ``config`` is provided, this constructor selects the correct\n    backend (Modbus or FPosBAPI) and creates axes automatically from a JSON\n    configuration dict or file.\n\n    Args:\n        axes: Dict mapping axis names to axis instances.  Accepts both\n            [`EdconAxis`][applied_motion.backends.edcon_axis.EdconAxis] (Modbus backend) and\n            [`FPosBAxis`][applied_motion.backends.fposbapi_axis.FPosBAxis]\n            (FPosBAPI backend) \u2014 any object satisfying\n            [`Axis`][applied_motion.backends.axis_protocol.Axis].\n        concurrent_axes: Optional dict of axes that are allowed to move\n            simultaneously.  Pass ``None`` (default) to disable concurrent\n            grouping.\n        config: Optional configuration dict or JSON file path. When\n            provided, the gantry builds axes and backend from config and\n            ignores ``axes`` / ``concurrent_axes``.\n        name: Gantry component name to load from ``config``.\n        _backend: Internal backend strategy object that owns backend-\n            specific gantry behavior.\n    \"\"\"\n    if config is not None:\n        if axes is not None or concurrent_axes is not None or _backend is not None:\n            raise ValueError(\"Pass either config or axes/concurrent_axes, not both\")\n        build = build_gantry_from_config(config, name)\n        axes = build.axes\n        concurrent_axes = build.concurrent_axes\n        _backend = build.backend\n\n    if axes is None:\n        raise ValueError(\"axes must be provided when config is not supplied\")\n\n    self.axes = axes\n    self.concurrent_axes = concurrent_axes\n    self._backend = _backend\n    logger.info(\"Gantry: initialized with axes=%s\", list(axes.keys()))\n    if concurrent_axes:\n        logger.debug(\"Gantry: concurrent axes=%s\", list(concurrent_axes.keys()))\n</code></pre>"},{"location":"autoapi/applied_motion/applied_motion/#applied_motion.applied_motion.Gantry.from_config","title":"from_config  <code>classmethod</code>","text":"<pre><code>from_config(\n    config: dict | str | Path, name: str = \"gantry_1\"\n) -&gt; Gantry\n</code></pre> <p>Instantiate a <code>Gantry</code> from a configuration dict or JSON file.</p> <p>Parameters:</p> Name Type Description Default <code>config</code> <code>dict | str | Path</code> <p>Parsed configuration mapping or JSON file path.</p> required <code>name</code> <code>str</code> <p>Component name to load from config.</p> <code>'gantry_1'</code> <p>Returns:</p> Type Description <code>Gantry</code> <p>Initialised <code>Gantry</code> instance.</p> Source code in <code>src/applied_motion/applied_motion.py</code> <pre><code>@classmethod\ndef from_config(cls, config: dict | str | Path, name: str = \"gantry_1\") -&gt; \"Gantry\":\n    \"\"\"Instantiate a [`Gantry`][applied_motion.applied_motion.Gantry] from a configuration dict or JSON file.\n\n    Args:\n        config: Parsed configuration mapping or JSON file path.\n        name: Component name to load from config.\n\n    Returns:\n        Initialised [`Gantry`][applied_motion.applied_motion.Gantry] instance.\n    \"\"\"\n    return cls(config=config, name=name)\n</code></pre>"},{"location":"autoapi/applied_motion/applied_motion/#applied_motion.applied_motion.Gantry.move_to","title":"move_to","text":"<pre><code>move_to(\n    movements: MovementBatch,\n    timeout: int | None = None,\n    concurrent: bool = False,\n) -&gt; None\n</code></pre> <p>Dispatch a queue of movements to the gantry axes.</p> <p>Processes each movement dict in movements, dispatching them either sequentially or concurrently according to concurrent and the gantry's <code>concurrent_axes</code> configuration.</p> <p>Parameters:</p> Name Type Description Default <code>movements</code> <code>MovementBatch</code> <p>A <code>deque</code> of movement dicts.  Each dict maps a single axis name to its kinematic parameters, e.g. <code>{\"X\": {\"position\": 100.0, \"velocity\": 50.0}}</code>.</p> required <code>timeout</code> <code>int | None</code> <p>Optional per-move time limit in seconds.  Passed through to each <code>EdconAxis.move</code> call.</p> <code>None</code> <code>concurrent</code> <code>bool</code> <p>When <code>True</code>, all movements in the current batch are dispatched in parallel threads.  When <code>False</code> (default), movements are grouped using <code>concurrent_axes</code> and dispatched sequentially.</p> <code>False</code> Notes <p>Axis references are validated before dispatch. Malformed movement entries or entries for unknown axes are logged and skipped.</p> Source code in <code>src/applied_motion/applied_motion.py</code> <pre><code>def move_to(\n    self,\n    movements: MovementBatch,\n    timeout: int | None = None,\n    concurrent: bool = False,\n) -&gt; None:\n    \"\"\"Dispatch a queue of movements to the gantry axes.\n\n    Processes each movement dict in *movements*, dispatching them either\n    sequentially or concurrently according to *concurrent* and the\n    gantry's ``concurrent_axes`` configuration.\n\n    Args:\n        movements: A `deque` of movement dicts.  Each\n            dict maps a single axis name to its kinematic parameters, e.g.\n            ``{\"X\": {\"position\": 100.0, \"velocity\": 50.0}}``.\n        timeout: Optional per-move time limit in seconds.  Passed through\n            to each [`EdconAxis.move`][applied_motion.backends.edcon_axis.EdconAxis.move] call.\n        concurrent: When ``True``, all movements in the current batch are\n            dispatched in parallel threads.  When ``False`` (default),\n            movements are grouped using ``concurrent_axes`` and dispatched\n            sequentially.\n\n    Notes:\n        Axis references are validated before dispatch. Malformed movement\n        entries or entries for unknown axes are logged and skipped.\n    \"\"\"\n    logger.info(\"Gantry.move_to: queued=%d concurrent=%s timeout=%s\", len(movements), concurrent, timeout)\n\n    valid_movements, validated_axis_names = self._collect_valid_movements(movements)\n    if not valid_movements:\n        logger.warning(\"Gantry.move_to: no valid movement entries to dispatch\")\n        return\n\n    skipped_count = len(movements) - len(valid_movements)\n    if skipped_count &gt; 0:\n        logger.warning(\"Gantry.move_to: skipped %d invalid movement entrie(s)\", skipped_count)\n\n    if concurrent:\n        results = self._move_dispatch(valid_movements, concurrent=concurrent, timeout=timeout)\n        self._log_move_results(validated_axis_names, results, concurrent=True)\n        return\n    else:\n        concurrent_axes = self.concurrent_axes or {}\n        while valid_movements:\n            next_moves = self._get_next_moves(valid_movements, concurrent_axes)\n            axis_names = tuple(next(iter(movement)) for movement in next_moves)\n            results = self._move_dispatch(next_moves, concurrent=True, timeout=timeout)\n            self._log_move_results(axis_names, results, concurrent=False)\n</code></pre>"},{"location":"autoapi/applied_motion/applied_motion/#applied_motion.applied_motion.Gantry.home","title":"home","text":"<pre><code>home() -&gt; None\n</code></pre> <p>Home all registered axes.</p> <p>For the FPosBAPI backend, sends a single <code>HOME</code> command to the CECC-X PLC which homes all axes in a coordinated sequence.</p> <p>For the Modbus backend, iterates over every axis in insertion order and calls <code>Axis.home</code> on each one sequentially.</p> Source code in <code>src/applied_motion/applied_motion.py</code> <pre><code>def home(self) -&gt; None:\n    \"\"\"Home all registered axes.\n\n    For the **FPosBAPI backend**, sends a single ``HOME`` command to the\n    CECC-X PLC which homes all axes in a coordinated sequence.\n\n    For the **Modbus backend**, iterates over every axis in insertion\n    order and calls [`Axis.home`][applied_motion.backends.axis_protocol.Axis.home] on each one sequentially.\n    \"\"\"\n    if self._backend is None:\n        for axis in self.axes.values():\n            axis.home()\n    else:\n        self._backend.home(self.axes)\n    logger.info(\"Gantry.home: complete\")\n</code></pre>"},{"location":"autoapi/applied_motion/applied_motion/#applied_motion.applied_motion.Gantry.close","title":"close","text":"<pre><code>close() -&gt; None\n</code></pre> <p>Close backend-owned resources.</p> <p>Safe to call multiple times.</p> Source code in <code>src/applied_motion/applied_motion.py</code> <pre><code>def close(self) -&gt; None:\n    \"\"\"Close backend-owned resources.\n\n    Safe to call multiple times.\n    \"\"\"\n    if self._backend is not None:\n        self._backend.close()\n</code></pre>"},{"location":"autoapi/applied_motion/applied_motion/#applied_motion.applied_motion.Gantry.supports_teach","title":"supports_teach","text":"<pre><code>supports_teach() -&gt; bool\n</code></pre> <p>Return whether PLC teaching commands are supported by this backend.</p> Source code in <code>src/applied_motion/applied_motion.py</code> <pre><code>def supports_teach(self) -&gt; bool:\n    \"\"\"Return whether PLC teaching commands are supported by this backend.\"\"\"\n    if self._backend is None:\n        return False\n    return self._backend.supports_teach()\n</code></pre>"},{"location":"autoapi/applied_motion/applied_motion/#applied_motion.applied_motion.Gantry.teach_pos","title":"teach_pos","text":"<pre><code>teach_pos(pos_id: int) -&gt; None\n</code></pre> <p>Teach current location into PLC position slot.</p> <p>Parameters:</p> Name Type Description Default <code>pos_id</code> <code>int</code> <p>PLC position slot ID.</p> required <p>Raises:</p> Type Description <code>NotImplementedError</code> <p>If backend does not support PLC teaching.</p> Source code in <code>src/applied_motion/applied_motion.py</code> <pre><code>def teach_pos(self, pos_id: int) -&gt; None:\n    \"\"\"Teach current location into PLC position slot.\n\n    Args:\n        pos_id: PLC position slot ID.\n\n    Raises:\n        NotImplementedError: If backend does not support PLC teaching.\n    \"\"\"\n    if self._backend is None:\n        raise NotImplementedError(\"teach_pos is only available for FPosBAPI backend\")\n    self._backend.teach_pos(pos_id)\n</code></pre>"},{"location":"autoapi/applied_motion/applied_motion/#applied_motion.applied_motion.Gantry.teach_tray","title":"teach_tray","text":"<pre><code>teach_tray(tray_id: int, tray_pos: int) -&gt; None\n</code></pre> <p>Teach current location into PLC tray slot.</p> <p>Parameters:</p> Name Type Description Default <code>tray_id</code> <code>int</code> <p>Tray ID.</p> required <code>tray_pos</code> <code>int</code> <p>Position index within tray.</p> required <p>Raises:</p> Type Description <code>NotImplementedError</code> <p>If backend does not support PLC teaching.</p> Source code in <code>src/applied_motion/applied_motion.py</code> <pre><code>def teach_tray(self, tray_id: int, tray_pos: int) -&gt; None:\n    \"\"\"Teach current location into PLC tray slot.\n\n    Args:\n        tray_id: Tray ID.\n        tray_pos: Position index within tray.\n\n    Raises:\n        NotImplementedError: If backend does not support PLC teaching.\n    \"\"\"\n    if self._backend is None:\n        raise NotImplementedError(\"teach_tray is only available for FPosBAPI backend\")\n    self._backend.teach_tray(tray_id, tray_pos)\n</code></pre>"},{"location":"autoapi/applied_motion/applied_motion/#applied_motion.applied_motion.Gantry.list_commands","title":"list_commands","text":"<pre><code>list_commands() -&gt; list[str]\n</code></pre> <p>Return backend command list when available.</p> Source code in <code>src/applied_motion/applied_motion.py</code> <pre><code>def list_commands(self) -&gt; list[str]:\n    \"\"\"Return backend command list when available.\"\"\"\n    if self._backend is None:\n        return []\n    return self._backend.list_commands()\n</code></pre>"},{"location":"autoapi/applied_motion/applied_motion/#applied_motion.applied_motion.Gantry.get_status","title":"get_status","text":"<pre><code>get_status() -&gt; GantryStatus\n</code></pre> <p>Return a comprehensive status snapshot for the gantry.</p> <p>The returned mapping combines per-axis health, aggregate summary booleans, and (when available) controller-level diagnostics exposed by the backend's shared client.</p> <p>Returns:</p> Type Description <code>GantryStatus</code> <p>A dict with keys:</p> <code>GantryStatus</code> <ul> <li><code>backend</code>: Backend class name.</li> </ul> <code>GantryStatus</code> <ul> <li><code>supports_teach</code>: Whether backend supports TEACH commands.</li> </ul> <code>GantryStatus</code> <ul> <li><code>axes</code>: Mapping of axis name to status details.</li> </ul> <code>GantryStatus</code> <ul> <li><code>summary</code>: Aggregate booleans and counts.</li> </ul> <code>GantryStatus</code> <ul> <li><code>controller</code>: PLC/controller diagnostics (for FPosBAPI).</li> </ul> Source code in <code>src/applied_motion/applied_motion.py</code> <pre><code>def get_status(self) -&gt; GantryStatus:\n    \"\"\"Return a comprehensive status snapshot for the gantry.\n\n    The returned mapping combines per-axis health, aggregate summary\n    booleans, and (when available) controller-level diagnostics exposed\n    by the backend's shared client.\n\n    Returns:\n        A dict with keys:\n\n        - ``backend``: Backend class name.\n        - ``supports_teach``: Whether backend supports TEACH commands.\n        - ``axes``: Mapping of axis name to status details.\n        - ``summary``: Aggregate booleans and counts.\n        - ``controller``: PLC/controller diagnostics (for FPosBAPI).\n    \"\"\"\n    axis_statuses = {axis_name: self._collect_axis_status(axis_name, axis) for axis_name, axis in self.axes.items()}\n    controller_status = self._collect_controller_status()\n    summary = self._build_status_summary(axis_statuses, controller_status)\n\n    status: GantryStatus = {\n        \"backend\": \"ModbusGantryBackend\" if self._backend is None else type(self._backend).__name__,\n        \"supports_teach\": self.supports_teach(),\n        \"axes\": axis_statuses,\n        \"summary\": summary,\n        \"controller\": controller_status,\n    }\n    logger.debug(\"Gantry.get_status: %s\", status)\n    return status\n</code></pre>"},{"location":"autoapi/applied_motion/applied_motion/#applied_motion.applied_motion.Gantry.get_location","title":"get_location","text":"<pre><code>get_location() -&gt; dict[str, float]\n</code></pre> <p>Return the current position of every axis in millimetres.</p> <p>Calls <code>EdconAxis.get_current_axis_position</code> for each registered axis and returns the results as a dict keyed by axis name.</p> <p>Returns:</p> Type Description <code>dict[str, float]</code> <p>Mapping of axis name \u2192 current position in mm.</p> Source code in <code>src/applied_motion/applied_motion.py</code> <pre><code>def get_location(self) -&gt; dict[str, float]:\n    \"\"\"Return the current position of every axis in millimetres.\n\n    Calls [`EdconAxis.get_current_axis_position`][applied_motion.backends.edcon_axis.EdconAxis.get_current_axis_position] for each registered\n    axis and returns the results as a dict keyed by axis name.\n\n    Returns:\n        Mapping of axis name \u2192 current position in mm.\n    \"\"\"\n    coordinates = {axis: self.axes[axis].get_current_axis_position() for axis in self.axes}\n    logger.debug(\"Gantry.get_location: %s\", coordinates)\n    return coordinates\n</code></pre>"},{"location":"autoapi/applied_motion/applied_motion/#applied_motion.applied_motion.Gantry.is_stopped","title":"is_stopped","text":"<pre><code>is_stopped() -&gt; bool\n</code></pre> <p>Return True when every registered axis has stopped moving.</p> <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> if <code>stopped</code> returns <code>True</code> for all axes; <code>False</code> if any axis is still in motion.</p> Source code in <code>src/applied_motion/applied_motion.py</code> <pre><code>def is_stopped(self) -&gt; bool:\n    \"\"\"Return True when every registered axis has stopped moving.\n\n    Returns:\n        ``True`` if `stopped`\n            returns ``True`` for all axes; ``False`` if any axis is still in motion.\n    \"\"\"\n    return all(axis.stopped() for axis in self.axes.values())\n</code></pre>"},{"location":"autoapi/applied_motion/applied_motion/#applied_motion.applied_motion.Gantry.is_ready_for_motion","title":"is_ready_for_motion","text":"<pre><code>is_ready_for_motion() -&gt; bool\n</code></pre> <p>Return True when every registered axis is ready to accept a move command.</p> <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> if <code>ready_for_motion</code> returns <code>True</code> for all axes; <code>False</code> otherwise.</p> Source code in <code>src/applied_motion/applied_motion.py</code> <pre><code>def is_ready_for_motion(self) -&gt; bool:\n    \"\"\"Return True when every registered axis is ready to accept a move command.\n\n    Returns:\n        ``True`` if `ready_for_motion` returns ``True`` for all axes; ``False`` otherwise.\n    \"\"\"\n    return all(axis.ready_for_motion() for axis in self.axes.values())\n</code></pre>"},{"location":"autoapi/applied_motion/config/","title":"config","text":""},{"location":"autoapi/applied_motion/config/#applied_motion.config","title":"config","text":"<p>Configuration class for loading and validating the configuration for config.</p> <p>Prototype to be move into its own module eventually.</p>"},{"location":"autoapi/applied_motion/config/#applied_motion.config.SystemConfig","title":"SystemConfig","text":"<pre><code>SystemConfig(config: dict | Path)\n</code></pre> <p>               Bases: <code>dict</code></p> <p>Configuration class for Festo modules and subsystems.</p> <p>Used to dynamically instantiate a subsystem running using the Festo Python framework and specify the topology and operating parameters of said system.</p> <p>Load and normalise config from a dict or JSON file path.</p> Source code in <code>src/applied_motion/config.py</code> <pre><code>def __init__(self, config: dict | Path) -&gt; None:\n    \"\"\"Load and normalise *config* from a dict or JSON file path.\"\"\"\n    if isinstance(config, SystemConfig):\n        self.config = config\n    else:\n        config = self._load_config_source(config)\n        logger.debug(\"SystemConfig: loaded config source=%s\", type(config).__name__)\n        parsed_config = self._normalize_config(config)\n        self.config = parsed_config\n</code></pre>"},{"location":"autoapi/applied_motion/config/#applied_motion.config.GantryConfig","title":"GantryConfig","text":"<pre><code>GantryConfig(sys_config: dict, name: str)\n</code></pre> <p>Validated, structured configuration for a single named gantry component.</p> <p>Validate and store gantry config fields from sys_config for component name.</p> Source code in <code>src/applied_motion/config.py</code> <pre><code>def __init__(self, sys_config: dict, name: str) -&gt; None:\n    \"\"\"Validate and store gantry config fields from *sys_config* for component *name*.\"\"\"\n    self.name = name\n    gantry_cfg, backend, axes_cfg, axis_order, concurrent_raw = self._validate_config(sys_config, name)\n    self.backend = backend\n    self.axes_cfg = axes_cfg\n    self.axis_order = axis_order\n    self.concurrent_raw = concurrent_raw\n    self.interface = gantry_cfg.get(\"interface\")\n</code></pre>"},{"location":"autoapi/applied_motion/gantry_factory/","title":"gantry_factory","text":""},{"location":"autoapi/applied_motion/gantry_factory/#applied_motion.gantry_factory","title":"gantry_factory","text":"<p>Factory helpers for building gantry axes and backend state from config.</p>"},{"location":"autoapi/applied_motion/gantry_factory/#applied_motion.gantry_factory.GantryConstruction","title":"GantryConstruction  <code>dataclass</code>","text":"<pre><code>GantryConstruction(\n    axes: AxisMap,\n    concurrent_axes: AxisMap | None,\n    backend: GantryBackend,\n)\n</code></pre> <p>Fully built gantry components returned by config factories.</p> <p>Parameters:</p> Name Type Description Default <code>axes</code> <code>AxisMap</code> <p>Mapping of axis names to constructed axis objects.</p> required <code>concurrent_axes</code> <code>AxisMap | None</code> <p>Optional concurrent movement subset.</p> required <code>backend</code> <code>GantryBackend</code> <p>Backend strategy that owns shared gantry behavior.</p> required"},{"location":"autoapi/applied_motion/gantry_factory/#applied_motion.gantry_factory.build_modbus_gantry","title":"build_modbus_gantry","text":"<pre><code>build_modbus_gantry(\n    config: dict | str | Path, name: str = \"gantry_1\"\n) -&gt; GantryConstruction\n</code></pre> <p>Build Modbus gantry axes and backend from config.</p> <p>Parameters:</p> Name Type Description Default <code>config</code> <code>dict | str | Path</code> <p>Parsed config mapping or JSON file path.</p> required <code>name</code> <code>str</code> <p>Component name to load.</p> <code>'gantry_1'</code> <p>Returns:</p> Type Description <code>GantryConstruction</code> <p>Gantry construction bundle for Modbus backend.</p> Source code in <code>src/applied_motion/gantry_factory.py</code> <pre><code>def build_modbus_gantry(config: dict | str | Path, name: str = \"gantry_1\") -&gt; GantryConstruction:\n    \"\"\"Build Modbus gantry axes and backend from config.\n\n    Args:\n        config: Parsed config mapping or JSON file path.\n        name: Component name to load.\n\n    Returns:\n        Gantry construction bundle for Modbus backend.\n    \"\"\"\n    gcfg = GantryConfig(SystemConfig(_coerce_config_source(config))(), name)\n    return _build_modbus_from_gcfg(gcfg)\n</code></pre>"},{"location":"autoapi/applied_motion/gantry_factory/#applied_motion.gantry_factory.build_fposbapi_gantry","title":"build_fposbapi_gantry","text":"<pre><code>build_fposbapi_gantry(\n    config: dict | str | Path, name: str = \"gantry_1\"\n) -&gt; GantryConstruction\n</code></pre> <p>Build FPosBAPI gantry axes and backend from config.</p> <p>Parameters:</p> Name Type Description Default <code>config</code> <code>dict | str | Path</code> <p>Parsed config mapping or JSON file path.</p> required <code>name</code> <code>str</code> <p>Component name to load.</p> <code>'gantry_1'</code> <p>Returns:</p> Type Description <code>GantryConstruction</code> <p>Gantry construction bundle for FPosBAPI backend.</p> Source code in <code>src/applied_motion/gantry_factory.py</code> <pre><code>def build_fposbapi_gantry(config: dict | str | Path, name: str = \"gantry_1\") -&gt; GantryConstruction:\n    \"\"\"Build FPosBAPI gantry axes and backend from config.\n\n    Args:\n        config: Parsed config mapping or JSON file path.\n        name: Component name to load.\n\n    Returns:\n        Gantry construction bundle for FPosBAPI backend.\n    \"\"\"\n    gcfg = GantryConfig(SystemConfig(_coerce_config_source(config))(), name)\n    return _build_fposbapi_from_gcfg(gcfg)\n</code></pre>"},{"location":"autoapi/applied_motion/gantry_factory/#applied_motion.gantry_factory.build_gantry_from_config","title":"build_gantry_from_config","text":"<pre><code>build_gantry_from_config(\n    config: dict | str | Path, name: str = \"gantry_1\"\n) -&gt; GantryConstruction\n</code></pre> <p>Build gantry components for configured backend.</p> <p>Parameters:</p> Name Type Description Default <code>config</code> <code>dict | str | Path</code> <p>Parsed config mapping or JSON file path.</p> required <code>name</code> <code>str</code> <p>Component name to load.</p> <code>'gantry_1'</code> <p>Returns:</p> Type Description <code>GantryConstruction</code> <p>Gantry construction bundle.</p> Source code in <code>src/applied_motion/gantry_factory.py</code> <pre><code>def build_gantry_from_config(config: dict | str | Path, name: str = \"gantry_1\") -&gt; GantryConstruction:\n    \"\"\"Build gantry components for configured backend.\n\n    Args:\n        config: Parsed config mapping or JSON file path.\n        name: Component name to load.\n\n    Returns:\n        Gantry construction bundle.\n    \"\"\"\n    gcfg = GantryConfig(SystemConfig(_coerce_config_source(config))(), name)\n    if gcfg.backend == \"modbus\":\n        return _build_modbus_from_gcfg(gcfg)\n    return _build_fposbapi_from_gcfg(gcfg)\n</code></pre>"},{"location":"autoapi/applied_motion/backends/","title":"backends","text":""},{"location":"autoapi/applied_motion/backends/#applied_motion.backends","title":"backends","text":"<p>Backend implementations for festo-dev-applied-motion axis communication.</p>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.Axis","title":"Axis","text":"<p>               Bases: <code>Protocol</code></p> <p>Structural interface for a single controllable axis.</p> <p>Both <code>EdconAxis</code> (Modbus/festo-edcon backend) and <code>FPosBAxis</code> (FPosBAPI backend) satisfy this protocol structurally \u2014 no explicit inheritance required.</p> <p>All position values are in millimetres (mm). All velocity values are in mm/s.</p>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.Axis.move","title":"move","text":"<pre><code>move(position: float, velocity: float, **kwargs) -&gt; bool\n</code></pre> <p>Move the axis to position at velocity.</p> <p>Parameters:</p> Name Type Description Default <code>position</code> <code>float</code> <p>Target position in mm (absolute or relative depending on <code>position_type</code> kwarg).</p> required <code>velocity</code> <code>float</code> <p>Move speed in mm/s.</p> required <code>**kwargs</code> <p>Backend-specific options (e.g. <code>position_type</code>, <code>timeout</code>).</p> <code>{}</code> <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> if the move completed successfully.</p> Source code in <code>src/applied_motion/backends/axis_protocol.py</code> <pre><code>def move(self, position: float, velocity: float, **kwargs) -&gt; bool:\n    \"\"\"Move the axis to *position* at *velocity*.\n\n    Args:\n        position: Target position in mm (absolute or relative depending on\n            ``position_type`` kwarg).\n        velocity: Move speed in mm/s.\n        **kwargs: Backend-specific options (e.g. ``position_type``,\n            ``timeout``).\n\n    Returns:\n        ``True`` if the move completed successfully.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.Axis.home","title":"home","text":"<pre><code>home() -&gt; bool\n</code></pre> <p>Run the axis homing/referencing sequence.</p> Source code in <code>src/applied_motion/backends/axis_protocol.py</code> <pre><code>def home(self) -&gt; bool:\n    \"\"\"Run the axis homing/referencing sequence.\"\"\"\n    ...\n</code></pre>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.Axis.get_current_axis_position","title":"get_current_axis_position","text":"<pre><code>get_current_axis_position() -&gt; float\n</code></pre> <p>Return the current axis position in mm.</p> <p>Returns:</p> Type Description <code>float</code> <p>Current position in mm.</p> Source code in <code>src/applied_motion/backends/axis_protocol.py</code> <pre><code>def get_current_axis_position(self) -&gt; float:\n    \"\"\"Return the current axis position in mm.\n\n    Returns:\n        Current position in mm.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.Axis.is_homed","title":"is_homed","text":"<pre><code>is_homed() -&gt; bool\n</code></pre> <p>Return whether this axis reports a valid homed/reference state.</p> <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> if this axis is homed/referenced; <code>False</code> otherwise.</p> Source code in <code>src/applied_motion/backends/axis_protocol.py</code> <pre><code>def is_homed(self) -&gt; bool:\n    \"\"\"Return whether this axis reports a valid homed/reference state.\n\n    Returns:\n        ``True`` if this axis is homed/referenced; ``False`` otherwise.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.Axis.stopped","title":"stopped","text":"<pre><code>stopped() -&gt; bool\n</code></pre> <p>Return <code>True</code> when the axis is not currently in motion.</p> <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> if the axis has stopped; <code>False</code> if motion is ongoing.</p> Source code in <code>src/applied_motion/backends/axis_protocol.py</code> <pre><code>def stopped(self) -&gt; bool:\n    \"\"\"Return ``True`` when the axis is not currently in motion.\n\n    Returns:\n        ``True`` if the axis has stopped; ``False`` if motion is ongoing.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.Axis.acknowledge_faults","title":"acknowledge_faults","text":"<pre><code>acknowledge_faults()\n</code></pre> <p>Clear errors.</p> Source code in <code>src/applied_motion/backends/axis_protocol.py</code> <pre><code>def acknowledge_faults(self):\n    \"\"\"Clear errors.\"\"\"\n    # TODO: This shouldn't be exposed to end-users. This is done automatically for almost every motion task to proceed and is part of an internal interface.\n    ...\n</code></pre>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.Axis.enable_powerstage","title":"enable_powerstage","text":"<pre><code>enable_powerstage()\n</code></pre> <p>Turn on torque to motor controlling this axis.</p> Source code in <code>src/applied_motion/backends/axis_protocol.py</code> <pre><code>def enable_powerstage(self):\n    \"\"\"Turn on torque to motor controlling this axis.\"\"\"\n    # TODO: This shouldn't be exposed to end-users. This is done automatically for almost every motion task to proceed and is part of an internal interface.\n    ...\n</code></pre>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.Axis.disable_powerstage","title":"disable_powerstage","text":"<pre><code>disable_powerstage()\n</code></pre> <p>Turn off torque to motor controlling this axis.</p> Source code in <code>src/applied_motion/backends/axis_protocol.py</code> <pre><code>def disable_powerstage(self):\n    \"\"\"Turn off torque to motor controlling this axis.\"\"\"\n    ...\n</code></pre>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.Axis.current_position","title":"current_position","text":"<pre><code>current_position()\n</code></pre> <p>Return current position.</p> Source code in <code>src/applied_motion/backends/axis_protocol.py</code> <pre><code>def current_position(self):\n    \"\"\"Return current position.\"\"\"\n    ...\n</code></pre>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.Axis.current_velocity","title":"current_velocity","text":"<pre><code>current_velocity()\n</code></pre> <p>Return current velocity.</p> Source code in <code>src/applied_motion/backends/axis_protocol.py</code> <pre><code>def current_velocity(self):\n    \"\"\"Return current velocity.\"\"\"\n    ...\n</code></pre>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.Axis.jog_task","title":"jog_task","text":"<pre><code>jog_task(\n    jog_positive: bool = True,\n    jog_negative: bool = False,\n    incremental: bool = False,\n    duration: float = 0.0,\n) -&gt; bool\n</code></pre> <p>Jog the axis according to backend-specific jog semantics.</p> <p>Parameters:</p> Name Type Description Default <code>jog_positive</code> <code>bool</code> <p>When <code>True</code>, request jog in the positive direction.</p> <code>True</code> <code>jog_negative</code> <code>bool</code> <p>When <code>True</code>, request jog in the negative direction.</p> <code>False</code> <code>incremental</code> <code>bool</code> <p>Backend-specific incremental jog mode flag.</p> <code>False</code> <code>duration</code> <code>float</code> <p>Optional jog duration in seconds. A value of <code>0</code> starts the jog and returns immediately.</p> <code>0.0</code> <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> if the jog command succeeds; <code>False</code> otherwise.</p> Source code in <code>src/applied_motion/backends/axis_protocol.py</code> <pre><code>def jog_task(\n    self,\n    jog_positive: bool = True,\n    jog_negative: bool = False,\n    incremental: bool = False,\n    duration: float = 0.0,\n) -&gt; bool:\n    \"\"\"Jog the axis according to backend-specific jog semantics.\n\n    Args:\n        jog_positive: When ``True``, request jog in the positive direction.\n        jog_negative: When ``True``, request jog in the negative direction.\n        incremental: Backend-specific incremental jog mode flag.\n        duration: Optional jog duration in seconds. A value of ``0``\n            starts the jog and returns immediately.\n\n    Returns:\n        ``True`` if the jog command succeeds; ``False`` otherwise.\n    \"\"\"\n    # TODO: Implement this functionality using corresponding FPosBAPI command\n\n    ...\n</code></pre>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.Axis.ready_for_motion","title":"ready_for_motion","text":"<pre><code>ready_for_motion() -&gt; bool\n</code></pre> <p>Return <code>True</code> when the axis is ready to accept a move command.</p> <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> if the axis is enabled, homed, and fault-free.</p> Source code in <code>src/applied_motion/backends/axis_protocol.py</code> <pre><code>def ready_for_motion(self) -&gt; bool:\n    \"\"\"Return ``True`` when the axis is ready to accept a move command.\n\n    Returns:\n        ``True`` if the axis is enabled, homed, and fault-free.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.FPosBAxis","title":"FPosBAxis","text":"<pre><code>FPosBAxis(name: str, index: int, client: FPosBAPIClient)\n</code></pre> <p>FPosBAPI-backed representation of a single CECC-X gantry axis.</p> <p>Wraps <code>MOVE_AXIS</code>, <code>HOME</code>, and <code>ROB_POS</code> FPosBAPI commands behind the same public interface as <code>EdconAxis</code>.  All socket communication is performed via the shared client instance which must be owned by the parent <code>Gantry</code>.</p> <p>Attributes:</p> Name Type Description <code>name</code> <p>Human-readable axis label, e.g. <code>\"X\"</code>.</p> <code>index</code> <p>1-based axis number sent to <code>MOV_AXIS</code> (<code>1</code>=X, <code>2</code>=Y, <code>3</code>=Z by CECC-X convention).</p> <p>Initialise the axis proxy.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Human-readable axis label used in log messages and equality checks.</p> required <code>index</code> <code>int</code> <p>1-based axis index for <code>MOVE_AXIS</code> commands.  Must match the axis numbering configured in the CECC-X CoDeSys program.</p> required <code>client</code> <code>FPosBAPIClient</code> <p>Shared <code>FPosBAPIClient</code> instance owned by the parent gantry.</p> required Source code in <code>src/applied_motion/backends/fposbapi_axis.py</code> <pre><code>def __init__(self, name: str, index: int, client: FPosBAPIClient) -&gt; None:\n    \"\"\"Initialise the axis proxy.\n\n    Args:\n        name: Human-readable axis label used in log messages and equality\n            checks.\n        index: 1-based axis index for ``MOVE_AXIS`` commands.  Must match\n            the axis numbering configured in the CECC-X CoDeSys program.\n        client: Shared [`FPosBAPIClient`][applied_motion.backends.fposbapi_client.FPosBAPIClient]\n            instance owned by the parent gantry.\n    \"\"\"\n    self.name = name\n    self.index = index\n    self._client = client\n    self.min_position: float = -inf\n    self.max_position: float = inf\n    logger.info(\"FPosBAxis '%s' (index=%d) created\", name, index)\n</code></pre>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.FPosBAxis.move","title":"move","text":"<pre><code>move(\n    position: float,\n    velocity: float,\n    timeout: int | None = None,\n    **kwargs,\n) -&gt; bool\n</code></pre> <p>Move this axis to position at velocity.</p> <p>Sets the global gantry speed (parameter 103) to velocity before issuing the <code>MOVE_AXIS</code> command.  The call blocks until the PLC reports <code>SUCCESS</code>.</p> <p>Parameters:</p> Name Type Description Default <code>position</code> <code>float</code> <p>Target position in mm.  Interpreted as absolute by default; pass <code>position_type=\"relative\"</code> in kwargs for a relative displacement.</p> required <code>velocity</code> <code>float</code> <p>Move speed in mm/s.  Written to PLC parameter 103 (global speed) before the move.</p> required <code>timeout</code> <code>int | None</code> <p>Optional per-command socket timeout in seconds for the <code>MOVE_AXIS</code> request.  <code>None</code> means blocking mode for this request.</p> <code>None</code> <code>**kwargs</code> <p>Optional keyword overrides.  Recognised keys:</p> <ul> <li><code>position_type</code> (<code>str</code>): <code>\"absolute\"</code> (default) or   <code>\"relative\"</code>.</li> </ul> <code>{}</code> <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> when both the speed parameter and move commands succeed;</p> <code>bool</code> <p><code>False</code> if the PLC rejects either command.</p> <p>Raises:</p> Type Description <code>~FPosBAPICommunicationError</code> <p>If a communication or protocol failure occurs.</p> Source code in <code>src/applied_motion/backends/fposbapi_axis.py</code> <pre><code>def move(self, position: float, velocity: float, timeout: int | None = None, **kwargs) -&gt; bool:\n    \"\"\"Move this axis to *position* at *velocity*.\n\n    Sets the global gantry speed (parameter 103) to *velocity* before\n    issuing the ``MOVE_AXIS`` command.  The call blocks until the PLC\n    reports ``SUCCESS``.\n\n    Args:\n        position: Target position in mm.  Interpreted as absolute by\n            default; pass ``position_type=\"relative\"`` in *kwargs* for a\n            relative displacement.\n        velocity: Move speed in mm/s.  Written to PLC parameter 103\n            (global speed) before the move.\n        timeout: Optional per-command socket timeout in seconds for the\n            ``MOVE_AXIS`` request.  ``None`` means blocking mode for this\n            request.\n        **kwargs: Optional keyword overrides.  Recognised keys:\n\n            - ``position_type`` (``str``): ``\"absolute\"`` (default) or\n              ``\"relative\"``.\n\n    Returns:\n        ``True`` when both the speed parameter and move commands succeed;\n        ``False`` if the PLC rejects either command.\n\n    Raises:\n        ~applied_motion.backends.fposbapi_client.FPosBAPICommunicationError: If a\n            communication or protocol failure occurs.\n    \"\"\"\n    position_type = kwargs.get(\"position_type\", \"absolute\")\n    rel_flag = 0 if position_type == \"absolute\" else 1\n    logger.debug(\n        \"FPosBAxis '%s': move position=%s velocity=%s rel_flag=%d\",\n        self.name,\n        position,\n        velocity,\n        rel_flag,\n    )\n    if not self._client.try_command(\"SET_PAR\", 103, velocity):\n        return False\n    result = self._client.try_command(\"MOVE_AXIS\", self.index, rel_flag, position, timeout=timeout)\n    if result:\n        logger.info(\n            \"FPosBAxis '%s': move complete (position=%s mm, velocity=%s mm/s)\",\n            self.name,\n            position,\n            velocity,\n        )\n    return result\n</code></pre>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.FPosBAxis.home","title":"home","text":"<pre><code>home() -&gt; bool\n</code></pre> <p>Issue the <code>HOME</code> command, homing all axes together.</p> <p>The FPosBAPI <code>HOME</code> command references every axis simultaneously \u2014 there is no per-axis home.  Calling this method on any individual proxy triggers a full multi-axis homing sequence.</p> <p>.. note::     <code>Gantry.home</code> sends a single     <code>HOME</code> command at the gantry level and does not call this method     on each proxy individually, avoiding duplicate <code>HOME</code> requests.</p> <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> when the PLC reports the HOME command succeeded; <code>False</code></p> <code>bool</code> <p>if the PLC rejects it.</p> <p>Raises:</p> Type Description <code>~FPosBAPICommunicationError</code> <p>If a communication or protocol failure occurs.</p> Source code in <code>src/applied_motion/backends/fposbapi_axis.py</code> <pre><code>def home(self) -&gt; bool:\n    \"\"\"Issue the ``HOME`` command, homing all axes together.\n\n    The FPosBAPI ``HOME`` command references every axis simultaneously \u2014\n    there is no per-axis home.  Calling this method on any individual\n    proxy triggers a full multi-axis homing sequence.\n\n    .. note::\n        [`Gantry.home`][applied_motion.applied_motion.Gantry.home] sends a single\n        ``HOME`` command at the gantry level and does not call this method\n        on each proxy individually, avoiding duplicate ``HOME`` requests.\n\n    Returns:\n        ``True`` when the PLC reports the HOME command succeeded; ``False``\n        if the PLC rejects it.\n\n    Raises:\n        ~applied_motion.backends.fposbapi_client.FPosBAPICommunicationError: If a\n            communication or protocol failure occurs.\n    \"\"\"\n    logger.info(\"FPosBAxis '%s': issuing HOME command (homes all axes)\", self.name)\n    return self._client.try_command(\"HOME\", timeout=None)\n</code></pre>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.FPosBAxis.get_current_axis_position","title":"get_current_axis_position","text":"<pre><code>get_current_axis_position() -&gt; float\n</code></pre> <p>Return this axis's current position in mm via <code>ROB_POS</code>.</p> <p>Sends <code>ROB_POS</code> and extracts this axis's coordinate from the response.  Assumes the response layout::</p> <pre><code>msg_id, ROB_POS, x_mm, y_mm, z_mm, 0, NULL, SUCCESS\n</code></pre> <p>where the position field for axis n is at <code>fields[n + 1]</code> (1-based field numbering after stripping the leading <code>msg_id</code> and <code>ROB_POS</code> label fields).</p> <p>Returns:</p> Type Description <code>float</code> <p>Current axis position in mm.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>If the response cannot be parsed.</p> <code>~FPosBAPIClientError</code> <p>If the PLC returns an error response.</p> Source code in <code>src/applied_motion/backends/fposbapi_axis.py</code> <pre><code>def get_current_axis_position(self) -&gt; float:\n    \"\"\"Return this axis's current position in mm via ``ROB_POS``.\n\n    Sends ``ROB_POS`` and extracts this axis's coordinate from the\n    response.  Assumes the response layout::\n\n        msg_id, ROB_POS, x_mm, y_mm, z_mm, 0, NULL, SUCCESS\n\n    where the position field for axis *n* is at ``fields[n + 1]``\n    (1-based field numbering after stripping the leading ``msg_id``\n    and ``ROB_POS`` label fields).\n\n    Returns:\n        Current axis position in mm.\n\n    Raises:\n        RuntimeError: If the response cannot be parsed.\n        ~applied_motion.backends.fposbapi_client.FPosBAPIClientError: If the\n            PLC returns an error response.\n    \"\"\"\n    response = self._client.send_command(\"ROB_POS\")\n    fields = [\n        f.strip() for f in response[-1].split(\",\")\n    ]  # TODO: Make this a function in the client that is passed in here\n    # fields[0] = msg_id, fields[1] = \"ROB_POS\", fields[2] = X, fields[3] = Y, fields[4] = Z\n    position_field_index = self.index + 1\n    try:\n        value = float(fields[position_field_index])\n    except (IndexError, ValueError) as exc:\n        logger.exception(\n            \"FPosBAxis '%s': cannot parse ROB_POS response \u2014 %s\",\n            self.name,\n            response,\n        )\n        raise RuntimeError(\n            f\"Failed to parse ROB_POS position for axis '{self.name}' \"\n            f\"(field index {position_field_index}): {response!r}\"\n        ) from exc\n    logger.debug(\"FPosBAxis '%s': current position = %s mm\", self.name, value)\n    return value\n</code></pre>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.FPosBAxis.is_homed","title":"is_homed","text":"<pre><code>is_homed() -&gt; bool\n</code></pre> <p>Return whether the gantry has been homed via <code>IS_HOME</code>.</p> <p>Sends <code>IS_HOME</code> and interprets the response field that follows the echoed command name.  The PLC returns <code>1</code> when homing is complete and <code>0</code> when it is not.</p> <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> if the PLC reports the gantry is homed; <code>False</code></p> <code>bool</code> <p>otherwise.</p> <p>Raises:</p> Type Description <code>~FPosBAPIClientError</code> <p>If the PLC returns an error response.</p> <code>RuntimeError</code> <p>If the response cannot be parsed.</p> Source code in <code>src/applied_motion/backends/fposbapi_axis.py</code> <pre><code>def is_homed(self) -&gt; bool:\n    \"\"\"Return whether the gantry has been homed via ``IS_HOME``.\n\n    Sends ``IS_HOME`` and interprets the response field that follows the\n    echoed command name.  The PLC returns ``1`` when homing is complete\n    and ``0`` when it is not.\n\n    Returns:\n        ``True`` if the PLC reports the gantry is homed; ``False``\n        otherwise.\n\n    Raises:\n        ~applied_motion.backends.fposbapi_client.FPosBAPIClientError: If the\n            PLC returns an error response.\n        RuntimeError: If the response cannot be parsed.\n    \"\"\"\n    response = self._client.send_command(\"IS_HOME\")\n    fields = [f.strip() for f in response[-1].split(\",\")]\n    # fields: msg_id, IS_HOME, value, ERR_ID, ERR_TYPE, SUCCESS\n    try:\n        homed = bool(int(fields[2]))\n    except (IndexError, ValueError) as exc:\n        raise RuntimeError(f\"Failed to parse IS_HOME response for axis '{self.name}': {response!r}\") from exc\n    logger.debug(\"FPosBAxis '%s': is_homed=%s\", self.name, homed)\n    return homed\n</code></pre>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.FPosBAxis.stopped","title":"stopped","text":"<pre><code>stopped() -&gt; bool\n</code></pre> <p>Return <code>True</code>; FPosBAPI moves are blocking so motion is always complete on return.</p> <p><code>move</code> blocks until the PLC emits a <code>SUCCESS</code> response, meaning by the time Python regains control the axis has stopped. This method always returns <code>True</code> to satisfy the <code>Axis</code> interface.</p> <p>Returns:</p> Type Description <code>bool</code> <p>Always <code>True</code>.</p> Source code in <code>src/applied_motion/backends/fposbapi_axis.py</code> <pre><code>def stopped(self) -&gt; bool:\n    \"\"\"Return ``True``; FPosBAPI moves are blocking so motion is always complete on return.\n\n    [`move`][applied_motion.backends.fposbapi_axis.FPosBAxis.move] blocks until the PLC emits a ``SUCCESS`` response,\n    meaning by the time Python regains control the axis has stopped.\n    This method always returns ``True`` to satisfy the\n    [`Axis`][applied_motion.backends.axis_protocol.Axis] interface.\n\n    Returns:\n        Always ``True``.\n    \"\"\"\n    return True\n</code></pre>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.FPosBAxis.acknowledge_faults","title":"acknowledge_faults","text":"<pre><code>acknowledge_faults()\n</code></pre> <p>Clear errors.</p> Source code in <code>src/applied_motion/backends/fposbapi_axis.py</code> <pre><code>def acknowledge_faults(self):\n    \"\"\"Clear errors.\"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.FPosBAxis.enable_powerstage","title":"enable_powerstage","text":"<pre><code>enable_powerstage()\n</code></pre> <p>Turn on torque to motor controlling this axis.</p> Source code in <code>src/applied_motion/backends/fposbapi_axis.py</code> <pre><code>def enable_powerstage(self):\n    \"\"\"Turn on torque to motor controlling this axis.\"\"\"\n    # TODO: Implement this functionality using corresponding FPosBAPI command\n    pass\n</code></pre>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.FPosBAxis.disable_powerstage","title":"disable_powerstage","text":"<pre><code>disable_powerstage()\n</code></pre> <p>Turn off torque to motor controlling this axis.</p> Source code in <code>src/applied_motion/backends/fposbapi_axis.py</code> <pre><code>def disable_powerstage(self):\n    \"\"\"Turn off torque to motor controlling this axis.\"\"\"\n    # TODO: Implement this functionality using corresponding FPosBAPI command\n    pass\n</code></pre>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.FPosBAxis.current_position","title":"current_position","text":"<pre><code>current_position()\n</code></pre> <p>Return current position.</p> Source code in <code>src/applied_motion/backends/fposbapi_axis.py</code> <pre><code>def current_position(self):\n    \"\"\"Return current position.\"\"\"\n    # TODO: Implement this functionality using corresponding FPosBAPI command\n    pass\n</code></pre>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.FPosBAxis.current_velocity","title":"current_velocity","text":"<pre><code>current_velocity()\n</code></pre> <p>Return current velocity.</p> Source code in <code>src/applied_motion/backends/fposbapi_axis.py</code> <pre><code>def current_velocity(self):\n    \"\"\"Return current velocity.\"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.FPosBAxis.jog_task","title":"jog_task","text":"<pre><code>jog_task(\n    jog_positive: bool = True,\n    jog_negative: bool = False,\n    incremental: bool = False,\n    duration: float = 0.0,\n) -&gt; bool\n</code></pre> <p>Jog axis.</p> Source code in <code>src/applied_motion/backends/fposbapi_axis.py</code> <pre><code>def jog_task(\n    self,\n    jog_positive: bool = True,\n    jog_negative: bool = False,\n    incremental: bool = False,\n    duration: float = 0.0,\n) -&gt; bool:\n    \"\"\"Jog axis.\"\"\"\n    # TODO: Implement this functionality using corresponding FPosBAPI command\n    return True\n</code></pre>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.FPosBAxis.ready_for_motion","title":"ready_for_motion","text":"<pre><code>ready_for_motion() -&gt; bool\n</code></pre> <p>Return whether the gantry drives are enabled via <code>IS_ENBL</code>.</p> <p>Sends <code>IS_ENBL</code> and interprets the response.  The PLC returns <code>1</code> when drives are enabled and ready to accept motion commands, <code>0</code> when they are disabled.</p> <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> if the PLC reports drives are enabled; <code>False</code></p> <code>bool</code> <p>otherwise.</p> <p>Raises:</p> Type Description <code>~FPosBAPIClientError</code> <p>If the PLC returns an error response.</p> <code>RuntimeError</code> <p>If the response cannot be parsed.</p> Source code in <code>src/applied_motion/backends/fposbapi_axis.py</code> <pre><code>def ready_for_motion(self) -&gt; bool:\n    \"\"\"Return whether the gantry drives are enabled via ``IS_ENBL``.\n\n    Sends ``IS_ENBL`` and interprets the response.  The PLC returns\n    ``1`` when drives are enabled and ready to accept motion commands,\n    ``0`` when they are disabled.\n\n    Returns:\n        ``True`` if the PLC reports drives are enabled; ``False``\n        otherwise.\n\n    Raises:\n        ~applied_motion.backends.fposbapi_client.FPosBAPIClientError: If the\n            PLC returns an error response.\n        RuntimeError: If the response cannot be parsed.\n    \"\"\"\n    response = self._client.send_command(\"IS_ENBL\")\n    fields = [f.strip() for f in response[-1].split(\",\")]\n    # fields: msg_id, IS_ENBL, value, ERR_ID, ERR_TYPE, SUCCESS\n    try:\n        enabled = bool(int(fields[2]))\n    except (IndexError, ValueError) as exc:\n        raise RuntimeError(f\"Failed to parse IS_ENBL response for axis '{self.name}': {response!r}\") from exc\n    logger.debug(\"FPosBAxis '%s': ready_for_motion=%s\", self.name, enabled)\n    return enabled\n</code></pre>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.FPosBAPIClient","title":"FPosBAPIClient","text":"<pre><code>FPosBAPIClient(\n    ip: str, port: int = 1234, timeout: float | None = None\n)\n</code></pre> <p>Thread-safe TCP socket client for the Festo FPosBAPI ASCII protocol.</p> <p>Connects to the CECC-X PLC on ip:port and wraps the request/response cycle in <code>send_command</code>.  A <code>threading.Lock</code> serialises all socket I/O so multiple <code>FPosBAxis</code> objects sharing the same client do not interleave their frames.</p> <p>Attributes:</p> Name Type Description <code>ip</code> <p>IPv4 address of the CECC-X TCP server.</p> <code>port</code> <p>TCP port of the FPosBAPI server (default <code>1234</code>).</p> <p>Connect to the FPosBAPI server.</p> <p>Parameters:</p> Name Type Description Default <code>ip</code> <code>str</code> <p>IPv4 address of the CECC-X PLC.</p> required <code>port</code> <code>int</code> <p>TCP port the FPosBAPI server is listening on.  Defaults to <code>1234</code>.</p> <code>1234</code> <code>timeout</code> <code>float | None</code> <p>Socket timeout in seconds.  Applied to both <code>connect</code> and <code>recv</code> operations.  <code>None</code> (default) means blocking with no timeout.</p> <code>None</code> <p>Raises:</p> Type Description <code>OSError</code> <p>If the TCP connection cannot be established within timeout seconds.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def __init__(self, ip: str, port: int = 1234, timeout: float | None = None) -&gt; None:\n    \"\"\"Connect to the FPosBAPI server.\n\n    Args:\n        ip: IPv4 address of the CECC-X PLC.\n        port: TCP port the FPosBAPI server is listening on.  Defaults to\n            ``1234``.\n        timeout: Socket timeout in seconds.  Applied to both ``connect``\n            and ``recv`` operations.  ``None`` (default) means blocking\n            with no timeout.\n\n    Raises:\n        OSError: If the TCP connection cannot be established within\n            *timeout* seconds.\n    \"\"\"\n    self.ip = ip\n    self.port = port\n    self._timeout = timeout\n    self._lock = threading.Lock()\n    self._msg_id = 0\n    self._sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)\n\n    logger.info(\"FPosBAPIClient attempting to connect to %s:%d\", ip, port)\n    self._connect()\n    logger.info(\"FPosBAPIClient connected to %s:%d\", ip, port)\n</code></pre>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.FPosBAPIClient.send_command","title":"send_command","text":"<pre><code>send_command(\n    command: str,\n    *params,\n    timeout: float | None | _Unset = _UNSET,\n) -&gt; list[str]\n</code></pre> <p>Send a command to the FPosBAPI server and return all response lines.</p> <p>Acquires the internal lock, increments the message ID, formats the ASCII request frame, sends it, then reads lines via <code>_collect_response</code> until the terminal (non-<code>ACK</code>) line arrives.  The terminal (last) line is validated for correct MSG_ID echo, CMD echo, and <code>SUCCESS</code> status before returning.</p> <p>Parameters:</p> Name Type Description Default <code>command</code> <code>str</code> <p>FPosBAPI command string, e.g. <code>\"ENABLE\"</code>, <code>\"MOVE_AXIS\"</code>.</p> required <code>*params</code> <p>Zero or more positional parameters appended to the frame.</p> <code>()</code> <code>timeout</code> <code>float | None | _Unset</code> <p>Per-call socket timeout in seconds.  Pass <code>None</code> for blocking mode (no timeout), e.g. for long-running motion commands.  Omit to use the client's configured defaults.</p> <code>_UNSET</code> <p>Returns:</p> Type Description <code>list[str]</code> <p>All response lines received before the empty terminator, in arrival</p> <code>list[str]</code> <p>order.  The last element is always the terminal status line echoing</p> <code>list[str]</code> <p>the command and carrying <code>SUCCESS</code>.</p> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error status, the echoed MSG_ID or command name does not match what was sent, the response is empty, or the connection is closed.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def send_command(self, command: str, *params, timeout: float | None | _Unset = _UNSET) -&gt; list[str]:\n    \"\"\"Send a command to the FPosBAPI server and return all response lines.\n\n    Acquires the internal lock, increments the message ID, formats the\n    ASCII request frame, sends it, then reads lines via\n    `_collect_response` until the terminal (non-``ACK``) line\n    arrives.  The terminal (last) line is validated for correct MSG_ID\n    echo, CMD echo, and ``SUCCESS`` status before returning.\n\n    Args:\n        command: FPosBAPI command string, e.g. ``\"ENABLE\"``, ``\"MOVE_AXIS\"``.\n        *params: Zero or more positional parameters appended to the frame.\n        timeout: Per-call socket timeout in seconds.  Pass ``None`` for\n            blocking mode (no timeout), e.g. for long-running motion\n            commands.  Omit to use the client's configured defaults.\n\n    Returns:\n        All response lines received before the empty terminator, in arrival\n        order.  The last element is always the terminal status line echoing\n        the command and carrying ``SUCCESS``.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error status, the\n            echoed MSG_ID or command name does not match what was sent, the\n            response is empty, or the connection is closed.\n    \"\"\"\n    with self._lock:\n        if not isinstance(timeout, _Unset):\n            self._sock.settimeout(timeout)\n        msg_id = self._next_id()\n        raw = self._build_frame(msg_id, command, params)\n        logger.debug(\"FPosBAPIClient tx: %s\", raw.strip())\n        self._send_frame(raw, command, timeout)\n        try:\n            lines = self._collect_response()\n        finally:\n            if not isinstance(timeout, _Unset):\n                self._sock.settimeout(self._timeout)\n        logger.debug(\"FPosBAPIClient rx: %s\", \", \".join(lines))\n        self._validate_terminal(lines, msg_id, command)\n    return lines\n</code></pre>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.FPosBAPIClient.try_command","title":"try_command","text":"<pre><code>try_command(\n    command: str,\n    *params,\n    timeout: float | None | _Unset = _UNSET,\n) -&gt; bool\n</code></pre> <p>Send a command; raise on communication failure, return bool for PLC result.</p> <p>Parameters:</p> Name Type Description Default <code>command</code> <code>str</code> <p>FPosBAPI command string, e.g. <code>\"HOME\"</code>, <code>\"MOVE_AXIS\"</code>.</p> required <code>*params</code> <p>Zero or more positional parameters.</p> <code>()</code> <code>timeout</code> <code>float | None | _Unset</code> <p>Per-call socket timeout forwarded to <code>send_command</code>. Pass <code>None</code> for blocking mode on long-running motion commands.</p> <code>_UNSET</code> <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> when the PLC reports <code>SUCCESS</code>; <code>False</code> when the PLC</p> <code>bool</code> <p>returns a valid but non-SUCCESS response.</p> <p>Raises:</p> Type Description <code>FPosBAPICommunicationError</code> <p>If a transport or protocol-level failure occurs.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def try_command(self, command: str, *params, timeout: float | None | _Unset = _UNSET) -&gt; bool:\n    \"\"\"Send a command; raise on communication failure, return bool for PLC result.\n\n    Args:\n        command: FPosBAPI command string, e.g. ``\"HOME\"``, ``\"MOVE_AXIS\"``.\n        *params: Zero or more positional parameters.\n        timeout: Per-call socket timeout forwarded to [`send_command`][applied_motion.backends.fposbapi_client.FPosBAPIClient.send_command].\n            Pass ``None`` for blocking mode on long-running motion commands.\n\n    Returns:\n        ``True`` when the PLC reports ``SUCCESS``; ``False`` when the PLC\n        returns a valid but non-SUCCESS response.\n\n    Raises:\n        FPosBAPICommunicationError: If a transport or protocol-level failure occurs.\n    \"\"\"\n    try:\n        self.send_command(command, *params, timeout=timeout)\n        return True\n    except FPosBAPICommandError:\n        logger.warning(\"FPosBAPIClient: command %r rejected by PLC\", command)\n        return False\n</code></pre>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.FPosBAPIClient.get_veab","title":"get_veab","text":"<pre><code>get_veab() -&gt; int\n</code></pre> <p>Get the pressure set value of the VEAB.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def get_veab(self) -&gt; int:\n    \"\"\"Get the pressure set value of the VEAB.\"\"\"\n    # TODO: This __REALLY__ needs to be contingent on the config and how it is passed in\n    # TODO: The ideal would be a PressureControl() class that shares the intantiated gantry and can take it in as input parameter. All of these classes would benefit from that bc of necessary shared socket on CECC-X\n\n    lines = self.send_command(\"GET_VEAB\")\n    terminal = lines[-1]\n    fields = [f.strip() for f in terminal.split(\",\")]\n\n    return int(fields[2])\n</code></pre>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.FPosBAPIClient.set_veab","title":"set_veab","text":"<pre><code>set_veab(set_value: int) -&gt; None\n</code></pre> <p>Set the pressure value of the VEAB.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def set_veab(self, set_value: int) -&gt; None:\n    \"\"\"Set the pressure value of the VEAB.\"\"\"\n    self.send_command(\"SET_VEAB\", set_value)\n</code></pre>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.FPosBAPIClient.list_commands","title":"list_commands","text":"<pre><code>list_commands() -&gt; list[str]\n</code></pre> <p>Query the server for its supported command set via <code>CMD_LIST</code>.</p> <p>Sends <code>CMD_LIST</code> and parses the response to extract the command names the connected FPosBAPI server advertises.  The set varies between firmware versions, so callers can use this to feature-detect before issuing optional commands (e.g. <code>IS_HOME</code>, <code>SYS_STATUS</code>).</p> <p>Response layout assumed::</p> <pre><code>MSG_ID, CMD_LIST, CMD_1, CMD_2, ..., CMD_N, ERR_ID, ERR_TYPE, ERR_MSG\n</code></pre> <p>The first two fields (<code>MSG_ID</code> and <code>CMD_LIST</code>) and the last three (error fields ending with <code>SUCCESS</code>) are stripped; the remaining fields are returned as a list of command name strings.</p> <p>Returns:</p> Type Description <code>list[str]</code> <p>List of command name strings advertised by the server,</p> <code>list[str]</code> <p>e.g. <code>[\"ENABLE\", \"DISABLE\", \"HOME\", \"MOVE_AXIS\", ...]</code>.</p> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def list_commands(self) -&gt; list[str]:\n    \"\"\"Query the server for its supported command set via ``CMD_LIST``.\n\n    Sends ``CMD_LIST`` and parses the response to extract the command\n    names the connected FPosBAPI server advertises.  The set varies\n    between firmware versions, so callers can use this to feature-detect\n    before issuing optional commands (e.g. ``IS_HOME``, ``SYS_STATUS``).\n\n    Response layout assumed::\n\n        MSG_ID, CMD_LIST, CMD_1, CMD_2, ..., CMD_N, ERR_ID, ERR_TYPE, ERR_MSG\n\n    The first two fields (``MSG_ID`` and ``CMD_LIST``) and the last\n    three (error fields ending with ``SUCCESS``) are stripped; the\n    remaining fields are returned as a list of command name strings.\n\n    Returns:\n        List of command name strings advertised by the server,\n        e.g. ``[\"ENABLE\", \"DISABLE\", \"HOME\", \"MOVE_AXIS\", ...]``.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    lines = self.send_command(\"CMD_LIST\")\n    # The server sends one bare command name per intermediate line.\n    # lines[0] is the ACK, lines[1:-1] are the bare command names,\n    # lines[-1] is the terminal SUCCESS line.\n    commands = [line.strip() for line in lines[1:-1] if line.strip()]\n    logger.debug(\"FPosBAPIClient: CMD_LIST returned %d command(s): %s\", len(commands), commands)\n    return commands\n</code></pre>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.FPosBAPIClient.enable","title":"enable","text":"<pre><code>enable() -&gt; None\n</code></pre> <p>Enable the gantry drives via <code>ENABLE</code>.</p> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def enable(self) -&gt; None:\n    \"\"\"Enable the gantry drives via ``ENABLE``.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    self.send_command(\"ENABLE\")\n    logger.info(\"FPosBAPIClient: drives enabled\")\n</code></pre>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.FPosBAPIClient.disable","title":"disable","text":"<pre><code>disable() -&gt; None\n</code></pre> <p>Disable the gantry drives via <code>DISABLE</code>.</p> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def disable(self) -&gt; None:\n    \"\"\"Disable the gantry drives via ``DISABLE``.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    self.send_command(\"DISABLE\")\n    logger.info(\"FPosBAPIClient: drives disabled\")\n</code></pre>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.FPosBAPIClient.home","title":"home","text":"<pre><code>home() -&gt; None\n</code></pre> <p>Home all axes via <code>HOME</code>.</p> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def home(self) -&gt; None:\n    \"\"\"Home all axes via ``HOME``.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    self.send_command(\"HOME\")\n    logger.info(\"FPosBAPIClient: homing complete\")\n</code></pre>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.FPosBAPIClient.move_pos","title":"move_pos","text":"<pre><code>move_pos(\n    pos_id: int,\n    tool_id: int = 0,\n    retract_z: int = 0,\n    slow_app: int = 0,\n) -&gt; None\n</code></pre> <p>Execute a motion sequence to a stored position via <code>MOVE_POS</code>.</p> <p>Parameters:</p> Name Type Description Default <code>pos_id</code> <code>int</code> <p>Position record ID (1-100).</p> required <code>tool_id</code> <code>int</code> <p>Tool index to apply.  <code>0</code> selects no tool.</p> <code>0</code> <code>retract_z</code> <code>int</code> <p><code>1</code> to retract Z before moving, <code>0</code> to skip.</p> <code>0</code> <code>slow_app</code> <code>int</code> <p><code>1</code> to slow approach the target position, <code>0</code> for full speed.</p> <code>0</code> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def move_pos(\n    self,\n    pos_id: int,\n    tool_id: int = 0,\n    retract_z: int = 0,\n    slow_app: int = 0,\n) -&gt; None:\n    \"\"\"Execute a motion sequence to a stored position via ``MOVE_POS``.\n\n    Args:\n        pos_id: Position record ID (1-100).\n        tool_id: Tool index to apply.  ``0`` selects no tool.\n        retract_z: ``1`` to retract Z before moving, ``0`` to skip.\n        slow_app: ``1`` to slow approach the target position, ``0`` for\n            full speed.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    self.send_command(\"MOVE_POS\", pos_id, tool_id, retract_z, slow_app)\n    logger.info(\"FPosBAPIClient: MOVE_POS complete (pos_id=%d, tool_id=%d)\", pos_id, tool_id)\n</code></pre>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.FPosBAPIClient.move_loc","title":"move_loc","text":"<pre><code>move_loc(\n    a1: float,\n    a2: float,\n    a3: float,\n    tool_id: int = 0,\n    move_typ: int = 0,\n    retract_z: int = 0,\n    slow_app: int = 0,\n) -&gt; None\n</code></pre> <p>Execute a coordinated 3-axis move to explicit coordinates via <code>MOVE_LOC</code>.</p> <p>Parameters:</p> Name Type Description Default <code>a1</code> <code>float</code> <p>Target X-axis position in mm.</p> required <code>a2</code> <code>float</code> <p>Target Y-axis position in mm.</p> required <code>a3</code> <code>float</code> <p>Target Z-axis position in mm.</p> required <code>tool_id</code> <code>int</code> <p>Tool offset to apply.  <code>0</code> selects no tool.</p> <code>0</code> <code>move_typ</code> <code>int</code> <p><code>0</code> for absolute, <code>1</code> for relative.</p> <code>0</code> <code>retract_z</code> <code>int</code> <p><code>1</code> to retract Z before moving, <code>0</code> to skip.</p> <code>0</code> <code>slow_app</code> <code>int</code> <p><code>1</code> to slow approach the target position, <code>0</code> for full speed.</p> <code>0</code> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def move_loc(\n    self,\n    a1: float,\n    a2: float,\n    a3: float,\n    tool_id: int = 0,\n    move_typ: int = 0,\n    retract_z: int = 0,\n    slow_app: int = 0,\n) -&gt; None:\n    \"\"\"Execute a coordinated 3-axis move to explicit coordinates via ``MOVE_LOC``.\n\n    Args:\n        a1: Target X-axis position in mm.\n        a2: Target Y-axis position in mm.\n        a3: Target Z-axis position in mm.\n        tool_id: Tool offset to apply.  ``0`` selects no tool.\n        move_typ: ``0`` for absolute, ``1`` for relative.\n        retract_z: ``1`` to retract Z before moving, ``0`` to skip.\n        slow_app: ``1`` to slow approach the target position, ``0`` for\n            full speed.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    self.send_command(\"MOVE_LOC\", a1, a2, a3, tool_id, move_typ, retract_z, slow_app)\n    logger.info(\"FPosBAPIClient: MOVE_LOC complete (a1=%s, a2=%s, a3=%s mm)\", a1, a2, a3)\n</code></pre>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.FPosBAPIClient.move_path","title":"move_path","text":"<pre><code>move_path() -&gt; None\n</code></pre> <p>Execute the programmed motion path via <code>MOVE_PATH</code>.</p> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def move_path(self) -&gt; None:\n    \"\"\"Execute the programmed motion path via ``MOVE_PATH``.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    # TODO: Needs WRITE_PATH?\n    self.send_command(\"MOVE_PATH\")\n    logger.info(\"FPosBAPIClient: MOVE_PATH complete\")\n</code></pre>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.FPosBAPIClient.halt","title":"halt","text":"<pre><code>halt() -&gt; None\n</code></pre> <p>Halt the current motion via <code>HALT</code>.</p> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def halt(self) -&gt; None:\n    \"\"\"Halt the current motion via ``HALT``.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    self.send_command(\"HALT\")\n    logger.info(\"FPosBAPIClient: HALT issued\")\n</code></pre>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.FPosBAPIClient.resume","title":"resume","text":"<pre><code>resume() -&gt; None\n</code></pre> <p>Resume a halted motion via <code>RESUME</code>.</p> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def resume(self) -&gt; None:\n    \"\"\"Resume a halted motion via ``RESUME``.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    self.send_command(\"RESUME\")\n    logger.info(\"FPosBAPIClient: RESUME issued\")\n</code></pre>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.FPosBAPIClient.abort","title":"abort","text":"<pre><code>abort() -&gt; None\n</code></pre> <p>Abort the current motion via <code>ABORT</code>.</p> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def abort(self) -&gt; None:\n    \"\"\"Abort the current motion via ``ABORT``.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    self.send_command(\"ABORT\")\n    logger.info(\"FPosBAPIClient: ABORT issued\")\n</code></pre>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.FPosBAPIClient.reset_err","title":"reset_err","text":"<pre><code>reset_err() -&gt; None\n</code></pre> <p>Reset the active error via <code>RESET_ERR</code>.</p> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def reset_err(self) -&gt; None:\n    \"\"\"Reset the active error via ``RESET_ERR``.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    self.send_command(\"RESET_ERR\")\n    logger.info(\"FPosBAPIClient: error reset\")\n</code></pre>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.FPosBAPIClient.open_valve","title":"open_valve","text":"<pre><code>open_valve(\n    v1_time: int = 0,\n    v2_time: int = 0,\n    v3_time: int = 0,\n    v4_time: int = 0,\n    v5_time: int = 0,\n    v6_time: int = 0,\n    v7_time: int = 0,\n    v8_time: int = 0,\n) -&gt; None\n</code></pre> <p>Open one or more valves for the specified durations via <code>OPEN_VALVE</code>.</p> <p>Parameters:</p> Name Type Description Default <code>v1_time</code> <code>int</code> <p>Valve 1 open duration in ms.  <code>0</code> keeps the valve closed.</p> <code>0</code> <code>v2_time</code> <code>int</code> <p>Valve 2 open duration in ms.</p> <code>0</code> <code>v3_time</code> <code>int</code> <p>Valve 3 open duration in ms.</p> <code>0</code> <code>v4_time</code> <code>int</code> <p>Valve 4 open duration in ms.</p> <code>0</code> <code>v5_time</code> <code>int</code> <p>Valve 5 open duration in ms.</p> <code>0</code> <code>v6_time</code> <code>int</code> <p>Valve 6 open duration in ms.</p> <code>0</code> <code>v7_time</code> <code>int</code> <p>Valve 7 open duration in ms.</p> <code>0</code> <code>v8_time</code> <code>int</code> <p>Valve 8 open duration in ms.</p> <code>0</code> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def open_valve(\n    self,\n    v1_time: int = 0,\n    v2_time: int = 0,\n    v3_time: int = 0,\n    v4_time: int = 0,\n    v5_time: int = 0,\n    v6_time: int = 0,\n    v7_time: int = 0,\n    v8_time: int = 0,\n) -&gt; None:\n    \"\"\"Open one or more valves for the specified durations via ``OPEN_VALVE``.\n\n    Args:\n        v1_time: Valve 1 open duration in ms.  ``0`` keeps the valve closed.\n        v2_time: Valve 2 open duration in ms.\n        v3_time: Valve 3 open duration in ms.\n        v4_time: Valve 4 open duration in ms.\n        v5_time: Valve 5 open duration in ms.\n        v6_time: Valve 6 open duration in ms.\n        v7_time: Valve 7 open duration in ms.\n        v8_time: Valve 8 open duration in ms.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    self.send_command(\n        \"OPEN_VALVE\",\n        v1_time,\n        v2_time,\n        v3_time,\n        v4_time,\n        v5_time,\n        v6_time,\n        v7_time,\n        v8_time,\n    )\n    logger.info(\"FPosBAPIClient: OPEN_VALVE complete\")\n</code></pre>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.FPosBAPIClient.read_pos","title":"read_pos","text":"<pre><code>read_pos(pos_id: int) -&gt; tuple[float, float, float]\n</code></pre> <p>Read the stored absolute position for pos_id via <code>READ_POS</code>.</p> <p>Response layout::</p> <pre><code>MSG_ID, READ_POS, POS_ID, ABS_A1, ABS_A2, ABS_A3, 0, NULL, SUCCESS\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>pos_id</code> <code>int</code> <p>Position record ID (1-100).</p> required <p>Returns:</p> Type Description <code>tuple[float, float, float]</code> <p>Tuple of <code>(abs_a1, abs_a2, abs_a3)</code> in mm.</p> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> <code>RuntimeError</code> <p>If the response cannot be parsed.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def read_pos(self, pos_id: int) -&gt; tuple[float, float, float]:\n    \"\"\"Read the stored absolute position for *pos_id* via ``READ_POS``.\n\n    Response layout::\n\n        MSG_ID, READ_POS, POS_ID, ABS_A1, ABS_A2, ABS_A3, 0, NULL, SUCCESS\n\n    Args:\n        pos_id: Position record ID (1-100).\n\n    Returns:\n        Tuple of ``(abs_a1, abs_a2, abs_a3)`` in mm.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n        RuntimeError: If the response cannot be parsed.\n    \"\"\"\n    lines = self.send_command(\"READ_POS\", pos_id)\n    fields = [f.strip() for f in lines[-1].split(\",\")]\n    # fields: msg_id, READ_POS, pos_id, abs_a1, abs_a2, abs_a3, 0, NULL, SUCCESS\n    try:\n        result = float(fields[3]), float(fields[4]), float(fields[5])\n    except (IndexError, ValueError) as exc:\n        raise RuntimeError(f\"Failed to parse READ_POS response for pos_id={pos_id}: {lines!r}\") from exc\n    logger.debug(\"FPosBAPIClient: READ_POS pos_id=%d \u2192 %s\", pos_id, result)\n    return result\n</code></pre>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.FPosBAPIClient.teach_pos","title":"teach_pos","text":"<pre><code>teach_pos(pos_id: int, tool_id: int = 1) -&gt; None\n</code></pre> <p>Save the current axis positions to pos_id via <code>TEACH_POS</code>.</p> <p>Parameters:</p> Name Type Description Default <code>pos_id</code> <code>int</code> <p>Position record ID to write (1-100).</p> required <code>tool_id</code> <code>int</code> <p>Tool offset to associate with this position.</p> <code>1</code> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def teach_pos(self, pos_id: int, tool_id: int = 1) -&gt; None:\n    \"\"\"Save the current axis positions to *pos_id* via ``TEACH_POS``.\n\n    Args:\n        pos_id: Position record ID to write (1-100).\n        tool_id: Tool offset to associate with this position.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    self.send_command(\"TEACH_POS\", pos_id, tool_id)\n    logger.info(\"FPosBAPIClient: TEACH_POS complete (pos_id=%d)\", pos_id)\n</code></pre>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.FPosBAPIClient.write_pos","title":"write_pos","text":"<pre><code>write_pos(\n    pos_id: int, abs_a1: float, abs_a2: float, abs_a3: float\n) -&gt; None\n</code></pre> <p>Write absolute axis coordinates to pos_id via <code>WRITE_POS</code>.</p> <p>Parameters:</p> Name Type Description Default <code>pos_id</code> <code>int</code> <p>Position record ID to write (1-100).</p> required <code>abs_a1</code> <code>float</code> <p>Absolute X-axis position in mm.</p> required <code>abs_a2</code> <code>float</code> <p>Absolute Y-axis position in mm.</p> required <code>abs_a3</code> <code>float</code> <p>Absolute Z-axis position in mm.</p> required <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def write_pos(\n    self,\n    pos_id: int,\n    abs_a1: float,\n    abs_a2: float,\n    abs_a3: float,\n) -&gt; None:\n    \"\"\"Write absolute axis coordinates to *pos_id* via ``WRITE_POS``.\n\n    Args:\n        pos_id: Position record ID to write (1-100).\n        abs_a1: Absolute X-axis position in mm.\n        abs_a2: Absolute Y-axis position in mm.\n        abs_a3: Absolute Z-axis position in mm.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    self.send_command(\"WRITE_POS\", pos_id, abs_a1, abs_a2, abs_a3)\n    logger.info(\n        \"FPosBAPIClient: WRITE_POS complete (pos_id=%d, a1=%s, a2=%s, a3=%s mm)\",\n        pos_id,\n        abs_a1,\n        abs_a2,\n        abs_a3,\n    )\n</code></pre>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.FPosBAPIClient.mod_pos","title":"mod_pos","text":"<pre><code>mod_pos(\n    pos_id: int, rel_a1: float, rel_a2: float, rel_a3: float\n) -&gt; None\n</code></pre> <p>Modify a stored position by relative offsets via <code>MOD_POS</code>.</p> <p>Parameters:</p> Name Type Description Default <code>pos_id</code> <code>int</code> <p>Position record ID to modify (1-100).</p> required <code>rel_a1</code> <code>float</code> <p>Relative X-axis offset in mm.</p> required <code>rel_a2</code> <code>float</code> <p>Relative Y-axis offset in mm.</p> required <code>rel_a3</code> <code>float</code> <p>Relative Z-axis offset in mm.</p> required <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def mod_pos(\n    self,\n    pos_id: int,\n    rel_a1: float,\n    rel_a2: float,\n    rel_a3: float,\n) -&gt; None:\n    \"\"\"Modify a stored position by relative offsets via ``MOD_POS``.\n\n    Args:\n        pos_id: Position record ID to modify (1-100).\n        rel_a1: Relative X-axis offset in mm.\n        rel_a2: Relative Y-axis offset in mm.\n        rel_a3: Relative Z-axis offset in mm.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    self.send_command(\"MOD_POS\", pos_id, rel_a1, rel_a2, rel_a3)\n    logger.info(\"FPosBAPIClient: MOD_POS complete (pos_id=%d)\", pos_id)\n</code></pre>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.FPosBAPIClient.write_path","title":"write_path","text":"<pre><code>write_path(\n    pa_pos_id: int,\n    abs_a1: float,\n    abs_a2: float,\n    abs_a3: float,\n) -&gt; None\n</code></pre> <p>Write one waypoint along the motion path via <code>WRITE_PATH</code>.</p> <p>Parameters:</p> Name Type Description Default <code>pa_pos_id</code> <code>int</code> <p>Path position index (1-10).</p> required <code>abs_a1</code> <code>float</code> <p>Absolute X-axis position in mm.</p> required <code>abs_a2</code> <code>float</code> <p>Absolute Y-axis position in mm.</p> required <code>abs_a3</code> <code>float</code> <p>Absolute Z-axis position in mm.</p> required <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def write_path(\n    self,\n    pa_pos_id: int,\n    abs_a1: float,\n    abs_a2: float,\n    abs_a3: float,\n) -&gt; None:\n    \"\"\"Write one waypoint along the motion path via ``WRITE_PATH``.\n\n    Args:\n        pa_pos_id: Path position index (1-10).\n        abs_a1: Absolute X-axis position in mm.\n        abs_a2: Absolute Y-axis position in mm.\n        abs_a3: Absolute Z-axis position in mm.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    self.send_command(\"WRITE_PATH\", pa_pos_id, abs_a1, abs_a2, abs_a3)\n    logger.info(\"FPosBAPIClient: WRITE_PATH complete (pa_pos_id=%d)\", pa_pos_id)\n</code></pre>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.FPosBAPIClient.read_path","title":"read_path","text":"<pre><code>read_path(pa_pos_id: int) -&gt; tuple[float, float, float]\n</code></pre> <p>Read one waypoint from the motion path via <code>READ_PATH</code>.</p> <p>Parameters:</p> Name Type Description Default <code>pa_pos_id</code> <code>int</code> <p>Path position index (1-10).</p> required <p>Returns:</p> Type Description <code>tuple[float, float, float]</code> <p>Tuple of <code>(abs_a1, abs_a2, abs_a3)</code> in mm.</p> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> <code>RuntimeError</code> <p>If the response cannot be parsed.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def read_path(self, pa_pos_id: int) -&gt; tuple[float, float, float]:\n    \"\"\"Read one waypoint from the motion path via ``READ_PATH``.\n\n    Args:\n        pa_pos_id: Path position index (1-10).\n\n    Returns:\n        Tuple of ``(abs_a1, abs_a2, abs_a3)`` in mm.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n        RuntimeError: If the response cannot be parsed.\n    \"\"\"\n    lines = self.send_command(\"READ_PATH\", pa_pos_id)\n    fields = [f.strip() for f in lines[-1].split(\",\")]\n    # fields: msg_id, READ_PATH, pa_pos_id, abs_a1, abs_a2, abs_a3, 0, NULL, SUCCESS\n    try:\n        result = float(fields[3]), float(fields[4]), float(fields[5])\n    except (IndexError, ValueError) as exc:\n        raise RuntimeError(f\"Failed to parse READ_PATH response for pa_pos_id={pa_pos_id}: {lines!r}\") from exc\n    logger.debug(\"FPosBAPIClient: READ_PATH pa_pos_id=%d \u2192 %s\", pa_pos_id, result)\n    return result\n</code></pre>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.FPosBAPIClient.sys_status","title":"sys_status","text":"<pre><code>sys_status() -&gt; str\n</code></pre> <p>Return the system status string via <code>SYS_STATUS</code>.</p> <p>Response layout::</p> <pre><code>MSG_ID, SYS_STATUS, STATUS, 0, NULL, SUCCESS\n</code></pre> <p>Returns:</p> Type Description <code>str</code> <p>Status string reported by the PLC (e.g. <code>\"IDLE\"</code>).</p> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> <code>RuntimeError</code> <p>If the response cannot be parsed.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def sys_status(self) -&gt; str:\n    \"\"\"Return the system status string via ``SYS_STATUS``.\n\n    Response layout::\n\n        MSG_ID, SYS_STATUS, STATUS, 0, NULL, SUCCESS\n\n    Returns:\n        Status string reported by the PLC (e.g. ``\"IDLE\"``).\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n        RuntimeError: If the response cannot be parsed.\n    \"\"\"\n    lines = self.send_command(\"SYS_STATUS\")\n    fields = [f.strip() for f in lines[-1].split(\",\")]\n    # fields: msg_id, SYS_STATUS, status, 0, NULL, SUCCESS\n    try:\n        status = fields[2]\n    except IndexError as exc:\n        raise RuntimeError(f\"Failed to parse SYS_STATUS response: {lines!r}\") from exc\n    logger.debug(\"FPosBAPIClient: SYS_STATUS \u2192 %r\", status)\n    return status\n</code></pre>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.FPosBAPIClient.is_error","title":"is_error","text":"<pre><code>is_error() -&gt; bool\n</code></pre> <p>Return whether the gantry is in an error state via <code>IS_ERROR</code>.</p> <p>Response layout::</p> <pre><code>MSG_ID, IS_ERROR, FLAG, 0, NULL, SUCCESS\n</code></pre> <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> if the PLC reports an active error; <code>False</code> otherwise.</p> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> <code>RuntimeError</code> <p>If the response cannot be parsed.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def is_error(self) -&gt; bool:\n    \"\"\"Return whether the gantry is in an error state via ``IS_ERROR``.\n\n    Response layout::\n\n        MSG_ID, IS_ERROR, FLAG, 0, NULL, SUCCESS\n\n    Returns:\n        ``True`` if the PLC reports an active error; ``False`` otherwise.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n        RuntimeError: If the response cannot be parsed.\n    \"\"\"\n    lines = self.send_command(\"IS_ERROR\")\n    fields = [f.strip() for f in lines[-1].split(\",\")]\n    # fields: msg_id, IS_ERROR, flag, 0, NULL, SUCCESS\n    try:\n        result = bool(int(fields[2]))\n    except (IndexError, ValueError) as exc:\n        raise RuntimeError(f\"Failed to parse IS_ERROR response: {lines!r}\") from exc\n    logger.debug(\"FPosBAPIClient: IS_ERROR \u2192 %s\", result)\n    return result\n</code></pre>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.FPosBAPIClient.fpb_error","title":"fpb_error","text":"<pre><code>fpb_error() -&gt; str\n</code></pre> <p>Return the fieldbus error state via <code>FPB_ERROR</code>.</p> <p>Response layout::</p> <pre><code>MSG_ID, FPB_ERROR, STATUS, 0, NULL, SUCCESS\n</code></pre> <p>Returns:</p> Type Description <code>str</code> <p>Raw status string from the terminal response field.</p> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def fpb_error(self) -&gt; str:\n    \"\"\"Return the fieldbus error state via ``FPB_ERROR``.\n\n    Response layout::\n\n        MSG_ID, FPB_ERROR, STATUS, 0, NULL, SUCCESS\n\n    Returns:\n        Raw status string from the terminal response field.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    lines = self.send_command(\"FPB_ERROR\")\n    fields = [f.strip() for f in lines[-1].split(\",\")]\n    status = fields[2] if len(fields) &gt; 2 else \"\"\n    logger.debug(\"FPosBAPIClient: FPB_ERROR \u2192 %r\", status)\n    return status\n</code></pre>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.FPosBAPIClient.read_err","title":"read_err","text":"<pre><code>read_err() -&gt; str\n</code></pre> <p>Return the current error description via <code>READ_ERR</code>.</p> <p>Returns:</p> Type Description <code>str</code> <p>The raw terminal response line from the server.</p> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def read_err(self) -&gt; str:\n    \"\"\"Return the current error description via ``READ_ERR``.\n\n    Returns:\n        The raw terminal response line from the server.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    lines = self.send_command(\"READ_ERR\")\n    logger.debug(\"FPosBAPIClient: READ_ERR \u2192 %r\", lines[-1])\n    return lines[-1]\n</code></pre>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.FPosBAPIClient.err_log","title":"err_log","text":"<pre><code>err_log() -&gt; list[str]\n</code></pre> <p>Return the error log entries via <code>ERR_LOG</code>.</p> <p>The server may return multiple intermediate lines before the terminal SUCCESS line.  All non-terminal lines are returned as the log.</p> <p>Returns:</p> Type Description <code>list[str]</code> <p>List of error log strings (all lines except the terminal status).</p> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def err_log(self) -&gt; list[str]:\n    \"\"\"Return the error log entries via ``ERR_LOG``.\n\n    The server may return multiple intermediate lines before the terminal\n    SUCCESS line.  All non-terminal lines are returned as the log.\n\n    Returns:\n        List of error log strings (all lines except the terminal status).\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    lines = self.send_command(\"ERR_LOG\")\n    log = lines[:-1]  # strip terminal SUCCESS line\n    logger.debug(\"FPosBAPIClient: ERR_LOG returned %d line(s)\", len(log))\n    return log\n</code></pre>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.FPosBAPIClient.com_log","title":"com_log","text":"<pre><code>com_log() -&gt; list[str]\n</code></pre> <p>Return the communication log entries via <code>COM_LOG</code>.</p> <p>The server may return multiple intermediate lines before the terminal SUCCESS line.  All non-terminal lines are returned as the log.</p> <p>Returns:</p> Type Description <code>list[str]</code> <p>List of communication log strings (all lines except the terminal status).</p> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def com_log(self) -&gt; list[str]:\n    \"\"\"Return the communication log entries via ``COM_LOG``.\n\n    The server may return multiple intermediate lines before the terminal\n    SUCCESS line.  All non-terminal lines are returned as the log.\n\n    Returns:\n        List of communication log strings (all lines except the terminal status).\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    lines = self.send_command(\"COM_LOG\")\n    log = lines[:-1]  # strip terminal SUCCESS line\n    logger.debug(\"FPosBAPIClient: COM_LOG returned %d line(s)\", len(log))\n    return log\n</code></pre>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.FPosBAPIClient.err_desc","title":"err_desc","text":"<pre><code>err_desc() -&gt; str\n</code></pre> <p>Return the error description via <code>ERR_DESC</code>.</p> <p>Returns:</p> Type Description <code>str</code> <p>Raw terminal response line from the server.</p> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def err_desc(self) -&gt; str:\n    \"\"\"Return the error description via ``ERR_DESC``.\n\n    Returns:\n        Raw terminal response line from the server.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    lines = self.send_command(\"ERR_DESC\")\n    logger.debug(\"FPosBAPIClient: ERR_DESC \u2192 %r\", lines[-1])\n    return lines[-1]\n</code></pre>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.FPosBAPIClient.get_io","title":"get_io","text":"<pre><code>get_io(io_id: int) -&gt; float\n</code></pre> <p>Return the value of an I/O channel via <code>GET_IO</code>.</p> <p>Response layout::</p> <pre><code>MSG_ID, GET_IO, IO_ID, VALUE, 0, NULL, SUCCESS\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>io_id</code> <code>int</code> <p>I/O channel ID.</p> required <p>Returns:</p> Type Description <code>float</code> <p>Current value of the I/O channel.</p> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> <code>RuntimeError</code> <p>If the response cannot be parsed.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def get_io(self, io_id: int) -&gt; float:\n    \"\"\"Return the value of an I/O channel via ``GET_IO``.\n\n    Response layout::\n\n        MSG_ID, GET_IO, IO_ID, VALUE, 0, NULL, SUCCESS\n\n    Args:\n        io_id: I/O channel ID.\n\n    Returns:\n        Current value of the I/O channel.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n        RuntimeError: If the response cannot be parsed.\n    \"\"\"\n    lines = self.send_command(\"GET_IO\", io_id)\n    fields = [f.strip() for f in lines[-1].split(\",\")]\n    # fields: msg_id, GET_IO, io_id, value, 0, NULL, SUCCESS\n    try:\n        value = float(fields[3])\n    except (IndexError, ValueError) as exc:\n        raise RuntimeError(f\"Failed to parse GET_IO response for io_id={io_id}: {lines!r}\") from exc\n    logger.debug(\"FPosBAPIClient: GET_IO io_id=%d \u2192 %s\", io_id, value)\n    return value\n</code></pre>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.FPosBAPIClient.set_io","title":"set_io","text":"<pre><code>set_io(io_id: int, value: float) -&gt; None\n</code></pre> <p>Set the value of an I/O channel via <code>SET_IO</code>.</p> <p>Parameters:</p> Name Type Description Default <code>io_id</code> <code>int</code> <p>I/O channel ID.</p> required <code>value</code> <code>float</code> <p>Value to write to the I/O channel.</p> required <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def set_io(self, io_id: int, value: float) -&gt; None:\n    \"\"\"Set the value of an I/O channel via ``SET_IO``.\n\n    Args:\n        io_id: I/O channel ID.\n        value: Value to write to the I/O channel.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    self.send_command(\"SET_IO\", io_id, value)\n    logger.info(\"FPosBAPIClient: SET_IO io_id=%d value=%s\", io_id, value)\n</code></pre>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.FPosBAPIClient.get_par","title":"get_par","text":"<pre><code>get_par(par_id: int) -&gt; list[float]\n</code></pre> <p>Return parameter values for par_id via <code>GET_PAR</code>.</p> <p>The FPosBAPI server returns up to four value fields per parameter (e.g. tray parameters include column count, column offset, row count, and row offset).  Fields that are absent or non-numeric are omitted from the returned list.</p> <p>Response layout::</p> <pre><code>MSG_ID, GET_PAR, PAR_ID, VALUE, VALUE, VALUE, VALUE, 0, NULL, SUCCESS\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>par_id</code> <code>int</code> <p>Parameter ID (see API Rev 5 Parameter ID sheet).</p> required <p>Returns:</p> Type Description <code>list[float]</code> <p>List of up to four float values for the requested parameter.</p> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def get_par(self, par_id: int) -&gt; list[float]:\n    \"\"\"Return parameter values for *par_id* via ``GET_PAR``.\n\n    The FPosBAPI server returns up to four value fields per parameter\n    (e.g. tray parameters include column count, column offset, row count,\n    and row offset).  Fields that are absent or non-numeric are omitted\n    from the returned list.\n\n    Response layout::\n\n        MSG_ID, GET_PAR, PAR_ID, VALUE, VALUE, VALUE, VALUE, 0, NULL, SUCCESS\n\n    Args:\n        par_id: Parameter ID (see API Rev 5 Parameter ID sheet).\n\n    Returns:\n        List of up to four float values for the requested parameter.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    lines = self.send_command(\"GET_PAR\", par_id)\n    fields = [f.strip() for f in lines[-1].split(\",\")]\n    # fields: msg_id, GET_PAR, par_id, v1, v2, v3, v4, 0, NULL, SUCCESS\n    # Value fields are at indices 3-6; trailing error triplet at -3 to -1.\n    value_fields = fields[3:-3]\n    values: list[float] = []\n    for f in value_fields:\n        try:\n            values.append(float(f))\n        except ValueError:\n            break\n    logger.debug(\"FPosBAPIClient: GET_PAR par_id=%d \u2192 %s\", par_id, values)\n    return values\n</code></pre>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.FPosBAPIClient.set_par","title":"set_par","text":"<pre><code>set_par(par_id: int, *values: float) -&gt; None\n</code></pre> <p>Write one or more values to parameter par_id via <code>SET_PAR</code>.</p> <p>Parameters:</p> Name Type Description Default <code>par_id</code> <code>int</code> <p>Parameter ID (see API Rev 5 Parameter ID sheet).</p> required <code>*values</code> <code>float</code> <p>Up to four values to write to the parameter.</p> <code>()</code> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def set_par(self, par_id: int, *values: float) -&gt; None:\n    \"\"\"Write one or more values to parameter *par_id* via ``SET_PAR``.\n\n    Args:\n        par_id: Parameter ID (see API Rev 5 Parameter ID sheet).\n        *values: Up to four values to write to the parameter.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    self.send_command(\"SET_PAR\", par_id, *values)\n    logger.info(\"FPosBAPIClient: SET_PAR par_id=%d values=%s\", par_id, values)\n</code></pre>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.FPosBAPIClient.teach_tray","title":"teach_tray","text":"<pre><code>teach_tray(\n    tray_id: int, tray_pos: int, tool_id: int = 0\n) -&gt; None\n</code></pre> <p>Save the current axis positions as a tray position via <code>TEACH_TRAY</code>.</p> <p>Parameters:</p> Name Type Description Default <code>tray_id</code> <code>int</code> <p>Tray ID (1-20).</p> required <code>tray_pos</code> <code>int</code> <p>Position within the tray to teach.</p> required <code>tool_id</code> <code>int</code> <p>Tool offset to associate with this position.</p> <code>0</code> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def teach_tray(self, tray_id: int, tray_pos: int, tool_id: int = 0) -&gt; None:\n    \"\"\"Save the current axis positions as a tray position via ``TEACH_TRAY``.\n\n    Args:\n        tray_id: Tray ID (1-20).\n        tray_pos: Position within the tray to teach.\n        tool_id: Tool offset to associate with this position.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    self.send_command(\"TEACH_TRAY\", tray_id, tray_pos, tool_id)\n    logger.info(\"FPosBAPIClient: TEACH_TRAY complete (tray_id=%d, tray_pos=%d)\", tray_id, tray_pos)\n</code></pre>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.FPosBAPIClient.write_tray","title":"write_tray","text":"<pre><code>write_tray(\n    tray_id: int,\n    tray_pos: int,\n    abs_a1: float,\n    abs_a2: float,\n    abs_a3: float,\n) -&gt; None\n</code></pre> <p>Write absolute coordinates to a tray position via <code>WRITE_TRAY</code>.</p> <p>Parameters:</p> Name Type Description Default <code>tray_id</code> <code>int</code> <p>Tray ID (1-20).</p> required <code>tray_pos</code> <code>int</code> <p>Position index within the tray.</p> required <code>abs_a1</code> <code>float</code> <p>Absolute X-axis position in mm.</p> required <code>abs_a2</code> <code>float</code> <p>Absolute Y-axis position in mm.</p> required <code>abs_a3</code> <code>float</code> <p>Absolute Z-axis position in mm.</p> required <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def write_tray(\n    self,\n    tray_id: int,\n    tray_pos: int,\n    abs_a1: float,\n    abs_a2: float,\n    abs_a3: float,\n) -&gt; None:\n    \"\"\"Write absolute coordinates to a tray position via ``WRITE_TRAY``.\n\n    Args:\n        tray_id: Tray ID (1-20).\n        tray_pos: Position index within the tray.\n        abs_a1: Absolute X-axis position in mm.\n        abs_a2: Absolute Y-axis position in mm.\n        abs_a3: Absolute Z-axis position in mm.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    self.send_command(\"WRITE_TRAY\", tray_id, tray_pos, abs_a1, abs_a2, abs_a3)\n    logger.info(\"FPosBAPIClient: WRITE_TRAY complete (tray_id=%d, tray_pos=%d)\", tray_id, tray_pos)\n</code></pre>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.FPosBAPIClient.read_tray","title":"read_tray","text":"<pre><code>read_tray(\n    tray_id: int, tray_pos: int\n) -&gt; tuple[float, float, float]\n</code></pre> <p>Read the stored coordinates of a tray position via <code>READ_TRAY</code>.</p> <p>Response layout::</p> <pre><code>MSG_ID, READ_TRAY, TRAY_ID, TRAY_POS, ABS_A1, ABS_A2, ABS_A3, 0, NULL, SUCCESS\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>tray_id</code> <code>int</code> <p>Tray ID (1-20).</p> required <code>tray_pos</code> <code>int</code> <p>Position index within the tray.</p> required <p>Returns:</p> Type Description <code>tuple[float, float, float]</code> <p>Tuple of <code>(abs_a1, abs_a2, abs_a3)</code> in mm.</p> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> <code>RuntimeError</code> <p>If the response cannot be parsed.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def read_tray(self, tray_id: int, tray_pos: int) -&gt; tuple[float, float, float]:\n    \"\"\"Read the stored coordinates of a tray position via ``READ_TRAY``.\n\n    Response layout::\n\n        MSG_ID, READ_TRAY, TRAY_ID, TRAY_POS, ABS_A1, ABS_A2, ABS_A3, 0, NULL, SUCCESS\n\n    Args:\n        tray_id: Tray ID (1-20).\n        tray_pos: Position index within the tray.\n\n    Returns:\n        Tuple of ``(abs_a1, abs_a2, abs_a3)`` in mm.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n        RuntimeError: If the response cannot be parsed.\n    \"\"\"\n    lines = self.send_command(\"READ_TRAY\", tray_id, tray_pos)\n    fields = [f.strip() for f in lines[-1].split(\",\")]\n    # fields: msg_id, READ_TRAY, tray_id, tray_pos, abs_a1, abs_a2, abs_a3, 0, NULL, SUCCESS\n    try:\n        result = float(fields[4]), float(fields[5]), float(fields[6])\n    except (IndexError, ValueError) as exc:\n        raise RuntimeError(\n            f\"Failed to parse READ_TRAY response for tray_id={tray_id}, tray_pos={tray_pos}: {lines!r}\"\n        ) from exc\n    logger.debug(\"FPosBAPIClient: READ_TRAY tray_id=%d tray_pos=%d \u2192 %s\", tray_id, tray_pos, result)\n    return result\n</code></pre>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.FPosBAPIClient.mod_tray","title":"mod_tray","text":"<pre><code>mod_tray(\n    tray_id: int,\n    tray_pos: int,\n    rel_a1: float,\n    rel_a2: float,\n    rel_a3: float,\n) -&gt; None\n</code></pre> <p>Modify a stored tray position by relative offsets via <code>MOD_TRAY</code>.</p> <p>Parameters:</p> Name Type Description Default <code>tray_id</code> <code>int</code> <p>Tray ID (1-20).</p> required <code>tray_pos</code> <code>int</code> <p>Position index within the tray.</p> required <code>rel_a1</code> <code>float</code> <p>Relative X-axis offset in mm.</p> required <code>rel_a2</code> <code>float</code> <p>Relative Y-axis offset in mm.</p> required <code>rel_a3</code> <code>float</code> <p>Relative Z-axis offset in mm.</p> required <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def mod_tray(\n    self,\n    tray_id: int,\n    tray_pos: int,\n    rel_a1: float,\n    rel_a2: float,\n    rel_a3: float,\n) -&gt; None:\n    \"\"\"Modify a stored tray position by relative offsets via ``MOD_TRAY``.\n\n    Args:\n        tray_id: Tray ID (1-20).\n        tray_pos: Position index within the tray.\n        rel_a1: Relative X-axis offset in mm.\n        rel_a2: Relative Y-axis offset in mm.\n        rel_a3: Relative Z-axis offset in mm.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    self.send_command(\"MOD_TRAY\", tray_id, tray_pos, rel_a1, rel_a2, rel_a3)\n    logger.info(\"FPosBAPIClient: MOD_TRAY complete (tray_id=%d, tray_pos=%d)\", tray_id, tray_pos)\n</code></pre>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.FPosBAPIClient.move_tray","title":"move_tray","text":"<pre><code>move_tray(\n    tray_id: int,\n    tray_col: int,\n    tray_row: int,\n    tool_id: int = 0,\n    retract_z: int = 0,\n    slow_app: int = 0,\n) -&gt; None\n</code></pre> <p>Move the gantry to a tray location via <code>MOVE_TRAY</code>.</p> <p>Parameters:</p> Name Type Description Default <code>tray_id</code> <code>int</code> <p>Tray ID (1-20).</p> required <code>tray_col</code> <code>int</code> <p>Target column within the tray.</p> required <code>tray_row</code> <code>int</code> <p>Target row within the tray.</p> required <code>tool_id</code> <code>int</code> <p>Tool offset to apply.  <code>0</code> selects no tool.</p> <code>0</code> <code>retract_z</code> <code>int</code> <p><code>1</code> to retract Z before moving, <code>0</code> to skip.</p> <code>0</code> <code>slow_app</code> <code>int</code> <p><code>1</code> to slow approach the target position, <code>0</code> for full speed.</p> <code>0</code> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def move_tray(\n    self,\n    tray_id: int,\n    tray_col: int,\n    tray_row: int,\n    tool_id: int = 0,\n    retract_z: int = 0,\n    slow_app: int = 0,\n) -&gt; None:\n    \"\"\"Move the gantry to a tray location via ``MOVE_TRAY``.\n\n    Args:\n        tray_id: Tray ID (1-20).\n        tray_col: Target column within the tray.\n        tray_row: Target row within the tray.\n        tool_id: Tool offset to apply.  ``0`` selects no tool.\n        retract_z: ``1`` to retract Z before moving, ``0`` to skip.\n        slow_app: ``1`` to slow approach the target position, ``0`` for\n            full speed.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    self.send_command(\"MOVE_TRAY\", tray_id, tray_col, tray_row, tool_id, retract_z, slow_app)\n    logger.info(\n        \"FPosBAPIClient: MOVE_TRAY complete (tray_id=%d, col=%d, row=%d)\",\n        tray_id,\n        tray_col,\n        tray_row,\n    )\n</code></pre>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.FPosBAPIClient.init_sys","title":"init_sys","text":"<pre><code>init_sys() -&gt; None\n</code></pre> <p>Initialize system parameters via <code>INIT_SYS</code>.</p> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def init_sys(self) -&gt; None:\n    \"\"\"Initialize system parameters via ``INIT_SYS``.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    self.send_command(\"INIT_SYS\")\n    logger.info(\"FPosBAPIClient: system initialized\")\n</code></pre>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.FPosBAPIClient.close","title":"close","text":"<pre><code>close() -&gt; None\n</code></pre> <p>Close the underlying TCP socket.</p> <p>Safe to call more than once; subsequent calls are no-ops because the OS will already have released the socket descriptor.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def close(self) -&gt; None:\n    \"\"\"Close the underlying TCP socket.\n\n    Safe to call more than once; subsequent calls are no-ops because the\n    OS will already have released the socket descriptor.\n    \"\"\"\n    self._sock.close()\n    logger.info(\"FPosBAPIClient disconnected from %s:%d\", self.ip, self.port)\n</code></pre>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.FPosBAPIClientError","title":"FPosBAPIClientError","text":"<p>               Bases: <code>Exception</code></p> <p>Raised when the FPosBAPI server returns an error response or the connection is lost.</p>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.EdconAxis","title":"EdconAxis","text":"<pre><code>EdconAxis(\n    name: str,\n    ip: str,\n    run_referencing: bool = False,\n    max_position: float = inf,\n    min_position: float = -inf,\n)\n</code></pre> <p>               Bases: <code>MotionHandler</code></p> <p>Modbus TCP axis: direct festo-edcon <code>MotionHandler</code> connection to one drive.</p> <p>Wraps low-level Modbus communication and position/velocity unit conversion behind a millimetre-based public API.  All position values accepted and returned by the public methods use millimetres (mm) as their unit.</p> <p>Attributes:</p> Name Type Description <code>name</code> <p>Human-readable label for this axis (e.g. <code>\"X\"</code>).</p> <code>ip</code> <p>IPv4 address of the drive's Modbus TCP endpoint.</p> <code>com</code> <p>Active <code>ComModbus</code> connection.</p> <code>max_speed</code> <code>float</code> <p>Maximum achievable speed in drive velocity units.</p> <code>max_position</code> <code>float</code> <p>Maximum achievable position in mm.</p> <code>min_position</code> <code>float</code> <p>Minimum achievable position in mm.</p> <p>Initialise the axis and establish a Modbus connection.</p> <p>Creates the <code>ComModbus</code> connection, initialises the parent <code>MotionHandler</code>, reads the software-limit PNUs, and optionally triggers homing.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Human-readable axis label used in log messages and equality checks.  Axis labels are unique within a gantry.</p> required <code>ip</code> <code>str</code> <p>IPv4 address of the Festo drive's Modbus TCP interface.</p> required <code>run_referencing</code> <code>bool</code> <p>When <code>True</code>, perform a homing (referencing) sequence during construction.  Defaults to <code>False</code>.</p> <code>False</code> <code>max_position</code> <code>float</code> <p>Optional max position limitation, if the drive is restricted more than the internal SW limit position check. This avoids an issue where traversing toward the actual endstop is interrupted because the interia of the motion causes the drive to overshoot the limit position, throwing an error and interrupting the power stage on state.</p> <code>inf</code> <code>min_position</code> <code>float</code> <p>Optional min position limitation, if the drive is restricted more than the internal SW limit position check. This avoids an issue where traversing toward the actual endstop is interrupted because the interia of the motion causes the drive to overshoot the limit position, throwing an error and interrupting the power stage on state.</p> <code>-inf</code> Source code in <code>src/applied_motion/backends/edcon_axis.py</code> <pre><code>def __init__(\n    self,\n    name: str,\n    ip: str,\n    run_referencing: bool = False,\n    max_position: float = inf,\n    min_position: float = -inf,\n) -&gt; None:\n    \"\"\"Initialise the axis and establish a Modbus connection.\n\n    Creates the `ComModbus` connection,\n    initialises the parent `MotionHandler`,\n    reads the software-limit PNUs, and optionally triggers homing.\n\n    Args:\n        name: Human-readable axis label used in log messages and equality\n            checks.  Axis labels are unique within a gantry.\n        ip: IPv4 address of the Festo drive's Modbus TCP interface.\n        run_referencing: When ``True``, perform a homing (referencing)\n            sequence during construction.  Defaults to ``False``.\n        max_position: Optional max position limitation, if the drive is restricted\n            more than the internal SW limit position check. This avoids an issue where\n            traversing toward the actual endstop is interrupted because the interia of\n            the motion causes the drive to overshoot the limit position, throwing an error\n            and interrupting the power stage on state.\n        min_position: Optional min position limitation, if the drive is restricted\n            more than the internal SW limit position check. This avoids an issue where\n            traversing toward the actual endstop is interrupted because the interia of\n            the motion causes the drive to overshoot the limit position, throwing an error\n            and interrupting the power stage on state.\n    \"\"\"\n    self.name = name\n    self.ip = ip\n    self.com = ComModbus(self.ip)\n    super().__init__(self.com)\n    self.acknowledge_faults()\n\n    self.acknowledge_faults()\n    # self.home()\n    logger.info(\"Axis '%s': initialized\", self.name)\n    self.configure_software_limit_switch(True)\n    logger.info(\"Axis '%s': software limit switch configured\", self.name)\n\n    self._neg_sw_limit: int = self.com.read_pnu(834)\n    self._pos_sw_limit: int = self.com.read_pnu(835)\n\n    self._min_vel: int = self.com.read_pnu(11212)\n    self._max_vel: int = self.com.read_pnu(11213)\n    self._pos_scale_exp: int = self.com.read_pnu(11724)\n    self._vel_scale_exp: int = self.com.read_pnu(11725)\n    self.input_pos_unit = {\"distance\": {\"unit\": \"m\", \"power\": 1, \"power_of_ten\": -3}}\n    self.input_vel_unit = {\n        \"distance\": {\"unit\": \"m\", \"power\": 1, \"power_of_ten\": -3},\n        \"time\": {\"unit\": \"s\", \"power\": -1, \"power_of_ten\": 1},\n    }\n    self.max_position = -1 + min(\n        max_position,\n        self._valid_position(self._pos_sw_limit, self.input_pos_unit, invert=True),\n    )  # TODO: Fix overshooting following error in drive control\n    self.min_position = 1 + max(\n        min_position,\n        self._valid_position(self._neg_sw_limit, self.input_pos_unit, invert=True),\n    )  # TODO: Fix overshooting following error in drive control\n    self.max_velocity = self._valid_velocity(\n        self._max_vel, self.input_vel_unit, invert=True\n    )  # TODO: Get these from config, compare with SW limits and take most restrictive superset\n    self.min_velocity = self._valid_velocity(\n        self._min_vel, self.input_vel_unit, invert=True\n    )  # TODO: Get these from config, compare with SW limits and take most restrictive superset\n\n    logger.info(\n        \"Axis '%s': SW limits loaded \u2014 neg=%s, pos=%s (drive units)\",\n        self.name,\n        self._neg_sw_limit,\n        self._pos_sw_limit,\n    )\n    logger.info(\n        \"Axis '%s': SW limits loaded \u2014 neg=%s, pos=%s (standard units)\",\n        self.name,\n        self.min_position,\n        self.max_position,\n    )\n\n    if self.fault_present():\n        logger.warning(\"Axis '%s': fault present on init\", self.name)\n    logger.debug(\"Axis '%s' fault string: %s\", self.name, self.fault_string())\n    logger.debug(\"Axis '%s' current fault code: %s\", self.name, self.current_fault_code())\n    self.acknowledge_faults()\n    self.enable_powerstage()\n\n    if run_referencing:\n        logger.info(\"Axis '%s': referencing requested during initialization\", self.name)\n        self.home()\n</code></pre>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.EdconAxis.max_speed","title":"max_speed  <code>property</code>","text":"<pre><code>max_speed: float\n</code></pre> <p>Return largest allowed speed magnitude for this axis in mm/s.</p> <p>This derives from configured signed velocity bounds and returns absolute maximum magnitude, independent of direction.</p> <p>Returns:</p> Type Description <code>float</code> <p>Maximum absolute velocity limit in mm/s.</p>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.EdconAxis.home","title":"home","text":"<pre><code>home() -&gt; bool\n</code></pre> <p>Home the axis by running a referencing task.</p> <p>Acknowledges any faults, enables the power stage, and then executes a blocking referencing task to establish the axis zero point.</p> Source code in <code>src/applied_motion/backends/edcon_axis.py</code> <pre><code>def home(self) -&gt; bool:\n    \"\"\"Home the axis by running a referencing task.\n\n    Acknowledges any faults, enables the power stage, and then\n    executes a blocking referencing task to establish the axis zero\n    point.\n    \"\"\"\n    logger.info(\"Axis '%s': starting homing sequence\", self.name)\n    self.acknowledge_faults()\n    self.enable_powerstage()\n    res = self.referencing_task(nonblocking=False)\n\n    logger.info(\"Axis '%s': homing result=%s\", self.name, res)\n    logger.info(\"Axis '%s': homing complete\", self.name)\n    return res\n</code></pre>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.EdconAxis.is_homed","title":"is_homed","text":"<pre><code>is_homed() -&gt; bool\n</code></pre> <p>Checks whether drive has been homed and has a valid frame of reference.</p> <p>Thin wrapper for interface consistency around <code>MotionHandler.referenced()</code>.</p> Source code in <code>src/applied_motion/backends/edcon_axis.py</code> <pre><code>def is_homed(self) -&gt; bool:\n    \"\"\"Checks whether drive has been homed and has a valid frame of reference.\n\n    Thin wrapper for interface consistency around `MotionHandler.referenced()`.\n\n    \"\"\"\n    return self.referenced()\n</code></pre>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.EdconAxis.current_position","title":"current_position","text":"<pre><code>current_position()\n</code></pre> <p>Transparent wrapper around <code>MotionHandler.current_position</code>.</p>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.EdconAxis.current_position--todo-is-this-necessary","title":"TODO: is this necessary?","text":"<p>Returns the raw position value in the drive's internal unit system (drive units, whose scale is determined by PNU 11724 and varies by firmware/configuration \u2014 typically 0.001 mm per unit).  Use <code>get_current_axis_position</code> when you need the position in a consistent, human-readable unit (mm).</p> Source code in <code>src/applied_motion/backends/edcon_axis.py</code> <pre><code>def current_position(self):\n    \"\"\"Transparent wrapper around ``MotionHandler.current_position``.\n\n    # TODO: is this necessary?\n\n    Returns the raw position value in the drive's **internal unit system**\n    (drive units, whose scale is determined by PNU 11724 and varies by\n    firmware/configuration \u2014 typically 0.001 mm per unit).  Use\n    [`get_current_axis_position`][applied_motion.backends.edcon_axis.EdconAxis.get_current_axis_position] when you need the position in a\n    consistent, human-readable unit (mm).\n    \"\"\"\n    logger.warning(\n        \"Axis '%s': current_position() returns a raw drive-unit value \u2014 \"\n        \"unit depends on drive configuration (PNU 11724). \"\n        \"Call get_current_axis_position() for a consistent mm value.\",\n        self.name,\n    )\n    return super().current_position()\n</code></pre>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.EdconAxis.get_current_axis_position","title":"get_current_axis_position","text":"<pre><code>get_current_axis_position() -&gt; float\n</code></pre> <p>Return the current axis position in the library's canonical output unit (mm).</p> <p>Reads the raw drive-unit position from <code>MotionHandler.current_position</code>, then converts it to mm via <code>_valid_position</code> with <code>invert=True</code>. The drive's own PNU-reported position-unit scale is used for the conversion, so the result is always consistent regardless of how the drive was configured.</p> <p>Internal methods (<code>_check_overshoot</code>, unit converters) continue to use <code>super().current_position()</code> directly so their drive-unit arithmetic is unaffected.</p> Source code in <code>src/applied_motion/backends/edcon_axis.py</code> <pre><code>def get_current_axis_position(self) -&gt; float:\n    \"\"\"Return the current axis position in the library's canonical output unit (mm).\n\n    Reads the raw drive-unit position from ``MotionHandler.current_position``,\n    then converts it to mm via `_valid_position` with ``invert=True``.\n    The drive's own PNU-reported position-unit scale is used for the\n    conversion, so the result is always consistent regardless of how the\n    drive was configured.\n\n    Internal methods (`_check_overshoot`, unit converters) continue to\n    use ``super().current_position()`` directly so their drive-unit arithmetic\n    is unaffected.\n    \"\"\"\n    _mm_unit = {\"distance\": {\"unit\": \"m\", \"power\": 1, \"power_of_ten\": -3}}\n    return self._valid_position(super().current_position(), _mm_unit, invert=True)\n</code></pre>"},{"location":"autoapi/applied_motion/backends/#applied_motion.backends.EdconAxis.move","title":"move","text":"<pre><code>move(\n    position: int | float,\n    velocity: int | float,\n    timeout: int | None = None,\n    **kwargs,\n) -&gt; bool\n</code></pre> <p>Move the axis to a specified position with a given velocity.</p> <p>Parameters:</p> Name Type Description Default <code>position</code> <code>int | float</code> <p>Target position in millimetres (mm).  Converted internally to drive units before the motion command is issued.</p> required <code>velocity</code> <code>int | float</code> <p>Move velocity in mm/s.  Converted internally to drive units.</p> required <code>timeout</code> <code>int | None</code> <p>Optional time limit in seconds for the move.  When provided, the motion is launched in a background thread and a stop command is issued if the thread has not finished by the deadline.  When <code>None</code> (default), the call blocks until motion completes.</p> <code>None</code> <code>**kwargs</code> <p>Optional keyword overrides.  Recognised keys:</p> <ul> <li><code>position_type</code> (<code>str</code>): <code>\"absolute\"</code> for an absolute   target position or <code>\"relative\"</code> for a relative displacement.   When omitted the move is treated as absolute.</li> </ul> <code>{}</code> <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> if the motion task succeeded, <code>False</code> otherwise.</p> Source code in <code>src/applied_motion/backends/edcon_axis.py</code> <pre><code>def move(self, position: int | float, velocity: int | float, timeout: int | None = None, **kwargs) -&gt; bool:\n    \"\"\"Move the axis to a specified position with a given velocity.\n\n    Args:\n        position: Target position in millimetres (mm).  Converted internally\n            to drive units before the motion command is issued.\n        velocity: Move velocity in mm/s.  Converted internally to drive units.\n        timeout: Optional time limit in seconds for the move.  When provided,\n            the motion is launched in a background thread and a stop command\n            is issued if the thread has not finished by the deadline.  When\n            ``None`` (default), the call blocks until motion completes.\n        **kwargs: Optional keyword overrides.  Recognised keys:\n\n            - ``position_type`` (``str``): ``\"absolute\"`` for an absolute\n              target position or ``\"relative\"`` for a relative displacement.\n              When omitted the move is treated as **absolute**.\n\n    Returns:\n        ``True`` if the motion task succeeded, ``False`` otherwise.\n    \"\"\"\n    # TODO: check valid move parameters against internal set values\n    # e.g. Position limit, velocity limit, acceleration limit, etc.\n    # if not valid, raise exception\n    pos_type = kwargs.get(\"position_type\", \"absolute\")\n    logger.debug(\n        \"Axis '%s': entering move \u2014 raw position=%s  raw velocity=%s raw timeout=%s raw position_type=%s\",\n        self.name,\n        position,\n        velocity,\n        timeout,\n        pos_type,\n    )\n    positioning_type = pos_type == \"absolute\"\n\n    clamped_position = self._clamped_target(position, positioning_type)\n    logger.debug(\"Axis '%s': processing move \u2014 clamped_position=%s\", self.name, clamped_position)\n    try:\n        validated_position = int(self._valid_position(clamped_position, self.input_pos_unit))\n    except Exception:\n        logger.exception(\"Axis '%s': failed to validate position\", self.name)\n        validated_position = -5  # TODO: This seems bad and wrong\n\n    try:\n        validated_velocity = int(self._valid_velocity(velocity, self.input_vel_unit))\n    except Exception:\n        logger.exception(\"Axis '%s': failed to validate velocity\", self.name)\n        validated_velocity = 5  # TODO: This seems bad and wrong\n    logger.debug(\n        \"Axis '%s': processing move \u2014 validated_position=%s  validated_velocity=%s\",\n        self.name,\n        validated_position,\n        validated_velocity,\n    )\n    # self.configure_continuous_update(True) # continous update so that new position tasks can be started while still in motion\n    #\n    # TODO: Check faults and abort if critical fault encountered\n\n    # self.acknowledge_faults()\n\n    logger.debug(\n        \"Axis '%s': fault_string=%s  fault_code=%s\", self.name, self.fault_string(), self.current_fault_code()\n    )\n    validated_position = self._check_overshoot(validated_position, absolute=positioning_type)\n    logger.debug(\n        \"Axis '%s': clamped_position=%s  validated_velocity=%s\", self.name, validated_position, validated_velocity\n    )\n    # Skip if already at target (absolute) or zero delta (relative).\n    at_target = (\n        abs(validated_position - int(super().current_position())) == 0\n        if positioning_type\n        else abs(validated_position) == 0\n    )\n    if at_target:\n        logger.info(\"Axis '%s': already at target drive position %s, skipping move\", self.name, validated_position)\n        return True\n    result: bool = False\n    # TODO: Check powerstage enabled, attempt enable if not\n    # TODO:\n    # TODO: Check for CRITICAL error stages,\n    # TODO: ack faults,\n    # TODO: then move.\n    # TODO: Incorporate jog mode into this function?\n    self.acknowledge_faults()\n    self.enable_powerstage()\n    if timeout is None:\n        result = self.position_task(\n            validated_position,\n            validated_velocity,\n            absolute=positioning_type,\n            nonblocking=False,\n        )\n    else:\n        _result_box: list[bool] = []\n        move_thread = Thread(\n            target=lambda: _result_box.append(\n                self.position_task(\n                    validated_position,\n                    validated_velocity,\n                    absolute=positioning_type,\n                    nonblocking=False,\n                )\n            )\n        )\n        move_thread.start()\n        move_thread.join(timeout=timeout)\n        if move_thread.is_alive():\n            logger.warning(\"Axis '%s': move timed out after %ss \u2014 sending stop motion task\", self.name, timeout)\n            self.stop_motion_task()\n            # Wait for the thread to actually exit before returning; 50 ms is not enough.\n            move_thread.join(timeout=timeout)\n            if move_thread.is_alive():\n                logger.error(\n                    f\"Axis '%s': position_task thread still alive {timeout}s after stop command\", self.name\n                )\n        else:\n            result = _result_box[0] if _result_box else False  # TODO: AllTrue?\n\n    logger.info(\"Axis '%s': motion task complete, result=%s\", self.name, result)\n    return result\n</code></pre>"},{"location":"autoapi/applied_motion/backends/axis_protocol/","title":"axis_protocol","text":""},{"location":"autoapi/applied_motion/backends/axis_protocol/#applied_motion.backends.axis_protocol","title":"axis_protocol","text":"<p>Structural protocol that all axis backend implementations must satisfy.</p>"},{"location":"autoapi/applied_motion/backends/axis_protocol/#applied_motion.backends.axis_protocol.Axis","title":"Axis","text":"<p>               Bases: <code>Protocol</code></p> <p>Structural interface for a single controllable axis.</p> <p>Both <code>EdconAxis</code> (Modbus/festo-edcon backend) and <code>FPosBAxis</code> (FPosBAPI backend) satisfy this protocol structurally \u2014 no explicit inheritance required.</p> <p>All position values are in millimetres (mm). All velocity values are in mm/s.</p>"},{"location":"autoapi/applied_motion/backends/axis_protocol/#applied_motion.backends.axis_protocol.Axis.move","title":"move","text":"<pre><code>move(position: float, velocity: float, **kwargs) -&gt; bool\n</code></pre> <p>Move the axis to position at velocity.</p> <p>Parameters:</p> Name Type Description Default <code>position</code> <code>float</code> <p>Target position in mm (absolute or relative depending on <code>position_type</code> kwarg).</p> required <code>velocity</code> <code>float</code> <p>Move speed in mm/s.</p> required <code>**kwargs</code> <p>Backend-specific options (e.g. <code>position_type</code>, <code>timeout</code>).</p> <code>{}</code> <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> if the move completed successfully.</p> Source code in <code>src/applied_motion/backends/axis_protocol.py</code> <pre><code>def move(self, position: float, velocity: float, **kwargs) -&gt; bool:\n    \"\"\"Move the axis to *position* at *velocity*.\n\n    Args:\n        position: Target position in mm (absolute or relative depending on\n            ``position_type`` kwarg).\n        velocity: Move speed in mm/s.\n        **kwargs: Backend-specific options (e.g. ``position_type``,\n            ``timeout``).\n\n    Returns:\n        ``True`` if the move completed successfully.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"autoapi/applied_motion/backends/axis_protocol/#applied_motion.backends.axis_protocol.Axis.home","title":"home","text":"<pre><code>home() -&gt; bool\n</code></pre> <p>Run the axis homing/referencing sequence.</p> Source code in <code>src/applied_motion/backends/axis_protocol.py</code> <pre><code>def home(self) -&gt; bool:\n    \"\"\"Run the axis homing/referencing sequence.\"\"\"\n    ...\n</code></pre>"},{"location":"autoapi/applied_motion/backends/axis_protocol/#applied_motion.backends.axis_protocol.Axis.get_current_axis_position","title":"get_current_axis_position","text":"<pre><code>get_current_axis_position() -&gt; float\n</code></pre> <p>Return the current axis position in mm.</p> <p>Returns:</p> Type Description <code>float</code> <p>Current position in mm.</p> Source code in <code>src/applied_motion/backends/axis_protocol.py</code> <pre><code>def get_current_axis_position(self) -&gt; float:\n    \"\"\"Return the current axis position in mm.\n\n    Returns:\n        Current position in mm.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"autoapi/applied_motion/backends/axis_protocol/#applied_motion.backends.axis_protocol.Axis.is_homed","title":"is_homed","text":"<pre><code>is_homed() -&gt; bool\n</code></pre> <p>Return whether this axis reports a valid homed/reference state.</p> <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> if this axis is homed/referenced; <code>False</code> otherwise.</p> Source code in <code>src/applied_motion/backends/axis_protocol.py</code> <pre><code>def is_homed(self) -&gt; bool:\n    \"\"\"Return whether this axis reports a valid homed/reference state.\n\n    Returns:\n        ``True`` if this axis is homed/referenced; ``False`` otherwise.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"autoapi/applied_motion/backends/axis_protocol/#applied_motion.backends.axis_protocol.Axis.stopped","title":"stopped","text":"<pre><code>stopped() -&gt; bool\n</code></pre> <p>Return <code>True</code> when the axis is not currently in motion.</p> <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> if the axis has stopped; <code>False</code> if motion is ongoing.</p> Source code in <code>src/applied_motion/backends/axis_protocol.py</code> <pre><code>def stopped(self) -&gt; bool:\n    \"\"\"Return ``True`` when the axis is not currently in motion.\n\n    Returns:\n        ``True`` if the axis has stopped; ``False`` if motion is ongoing.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"autoapi/applied_motion/backends/axis_protocol/#applied_motion.backends.axis_protocol.Axis.acknowledge_faults","title":"acknowledge_faults","text":"<pre><code>acknowledge_faults()\n</code></pre> <p>Clear errors.</p> Source code in <code>src/applied_motion/backends/axis_protocol.py</code> <pre><code>def acknowledge_faults(self):\n    \"\"\"Clear errors.\"\"\"\n    # TODO: This shouldn't be exposed to end-users. This is done automatically for almost every motion task to proceed and is part of an internal interface.\n    ...\n</code></pre>"},{"location":"autoapi/applied_motion/backends/axis_protocol/#applied_motion.backends.axis_protocol.Axis.enable_powerstage","title":"enable_powerstage","text":"<pre><code>enable_powerstage()\n</code></pre> <p>Turn on torque to motor controlling this axis.</p> Source code in <code>src/applied_motion/backends/axis_protocol.py</code> <pre><code>def enable_powerstage(self):\n    \"\"\"Turn on torque to motor controlling this axis.\"\"\"\n    # TODO: This shouldn't be exposed to end-users. This is done automatically for almost every motion task to proceed and is part of an internal interface.\n    ...\n</code></pre>"},{"location":"autoapi/applied_motion/backends/axis_protocol/#applied_motion.backends.axis_protocol.Axis.disable_powerstage","title":"disable_powerstage","text":"<pre><code>disable_powerstage()\n</code></pre> <p>Turn off torque to motor controlling this axis.</p> Source code in <code>src/applied_motion/backends/axis_protocol.py</code> <pre><code>def disable_powerstage(self):\n    \"\"\"Turn off torque to motor controlling this axis.\"\"\"\n    ...\n</code></pre>"},{"location":"autoapi/applied_motion/backends/axis_protocol/#applied_motion.backends.axis_protocol.Axis.current_position","title":"current_position","text":"<pre><code>current_position()\n</code></pre> <p>Return current position.</p> Source code in <code>src/applied_motion/backends/axis_protocol.py</code> <pre><code>def current_position(self):\n    \"\"\"Return current position.\"\"\"\n    ...\n</code></pre>"},{"location":"autoapi/applied_motion/backends/axis_protocol/#applied_motion.backends.axis_protocol.Axis.current_velocity","title":"current_velocity","text":"<pre><code>current_velocity()\n</code></pre> <p>Return current velocity.</p> Source code in <code>src/applied_motion/backends/axis_protocol.py</code> <pre><code>def current_velocity(self):\n    \"\"\"Return current velocity.\"\"\"\n    ...\n</code></pre>"},{"location":"autoapi/applied_motion/backends/axis_protocol/#applied_motion.backends.axis_protocol.Axis.jog_task","title":"jog_task","text":"<pre><code>jog_task(\n    jog_positive: bool = True,\n    jog_negative: bool = False,\n    incremental: bool = False,\n    duration: float = 0.0,\n) -&gt; bool\n</code></pre> <p>Jog the axis according to backend-specific jog semantics.</p> <p>Parameters:</p> Name Type Description Default <code>jog_positive</code> <code>bool</code> <p>When <code>True</code>, request jog in the positive direction.</p> <code>True</code> <code>jog_negative</code> <code>bool</code> <p>When <code>True</code>, request jog in the negative direction.</p> <code>False</code> <code>incremental</code> <code>bool</code> <p>Backend-specific incremental jog mode flag.</p> <code>False</code> <code>duration</code> <code>float</code> <p>Optional jog duration in seconds. A value of <code>0</code> starts the jog and returns immediately.</p> <code>0.0</code> <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> if the jog command succeeds; <code>False</code> otherwise.</p> Source code in <code>src/applied_motion/backends/axis_protocol.py</code> <pre><code>def jog_task(\n    self,\n    jog_positive: bool = True,\n    jog_negative: bool = False,\n    incremental: bool = False,\n    duration: float = 0.0,\n) -&gt; bool:\n    \"\"\"Jog the axis according to backend-specific jog semantics.\n\n    Args:\n        jog_positive: When ``True``, request jog in the positive direction.\n        jog_negative: When ``True``, request jog in the negative direction.\n        incremental: Backend-specific incremental jog mode flag.\n        duration: Optional jog duration in seconds. A value of ``0``\n            starts the jog and returns immediately.\n\n    Returns:\n        ``True`` if the jog command succeeds; ``False`` otherwise.\n    \"\"\"\n    # TODO: Implement this functionality using corresponding FPosBAPI command\n\n    ...\n</code></pre>"},{"location":"autoapi/applied_motion/backends/axis_protocol/#applied_motion.backends.axis_protocol.Axis.ready_for_motion","title":"ready_for_motion","text":"<pre><code>ready_for_motion() -&gt; bool\n</code></pre> <p>Return <code>True</code> when the axis is ready to accept a move command.</p> <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> if the axis is enabled, homed, and fault-free.</p> Source code in <code>src/applied_motion/backends/axis_protocol.py</code> <pre><code>def ready_for_motion(self) -&gt; bool:\n    \"\"\"Return ``True`` when the axis is ready to accept a move command.\n\n    Returns:\n        ``True`` if the axis is enabled, homed, and fault-free.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"autoapi/applied_motion/backends/edcon_axis/","title":"edcon_axis","text":""},{"location":"autoapi/applied_motion/backends/edcon_axis/#applied_motion.backends.edcon_axis","title":"edcon_axis","text":"<p>Modbus TCP axis backend \u2014 direct per-drive edcon/CMMT connection.</p> <p><code>EdconAxis</code> is the original Festo gantry axis implementation.  It connects directly to an individual CMMT servo/stepper drive via Modbus TCP using the <code>festo-edcon</code> library, and exposes a millimetre-based public API that satisfies <code>Axis</code>.</p> <p>This backend is appropriate when each drive is individually addressable on the network and <code>Gantry</code> manages concurrency in Python.</p> <p>For coordinated multi-axis motion managed by a CECC-X PLC using the Festo Easy Positioning API, use <code>FPosBAxis</code> with the FPosBAPI backend instead.</p>"},{"location":"autoapi/applied_motion/backends/edcon_axis/#applied_motion.backends.edcon_axis.EdconAxis","title":"EdconAxis","text":"<pre><code>EdconAxis(\n    name: str,\n    ip: str,\n    run_referencing: bool = False,\n    max_position: float = inf,\n    min_position: float = -inf,\n)\n</code></pre> <p>               Bases: <code>MotionHandler</code></p> <p>Modbus TCP axis: direct festo-edcon <code>MotionHandler</code> connection to one drive.</p> <p>Wraps low-level Modbus communication and position/velocity unit conversion behind a millimetre-based public API.  All position values accepted and returned by the public methods use millimetres (mm) as their unit.</p> <p>Attributes:</p> Name Type Description <code>name</code> <p>Human-readable label for this axis (e.g. <code>\"X\"</code>).</p> <code>ip</code> <p>IPv4 address of the drive's Modbus TCP endpoint.</p> <code>com</code> <p>Active <code>ComModbus</code> connection.</p> <code>max_speed</code> <code>float</code> <p>Maximum achievable speed in drive velocity units.</p> <code>max_position</code> <code>float</code> <p>Maximum achievable position in mm.</p> <code>min_position</code> <code>float</code> <p>Minimum achievable position in mm.</p> <p>Initialise the axis and establish a Modbus connection.</p> <p>Creates the <code>ComModbus</code> connection, initialises the parent <code>MotionHandler</code>, reads the software-limit PNUs, and optionally triggers homing.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Human-readable axis label used in log messages and equality checks.  Axis labels are unique within a gantry.</p> required <code>ip</code> <code>str</code> <p>IPv4 address of the Festo drive's Modbus TCP interface.</p> required <code>run_referencing</code> <code>bool</code> <p>When <code>True</code>, perform a homing (referencing) sequence during construction.  Defaults to <code>False</code>.</p> <code>False</code> <code>max_position</code> <code>float</code> <p>Optional max position limitation, if the drive is restricted more than the internal SW limit position check. This avoids an issue where traversing toward the actual endstop is interrupted because the interia of the motion causes the drive to overshoot the limit position, throwing an error and interrupting the power stage on state.</p> <code>inf</code> <code>min_position</code> <code>float</code> <p>Optional min position limitation, if the drive is restricted more than the internal SW limit position check. This avoids an issue where traversing toward the actual endstop is interrupted because the interia of the motion causes the drive to overshoot the limit position, throwing an error and interrupting the power stage on state.</p> <code>-inf</code> Source code in <code>src/applied_motion/backends/edcon_axis.py</code> <pre><code>def __init__(\n    self,\n    name: str,\n    ip: str,\n    run_referencing: bool = False,\n    max_position: float = inf,\n    min_position: float = -inf,\n) -&gt; None:\n    \"\"\"Initialise the axis and establish a Modbus connection.\n\n    Creates the `ComModbus` connection,\n    initialises the parent `MotionHandler`,\n    reads the software-limit PNUs, and optionally triggers homing.\n\n    Args:\n        name: Human-readable axis label used in log messages and equality\n            checks.  Axis labels are unique within a gantry.\n        ip: IPv4 address of the Festo drive's Modbus TCP interface.\n        run_referencing: When ``True``, perform a homing (referencing)\n            sequence during construction.  Defaults to ``False``.\n        max_position: Optional max position limitation, if the drive is restricted\n            more than the internal SW limit position check. This avoids an issue where\n            traversing toward the actual endstop is interrupted because the interia of\n            the motion causes the drive to overshoot the limit position, throwing an error\n            and interrupting the power stage on state.\n        min_position: Optional min position limitation, if the drive is restricted\n            more than the internal SW limit position check. This avoids an issue where\n            traversing toward the actual endstop is interrupted because the interia of\n            the motion causes the drive to overshoot the limit position, throwing an error\n            and interrupting the power stage on state.\n    \"\"\"\n    self.name = name\n    self.ip = ip\n    self.com = ComModbus(self.ip)\n    super().__init__(self.com)\n    self.acknowledge_faults()\n\n    self.acknowledge_faults()\n    # self.home()\n    logger.info(\"Axis '%s': initialized\", self.name)\n    self.configure_software_limit_switch(True)\n    logger.info(\"Axis '%s': software limit switch configured\", self.name)\n\n    self._neg_sw_limit: int = self.com.read_pnu(834)\n    self._pos_sw_limit: int = self.com.read_pnu(835)\n\n    self._min_vel: int = self.com.read_pnu(11212)\n    self._max_vel: int = self.com.read_pnu(11213)\n    self._pos_scale_exp: int = self.com.read_pnu(11724)\n    self._vel_scale_exp: int = self.com.read_pnu(11725)\n    self.input_pos_unit = {\"distance\": {\"unit\": \"m\", \"power\": 1, \"power_of_ten\": -3}}\n    self.input_vel_unit = {\n        \"distance\": {\"unit\": \"m\", \"power\": 1, \"power_of_ten\": -3},\n        \"time\": {\"unit\": \"s\", \"power\": -1, \"power_of_ten\": 1},\n    }\n    self.max_position = -1 + min(\n        max_position,\n        self._valid_position(self._pos_sw_limit, self.input_pos_unit, invert=True),\n    )  # TODO: Fix overshooting following error in drive control\n    self.min_position = 1 + max(\n        min_position,\n        self._valid_position(self._neg_sw_limit, self.input_pos_unit, invert=True),\n    )  # TODO: Fix overshooting following error in drive control\n    self.max_velocity = self._valid_velocity(\n        self._max_vel, self.input_vel_unit, invert=True\n    )  # TODO: Get these from config, compare with SW limits and take most restrictive superset\n    self.min_velocity = self._valid_velocity(\n        self._min_vel, self.input_vel_unit, invert=True\n    )  # TODO: Get these from config, compare with SW limits and take most restrictive superset\n\n    logger.info(\n        \"Axis '%s': SW limits loaded \u2014 neg=%s, pos=%s (drive units)\",\n        self.name,\n        self._neg_sw_limit,\n        self._pos_sw_limit,\n    )\n    logger.info(\n        \"Axis '%s': SW limits loaded \u2014 neg=%s, pos=%s (standard units)\",\n        self.name,\n        self.min_position,\n        self.max_position,\n    )\n\n    if self.fault_present():\n        logger.warning(\"Axis '%s': fault present on init\", self.name)\n    logger.debug(\"Axis '%s' fault string: %s\", self.name, self.fault_string())\n    logger.debug(\"Axis '%s' current fault code: %s\", self.name, self.current_fault_code())\n    self.acknowledge_faults()\n    self.enable_powerstage()\n\n    if run_referencing:\n        logger.info(\"Axis '%s': referencing requested during initialization\", self.name)\n        self.home()\n</code></pre>"},{"location":"autoapi/applied_motion/backends/edcon_axis/#applied_motion.backends.edcon_axis.EdconAxis.max_speed","title":"max_speed  <code>property</code>","text":"<pre><code>max_speed: float\n</code></pre> <p>Return largest allowed speed magnitude for this axis in mm/s.</p> <p>This derives from configured signed velocity bounds and returns absolute maximum magnitude, independent of direction.</p> <p>Returns:</p> Type Description <code>float</code> <p>Maximum absolute velocity limit in mm/s.</p>"},{"location":"autoapi/applied_motion/backends/edcon_axis/#applied_motion.backends.edcon_axis.EdconAxis.home","title":"home","text":"<pre><code>home() -&gt; bool\n</code></pre> <p>Home the axis by running a referencing task.</p> <p>Acknowledges any faults, enables the power stage, and then executes a blocking referencing task to establish the axis zero point.</p> Source code in <code>src/applied_motion/backends/edcon_axis.py</code> <pre><code>def home(self) -&gt; bool:\n    \"\"\"Home the axis by running a referencing task.\n\n    Acknowledges any faults, enables the power stage, and then\n    executes a blocking referencing task to establish the axis zero\n    point.\n    \"\"\"\n    logger.info(\"Axis '%s': starting homing sequence\", self.name)\n    self.acknowledge_faults()\n    self.enable_powerstage()\n    res = self.referencing_task(nonblocking=False)\n\n    logger.info(\"Axis '%s': homing result=%s\", self.name, res)\n    logger.info(\"Axis '%s': homing complete\", self.name)\n    return res\n</code></pre>"},{"location":"autoapi/applied_motion/backends/edcon_axis/#applied_motion.backends.edcon_axis.EdconAxis.is_homed","title":"is_homed","text":"<pre><code>is_homed() -&gt; bool\n</code></pre> <p>Checks whether drive has been homed and has a valid frame of reference.</p> <p>Thin wrapper for interface consistency around <code>MotionHandler.referenced()</code>.</p> Source code in <code>src/applied_motion/backends/edcon_axis.py</code> <pre><code>def is_homed(self) -&gt; bool:\n    \"\"\"Checks whether drive has been homed and has a valid frame of reference.\n\n    Thin wrapper for interface consistency around `MotionHandler.referenced()`.\n\n    \"\"\"\n    return self.referenced()\n</code></pre>"},{"location":"autoapi/applied_motion/backends/edcon_axis/#applied_motion.backends.edcon_axis.EdconAxis.current_position","title":"current_position","text":"<pre><code>current_position()\n</code></pre> <p>Transparent wrapper around <code>MotionHandler.current_position</code>.</p>"},{"location":"autoapi/applied_motion/backends/edcon_axis/#applied_motion.backends.edcon_axis.EdconAxis.current_position--todo-is-this-necessary","title":"TODO: is this necessary?","text":"<p>Returns the raw position value in the drive's internal unit system (drive units, whose scale is determined by PNU 11724 and varies by firmware/configuration \u2014 typically 0.001 mm per unit).  Use <code>get_current_axis_position</code> when you need the position in a consistent, human-readable unit (mm).</p> Source code in <code>src/applied_motion/backends/edcon_axis.py</code> <pre><code>def current_position(self):\n    \"\"\"Transparent wrapper around ``MotionHandler.current_position``.\n\n    # TODO: is this necessary?\n\n    Returns the raw position value in the drive's **internal unit system**\n    (drive units, whose scale is determined by PNU 11724 and varies by\n    firmware/configuration \u2014 typically 0.001 mm per unit).  Use\n    [`get_current_axis_position`][applied_motion.backends.edcon_axis.EdconAxis.get_current_axis_position] when you need the position in a\n    consistent, human-readable unit (mm).\n    \"\"\"\n    logger.warning(\n        \"Axis '%s': current_position() returns a raw drive-unit value \u2014 \"\n        \"unit depends on drive configuration (PNU 11724). \"\n        \"Call get_current_axis_position() for a consistent mm value.\",\n        self.name,\n    )\n    return super().current_position()\n</code></pre>"},{"location":"autoapi/applied_motion/backends/edcon_axis/#applied_motion.backends.edcon_axis.EdconAxis.get_current_axis_position","title":"get_current_axis_position","text":"<pre><code>get_current_axis_position() -&gt; float\n</code></pre> <p>Return the current axis position in the library's canonical output unit (mm).</p> <p>Reads the raw drive-unit position from <code>MotionHandler.current_position</code>, then converts it to mm via <code>_valid_position</code> with <code>invert=True</code>. The drive's own PNU-reported position-unit scale is used for the conversion, so the result is always consistent regardless of how the drive was configured.</p> <p>Internal methods (<code>_check_overshoot</code>, unit converters) continue to use <code>super().current_position()</code> directly so their drive-unit arithmetic is unaffected.</p> Source code in <code>src/applied_motion/backends/edcon_axis.py</code> <pre><code>def get_current_axis_position(self) -&gt; float:\n    \"\"\"Return the current axis position in the library's canonical output unit (mm).\n\n    Reads the raw drive-unit position from ``MotionHandler.current_position``,\n    then converts it to mm via `_valid_position` with ``invert=True``.\n    The drive's own PNU-reported position-unit scale is used for the\n    conversion, so the result is always consistent regardless of how the\n    drive was configured.\n\n    Internal methods (`_check_overshoot`, unit converters) continue to\n    use ``super().current_position()`` directly so their drive-unit arithmetic\n    is unaffected.\n    \"\"\"\n    _mm_unit = {\"distance\": {\"unit\": \"m\", \"power\": 1, \"power_of_ten\": -3}}\n    return self._valid_position(super().current_position(), _mm_unit, invert=True)\n</code></pre>"},{"location":"autoapi/applied_motion/backends/edcon_axis/#applied_motion.backends.edcon_axis.EdconAxis.move","title":"move","text":"<pre><code>move(\n    position: int | float,\n    velocity: int | float,\n    timeout: int | None = None,\n    **kwargs,\n) -&gt; bool\n</code></pre> <p>Move the axis to a specified position with a given velocity.</p> <p>Parameters:</p> Name Type Description Default <code>position</code> <code>int | float</code> <p>Target position in millimetres (mm).  Converted internally to drive units before the motion command is issued.</p> required <code>velocity</code> <code>int | float</code> <p>Move velocity in mm/s.  Converted internally to drive units.</p> required <code>timeout</code> <code>int | None</code> <p>Optional time limit in seconds for the move.  When provided, the motion is launched in a background thread and a stop command is issued if the thread has not finished by the deadline.  When <code>None</code> (default), the call blocks until motion completes.</p> <code>None</code> <code>**kwargs</code> <p>Optional keyword overrides.  Recognised keys:</p> <ul> <li><code>position_type</code> (<code>str</code>): <code>\"absolute\"</code> for an absolute   target position or <code>\"relative\"</code> for a relative displacement.   When omitted the move is treated as absolute.</li> </ul> <code>{}</code> <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> if the motion task succeeded, <code>False</code> otherwise.</p> Source code in <code>src/applied_motion/backends/edcon_axis.py</code> <pre><code>def move(self, position: int | float, velocity: int | float, timeout: int | None = None, **kwargs) -&gt; bool:\n    \"\"\"Move the axis to a specified position with a given velocity.\n\n    Args:\n        position: Target position in millimetres (mm).  Converted internally\n            to drive units before the motion command is issued.\n        velocity: Move velocity in mm/s.  Converted internally to drive units.\n        timeout: Optional time limit in seconds for the move.  When provided,\n            the motion is launched in a background thread and a stop command\n            is issued if the thread has not finished by the deadline.  When\n            ``None`` (default), the call blocks until motion completes.\n        **kwargs: Optional keyword overrides.  Recognised keys:\n\n            - ``position_type`` (``str``): ``\"absolute\"`` for an absolute\n              target position or ``\"relative\"`` for a relative displacement.\n              When omitted the move is treated as **absolute**.\n\n    Returns:\n        ``True`` if the motion task succeeded, ``False`` otherwise.\n    \"\"\"\n    # TODO: check valid move parameters against internal set values\n    # e.g. Position limit, velocity limit, acceleration limit, etc.\n    # if not valid, raise exception\n    pos_type = kwargs.get(\"position_type\", \"absolute\")\n    logger.debug(\n        \"Axis '%s': entering move \u2014 raw position=%s  raw velocity=%s raw timeout=%s raw position_type=%s\",\n        self.name,\n        position,\n        velocity,\n        timeout,\n        pos_type,\n    )\n    positioning_type = pos_type == \"absolute\"\n\n    clamped_position = self._clamped_target(position, positioning_type)\n    logger.debug(\"Axis '%s': processing move \u2014 clamped_position=%s\", self.name, clamped_position)\n    try:\n        validated_position = int(self._valid_position(clamped_position, self.input_pos_unit))\n    except Exception:\n        logger.exception(\"Axis '%s': failed to validate position\", self.name)\n        validated_position = -5  # TODO: This seems bad and wrong\n\n    try:\n        validated_velocity = int(self._valid_velocity(velocity, self.input_vel_unit))\n    except Exception:\n        logger.exception(\"Axis '%s': failed to validate velocity\", self.name)\n        validated_velocity = 5  # TODO: This seems bad and wrong\n    logger.debug(\n        \"Axis '%s': processing move \u2014 validated_position=%s  validated_velocity=%s\",\n        self.name,\n        validated_position,\n        validated_velocity,\n    )\n    # self.configure_continuous_update(True) # continous update so that new position tasks can be started while still in motion\n    #\n    # TODO: Check faults and abort if critical fault encountered\n\n    # self.acknowledge_faults()\n\n    logger.debug(\n        \"Axis '%s': fault_string=%s  fault_code=%s\", self.name, self.fault_string(), self.current_fault_code()\n    )\n    validated_position = self._check_overshoot(validated_position, absolute=positioning_type)\n    logger.debug(\n        \"Axis '%s': clamped_position=%s  validated_velocity=%s\", self.name, validated_position, validated_velocity\n    )\n    # Skip if already at target (absolute) or zero delta (relative).\n    at_target = (\n        abs(validated_position - int(super().current_position())) == 0\n        if positioning_type\n        else abs(validated_position) == 0\n    )\n    if at_target:\n        logger.info(\"Axis '%s': already at target drive position %s, skipping move\", self.name, validated_position)\n        return True\n    result: bool = False\n    # TODO: Check powerstage enabled, attempt enable if not\n    # TODO:\n    # TODO: Check for CRITICAL error stages,\n    # TODO: ack faults,\n    # TODO: then move.\n    # TODO: Incorporate jog mode into this function?\n    self.acknowledge_faults()\n    self.enable_powerstage()\n    if timeout is None:\n        result = self.position_task(\n            validated_position,\n            validated_velocity,\n            absolute=positioning_type,\n            nonblocking=False,\n        )\n    else:\n        _result_box: list[bool] = []\n        move_thread = Thread(\n            target=lambda: _result_box.append(\n                self.position_task(\n                    validated_position,\n                    validated_velocity,\n                    absolute=positioning_type,\n                    nonblocking=False,\n                )\n            )\n        )\n        move_thread.start()\n        move_thread.join(timeout=timeout)\n        if move_thread.is_alive():\n            logger.warning(\"Axis '%s': move timed out after %ss \u2014 sending stop motion task\", self.name, timeout)\n            self.stop_motion_task()\n            # Wait for the thread to actually exit before returning; 50 ms is not enough.\n            move_thread.join(timeout=timeout)\n            if move_thread.is_alive():\n                logger.error(\n                    f\"Axis '%s': position_task thread still alive {timeout}s after stop command\", self.name\n                )\n        else:\n            result = _result_box[0] if _result_box else False  # TODO: AllTrue?\n\n    logger.info(\"Axis '%s': motion task complete, result=%s\", self.name, result)\n    return result\n</code></pre>"},{"location":"autoapi/applied_motion/backends/fposbapi_axis/","title":"fposbapi_axis","text":""},{"location":"autoapi/applied_motion/backends/fposbapi_axis/#applied_motion.backends.fposbapi_axis","title":"fposbapi_axis","text":"<p>FPosBAPI axis proxy \u2014 represents one axis of a CECC-X controlled gantry.</p> <p><code>FPosBAxis</code> delegates all motion commands through a shared <code>FPosBAPIClient</code> socket connection to the CECC-X PLC.  It satisfies the <code>Axis</code> structural interface and can be used anywhere a <code>EdconAxis</code> is accepted by <code>Gantry</code>.</p> <p>Axis index convention (matches CECC-X CoDeSys program default):</p> <ul> <li><code>1</code> \u2014 X axis</li> <li><code>2</code> \u2014 Y axis</li> <li><code>3</code> \u2014 Z axis</li> </ul> <p>ROB_POS response field layout assumed by <code>FPosBAxis.get_current_axis_position</code>::</p> <pre><code>msg_id, ROB_POS, x_mm, y_mm, z_mm, 0, NULL, SUCCESS\n</code></pre> <p>Position at field index <code>axis_index + 1</code> (1-based field 2 = X, 3 = Y, 4 = Z).</p> <p>.. warning::     <code>move</code> issues a <code>SET_PAR 103</code> (global speed) command immediately     before <code>MOVE_AXIS</code>.  These two commands are serialised by the shared     <code>FPosBAPIClient</code> lock, but they     are not atomic \u2014 concurrent moves on different axes may interleave their     <code>SET_PAR</code> calls.  Use <code>Gantry.move_to</code>     with a single-axis movement queue (not concurrent) when per-move velocity     accuracy matters.</p>"},{"location":"autoapi/applied_motion/backends/fposbapi_axis/#applied_motion.backends.fposbapi_axis.FPosBAxis","title":"FPosBAxis","text":"<pre><code>FPosBAxis(name: str, index: int, client: FPosBAPIClient)\n</code></pre> <p>FPosBAPI-backed representation of a single CECC-X gantry axis.</p> <p>Wraps <code>MOVE_AXIS</code>, <code>HOME</code>, and <code>ROB_POS</code> FPosBAPI commands behind the same public interface as <code>EdconAxis</code>.  All socket communication is performed via the shared client instance which must be owned by the parent <code>Gantry</code>.</p> <p>Attributes:</p> Name Type Description <code>name</code> <p>Human-readable axis label, e.g. <code>\"X\"</code>.</p> <code>index</code> <p>1-based axis number sent to <code>MOV_AXIS</code> (<code>1</code>=X, <code>2</code>=Y, <code>3</code>=Z by CECC-X convention).</p> <p>Initialise the axis proxy.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Human-readable axis label used in log messages and equality checks.</p> required <code>index</code> <code>int</code> <p>1-based axis index for <code>MOVE_AXIS</code> commands.  Must match the axis numbering configured in the CECC-X CoDeSys program.</p> required <code>client</code> <code>FPosBAPIClient</code> <p>Shared <code>FPosBAPIClient</code> instance owned by the parent gantry.</p> required Source code in <code>src/applied_motion/backends/fposbapi_axis.py</code> <pre><code>def __init__(self, name: str, index: int, client: FPosBAPIClient) -&gt; None:\n    \"\"\"Initialise the axis proxy.\n\n    Args:\n        name: Human-readable axis label used in log messages and equality\n            checks.\n        index: 1-based axis index for ``MOVE_AXIS`` commands.  Must match\n            the axis numbering configured in the CECC-X CoDeSys program.\n        client: Shared [`FPosBAPIClient`][applied_motion.backends.fposbapi_client.FPosBAPIClient]\n            instance owned by the parent gantry.\n    \"\"\"\n    self.name = name\n    self.index = index\n    self._client = client\n    self.min_position: float = -inf\n    self.max_position: float = inf\n    logger.info(\"FPosBAxis '%s' (index=%d) created\", name, index)\n</code></pre>"},{"location":"autoapi/applied_motion/backends/fposbapi_axis/#applied_motion.backends.fposbapi_axis.FPosBAxis.move","title":"move","text":"<pre><code>move(\n    position: float,\n    velocity: float,\n    timeout: int | None = None,\n    **kwargs,\n) -&gt; bool\n</code></pre> <p>Move this axis to position at velocity.</p> <p>Sets the global gantry speed (parameter 103) to velocity before issuing the <code>MOVE_AXIS</code> command.  The call blocks until the PLC reports <code>SUCCESS</code>.</p> <p>Parameters:</p> Name Type Description Default <code>position</code> <code>float</code> <p>Target position in mm.  Interpreted as absolute by default; pass <code>position_type=\"relative\"</code> in kwargs for a relative displacement.</p> required <code>velocity</code> <code>float</code> <p>Move speed in mm/s.  Written to PLC parameter 103 (global speed) before the move.</p> required <code>timeout</code> <code>int | None</code> <p>Optional per-command socket timeout in seconds for the <code>MOVE_AXIS</code> request.  <code>None</code> means blocking mode for this request.</p> <code>None</code> <code>**kwargs</code> <p>Optional keyword overrides.  Recognised keys:</p> <ul> <li><code>position_type</code> (<code>str</code>): <code>\"absolute\"</code> (default) or   <code>\"relative\"</code>.</li> </ul> <code>{}</code> <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> when both the speed parameter and move commands succeed;</p> <code>bool</code> <p><code>False</code> if the PLC rejects either command.</p> <p>Raises:</p> Type Description <code>~FPosBAPICommunicationError</code> <p>If a communication or protocol failure occurs.</p> Source code in <code>src/applied_motion/backends/fposbapi_axis.py</code> <pre><code>def move(self, position: float, velocity: float, timeout: int | None = None, **kwargs) -&gt; bool:\n    \"\"\"Move this axis to *position* at *velocity*.\n\n    Sets the global gantry speed (parameter 103) to *velocity* before\n    issuing the ``MOVE_AXIS`` command.  The call blocks until the PLC\n    reports ``SUCCESS``.\n\n    Args:\n        position: Target position in mm.  Interpreted as absolute by\n            default; pass ``position_type=\"relative\"`` in *kwargs* for a\n            relative displacement.\n        velocity: Move speed in mm/s.  Written to PLC parameter 103\n            (global speed) before the move.\n        timeout: Optional per-command socket timeout in seconds for the\n            ``MOVE_AXIS`` request.  ``None`` means blocking mode for this\n            request.\n        **kwargs: Optional keyword overrides.  Recognised keys:\n\n            - ``position_type`` (``str``): ``\"absolute\"`` (default) or\n              ``\"relative\"``.\n\n    Returns:\n        ``True`` when both the speed parameter and move commands succeed;\n        ``False`` if the PLC rejects either command.\n\n    Raises:\n        ~applied_motion.backends.fposbapi_client.FPosBAPICommunicationError: If a\n            communication or protocol failure occurs.\n    \"\"\"\n    position_type = kwargs.get(\"position_type\", \"absolute\")\n    rel_flag = 0 if position_type == \"absolute\" else 1\n    logger.debug(\n        \"FPosBAxis '%s': move position=%s velocity=%s rel_flag=%d\",\n        self.name,\n        position,\n        velocity,\n        rel_flag,\n    )\n    if not self._client.try_command(\"SET_PAR\", 103, velocity):\n        return False\n    result = self._client.try_command(\"MOVE_AXIS\", self.index, rel_flag, position, timeout=timeout)\n    if result:\n        logger.info(\n            \"FPosBAxis '%s': move complete (position=%s mm, velocity=%s mm/s)\",\n            self.name,\n            position,\n            velocity,\n        )\n    return result\n</code></pre>"},{"location":"autoapi/applied_motion/backends/fposbapi_axis/#applied_motion.backends.fposbapi_axis.FPosBAxis.home","title":"home","text":"<pre><code>home() -&gt; bool\n</code></pre> <p>Issue the <code>HOME</code> command, homing all axes together.</p> <p>The FPosBAPI <code>HOME</code> command references every axis simultaneously \u2014 there is no per-axis home.  Calling this method on any individual proxy triggers a full multi-axis homing sequence.</p> <p>.. note::     <code>Gantry.home</code> sends a single     <code>HOME</code> command at the gantry level and does not call this method     on each proxy individually, avoiding duplicate <code>HOME</code> requests.</p> <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> when the PLC reports the HOME command succeeded; <code>False</code></p> <code>bool</code> <p>if the PLC rejects it.</p> <p>Raises:</p> Type Description <code>~FPosBAPICommunicationError</code> <p>If a communication or protocol failure occurs.</p> Source code in <code>src/applied_motion/backends/fposbapi_axis.py</code> <pre><code>def home(self) -&gt; bool:\n    \"\"\"Issue the ``HOME`` command, homing all axes together.\n\n    The FPosBAPI ``HOME`` command references every axis simultaneously \u2014\n    there is no per-axis home.  Calling this method on any individual\n    proxy triggers a full multi-axis homing sequence.\n\n    .. note::\n        [`Gantry.home`][applied_motion.applied_motion.Gantry.home] sends a single\n        ``HOME`` command at the gantry level and does not call this method\n        on each proxy individually, avoiding duplicate ``HOME`` requests.\n\n    Returns:\n        ``True`` when the PLC reports the HOME command succeeded; ``False``\n        if the PLC rejects it.\n\n    Raises:\n        ~applied_motion.backends.fposbapi_client.FPosBAPICommunicationError: If a\n            communication or protocol failure occurs.\n    \"\"\"\n    logger.info(\"FPosBAxis '%s': issuing HOME command (homes all axes)\", self.name)\n    return self._client.try_command(\"HOME\", timeout=None)\n</code></pre>"},{"location":"autoapi/applied_motion/backends/fposbapi_axis/#applied_motion.backends.fposbapi_axis.FPosBAxis.get_current_axis_position","title":"get_current_axis_position","text":"<pre><code>get_current_axis_position() -&gt; float\n</code></pre> <p>Return this axis's current position in mm via <code>ROB_POS</code>.</p> <p>Sends <code>ROB_POS</code> and extracts this axis's coordinate from the response.  Assumes the response layout::</p> <pre><code>msg_id, ROB_POS, x_mm, y_mm, z_mm, 0, NULL, SUCCESS\n</code></pre> <p>where the position field for axis n is at <code>fields[n + 1]</code> (1-based field numbering after stripping the leading <code>msg_id</code> and <code>ROB_POS</code> label fields).</p> <p>Returns:</p> Type Description <code>float</code> <p>Current axis position in mm.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>If the response cannot be parsed.</p> <code>~FPosBAPIClientError</code> <p>If the PLC returns an error response.</p> Source code in <code>src/applied_motion/backends/fposbapi_axis.py</code> <pre><code>def get_current_axis_position(self) -&gt; float:\n    \"\"\"Return this axis's current position in mm via ``ROB_POS``.\n\n    Sends ``ROB_POS`` and extracts this axis's coordinate from the\n    response.  Assumes the response layout::\n\n        msg_id, ROB_POS, x_mm, y_mm, z_mm, 0, NULL, SUCCESS\n\n    where the position field for axis *n* is at ``fields[n + 1]``\n    (1-based field numbering after stripping the leading ``msg_id``\n    and ``ROB_POS`` label fields).\n\n    Returns:\n        Current axis position in mm.\n\n    Raises:\n        RuntimeError: If the response cannot be parsed.\n        ~applied_motion.backends.fposbapi_client.FPosBAPIClientError: If the\n            PLC returns an error response.\n    \"\"\"\n    response = self._client.send_command(\"ROB_POS\")\n    fields = [\n        f.strip() for f in response[-1].split(\",\")\n    ]  # TODO: Make this a function in the client that is passed in here\n    # fields[0] = msg_id, fields[1] = \"ROB_POS\", fields[2] = X, fields[3] = Y, fields[4] = Z\n    position_field_index = self.index + 1\n    try:\n        value = float(fields[position_field_index])\n    except (IndexError, ValueError) as exc:\n        logger.exception(\n            \"FPosBAxis '%s': cannot parse ROB_POS response \u2014 %s\",\n            self.name,\n            response,\n        )\n        raise RuntimeError(\n            f\"Failed to parse ROB_POS position for axis '{self.name}' \"\n            f\"(field index {position_field_index}): {response!r}\"\n        ) from exc\n    logger.debug(\"FPosBAxis '%s': current position = %s mm\", self.name, value)\n    return value\n</code></pre>"},{"location":"autoapi/applied_motion/backends/fposbapi_axis/#applied_motion.backends.fposbapi_axis.FPosBAxis.is_homed","title":"is_homed","text":"<pre><code>is_homed() -&gt; bool\n</code></pre> <p>Return whether the gantry has been homed via <code>IS_HOME</code>.</p> <p>Sends <code>IS_HOME</code> and interprets the response field that follows the echoed command name.  The PLC returns <code>1</code> when homing is complete and <code>0</code> when it is not.</p> <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> if the PLC reports the gantry is homed; <code>False</code></p> <code>bool</code> <p>otherwise.</p> <p>Raises:</p> Type Description <code>~FPosBAPIClientError</code> <p>If the PLC returns an error response.</p> <code>RuntimeError</code> <p>If the response cannot be parsed.</p> Source code in <code>src/applied_motion/backends/fposbapi_axis.py</code> <pre><code>def is_homed(self) -&gt; bool:\n    \"\"\"Return whether the gantry has been homed via ``IS_HOME``.\n\n    Sends ``IS_HOME`` and interprets the response field that follows the\n    echoed command name.  The PLC returns ``1`` when homing is complete\n    and ``0`` when it is not.\n\n    Returns:\n        ``True`` if the PLC reports the gantry is homed; ``False``\n        otherwise.\n\n    Raises:\n        ~applied_motion.backends.fposbapi_client.FPosBAPIClientError: If the\n            PLC returns an error response.\n        RuntimeError: If the response cannot be parsed.\n    \"\"\"\n    response = self._client.send_command(\"IS_HOME\")\n    fields = [f.strip() for f in response[-1].split(\",\")]\n    # fields: msg_id, IS_HOME, value, ERR_ID, ERR_TYPE, SUCCESS\n    try:\n        homed = bool(int(fields[2]))\n    except (IndexError, ValueError) as exc:\n        raise RuntimeError(f\"Failed to parse IS_HOME response for axis '{self.name}': {response!r}\") from exc\n    logger.debug(\"FPosBAxis '%s': is_homed=%s\", self.name, homed)\n    return homed\n</code></pre>"},{"location":"autoapi/applied_motion/backends/fposbapi_axis/#applied_motion.backends.fposbapi_axis.FPosBAxis.stopped","title":"stopped","text":"<pre><code>stopped() -&gt; bool\n</code></pre> <p>Return <code>True</code>; FPosBAPI moves are blocking so motion is always complete on return.</p> <p><code>move</code> blocks until the PLC emits a <code>SUCCESS</code> response, meaning by the time Python regains control the axis has stopped. This method always returns <code>True</code> to satisfy the <code>Axis</code> interface.</p> <p>Returns:</p> Type Description <code>bool</code> <p>Always <code>True</code>.</p> Source code in <code>src/applied_motion/backends/fposbapi_axis.py</code> <pre><code>def stopped(self) -&gt; bool:\n    \"\"\"Return ``True``; FPosBAPI moves are blocking so motion is always complete on return.\n\n    [`move`][applied_motion.backends.fposbapi_axis.FPosBAxis.move] blocks until the PLC emits a ``SUCCESS`` response,\n    meaning by the time Python regains control the axis has stopped.\n    This method always returns ``True`` to satisfy the\n    [`Axis`][applied_motion.backends.axis_protocol.Axis] interface.\n\n    Returns:\n        Always ``True``.\n    \"\"\"\n    return True\n</code></pre>"},{"location":"autoapi/applied_motion/backends/fposbapi_axis/#applied_motion.backends.fposbapi_axis.FPosBAxis.acknowledge_faults","title":"acknowledge_faults","text":"<pre><code>acknowledge_faults()\n</code></pre> <p>Clear errors.</p> Source code in <code>src/applied_motion/backends/fposbapi_axis.py</code> <pre><code>def acknowledge_faults(self):\n    \"\"\"Clear errors.\"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/applied_motion/backends/fposbapi_axis/#applied_motion.backends.fposbapi_axis.FPosBAxis.enable_powerstage","title":"enable_powerstage","text":"<pre><code>enable_powerstage()\n</code></pre> <p>Turn on torque to motor controlling this axis.</p> Source code in <code>src/applied_motion/backends/fposbapi_axis.py</code> <pre><code>def enable_powerstage(self):\n    \"\"\"Turn on torque to motor controlling this axis.\"\"\"\n    # TODO: Implement this functionality using corresponding FPosBAPI command\n    pass\n</code></pre>"},{"location":"autoapi/applied_motion/backends/fposbapi_axis/#applied_motion.backends.fposbapi_axis.FPosBAxis.disable_powerstage","title":"disable_powerstage","text":"<pre><code>disable_powerstage()\n</code></pre> <p>Turn off torque to motor controlling this axis.</p> Source code in <code>src/applied_motion/backends/fposbapi_axis.py</code> <pre><code>def disable_powerstage(self):\n    \"\"\"Turn off torque to motor controlling this axis.\"\"\"\n    # TODO: Implement this functionality using corresponding FPosBAPI command\n    pass\n</code></pre>"},{"location":"autoapi/applied_motion/backends/fposbapi_axis/#applied_motion.backends.fposbapi_axis.FPosBAxis.current_position","title":"current_position","text":"<pre><code>current_position()\n</code></pre> <p>Return current position.</p> Source code in <code>src/applied_motion/backends/fposbapi_axis.py</code> <pre><code>def current_position(self):\n    \"\"\"Return current position.\"\"\"\n    # TODO: Implement this functionality using corresponding FPosBAPI command\n    pass\n</code></pre>"},{"location":"autoapi/applied_motion/backends/fposbapi_axis/#applied_motion.backends.fposbapi_axis.FPosBAxis.current_velocity","title":"current_velocity","text":"<pre><code>current_velocity()\n</code></pre> <p>Return current velocity.</p> Source code in <code>src/applied_motion/backends/fposbapi_axis.py</code> <pre><code>def current_velocity(self):\n    \"\"\"Return current velocity.\"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/applied_motion/backends/fposbapi_axis/#applied_motion.backends.fposbapi_axis.FPosBAxis.jog_task","title":"jog_task","text":"<pre><code>jog_task(\n    jog_positive: bool = True,\n    jog_negative: bool = False,\n    incremental: bool = False,\n    duration: float = 0.0,\n) -&gt; bool\n</code></pre> <p>Jog axis.</p> Source code in <code>src/applied_motion/backends/fposbapi_axis.py</code> <pre><code>def jog_task(\n    self,\n    jog_positive: bool = True,\n    jog_negative: bool = False,\n    incremental: bool = False,\n    duration: float = 0.0,\n) -&gt; bool:\n    \"\"\"Jog axis.\"\"\"\n    # TODO: Implement this functionality using corresponding FPosBAPI command\n    return True\n</code></pre>"},{"location":"autoapi/applied_motion/backends/fposbapi_axis/#applied_motion.backends.fposbapi_axis.FPosBAxis.ready_for_motion","title":"ready_for_motion","text":"<pre><code>ready_for_motion() -&gt; bool\n</code></pre> <p>Return whether the gantry drives are enabled via <code>IS_ENBL</code>.</p> <p>Sends <code>IS_ENBL</code> and interprets the response.  The PLC returns <code>1</code> when drives are enabled and ready to accept motion commands, <code>0</code> when they are disabled.</p> <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> if the PLC reports drives are enabled; <code>False</code></p> <code>bool</code> <p>otherwise.</p> <p>Raises:</p> Type Description <code>~FPosBAPIClientError</code> <p>If the PLC returns an error response.</p> <code>RuntimeError</code> <p>If the response cannot be parsed.</p> Source code in <code>src/applied_motion/backends/fposbapi_axis.py</code> <pre><code>def ready_for_motion(self) -&gt; bool:\n    \"\"\"Return whether the gantry drives are enabled via ``IS_ENBL``.\n\n    Sends ``IS_ENBL`` and interprets the response.  The PLC returns\n    ``1`` when drives are enabled and ready to accept motion commands,\n    ``0`` when they are disabled.\n\n    Returns:\n        ``True`` if the PLC reports drives are enabled; ``False``\n        otherwise.\n\n    Raises:\n        ~applied_motion.backends.fposbapi_client.FPosBAPIClientError: If the\n            PLC returns an error response.\n        RuntimeError: If the response cannot be parsed.\n    \"\"\"\n    response = self._client.send_command(\"IS_ENBL\")\n    fields = [f.strip() for f in response[-1].split(\",\")]\n    # fields: msg_id, IS_ENBL, value, ERR_ID, ERR_TYPE, SUCCESS\n    try:\n        enabled = bool(int(fields[2]))\n    except (IndexError, ValueError) as exc:\n        raise RuntimeError(f\"Failed to parse IS_ENBL response for axis '{self.name}': {response!r}\") from exc\n    logger.debug(\"FPosBAxis '%s': ready_for_motion=%s\", self.name, enabled)\n    return enabled\n</code></pre>"},{"location":"autoapi/applied_motion/backends/fposbapi_client/","title":"fposbapi_client","text":""},{"location":"autoapi/applied_motion/backends/fposbapi_client/#applied_motion.backends.fposbapi_client","title":"fposbapi_client","text":"<p>Thread-safe ASCII TCP socket client for the Festo FPosBAPI server.</p> <p>The FPosBAPI server runs on the Festo CECC-X PLC (CoDeSys) and listens on TCP port 1234.  It speaks a line-oriented ASCII protocol::</p> <pre><code>Request:  MSG_ID, COMMAND[, PARAM, ...]\\r\\n\nResponse: MSG_ID, COMMAND[, ECHO_PARAMS..., RETURN_VALS...], ERR_ID, ERR_TYPE, ERR_MSG\\r\\n\n</code></pre> <p>On success the last three comma-delimited fields are <code>0, NULL, SUCCESS</code>. On error the last three fields carry a non-zero error id, type string, and message string.</p> <p>A single <code>FPosBAPIClient</code> instance should be shared across all <code>FPosBAxis</code> objects belonging to the same gantry, and is owned by <code>Gantry</code>. All send/receive operations are serialised through an internal <code>threading.Lock</code>.</p>"},{"location":"autoapi/applied_motion/backends/fposbapi_client/#applied_motion.backends.fposbapi_client.FPosBAPIClientError","title":"FPosBAPIClientError","text":"<p>               Bases: <code>Exception</code></p> <p>Raised when the FPosBAPI server returns an error response or the connection is lost.</p>"},{"location":"autoapi/applied_motion/backends/fposbapi_client/#applied_motion.backends.fposbapi_client.FPosBAPICommunicationError","title":"FPosBAPICommunicationError","text":"<p>               Bases: <code>FPosBAPIClientError</code></p> <p>Raised when a transport or protocol-level failure occurs (socket, framing, ID mismatch).</p>"},{"location":"autoapi/applied_motion/backends/fposbapi_client/#applied_motion.backends.fposbapi_client.FPosBAPICommandError","title":"FPosBAPICommandError","text":"<p>               Bases: <code>FPosBAPIClientError</code></p> <p>Raised when the PLC returns a structurally valid but non-SUCCESS response.</p>"},{"location":"autoapi/applied_motion/backends/fposbapi_client/#applied_motion.backends.fposbapi_client.FPosBAPIClient","title":"FPosBAPIClient","text":"<pre><code>FPosBAPIClient(\n    ip: str, port: int = 1234, timeout: float | None = None\n)\n</code></pre> <p>Thread-safe TCP socket client for the Festo FPosBAPI ASCII protocol.</p> <p>Connects to the CECC-X PLC on ip:port and wraps the request/response cycle in <code>send_command</code>.  A <code>threading.Lock</code> serialises all socket I/O so multiple <code>FPosBAxis</code> objects sharing the same client do not interleave their frames.</p> <p>Attributes:</p> Name Type Description <code>ip</code> <p>IPv4 address of the CECC-X TCP server.</p> <code>port</code> <p>TCP port of the FPosBAPI server (default <code>1234</code>).</p> <p>Connect to the FPosBAPI server.</p> <p>Parameters:</p> Name Type Description Default <code>ip</code> <code>str</code> <p>IPv4 address of the CECC-X PLC.</p> required <code>port</code> <code>int</code> <p>TCP port the FPosBAPI server is listening on.  Defaults to <code>1234</code>.</p> <code>1234</code> <code>timeout</code> <code>float | None</code> <p>Socket timeout in seconds.  Applied to both <code>connect</code> and <code>recv</code> operations.  <code>None</code> (default) means blocking with no timeout.</p> <code>None</code> <p>Raises:</p> Type Description <code>OSError</code> <p>If the TCP connection cannot be established within timeout seconds.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def __init__(self, ip: str, port: int = 1234, timeout: float | None = None) -&gt; None:\n    \"\"\"Connect to the FPosBAPI server.\n\n    Args:\n        ip: IPv4 address of the CECC-X PLC.\n        port: TCP port the FPosBAPI server is listening on.  Defaults to\n            ``1234``.\n        timeout: Socket timeout in seconds.  Applied to both ``connect``\n            and ``recv`` operations.  ``None`` (default) means blocking\n            with no timeout.\n\n    Raises:\n        OSError: If the TCP connection cannot be established within\n            *timeout* seconds.\n    \"\"\"\n    self.ip = ip\n    self.port = port\n    self._timeout = timeout\n    self._lock = threading.Lock()\n    self._msg_id = 0\n    self._sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)\n\n    logger.info(\"FPosBAPIClient attempting to connect to %s:%d\", ip, port)\n    self._connect()\n    logger.info(\"FPosBAPIClient connected to %s:%d\", ip, port)\n</code></pre>"},{"location":"autoapi/applied_motion/backends/fposbapi_client/#applied_motion.backends.fposbapi_client.FPosBAPIClient.send_command","title":"send_command","text":"<pre><code>send_command(\n    command: str,\n    *params,\n    timeout: float | None | _Unset = _UNSET,\n) -&gt; list[str]\n</code></pre> <p>Send a command to the FPosBAPI server and return all response lines.</p> <p>Acquires the internal lock, increments the message ID, formats the ASCII request frame, sends it, then reads lines via <code>_collect_response</code> until the terminal (non-<code>ACK</code>) line arrives.  The terminal (last) line is validated for correct MSG_ID echo, CMD echo, and <code>SUCCESS</code> status before returning.</p> <p>Parameters:</p> Name Type Description Default <code>command</code> <code>str</code> <p>FPosBAPI command string, e.g. <code>\"ENABLE\"</code>, <code>\"MOVE_AXIS\"</code>.</p> required <code>*params</code> <p>Zero or more positional parameters appended to the frame.</p> <code>()</code> <code>timeout</code> <code>float | None | _Unset</code> <p>Per-call socket timeout in seconds.  Pass <code>None</code> for blocking mode (no timeout), e.g. for long-running motion commands.  Omit to use the client's configured defaults.</p> <code>_UNSET</code> <p>Returns:</p> Type Description <code>list[str]</code> <p>All response lines received before the empty terminator, in arrival</p> <code>list[str]</code> <p>order.  The last element is always the terminal status line echoing</p> <code>list[str]</code> <p>the command and carrying <code>SUCCESS</code>.</p> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error status, the echoed MSG_ID or command name does not match what was sent, the response is empty, or the connection is closed.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def send_command(self, command: str, *params, timeout: float | None | _Unset = _UNSET) -&gt; list[str]:\n    \"\"\"Send a command to the FPosBAPI server and return all response lines.\n\n    Acquires the internal lock, increments the message ID, formats the\n    ASCII request frame, sends it, then reads lines via\n    `_collect_response` until the terminal (non-``ACK``) line\n    arrives.  The terminal (last) line is validated for correct MSG_ID\n    echo, CMD echo, and ``SUCCESS`` status before returning.\n\n    Args:\n        command: FPosBAPI command string, e.g. ``\"ENABLE\"``, ``\"MOVE_AXIS\"``.\n        *params: Zero or more positional parameters appended to the frame.\n        timeout: Per-call socket timeout in seconds.  Pass ``None`` for\n            blocking mode (no timeout), e.g. for long-running motion\n            commands.  Omit to use the client's configured defaults.\n\n    Returns:\n        All response lines received before the empty terminator, in arrival\n        order.  The last element is always the terminal status line echoing\n        the command and carrying ``SUCCESS``.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error status, the\n            echoed MSG_ID or command name does not match what was sent, the\n            response is empty, or the connection is closed.\n    \"\"\"\n    with self._lock:\n        if not isinstance(timeout, _Unset):\n            self._sock.settimeout(timeout)\n        msg_id = self._next_id()\n        raw = self._build_frame(msg_id, command, params)\n        logger.debug(\"FPosBAPIClient tx: %s\", raw.strip())\n        self._send_frame(raw, command, timeout)\n        try:\n            lines = self._collect_response()\n        finally:\n            if not isinstance(timeout, _Unset):\n                self._sock.settimeout(self._timeout)\n        logger.debug(\"FPosBAPIClient rx: %s\", \", \".join(lines))\n        self._validate_terminal(lines, msg_id, command)\n    return lines\n</code></pre>"},{"location":"autoapi/applied_motion/backends/fposbapi_client/#applied_motion.backends.fposbapi_client.FPosBAPIClient.try_command","title":"try_command","text":"<pre><code>try_command(\n    command: str,\n    *params,\n    timeout: float | None | _Unset = _UNSET,\n) -&gt; bool\n</code></pre> <p>Send a command; raise on communication failure, return bool for PLC result.</p> <p>Parameters:</p> Name Type Description Default <code>command</code> <code>str</code> <p>FPosBAPI command string, e.g. <code>\"HOME\"</code>, <code>\"MOVE_AXIS\"</code>.</p> required <code>*params</code> <p>Zero or more positional parameters.</p> <code>()</code> <code>timeout</code> <code>float | None | _Unset</code> <p>Per-call socket timeout forwarded to <code>send_command</code>. Pass <code>None</code> for blocking mode on long-running motion commands.</p> <code>_UNSET</code> <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> when the PLC reports <code>SUCCESS</code>; <code>False</code> when the PLC</p> <code>bool</code> <p>returns a valid but non-SUCCESS response.</p> <p>Raises:</p> Type Description <code>FPosBAPICommunicationError</code> <p>If a transport or protocol-level failure occurs.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def try_command(self, command: str, *params, timeout: float | None | _Unset = _UNSET) -&gt; bool:\n    \"\"\"Send a command; raise on communication failure, return bool for PLC result.\n\n    Args:\n        command: FPosBAPI command string, e.g. ``\"HOME\"``, ``\"MOVE_AXIS\"``.\n        *params: Zero or more positional parameters.\n        timeout: Per-call socket timeout forwarded to [`send_command`][applied_motion.backends.fposbapi_client.FPosBAPIClient.send_command].\n            Pass ``None`` for blocking mode on long-running motion commands.\n\n    Returns:\n        ``True`` when the PLC reports ``SUCCESS``; ``False`` when the PLC\n        returns a valid but non-SUCCESS response.\n\n    Raises:\n        FPosBAPICommunicationError: If a transport or protocol-level failure occurs.\n    \"\"\"\n    try:\n        self.send_command(command, *params, timeout=timeout)\n        return True\n    except FPosBAPICommandError:\n        logger.warning(\"FPosBAPIClient: command %r rejected by PLC\", command)\n        return False\n</code></pre>"},{"location":"autoapi/applied_motion/backends/fposbapi_client/#applied_motion.backends.fposbapi_client.FPosBAPIClient.get_veab","title":"get_veab","text":"<pre><code>get_veab() -&gt; int\n</code></pre> <p>Get the pressure set value of the VEAB.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def get_veab(self) -&gt; int:\n    \"\"\"Get the pressure set value of the VEAB.\"\"\"\n    # TODO: This __REALLY__ needs to be contingent on the config and how it is passed in\n    # TODO: The ideal would be a PressureControl() class that shares the intantiated gantry and can take it in as input parameter. All of these classes would benefit from that bc of necessary shared socket on CECC-X\n\n    lines = self.send_command(\"GET_VEAB\")\n    terminal = lines[-1]\n    fields = [f.strip() for f in terminal.split(\",\")]\n\n    return int(fields[2])\n</code></pre>"},{"location":"autoapi/applied_motion/backends/fposbapi_client/#applied_motion.backends.fposbapi_client.FPosBAPIClient.set_veab","title":"set_veab","text":"<pre><code>set_veab(set_value: int) -&gt; None\n</code></pre> <p>Set the pressure value of the VEAB.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def set_veab(self, set_value: int) -&gt; None:\n    \"\"\"Set the pressure value of the VEAB.\"\"\"\n    self.send_command(\"SET_VEAB\", set_value)\n</code></pre>"},{"location":"autoapi/applied_motion/backends/fposbapi_client/#applied_motion.backends.fposbapi_client.FPosBAPIClient.list_commands","title":"list_commands","text":"<pre><code>list_commands() -&gt; list[str]\n</code></pre> <p>Query the server for its supported command set via <code>CMD_LIST</code>.</p> <p>Sends <code>CMD_LIST</code> and parses the response to extract the command names the connected FPosBAPI server advertises.  The set varies between firmware versions, so callers can use this to feature-detect before issuing optional commands (e.g. <code>IS_HOME</code>, <code>SYS_STATUS</code>).</p> <p>Response layout assumed::</p> <pre><code>MSG_ID, CMD_LIST, CMD_1, CMD_2, ..., CMD_N, ERR_ID, ERR_TYPE, ERR_MSG\n</code></pre> <p>The first two fields (<code>MSG_ID</code> and <code>CMD_LIST</code>) and the last three (error fields ending with <code>SUCCESS</code>) are stripped; the remaining fields are returned as a list of command name strings.</p> <p>Returns:</p> Type Description <code>list[str]</code> <p>List of command name strings advertised by the server,</p> <code>list[str]</code> <p>e.g. <code>[\"ENABLE\", \"DISABLE\", \"HOME\", \"MOVE_AXIS\", ...]</code>.</p> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def list_commands(self) -&gt; list[str]:\n    \"\"\"Query the server for its supported command set via ``CMD_LIST``.\n\n    Sends ``CMD_LIST`` and parses the response to extract the command\n    names the connected FPosBAPI server advertises.  The set varies\n    between firmware versions, so callers can use this to feature-detect\n    before issuing optional commands (e.g. ``IS_HOME``, ``SYS_STATUS``).\n\n    Response layout assumed::\n\n        MSG_ID, CMD_LIST, CMD_1, CMD_2, ..., CMD_N, ERR_ID, ERR_TYPE, ERR_MSG\n\n    The first two fields (``MSG_ID`` and ``CMD_LIST``) and the last\n    three (error fields ending with ``SUCCESS``) are stripped; the\n    remaining fields are returned as a list of command name strings.\n\n    Returns:\n        List of command name strings advertised by the server,\n        e.g. ``[\"ENABLE\", \"DISABLE\", \"HOME\", \"MOVE_AXIS\", ...]``.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    lines = self.send_command(\"CMD_LIST\")\n    # The server sends one bare command name per intermediate line.\n    # lines[0] is the ACK, lines[1:-1] are the bare command names,\n    # lines[-1] is the terminal SUCCESS line.\n    commands = [line.strip() for line in lines[1:-1] if line.strip()]\n    logger.debug(\"FPosBAPIClient: CMD_LIST returned %d command(s): %s\", len(commands), commands)\n    return commands\n</code></pre>"},{"location":"autoapi/applied_motion/backends/fposbapi_client/#applied_motion.backends.fposbapi_client.FPosBAPIClient.enable","title":"enable","text":"<pre><code>enable() -&gt; None\n</code></pre> <p>Enable the gantry drives via <code>ENABLE</code>.</p> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def enable(self) -&gt; None:\n    \"\"\"Enable the gantry drives via ``ENABLE``.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    self.send_command(\"ENABLE\")\n    logger.info(\"FPosBAPIClient: drives enabled\")\n</code></pre>"},{"location":"autoapi/applied_motion/backends/fposbapi_client/#applied_motion.backends.fposbapi_client.FPosBAPIClient.disable","title":"disable","text":"<pre><code>disable() -&gt; None\n</code></pre> <p>Disable the gantry drives via <code>DISABLE</code>.</p> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def disable(self) -&gt; None:\n    \"\"\"Disable the gantry drives via ``DISABLE``.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    self.send_command(\"DISABLE\")\n    logger.info(\"FPosBAPIClient: drives disabled\")\n</code></pre>"},{"location":"autoapi/applied_motion/backends/fposbapi_client/#applied_motion.backends.fposbapi_client.FPosBAPIClient.home","title":"home","text":"<pre><code>home() -&gt; None\n</code></pre> <p>Home all axes via <code>HOME</code>.</p> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def home(self) -&gt; None:\n    \"\"\"Home all axes via ``HOME``.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    self.send_command(\"HOME\")\n    logger.info(\"FPosBAPIClient: homing complete\")\n</code></pre>"},{"location":"autoapi/applied_motion/backends/fposbapi_client/#applied_motion.backends.fposbapi_client.FPosBAPIClient.move_pos","title":"move_pos","text":"<pre><code>move_pos(\n    pos_id: int,\n    tool_id: int = 0,\n    retract_z: int = 0,\n    slow_app: int = 0,\n) -&gt; None\n</code></pre> <p>Execute a motion sequence to a stored position via <code>MOVE_POS</code>.</p> <p>Parameters:</p> Name Type Description Default <code>pos_id</code> <code>int</code> <p>Position record ID (1-100).</p> required <code>tool_id</code> <code>int</code> <p>Tool index to apply.  <code>0</code> selects no tool.</p> <code>0</code> <code>retract_z</code> <code>int</code> <p><code>1</code> to retract Z before moving, <code>0</code> to skip.</p> <code>0</code> <code>slow_app</code> <code>int</code> <p><code>1</code> to slow approach the target position, <code>0</code> for full speed.</p> <code>0</code> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def move_pos(\n    self,\n    pos_id: int,\n    tool_id: int = 0,\n    retract_z: int = 0,\n    slow_app: int = 0,\n) -&gt; None:\n    \"\"\"Execute a motion sequence to a stored position via ``MOVE_POS``.\n\n    Args:\n        pos_id: Position record ID (1-100).\n        tool_id: Tool index to apply.  ``0`` selects no tool.\n        retract_z: ``1`` to retract Z before moving, ``0`` to skip.\n        slow_app: ``1`` to slow approach the target position, ``0`` for\n            full speed.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    self.send_command(\"MOVE_POS\", pos_id, tool_id, retract_z, slow_app)\n    logger.info(\"FPosBAPIClient: MOVE_POS complete (pos_id=%d, tool_id=%d)\", pos_id, tool_id)\n</code></pre>"},{"location":"autoapi/applied_motion/backends/fposbapi_client/#applied_motion.backends.fposbapi_client.FPosBAPIClient.move_loc","title":"move_loc","text":"<pre><code>move_loc(\n    a1: float,\n    a2: float,\n    a3: float,\n    tool_id: int = 0,\n    move_typ: int = 0,\n    retract_z: int = 0,\n    slow_app: int = 0,\n) -&gt; None\n</code></pre> <p>Execute a coordinated 3-axis move to explicit coordinates via <code>MOVE_LOC</code>.</p> <p>Parameters:</p> Name Type Description Default <code>a1</code> <code>float</code> <p>Target X-axis position in mm.</p> required <code>a2</code> <code>float</code> <p>Target Y-axis position in mm.</p> required <code>a3</code> <code>float</code> <p>Target Z-axis position in mm.</p> required <code>tool_id</code> <code>int</code> <p>Tool offset to apply.  <code>0</code> selects no tool.</p> <code>0</code> <code>move_typ</code> <code>int</code> <p><code>0</code> for absolute, <code>1</code> for relative.</p> <code>0</code> <code>retract_z</code> <code>int</code> <p><code>1</code> to retract Z before moving, <code>0</code> to skip.</p> <code>0</code> <code>slow_app</code> <code>int</code> <p><code>1</code> to slow approach the target position, <code>0</code> for full speed.</p> <code>0</code> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def move_loc(\n    self,\n    a1: float,\n    a2: float,\n    a3: float,\n    tool_id: int = 0,\n    move_typ: int = 0,\n    retract_z: int = 0,\n    slow_app: int = 0,\n) -&gt; None:\n    \"\"\"Execute a coordinated 3-axis move to explicit coordinates via ``MOVE_LOC``.\n\n    Args:\n        a1: Target X-axis position in mm.\n        a2: Target Y-axis position in mm.\n        a3: Target Z-axis position in mm.\n        tool_id: Tool offset to apply.  ``0`` selects no tool.\n        move_typ: ``0`` for absolute, ``1`` for relative.\n        retract_z: ``1`` to retract Z before moving, ``0`` to skip.\n        slow_app: ``1`` to slow approach the target position, ``0`` for\n            full speed.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    self.send_command(\"MOVE_LOC\", a1, a2, a3, tool_id, move_typ, retract_z, slow_app)\n    logger.info(\"FPosBAPIClient: MOVE_LOC complete (a1=%s, a2=%s, a3=%s mm)\", a1, a2, a3)\n</code></pre>"},{"location":"autoapi/applied_motion/backends/fposbapi_client/#applied_motion.backends.fposbapi_client.FPosBAPIClient.move_path","title":"move_path","text":"<pre><code>move_path() -&gt; None\n</code></pre> <p>Execute the programmed motion path via <code>MOVE_PATH</code>.</p> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def move_path(self) -&gt; None:\n    \"\"\"Execute the programmed motion path via ``MOVE_PATH``.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    # TODO: Needs WRITE_PATH?\n    self.send_command(\"MOVE_PATH\")\n    logger.info(\"FPosBAPIClient: MOVE_PATH complete\")\n</code></pre>"},{"location":"autoapi/applied_motion/backends/fposbapi_client/#applied_motion.backends.fposbapi_client.FPosBAPIClient.halt","title":"halt","text":"<pre><code>halt() -&gt; None\n</code></pre> <p>Halt the current motion via <code>HALT</code>.</p> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def halt(self) -&gt; None:\n    \"\"\"Halt the current motion via ``HALT``.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    self.send_command(\"HALT\")\n    logger.info(\"FPosBAPIClient: HALT issued\")\n</code></pre>"},{"location":"autoapi/applied_motion/backends/fposbapi_client/#applied_motion.backends.fposbapi_client.FPosBAPIClient.resume","title":"resume","text":"<pre><code>resume() -&gt; None\n</code></pre> <p>Resume a halted motion via <code>RESUME</code>.</p> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def resume(self) -&gt; None:\n    \"\"\"Resume a halted motion via ``RESUME``.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    self.send_command(\"RESUME\")\n    logger.info(\"FPosBAPIClient: RESUME issued\")\n</code></pre>"},{"location":"autoapi/applied_motion/backends/fposbapi_client/#applied_motion.backends.fposbapi_client.FPosBAPIClient.abort","title":"abort","text":"<pre><code>abort() -&gt; None\n</code></pre> <p>Abort the current motion via <code>ABORT</code>.</p> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def abort(self) -&gt; None:\n    \"\"\"Abort the current motion via ``ABORT``.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    self.send_command(\"ABORT\")\n    logger.info(\"FPosBAPIClient: ABORT issued\")\n</code></pre>"},{"location":"autoapi/applied_motion/backends/fposbapi_client/#applied_motion.backends.fposbapi_client.FPosBAPIClient.reset_err","title":"reset_err","text":"<pre><code>reset_err() -&gt; None\n</code></pre> <p>Reset the active error via <code>RESET_ERR</code>.</p> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def reset_err(self) -&gt; None:\n    \"\"\"Reset the active error via ``RESET_ERR``.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    self.send_command(\"RESET_ERR\")\n    logger.info(\"FPosBAPIClient: error reset\")\n</code></pre>"},{"location":"autoapi/applied_motion/backends/fposbapi_client/#applied_motion.backends.fposbapi_client.FPosBAPIClient.open_valve","title":"open_valve","text":"<pre><code>open_valve(\n    v1_time: int = 0,\n    v2_time: int = 0,\n    v3_time: int = 0,\n    v4_time: int = 0,\n    v5_time: int = 0,\n    v6_time: int = 0,\n    v7_time: int = 0,\n    v8_time: int = 0,\n) -&gt; None\n</code></pre> <p>Open one or more valves for the specified durations via <code>OPEN_VALVE</code>.</p> <p>Parameters:</p> Name Type Description Default <code>v1_time</code> <code>int</code> <p>Valve 1 open duration in ms.  <code>0</code> keeps the valve closed.</p> <code>0</code> <code>v2_time</code> <code>int</code> <p>Valve 2 open duration in ms.</p> <code>0</code> <code>v3_time</code> <code>int</code> <p>Valve 3 open duration in ms.</p> <code>0</code> <code>v4_time</code> <code>int</code> <p>Valve 4 open duration in ms.</p> <code>0</code> <code>v5_time</code> <code>int</code> <p>Valve 5 open duration in ms.</p> <code>0</code> <code>v6_time</code> <code>int</code> <p>Valve 6 open duration in ms.</p> <code>0</code> <code>v7_time</code> <code>int</code> <p>Valve 7 open duration in ms.</p> <code>0</code> <code>v8_time</code> <code>int</code> <p>Valve 8 open duration in ms.</p> <code>0</code> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def open_valve(\n    self,\n    v1_time: int = 0,\n    v2_time: int = 0,\n    v3_time: int = 0,\n    v4_time: int = 0,\n    v5_time: int = 0,\n    v6_time: int = 0,\n    v7_time: int = 0,\n    v8_time: int = 0,\n) -&gt; None:\n    \"\"\"Open one or more valves for the specified durations via ``OPEN_VALVE``.\n\n    Args:\n        v1_time: Valve 1 open duration in ms.  ``0`` keeps the valve closed.\n        v2_time: Valve 2 open duration in ms.\n        v3_time: Valve 3 open duration in ms.\n        v4_time: Valve 4 open duration in ms.\n        v5_time: Valve 5 open duration in ms.\n        v6_time: Valve 6 open duration in ms.\n        v7_time: Valve 7 open duration in ms.\n        v8_time: Valve 8 open duration in ms.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    self.send_command(\n        \"OPEN_VALVE\",\n        v1_time,\n        v2_time,\n        v3_time,\n        v4_time,\n        v5_time,\n        v6_time,\n        v7_time,\n        v8_time,\n    )\n    logger.info(\"FPosBAPIClient: OPEN_VALVE complete\")\n</code></pre>"},{"location":"autoapi/applied_motion/backends/fposbapi_client/#applied_motion.backends.fposbapi_client.FPosBAPIClient.read_pos","title":"read_pos","text":"<pre><code>read_pos(pos_id: int) -&gt; tuple[float, float, float]\n</code></pre> <p>Read the stored absolute position for pos_id via <code>READ_POS</code>.</p> <p>Response layout::</p> <pre><code>MSG_ID, READ_POS, POS_ID, ABS_A1, ABS_A2, ABS_A3, 0, NULL, SUCCESS\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>pos_id</code> <code>int</code> <p>Position record ID (1-100).</p> required <p>Returns:</p> Type Description <code>tuple[float, float, float]</code> <p>Tuple of <code>(abs_a1, abs_a2, abs_a3)</code> in mm.</p> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> <code>RuntimeError</code> <p>If the response cannot be parsed.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def read_pos(self, pos_id: int) -&gt; tuple[float, float, float]:\n    \"\"\"Read the stored absolute position for *pos_id* via ``READ_POS``.\n\n    Response layout::\n\n        MSG_ID, READ_POS, POS_ID, ABS_A1, ABS_A2, ABS_A3, 0, NULL, SUCCESS\n\n    Args:\n        pos_id: Position record ID (1-100).\n\n    Returns:\n        Tuple of ``(abs_a1, abs_a2, abs_a3)`` in mm.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n        RuntimeError: If the response cannot be parsed.\n    \"\"\"\n    lines = self.send_command(\"READ_POS\", pos_id)\n    fields = [f.strip() for f in lines[-1].split(\",\")]\n    # fields: msg_id, READ_POS, pos_id, abs_a1, abs_a2, abs_a3, 0, NULL, SUCCESS\n    try:\n        result = float(fields[3]), float(fields[4]), float(fields[5])\n    except (IndexError, ValueError) as exc:\n        raise RuntimeError(f\"Failed to parse READ_POS response for pos_id={pos_id}: {lines!r}\") from exc\n    logger.debug(\"FPosBAPIClient: READ_POS pos_id=%d \u2192 %s\", pos_id, result)\n    return result\n</code></pre>"},{"location":"autoapi/applied_motion/backends/fposbapi_client/#applied_motion.backends.fposbapi_client.FPosBAPIClient.teach_pos","title":"teach_pos","text":"<pre><code>teach_pos(pos_id: int, tool_id: int = 1) -&gt; None\n</code></pre> <p>Save the current axis positions to pos_id via <code>TEACH_POS</code>.</p> <p>Parameters:</p> Name Type Description Default <code>pos_id</code> <code>int</code> <p>Position record ID to write (1-100).</p> required <code>tool_id</code> <code>int</code> <p>Tool offset to associate with this position.</p> <code>1</code> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def teach_pos(self, pos_id: int, tool_id: int = 1) -&gt; None:\n    \"\"\"Save the current axis positions to *pos_id* via ``TEACH_POS``.\n\n    Args:\n        pos_id: Position record ID to write (1-100).\n        tool_id: Tool offset to associate with this position.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    self.send_command(\"TEACH_POS\", pos_id, tool_id)\n    logger.info(\"FPosBAPIClient: TEACH_POS complete (pos_id=%d)\", pos_id)\n</code></pre>"},{"location":"autoapi/applied_motion/backends/fposbapi_client/#applied_motion.backends.fposbapi_client.FPosBAPIClient.write_pos","title":"write_pos","text":"<pre><code>write_pos(\n    pos_id: int, abs_a1: float, abs_a2: float, abs_a3: float\n) -&gt; None\n</code></pre> <p>Write absolute axis coordinates to pos_id via <code>WRITE_POS</code>.</p> <p>Parameters:</p> Name Type Description Default <code>pos_id</code> <code>int</code> <p>Position record ID to write (1-100).</p> required <code>abs_a1</code> <code>float</code> <p>Absolute X-axis position in mm.</p> required <code>abs_a2</code> <code>float</code> <p>Absolute Y-axis position in mm.</p> required <code>abs_a3</code> <code>float</code> <p>Absolute Z-axis position in mm.</p> required <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def write_pos(\n    self,\n    pos_id: int,\n    abs_a1: float,\n    abs_a2: float,\n    abs_a3: float,\n) -&gt; None:\n    \"\"\"Write absolute axis coordinates to *pos_id* via ``WRITE_POS``.\n\n    Args:\n        pos_id: Position record ID to write (1-100).\n        abs_a1: Absolute X-axis position in mm.\n        abs_a2: Absolute Y-axis position in mm.\n        abs_a3: Absolute Z-axis position in mm.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    self.send_command(\"WRITE_POS\", pos_id, abs_a1, abs_a2, abs_a3)\n    logger.info(\n        \"FPosBAPIClient: WRITE_POS complete (pos_id=%d, a1=%s, a2=%s, a3=%s mm)\",\n        pos_id,\n        abs_a1,\n        abs_a2,\n        abs_a3,\n    )\n</code></pre>"},{"location":"autoapi/applied_motion/backends/fposbapi_client/#applied_motion.backends.fposbapi_client.FPosBAPIClient.mod_pos","title":"mod_pos","text":"<pre><code>mod_pos(\n    pos_id: int, rel_a1: float, rel_a2: float, rel_a3: float\n) -&gt; None\n</code></pre> <p>Modify a stored position by relative offsets via <code>MOD_POS</code>.</p> <p>Parameters:</p> Name Type Description Default <code>pos_id</code> <code>int</code> <p>Position record ID to modify (1-100).</p> required <code>rel_a1</code> <code>float</code> <p>Relative X-axis offset in mm.</p> required <code>rel_a2</code> <code>float</code> <p>Relative Y-axis offset in mm.</p> required <code>rel_a3</code> <code>float</code> <p>Relative Z-axis offset in mm.</p> required <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def mod_pos(\n    self,\n    pos_id: int,\n    rel_a1: float,\n    rel_a2: float,\n    rel_a3: float,\n) -&gt; None:\n    \"\"\"Modify a stored position by relative offsets via ``MOD_POS``.\n\n    Args:\n        pos_id: Position record ID to modify (1-100).\n        rel_a1: Relative X-axis offset in mm.\n        rel_a2: Relative Y-axis offset in mm.\n        rel_a3: Relative Z-axis offset in mm.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    self.send_command(\"MOD_POS\", pos_id, rel_a1, rel_a2, rel_a3)\n    logger.info(\"FPosBAPIClient: MOD_POS complete (pos_id=%d)\", pos_id)\n</code></pre>"},{"location":"autoapi/applied_motion/backends/fposbapi_client/#applied_motion.backends.fposbapi_client.FPosBAPIClient.write_path","title":"write_path","text":"<pre><code>write_path(\n    pa_pos_id: int,\n    abs_a1: float,\n    abs_a2: float,\n    abs_a3: float,\n) -&gt; None\n</code></pre> <p>Write one waypoint along the motion path via <code>WRITE_PATH</code>.</p> <p>Parameters:</p> Name Type Description Default <code>pa_pos_id</code> <code>int</code> <p>Path position index (1-10).</p> required <code>abs_a1</code> <code>float</code> <p>Absolute X-axis position in mm.</p> required <code>abs_a2</code> <code>float</code> <p>Absolute Y-axis position in mm.</p> required <code>abs_a3</code> <code>float</code> <p>Absolute Z-axis position in mm.</p> required <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def write_path(\n    self,\n    pa_pos_id: int,\n    abs_a1: float,\n    abs_a2: float,\n    abs_a3: float,\n) -&gt; None:\n    \"\"\"Write one waypoint along the motion path via ``WRITE_PATH``.\n\n    Args:\n        pa_pos_id: Path position index (1-10).\n        abs_a1: Absolute X-axis position in mm.\n        abs_a2: Absolute Y-axis position in mm.\n        abs_a3: Absolute Z-axis position in mm.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    self.send_command(\"WRITE_PATH\", pa_pos_id, abs_a1, abs_a2, abs_a3)\n    logger.info(\"FPosBAPIClient: WRITE_PATH complete (pa_pos_id=%d)\", pa_pos_id)\n</code></pre>"},{"location":"autoapi/applied_motion/backends/fposbapi_client/#applied_motion.backends.fposbapi_client.FPosBAPIClient.read_path","title":"read_path","text":"<pre><code>read_path(pa_pos_id: int) -&gt; tuple[float, float, float]\n</code></pre> <p>Read one waypoint from the motion path via <code>READ_PATH</code>.</p> <p>Parameters:</p> Name Type Description Default <code>pa_pos_id</code> <code>int</code> <p>Path position index (1-10).</p> required <p>Returns:</p> Type Description <code>tuple[float, float, float]</code> <p>Tuple of <code>(abs_a1, abs_a2, abs_a3)</code> in mm.</p> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> <code>RuntimeError</code> <p>If the response cannot be parsed.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def read_path(self, pa_pos_id: int) -&gt; tuple[float, float, float]:\n    \"\"\"Read one waypoint from the motion path via ``READ_PATH``.\n\n    Args:\n        pa_pos_id: Path position index (1-10).\n\n    Returns:\n        Tuple of ``(abs_a1, abs_a2, abs_a3)`` in mm.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n        RuntimeError: If the response cannot be parsed.\n    \"\"\"\n    lines = self.send_command(\"READ_PATH\", pa_pos_id)\n    fields = [f.strip() for f in lines[-1].split(\",\")]\n    # fields: msg_id, READ_PATH, pa_pos_id, abs_a1, abs_a2, abs_a3, 0, NULL, SUCCESS\n    try:\n        result = float(fields[3]), float(fields[4]), float(fields[5])\n    except (IndexError, ValueError) as exc:\n        raise RuntimeError(f\"Failed to parse READ_PATH response for pa_pos_id={pa_pos_id}: {lines!r}\") from exc\n    logger.debug(\"FPosBAPIClient: READ_PATH pa_pos_id=%d \u2192 %s\", pa_pos_id, result)\n    return result\n</code></pre>"},{"location":"autoapi/applied_motion/backends/fposbapi_client/#applied_motion.backends.fposbapi_client.FPosBAPIClient.sys_status","title":"sys_status","text":"<pre><code>sys_status() -&gt; str\n</code></pre> <p>Return the system status string via <code>SYS_STATUS</code>.</p> <p>Response layout::</p> <pre><code>MSG_ID, SYS_STATUS, STATUS, 0, NULL, SUCCESS\n</code></pre> <p>Returns:</p> Type Description <code>str</code> <p>Status string reported by the PLC (e.g. <code>\"IDLE\"</code>).</p> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> <code>RuntimeError</code> <p>If the response cannot be parsed.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def sys_status(self) -&gt; str:\n    \"\"\"Return the system status string via ``SYS_STATUS``.\n\n    Response layout::\n\n        MSG_ID, SYS_STATUS, STATUS, 0, NULL, SUCCESS\n\n    Returns:\n        Status string reported by the PLC (e.g. ``\"IDLE\"``).\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n        RuntimeError: If the response cannot be parsed.\n    \"\"\"\n    lines = self.send_command(\"SYS_STATUS\")\n    fields = [f.strip() for f in lines[-1].split(\",\")]\n    # fields: msg_id, SYS_STATUS, status, 0, NULL, SUCCESS\n    try:\n        status = fields[2]\n    except IndexError as exc:\n        raise RuntimeError(f\"Failed to parse SYS_STATUS response: {lines!r}\") from exc\n    logger.debug(\"FPosBAPIClient: SYS_STATUS \u2192 %r\", status)\n    return status\n</code></pre>"},{"location":"autoapi/applied_motion/backends/fposbapi_client/#applied_motion.backends.fposbapi_client.FPosBAPIClient.is_error","title":"is_error","text":"<pre><code>is_error() -&gt; bool\n</code></pre> <p>Return whether the gantry is in an error state via <code>IS_ERROR</code>.</p> <p>Response layout::</p> <pre><code>MSG_ID, IS_ERROR, FLAG, 0, NULL, SUCCESS\n</code></pre> <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> if the PLC reports an active error; <code>False</code> otherwise.</p> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> <code>RuntimeError</code> <p>If the response cannot be parsed.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def is_error(self) -&gt; bool:\n    \"\"\"Return whether the gantry is in an error state via ``IS_ERROR``.\n\n    Response layout::\n\n        MSG_ID, IS_ERROR, FLAG, 0, NULL, SUCCESS\n\n    Returns:\n        ``True`` if the PLC reports an active error; ``False`` otherwise.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n        RuntimeError: If the response cannot be parsed.\n    \"\"\"\n    lines = self.send_command(\"IS_ERROR\")\n    fields = [f.strip() for f in lines[-1].split(\",\")]\n    # fields: msg_id, IS_ERROR, flag, 0, NULL, SUCCESS\n    try:\n        result = bool(int(fields[2]))\n    except (IndexError, ValueError) as exc:\n        raise RuntimeError(f\"Failed to parse IS_ERROR response: {lines!r}\") from exc\n    logger.debug(\"FPosBAPIClient: IS_ERROR \u2192 %s\", result)\n    return result\n</code></pre>"},{"location":"autoapi/applied_motion/backends/fposbapi_client/#applied_motion.backends.fposbapi_client.FPosBAPIClient.fpb_error","title":"fpb_error","text":"<pre><code>fpb_error() -&gt; str\n</code></pre> <p>Return the fieldbus error state via <code>FPB_ERROR</code>.</p> <p>Response layout::</p> <pre><code>MSG_ID, FPB_ERROR, STATUS, 0, NULL, SUCCESS\n</code></pre> <p>Returns:</p> Type Description <code>str</code> <p>Raw status string from the terminal response field.</p> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def fpb_error(self) -&gt; str:\n    \"\"\"Return the fieldbus error state via ``FPB_ERROR``.\n\n    Response layout::\n\n        MSG_ID, FPB_ERROR, STATUS, 0, NULL, SUCCESS\n\n    Returns:\n        Raw status string from the terminal response field.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    lines = self.send_command(\"FPB_ERROR\")\n    fields = [f.strip() for f in lines[-1].split(\",\")]\n    status = fields[2] if len(fields) &gt; 2 else \"\"\n    logger.debug(\"FPosBAPIClient: FPB_ERROR \u2192 %r\", status)\n    return status\n</code></pre>"},{"location":"autoapi/applied_motion/backends/fposbapi_client/#applied_motion.backends.fposbapi_client.FPosBAPIClient.read_err","title":"read_err","text":"<pre><code>read_err() -&gt; str\n</code></pre> <p>Return the current error description via <code>READ_ERR</code>.</p> <p>Returns:</p> Type Description <code>str</code> <p>The raw terminal response line from the server.</p> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def read_err(self) -&gt; str:\n    \"\"\"Return the current error description via ``READ_ERR``.\n\n    Returns:\n        The raw terminal response line from the server.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    lines = self.send_command(\"READ_ERR\")\n    logger.debug(\"FPosBAPIClient: READ_ERR \u2192 %r\", lines[-1])\n    return lines[-1]\n</code></pre>"},{"location":"autoapi/applied_motion/backends/fposbapi_client/#applied_motion.backends.fposbapi_client.FPosBAPIClient.err_log","title":"err_log","text":"<pre><code>err_log() -&gt; list[str]\n</code></pre> <p>Return the error log entries via <code>ERR_LOG</code>.</p> <p>The server may return multiple intermediate lines before the terminal SUCCESS line.  All non-terminal lines are returned as the log.</p> <p>Returns:</p> Type Description <code>list[str]</code> <p>List of error log strings (all lines except the terminal status).</p> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def err_log(self) -&gt; list[str]:\n    \"\"\"Return the error log entries via ``ERR_LOG``.\n\n    The server may return multiple intermediate lines before the terminal\n    SUCCESS line.  All non-terminal lines are returned as the log.\n\n    Returns:\n        List of error log strings (all lines except the terminal status).\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    lines = self.send_command(\"ERR_LOG\")\n    log = lines[:-1]  # strip terminal SUCCESS line\n    logger.debug(\"FPosBAPIClient: ERR_LOG returned %d line(s)\", len(log))\n    return log\n</code></pre>"},{"location":"autoapi/applied_motion/backends/fposbapi_client/#applied_motion.backends.fposbapi_client.FPosBAPIClient.com_log","title":"com_log","text":"<pre><code>com_log() -&gt; list[str]\n</code></pre> <p>Return the communication log entries via <code>COM_LOG</code>.</p> <p>The server may return multiple intermediate lines before the terminal SUCCESS line.  All non-terminal lines are returned as the log.</p> <p>Returns:</p> Type Description <code>list[str]</code> <p>List of communication log strings (all lines except the terminal status).</p> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def com_log(self) -&gt; list[str]:\n    \"\"\"Return the communication log entries via ``COM_LOG``.\n\n    The server may return multiple intermediate lines before the terminal\n    SUCCESS line.  All non-terminal lines are returned as the log.\n\n    Returns:\n        List of communication log strings (all lines except the terminal status).\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    lines = self.send_command(\"COM_LOG\")\n    log = lines[:-1]  # strip terminal SUCCESS line\n    logger.debug(\"FPosBAPIClient: COM_LOG returned %d line(s)\", len(log))\n    return log\n</code></pre>"},{"location":"autoapi/applied_motion/backends/fposbapi_client/#applied_motion.backends.fposbapi_client.FPosBAPIClient.err_desc","title":"err_desc","text":"<pre><code>err_desc() -&gt; str\n</code></pre> <p>Return the error description via <code>ERR_DESC</code>.</p> <p>Returns:</p> Type Description <code>str</code> <p>Raw terminal response line from the server.</p> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def err_desc(self) -&gt; str:\n    \"\"\"Return the error description via ``ERR_DESC``.\n\n    Returns:\n        Raw terminal response line from the server.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    lines = self.send_command(\"ERR_DESC\")\n    logger.debug(\"FPosBAPIClient: ERR_DESC \u2192 %r\", lines[-1])\n    return lines[-1]\n</code></pre>"},{"location":"autoapi/applied_motion/backends/fposbapi_client/#applied_motion.backends.fposbapi_client.FPosBAPIClient.get_io","title":"get_io","text":"<pre><code>get_io(io_id: int) -&gt; float\n</code></pre> <p>Return the value of an I/O channel via <code>GET_IO</code>.</p> <p>Response layout::</p> <pre><code>MSG_ID, GET_IO, IO_ID, VALUE, 0, NULL, SUCCESS\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>io_id</code> <code>int</code> <p>I/O channel ID.</p> required <p>Returns:</p> Type Description <code>float</code> <p>Current value of the I/O channel.</p> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> <code>RuntimeError</code> <p>If the response cannot be parsed.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def get_io(self, io_id: int) -&gt; float:\n    \"\"\"Return the value of an I/O channel via ``GET_IO``.\n\n    Response layout::\n\n        MSG_ID, GET_IO, IO_ID, VALUE, 0, NULL, SUCCESS\n\n    Args:\n        io_id: I/O channel ID.\n\n    Returns:\n        Current value of the I/O channel.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n        RuntimeError: If the response cannot be parsed.\n    \"\"\"\n    lines = self.send_command(\"GET_IO\", io_id)\n    fields = [f.strip() for f in lines[-1].split(\",\")]\n    # fields: msg_id, GET_IO, io_id, value, 0, NULL, SUCCESS\n    try:\n        value = float(fields[3])\n    except (IndexError, ValueError) as exc:\n        raise RuntimeError(f\"Failed to parse GET_IO response for io_id={io_id}: {lines!r}\") from exc\n    logger.debug(\"FPosBAPIClient: GET_IO io_id=%d \u2192 %s\", io_id, value)\n    return value\n</code></pre>"},{"location":"autoapi/applied_motion/backends/fposbapi_client/#applied_motion.backends.fposbapi_client.FPosBAPIClient.set_io","title":"set_io","text":"<pre><code>set_io(io_id: int, value: float) -&gt; None\n</code></pre> <p>Set the value of an I/O channel via <code>SET_IO</code>.</p> <p>Parameters:</p> Name Type Description Default <code>io_id</code> <code>int</code> <p>I/O channel ID.</p> required <code>value</code> <code>float</code> <p>Value to write to the I/O channel.</p> required <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def set_io(self, io_id: int, value: float) -&gt; None:\n    \"\"\"Set the value of an I/O channel via ``SET_IO``.\n\n    Args:\n        io_id: I/O channel ID.\n        value: Value to write to the I/O channel.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    self.send_command(\"SET_IO\", io_id, value)\n    logger.info(\"FPosBAPIClient: SET_IO io_id=%d value=%s\", io_id, value)\n</code></pre>"},{"location":"autoapi/applied_motion/backends/fposbapi_client/#applied_motion.backends.fposbapi_client.FPosBAPIClient.get_par","title":"get_par","text":"<pre><code>get_par(par_id: int) -&gt; list[float]\n</code></pre> <p>Return parameter values for par_id via <code>GET_PAR</code>.</p> <p>The FPosBAPI server returns up to four value fields per parameter (e.g. tray parameters include column count, column offset, row count, and row offset).  Fields that are absent or non-numeric are omitted from the returned list.</p> <p>Response layout::</p> <pre><code>MSG_ID, GET_PAR, PAR_ID, VALUE, VALUE, VALUE, VALUE, 0, NULL, SUCCESS\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>par_id</code> <code>int</code> <p>Parameter ID (see API Rev 5 Parameter ID sheet).</p> required <p>Returns:</p> Type Description <code>list[float]</code> <p>List of up to four float values for the requested parameter.</p> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def get_par(self, par_id: int) -&gt; list[float]:\n    \"\"\"Return parameter values for *par_id* via ``GET_PAR``.\n\n    The FPosBAPI server returns up to four value fields per parameter\n    (e.g. tray parameters include column count, column offset, row count,\n    and row offset).  Fields that are absent or non-numeric are omitted\n    from the returned list.\n\n    Response layout::\n\n        MSG_ID, GET_PAR, PAR_ID, VALUE, VALUE, VALUE, VALUE, 0, NULL, SUCCESS\n\n    Args:\n        par_id: Parameter ID (see API Rev 5 Parameter ID sheet).\n\n    Returns:\n        List of up to four float values for the requested parameter.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    lines = self.send_command(\"GET_PAR\", par_id)\n    fields = [f.strip() for f in lines[-1].split(\",\")]\n    # fields: msg_id, GET_PAR, par_id, v1, v2, v3, v4, 0, NULL, SUCCESS\n    # Value fields are at indices 3-6; trailing error triplet at -3 to -1.\n    value_fields = fields[3:-3]\n    values: list[float] = []\n    for f in value_fields:\n        try:\n            values.append(float(f))\n        except ValueError:\n            break\n    logger.debug(\"FPosBAPIClient: GET_PAR par_id=%d \u2192 %s\", par_id, values)\n    return values\n</code></pre>"},{"location":"autoapi/applied_motion/backends/fposbapi_client/#applied_motion.backends.fposbapi_client.FPosBAPIClient.set_par","title":"set_par","text":"<pre><code>set_par(par_id: int, *values: float) -&gt; None\n</code></pre> <p>Write one or more values to parameter par_id via <code>SET_PAR</code>.</p> <p>Parameters:</p> Name Type Description Default <code>par_id</code> <code>int</code> <p>Parameter ID (see API Rev 5 Parameter ID sheet).</p> required <code>*values</code> <code>float</code> <p>Up to four values to write to the parameter.</p> <code>()</code> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def set_par(self, par_id: int, *values: float) -&gt; None:\n    \"\"\"Write one or more values to parameter *par_id* via ``SET_PAR``.\n\n    Args:\n        par_id: Parameter ID (see API Rev 5 Parameter ID sheet).\n        *values: Up to four values to write to the parameter.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    self.send_command(\"SET_PAR\", par_id, *values)\n    logger.info(\"FPosBAPIClient: SET_PAR par_id=%d values=%s\", par_id, values)\n</code></pre>"},{"location":"autoapi/applied_motion/backends/fposbapi_client/#applied_motion.backends.fposbapi_client.FPosBAPIClient.teach_tray","title":"teach_tray","text":"<pre><code>teach_tray(\n    tray_id: int, tray_pos: int, tool_id: int = 0\n) -&gt; None\n</code></pre> <p>Save the current axis positions as a tray position via <code>TEACH_TRAY</code>.</p> <p>Parameters:</p> Name Type Description Default <code>tray_id</code> <code>int</code> <p>Tray ID (1-20).</p> required <code>tray_pos</code> <code>int</code> <p>Position within the tray to teach.</p> required <code>tool_id</code> <code>int</code> <p>Tool offset to associate with this position.</p> <code>0</code> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def teach_tray(self, tray_id: int, tray_pos: int, tool_id: int = 0) -&gt; None:\n    \"\"\"Save the current axis positions as a tray position via ``TEACH_TRAY``.\n\n    Args:\n        tray_id: Tray ID (1-20).\n        tray_pos: Position within the tray to teach.\n        tool_id: Tool offset to associate with this position.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    self.send_command(\"TEACH_TRAY\", tray_id, tray_pos, tool_id)\n    logger.info(\"FPosBAPIClient: TEACH_TRAY complete (tray_id=%d, tray_pos=%d)\", tray_id, tray_pos)\n</code></pre>"},{"location":"autoapi/applied_motion/backends/fposbapi_client/#applied_motion.backends.fposbapi_client.FPosBAPIClient.write_tray","title":"write_tray","text":"<pre><code>write_tray(\n    tray_id: int,\n    tray_pos: int,\n    abs_a1: float,\n    abs_a2: float,\n    abs_a3: float,\n) -&gt; None\n</code></pre> <p>Write absolute coordinates to a tray position via <code>WRITE_TRAY</code>.</p> <p>Parameters:</p> Name Type Description Default <code>tray_id</code> <code>int</code> <p>Tray ID (1-20).</p> required <code>tray_pos</code> <code>int</code> <p>Position index within the tray.</p> required <code>abs_a1</code> <code>float</code> <p>Absolute X-axis position in mm.</p> required <code>abs_a2</code> <code>float</code> <p>Absolute Y-axis position in mm.</p> required <code>abs_a3</code> <code>float</code> <p>Absolute Z-axis position in mm.</p> required <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def write_tray(\n    self,\n    tray_id: int,\n    tray_pos: int,\n    abs_a1: float,\n    abs_a2: float,\n    abs_a3: float,\n) -&gt; None:\n    \"\"\"Write absolute coordinates to a tray position via ``WRITE_TRAY``.\n\n    Args:\n        tray_id: Tray ID (1-20).\n        tray_pos: Position index within the tray.\n        abs_a1: Absolute X-axis position in mm.\n        abs_a2: Absolute Y-axis position in mm.\n        abs_a3: Absolute Z-axis position in mm.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    self.send_command(\"WRITE_TRAY\", tray_id, tray_pos, abs_a1, abs_a2, abs_a3)\n    logger.info(\"FPosBAPIClient: WRITE_TRAY complete (tray_id=%d, tray_pos=%d)\", tray_id, tray_pos)\n</code></pre>"},{"location":"autoapi/applied_motion/backends/fposbapi_client/#applied_motion.backends.fposbapi_client.FPosBAPIClient.read_tray","title":"read_tray","text":"<pre><code>read_tray(\n    tray_id: int, tray_pos: int\n) -&gt; tuple[float, float, float]\n</code></pre> <p>Read the stored coordinates of a tray position via <code>READ_TRAY</code>.</p> <p>Response layout::</p> <pre><code>MSG_ID, READ_TRAY, TRAY_ID, TRAY_POS, ABS_A1, ABS_A2, ABS_A3, 0, NULL, SUCCESS\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>tray_id</code> <code>int</code> <p>Tray ID (1-20).</p> required <code>tray_pos</code> <code>int</code> <p>Position index within the tray.</p> required <p>Returns:</p> Type Description <code>tuple[float, float, float]</code> <p>Tuple of <code>(abs_a1, abs_a2, abs_a3)</code> in mm.</p> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> <code>RuntimeError</code> <p>If the response cannot be parsed.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def read_tray(self, tray_id: int, tray_pos: int) -&gt; tuple[float, float, float]:\n    \"\"\"Read the stored coordinates of a tray position via ``READ_TRAY``.\n\n    Response layout::\n\n        MSG_ID, READ_TRAY, TRAY_ID, TRAY_POS, ABS_A1, ABS_A2, ABS_A3, 0, NULL, SUCCESS\n\n    Args:\n        tray_id: Tray ID (1-20).\n        tray_pos: Position index within the tray.\n\n    Returns:\n        Tuple of ``(abs_a1, abs_a2, abs_a3)`` in mm.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n        RuntimeError: If the response cannot be parsed.\n    \"\"\"\n    lines = self.send_command(\"READ_TRAY\", tray_id, tray_pos)\n    fields = [f.strip() for f in lines[-1].split(\",\")]\n    # fields: msg_id, READ_TRAY, tray_id, tray_pos, abs_a1, abs_a2, abs_a3, 0, NULL, SUCCESS\n    try:\n        result = float(fields[4]), float(fields[5]), float(fields[6])\n    except (IndexError, ValueError) as exc:\n        raise RuntimeError(\n            f\"Failed to parse READ_TRAY response for tray_id={tray_id}, tray_pos={tray_pos}: {lines!r}\"\n        ) from exc\n    logger.debug(\"FPosBAPIClient: READ_TRAY tray_id=%d tray_pos=%d \u2192 %s\", tray_id, tray_pos, result)\n    return result\n</code></pre>"},{"location":"autoapi/applied_motion/backends/fposbapi_client/#applied_motion.backends.fposbapi_client.FPosBAPIClient.mod_tray","title":"mod_tray","text":"<pre><code>mod_tray(\n    tray_id: int,\n    tray_pos: int,\n    rel_a1: float,\n    rel_a2: float,\n    rel_a3: float,\n) -&gt; None\n</code></pre> <p>Modify a stored tray position by relative offsets via <code>MOD_TRAY</code>.</p> <p>Parameters:</p> Name Type Description Default <code>tray_id</code> <code>int</code> <p>Tray ID (1-20).</p> required <code>tray_pos</code> <code>int</code> <p>Position index within the tray.</p> required <code>rel_a1</code> <code>float</code> <p>Relative X-axis offset in mm.</p> required <code>rel_a2</code> <code>float</code> <p>Relative Y-axis offset in mm.</p> required <code>rel_a3</code> <code>float</code> <p>Relative Z-axis offset in mm.</p> required <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def mod_tray(\n    self,\n    tray_id: int,\n    tray_pos: int,\n    rel_a1: float,\n    rel_a2: float,\n    rel_a3: float,\n) -&gt; None:\n    \"\"\"Modify a stored tray position by relative offsets via ``MOD_TRAY``.\n\n    Args:\n        tray_id: Tray ID (1-20).\n        tray_pos: Position index within the tray.\n        rel_a1: Relative X-axis offset in mm.\n        rel_a2: Relative Y-axis offset in mm.\n        rel_a3: Relative Z-axis offset in mm.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    self.send_command(\"MOD_TRAY\", tray_id, tray_pos, rel_a1, rel_a2, rel_a3)\n    logger.info(\"FPosBAPIClient: MOD_TRAY complete (tray_id=%d, tray_pos=%d)\", tray_id, tray_pos)\n</code></pre>"},{"location":"autoapi/applied_motion/backends/fposbapi_client/#applied_motion.backends.fposbapi_client.FPosBAPIClient.move_tray","title":"move_tray","text":"<pre><code>move_tray(\n    tray_id: int,\n    tray_col: int,\n    tray_row: int,\n    tool_id: int = 0,\n    retract_z: int = 0,\n    slow_app: int = 0,\n) -&gt; None\n</code></pre> <p>Move the gantry to a tray location via <code>MOVE_TRAY</code>.</p> <p>Parameters:</p> Name Type Description Default <code>tray_id</code> <code>int</code> <p>Tray ID (1-20).</p> required <code>tray_col</code> <code>int</code> <p>Target column within the tray.</p> required <code>tray_row</code> <code>int</code> <p>Target row within the tray.</p> required <code>tool_id</code> <code>int</code> <p>Tool offset to apply.  <code>0</code> selects no tool.</p> <code>0</code> <code>retract_z</code> <code>int</code> <p><code>1</code> to retract Z before moving, <code>0</code> to skip.</p> <code>0</code> <code>slow_app</code> <code>int</code> <p><code>1</code> to slow approach the target position, <code>0</code> for full speed.</p> <code>0</code> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def move_tray(\n    self,\n    tray_id: int,\n    tray_col: int,\n    tray_row: int,\n    tool_id: int = 0,\n    retract_z: int = 0,\n    slow_app: int = 0,\n) -&gt; None:\n    \"\"\"Move the gantry to a tray location via ``MOVE_TRAY``.\n\n    Args:\n        tray_id: Tray ID (1-20).\n        tray_col: Target column within the tray.\n        tray_row: Target row within the tray.\n        tool_id: Tool offset to apply.  ``0`` selects no tool.\n        retract_z: ``1`` to retract Z before moving, ``0`` to skip.\n        slow_app: ``1`` to slow approach the target position, ``0`` for\n            full speed.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    self.send_command(\"MOVE_TRAY\", tray_id, tray_col, tray_row, tool_id, retract_z, slow_app)\n    logger.info(\n        \"FPosBAPIClient: MOVE_TRAY complete (tray_id=%d, col=%d, row=%d)\",\n        tray_id,\n        tray_col,\n        tray_row,\n    )\n</code></pre>"},{"location":"autoapi/applied_motion/backends/fposbapi_client/#applied_motion.backends.fposbapi_client.FPosBAPIClient.init_sys","title":"init_sys","text":"<pre><code>init_sys() -&gt; None\n</code></pre> <p>Initialize system parameters via <code>INIT_SYS</code>.</p> <p>Raises:</p> Type Description <code>FPosBAPIClientError</code> <p>If the server returns an error response or the connection is lost.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def init_sys(self) -&gt; None:\n    \"\"\"Initialize system parameters via ``INIT_SYS``.\n\n    Raises:\n        FPosBAPIClientError: If the server returns an error response or\n            the connection is lost.\n    \"\"\"\n    self.send_command(\"INIT_SYS\")\n    logger.info(\"FPosBAPIClient: system initialized\")\n</code></pre>"},{"location":"autoapi/applied_motion/backends/fposbapi_client/#applied_motion.backends.fposbapi_client.FPosBAPIClient.close","title":"close","text":"<pre><code>close() -&gt; None\n</code></pre> <p>Close the underlying TCP socket.</p> <p>Safe to call more than once; subsequent calls are no-ops because the OS will already have released the socket descriptor.</p> Source code in <code>src/applied_motion/backends/fposbapi_client.py</code> <pre><code>def close(self) -&gt; None:\n    \"\"\"Close the underlying TCP socket.\n\n    Safe to call more than once; subsequent calls are no-ops because the\n    OS will already have released the socket descriptor.\n    \"\"\"\n    self._sock.close()\n    logger.info(\"FPosBAPIClient disconnected from %s:%d\", self.ip, self.port)\n</code></pre>"},{"location":"autoapi/applied_motion/backends/gantry_backend/","title":"gantry_backend","text":""},{"location":"autoapi/applied_motion/backends/gantry_backend/#applied_motion.backends.gantry_backend","title":"gantry_backend","text":"<p>Backend abstractions for gantry-level behavior.</p> <p>This module centralizes backend-specific operations that are not purely per-axis concerns (for example a single HOME command for all FPosB axes).</p>"},{"location":"autoapi/applied_motion/backends/gantry_backend/#applied_motion.backends.gantry_backend.ControllerDiagnostics","title":"ControllerDiagnostics","text":"<p>               Bases: <code>TypedDict</code></p> <p>Controller diagnostics payload returned by gantry backends.</p>"},{"location":"autoapi/applied_motion/backends/gantry_backend/#applied_motion.backends.gantry_backend.GantryBackend","title":"GantryBackend","text":"<p>               Bases: <code>Protocol</code></p> <p>Protocol for backend-specific gantry actions.</p> <p>A backend may own shared resources (for example a TCP client) and can expose backend capabilities such as PLC teaching commands.</p>"},{"location":"autoapi/applied_motion/backends/gantry_backend/#applied_motion.backends.gantry_backend.GantryBackend.client","title":"client  <code>property</code>","text":"<pre><code>client: FPosBAPIClient | None\n</code></pre> <p>Return the shared FPosBAPI client if this backend uses one.</p>"},{"location":"autoapi/applied_motion/backends/gantry_backend/#applied_motion.backends.gantry_backend.GantryBackend.backend_identity","title":"backend_identity","text":"<pre><code>backend_identity() -&gt; tuple[\n    type[object], tuple[str, int] | None\n]\n</code></pre> <p>Return stable backend identity fields used for equality/hash.</p> Source code in <code>src/applied_motion/backends/gantry_backend.py</code> <pre><code>def backend_identity(self) -&gt; tuple[type[object], tuple[str, int] | None]:\n    \"\"\"Return stable backend identity fields used for equality/hash.\"\"\"\n    ...\n</code></pre>"},{"location":"autoapi/applied_motion/backends/gantry_backend/#applied_motion.backends.gantry_backend.GantryBackend.controller_diagnostics","title":"controller_diagnostics","text":"<pre><code>controller_diagnostics() -&gt; ControllerDiagnostics | None\n</code></pre> <p>Return controller diagnostics for status reporting, if available.</p> Source code in <code>src/applied_motion/backends/gantry_backend.py</code> <pre><code>def controller_diagnostics(self) -&gt; ControllerDiagnostics | None:\n    \"\"\"Return controller diagnostics for status reporting, if available.\"\"\"\n    ...\n</code></pre>"},{"location":"autoapi/applied_motion/backends/gantry_backend/#applied_motion.backends.gantry_backend.GantryBackend.home","title":"home","text":"<pre><code>home(axes: dict[str, Axis]) -&gt; None\n</code></pre> <p>Home all axes represented by axes.</p> Source code in <code>src/applied_motion/backends/gantry_backend.py</code> <pre><code>def home(self, axes: dict[str, Axis]) -&gt; None:\n    \"\"\"Home all axes represented by *axes*.\"\"\"\n    ...\n</code></pre>"},{"location":"autoapi/applied_motion/backends/gantry_backend/#applied_motion.backends.gantry_backend.GantryBackend.close","title":"close","text":"<pre><code>close() -&gt; None\n</code></pre> <p>Release backend-owned resources.</p> Source code in <code>src/applied_motion/backends/gantry_backend.py</code> <pre><code>def close(self) -&gt; None:\n    \"\"\"Release backend-owned resources.\"\"\"\n    ...\n</code></pre>"},{"location":"autoapi/applied_motion/backends/gantry_backend/#applied_motion.backends.gantry_backend.GantryBackend.supports_teach","title":"supports_teach","text":"<pre><code>supports_teach() -&gt; bool\n</code></pre> <p>Return whether TEACH_POS / TEACH_TRAY operations are supported.</p> Source code in <code>src/applied_motion/backends/gantry_backend.py</code> <pre><code>def supports_teach(self) -&gt; bool:\n    \"\"\"Return whether TEACH_POS / TEACH_TRAY operations are supported.\"\"\"\n    ...\n</code></pre>"},{"location":"autoapi/applied_motion/backends/gantry_backend/#applied_motion.backends.gantry_backend.GantryBackend.teach_pos","title":"teach_pos","text":"<pre><code>teach_pos(pos_id: int) -&gt; None\n</code></pre> <p>Commit current location into PLC position slot pos_id.</p> Source code in <code>src/applied_motion/backends/gantry_backend.py</code> <pre><code>def teach_pos(self, pos_id: int) -&gt; None:\n    \"\"\"Commit current location into PLC position slot *pos_id*.\"\"\"\n    ...\n</code></pre>"},{"location":"autoapi/applied_motion/backends/gantry_backend/#applied_motion.backends.gantry_backend.GantryBackend.teach_tray","title":"teach_tray","text":"<pre><code>teach_tray(tray_id: int, tray_pos: int) -&gt; None\n</code></pre> <p>Commit current location into PLC tray slot (tray_id, tray_pos).</p> Source code in <code>src/applied_motion/backends/gantry_backend.py</code> <pre><code>def teach_tray(self, tray_id: int, tray_pos: int) -&gt; None:\n    \"\"\"Commit current location into PLC tray slot (*tray_id*, *tray_pos*).\"\"\"\n    ...\n</code></pre>"},{"location":"autoapi/applied_motion/backends/gantry_backend/#applied_motion.backends.gantry_backend.GantryBackend.list_commands","title":"list_commands","text":"<pre><code>list_commands() -&gt; list[str]\n</code></pre> <p>Return backend command list when available.</p> Source code in <code>src/applied_motion/backends/gantry_backend.py</code> <pre><code>def list_commands(self) -&gt; list[str]:\n    \"\"\"Return backend command list when available.\"\"\"\n    ...\n</code></pre>"},{"location":"autoapi/applied_motion/backends/gantry_backend/#applied_motion.backends.gantry_backend.ModbusGantryBackend","title":"ModbusGantryBackend","text":"<p>Gantry backend for direct Modbus axes.</p> <p>This backend owns no shared resources and uses per-axis operations only.</p>"},{"location":"autoapi/applied_motion/backends/gantry_backend/#applied_motion.backends.gantry_backend.ModbusGantryBackend.client","title":"client  <code>property</code>","text":"<pre><code>client: FPosBAPIClient | None\n</code></pre> <p>Modbus backend has no shared FPosBAPI client.</p>"},{"location":"autoapi/applied_motion/backends/gantry_backend/#applied_motion.backends.gantry_backend.ModbusGantryBackend.backend_identity","title":"backend_identity","text":"<pre><code>backend_identity() -&gt; tuple[\n    type[object], tuple[str, int] | None\n]\n</code></pre> <p>Return Modbus backend identity.</p> Source code in <code>src/applied_motion/backends/gantry_backend.py</code> <pre><code>def backend_identity(self) -&gt; tuple[type[object], tuple[str, int] | None]:\n    \"\"\"Return Modbus backend identity.\"\"\"\n    return type(self), None\n</code></pre>"},{"location":"autoapi/applied_motion/backends/gantry_backend/#applied_motion.backends.gantry_backend.ModbusGantryBackend.controller_diagnostics","title":"controller_diagnostics","text":"<pre><code>controller_diagnostics() -&gt; ControllerDiagnostics | None\n</code></pre> <p>Modbus backend has no shared controller diagnostics.</p> Source code in <code>src/applied_motion/backends/gantry_backend.py</code> <pre><code>def controller_diagnostics(self) -&gt; ControllerDiagnostics | None:\n    \"\"\"Modbus backend has no shared controller diagnostics.\"\"\"\n    return None\n</code></pre>"},{"location":"autoapi/applied_motion/backends/gantry_backend/#applied_motion.backends.gantry_backend.ModbusGantryBackend.home","title":"home","text":"<pre><code>home(axes: dict[str, Axis]) -&gt; None\n</code></pre> <p>Home each axis sequentially.</p> Source code in <code>src/applied_motion/backends/gantry_backend.py</code> <pre><code>def home(self, axes: dict[str, Axis]) -&gt; None:\n    \"\"\"Home each axis sequentially.\"\"\"\n    for axis in axes.values():\n        axis.home()\n</code></pre>"},{"location":"autoapi/applied_motion/backends/gantry_backend/#applied_motion.backends.gantry_backend.ModbusGantryBackend.close","title":"close","text":"<pre><code>close() -&gt; None\n</code></pre> <p>Close backend resources.</p> <p>Modbus backend currently has no shared resources to close.</p> Source code in <code>src/applied_motion/backends/gantry_backend.py</code> <pre><code>def close(self) -&gt; None:\n    \"\"\"Close backend resources.\n\n    Modbus backend currently has no shared resources to close.\n    \"\"\"\n</code></pre>"},{"location":"autoapi/applied_motion/backends/gantry_backend/#applied_motion.backends.gantry_backend.ModbusGantryBackend.supports_teach","title":"supports_teach","text":"<pre><code>supports_teach() -&gt; bool\n</code></pre> <p>Return whether teaching commands are supported.</p> Source code in <code>src/applied_motion/backends/gantry_backend.py</code> <pre><code>def supports_teach(self) -&gt; bool:\n    \"\"\"Return whether teaching commands are supported.\"\"\"\n    return False\n</code></pre>"},{"location":"autoapi/applied_motion/backends/gantry_backend/#applied_motion.backends.gantry_backend.ModbusGantryBackend.teach_pos","title":"teach_pos","text":"<pre><code>teach_pos(pos_id: int) -&gt; None\n</code></pre> <p>Raise for unsupported teaching operations.</p> Source code in <code>src/applied_motion/backends/gantry_backend.py</code> <pre><code>def teach_pos(self, pos_id: int) -&gt; None:\n    \"\"\"Raise for unsupported teaching operations.\"\"\"\n    raise NotImplementedError(\"teach_pos is only available for FPosBAPI backend\")\n</code></pre>"},{"location":"autoapi/applied_motion/backends/gantry_backend/#applied_motion.backends.gantry_backend.ModbusGantryBackend.teach_tray","title":"teach_tray","text":"<pre><code>teach_tray(tray_id: int, tray_pos: int) -&gt; None\n</code></pre> <p>Raise for unsupported teaching operations.</p> Source code in <code>src/applied_motion/backends/gantry_backend.py</code> <pre><code>def teach_tray(self, tray_id: int, tray_pos: int) -&gt; None:\n    \"\"\"Raise for unsupported teaching operations.\"\"\"\n    raise NotImplementedError(\"teach_tray is only available for FPosBAPI backend\")\n</code></pre>"},{"location":"autoapi/applied_motion/backends/gantry_backend/#applied_motion.backends.gantry_backend.ModbusGantryBackend.list_commands","title":"list_commands","text":"<pre><code>list_commands() -&gt; list[str]\n</code></pre> <p>Return an empty command list for unsupported operation.</p> Source code in <code>src/applied_motion/backends/gantry_backend.py</code> <pre><code>def list_commands(self) -&gt; list[str]:\n    \"\"\"Return an empty command list for unsupported operation.\"\"\"\n    return []\n</code></pre>"},{"location":"autoapi/applied_motion/backends/gantry_backend/#applied_motion.backends.gantry_backend.FPosBAPIGantryBackend","title":"FPosBAPIGantryBackend","text":"<pre><code>FPosBAPIGantryBackend(\n    client: FPosBAPIClient, owns_client: bool = True\n)\n</code></pre> <p>Gantry backend for PLC-controlled FPosBAPI systems.</p> <p>Store the shared client.</p> <p>Parameters:</p> Name Type Description Default <code>client</code> <code>FPosBAPIClient</code> <p>Shared PLC TCP client.</p> required <code>owns_client</code> <code>bool</code> <p>When <code>True</code>, <code>close</code> closes client.</p> <code>True</code> Source code in <code>src/applied_motion/backends/gantry_backend.py</code> <pre><code>def __init__(self, client: FPosBAPIClient, owns_client: bool = True) -&gt; None:\n    \"\"\"Store the shared client.\n\n    Args:\n        client: Shared PLC TCP client.\n        owns_client: When ``True``, [`close`][applied_motion.backends.gantry_backend.FPosBAPIGantryBackend.close] closes *client*.\n    \"\"\"\n    self._client = client\n    self._owns_client = owns_client\n    self._closed = False\n</code></pre>"},{"location":"autoapi/applied_motion/backends/gantry_backend/#applied_motion.backends.gantry_backend.FPosBAPIGantryBackend.client","title":"client  <code>property</code>","text":"<pre><code>client: FPosBAPIClient | None\n</code></pre> <p>Return the shared FPosBAPI client.</p>"},{"location":"autoapi/applied_motion/backends/gantry_backend/#applied_motion.backends.gantry_backend.FPosBAPIGantryBackend.backend_identity","title":"backend_identity","text":"<pre><code>backend_identity() -&gt; tuple[\n    type[object], tuple[str, int] | None\n]\n</code></pre> <p>Return FPosBAPI backend identity including endpoint.</p> Source code in <code>src/applied_motion/backends/gantry_backend.py</code> <pre><code>def backend_identity(self) -&gt; tuple[type[object], tuple[str, int] | None]:\n    \"\"\"Return FPosBAPI backend identity including endpoint.\"\"\"\n    return type(self), (self._client.ip, self._client.port)\n</code></pre>"},{"location":"autoapi/applied_motion/backends/gantry_backend/#applied_motion.backends.gantry_backend.FPosBAPIGantryBackend.controller_diagnostics","title":"controller_diagnostics","text":"<pre><code>controller_diagnostics() -&gt; ControllerDiagnostics | None\n</code></pre> <p>Return controller diagnostics from shared client.</p> Source code in <code>src/applied_motion/backends/gantry_backend.py</code> <pre><code>def controller_diagnostics(self) -&gt; ControllerDiagnostics | None:\n    \"\"\"Return controller diagnostics from shared client.\"\"\"\n    try:\n        return {\n            \"sys_status\": self._client.sys_status(),\n            \"is_error\": self._client.is_error(),\n            \"fpb_error\": self._client.fpb_error(),\n            \"read_err\": self._client.read_err(),\n            \"error\": None,\n        }\n    except Exception as exc:\n        return {\n            \"sys_status\": None,\n            \"is_error\": None,\n            \"fpb_error\": None,\n            \"read_err\": None,\n            \"error\": f\"{type(exc).__name__}: {exc}\",\n        }\n</code></pre>"},{"location":"autoapi/applied_motion/backends/gantry_backend/#applied_motion.backends.gantry_backend.FPosBAPIGantryBackend.home","title":"home","text":"<pre><code>home(axes: dict[str, Axis]) -&gt; None\n</code></pre> <p>Home all axes through one PLC HOME command.</p> Source code in <code>src/applied_motion/backends/gantry_backend.py</code> <pre><code>def home(self, axes: dict[str, Axis]) -&gt; None:\n    \"\"\"Home all axes through one PLC HOME command.\"\"\"\n    self._client.send_command(\"HOME\")\n</code></pre>"},{"location":"autoapi/applied_motion/backends/gantry_backend/#applied_motion.backends.gantry_backend.FPosBAPIGantryBackend.close","title":"close","text":"<pre><code>close() -&gt; None\n</code></pre> <p>Close owned shared client exactly once.</p> Source code in <code>src/applied_motion/backends/gantry_backend.py</code> <pre><code>def close(self) -&gt; None:\n    \"\"\"Close owned shared client exactly once.\"\"\"\n    if self._closed or not self._owns_client:\n        return\n    self._client.close()\n    self._closed = True\n</code></pre>"},{"location":"autoapi/applied_motion/backends/gantry_backend/#applied_motion.backends.gantry_backend.FPosBAPIGantryBackend.supports_teach","title":"supports_teach","text":"<pre><code>supports_teach() -&gt; bool\n</code></pre> <p>Return whether teaching commands are supported.</p> Source code in <code>src/applied_motion/backends/gantry_backend.py</code> <pre><code>def supports_teach(self) -&gt; bool:\n    \"\"\"Return whether teaching commands are supported.\"\"\"\n    return True\n</code></pre>"},{"location":"autoapi/applied_motion/backends/gantry_backend/#applied_motion.backends.gantry_backend.FPosBAPIGantryBackend.teach_pos","title":"teach_pos","text":"<pre><code>teach_pos(pos_id: int) -&gt; None\n</code></pre> <p>Delegate TEACH_POS to shared client.</p> Source code in <code>src/applied_motion/backends/gantry_backend.py</code> <pre><code>def teach_pos(self, pos_id: int) -&gt; None:\n    \"\"\"Delegate TEACH_POS to shared client.\"\"\"\n    self._client.teach_pos(pos_id=pos_id)\n</code></pre>"},{"location":"autoapi/applied_motion/backends/gantry_backend/#applied_motion.backends.gantry_backend.FPosBAPIGantryBackend.teach_tray","title":"teach_tray","text":"<pre><code>teach_tray(tray_id: int, tray_pos: int) -&gt; None\n</code></pre> <p>Delegate TEACH_TRAY to shared client.</p> Source code in <code>src/applied_motion/backends/gantry_backend.py</code> <pre><code>def teach_tray(self, tray_id: int, tray_pos: int) -&gt; None:\n    \"\"\"Delegate TEACH_TRAY to shared client.\"\"\"\n    self._client.teach_tray(tray_id=tray_id, tray_pos=tray_pos)\n</code></pre>"},{"location":"autoapi/applied_motion/backends/gantry_backend/#applied_motion.backends.gantry_backend.FPosBAPIGantryBackend.list_commands","title":"list_commands","text":"<pre><code>list_commands() -&gt; list[str]\n</code></pre> <p>Return command list from the shared client.</p> Source code in <code>src/applied_motion/backends/gantry_backend.py</code> <pre><code>def list_commands(self) -&gt; list[str]:\n    \"\"\"Return command list from the shared client.\"\"\"\n    return self._client.list_commands()\n</code></pre>"},{"location":"autoapi/applied_motion/cli/","title":"cli","text":""},{"location":"autoapi/applied_motion/cli/#applied_motion.cli","title":"cli","text":"<p>Interactive commissioning and motion tooling for gantry position recording.</p> <p><code>MotionSession</code> is available without any extras \u2014 it has no dependency on <code>prompt_toolkit</code> or <code>rich</code>.</p> <p>The interactive REPL (<code>applied_motion.cli.cli</code>) and the <code>cli</code> optional-dependency extra require the <code>cli</code> extra::</p> <pre><code>pip install festo-dev-applied-motion[cli]\n</code></pre> <p>Typical usage::</p> <pre><code>from applied_motion.cli import MotionSession\n\nsession = MotionSession(gantry, on_capture=my_hook)\nsession.jog(\"X\", \"+\", 5.0)\nsession.capture(\"deck_a1\")\nsession.save(\"deck_layout.json\")\n</code></pre> <p>To launch the interactive REPL::</p> <pre><code>applied-motion --config gantry.json\n</code></pre>"},{"location":"autoapi/applied_motion/cli/#applied_motion.cli.MotionSession","title":"MotionSession","text":"<pre><code>MotionSession(\n    gantry: Gantry, on_capture: OnCaptureHook | None = None\n)\n</code></pre> <p>Records gantry positions interactively or programmatically.</p> <p>Provides step-jog and position capture on top of a connected <code>Gantry</code>.  Captured positions are kept in <code>positions</code> and can be persisted to and from JSON via <code>save</code> / <code>load</code>.</p> <p>This class has no knowledge of the gantry backend (Modbus vs FPosBAPI) and no dependency on <code>prompt_toolkit</code> or <code>rich</code>.  Backend-specific post-capture actions (e.g. sending <code>TEACH_POS</code> to the PLC) are performed by the caller through the on_capture hook.</p> <p>Parameters:</p> Name Type Description Default <code>gantry</code> <code>Gantry</code> <p>Connected and homed <code>Gantry</code> instance.</p> required <code>on_capture</code> <code>OnCaptureHook | None</code> <p>Optional hook called immediately after each successful <code>capture</code>.  Receives the position label and position dict. Exceptions raised by the hook propagate to the caller.</p> <code>None</code> <p>Attributes:</p> Name Type Description <code>positions</code> <code>dict[str, dict[str, float]]</code> <p>Ordered dict mapping label \u2192 <code>{axis_name: position_mm}</code>. Populated by <code>capture</code> and <code>load</code>.</p> <p>Example::</p> <pre><code># FPosBAPI: wire a hook that commits positions to the PLC\ndef plc_hook(label, pos):\n    gantry.teach_pos(pos_id=label_to_id[label])\n\nsession = MotionSession(gantry, on_capture=plc_hook)\nsession.jog(\"X\", \"+\", 5.0)\nsession.capture(\"deck_a1\")\nsession.save(\"deck_layout.json\")\n</code></pre> Source code in <code>src/applied_motion/cli/session.py</code> <pre><code>def __init__(  # noqa\n    self,\n    gantry: Gantry,\n    on_capture: OnCaptureHook | None = None,\n) -&gt; None:\n    self.gantry = gantry\n    self.on_capture = on_capture\n    self.positions: dict[str, dict[str, float]] = {}\n    logger.debug(\n        \"MotionSession: created for gantry=%r hook=%s\",\n        gantry,\n        getattr(on_capture, \"__name__\", repr(on_capture)) if on_capture is not None else \"None\",\n    )\n</code></pre>"},{"location":"autoapi/applied_motion/cli/#applied_motion.cli.MotionSession.jog","title":"jog","text":"<pre><code>jog(\n    axis_name: str,\n    direction: str,\n    step_mm: float,\n    velocity: float = 10.0,\n    timeout: int = 15,\n) -&gt; dict[str, float]\n</code></pre> <p>Step-move one axis by step_mm in direction.</p> <p>Reads the current axis position, computes an absolute target, and dispatches a single-axis move via <code>Gantry.move_to</code>.</p> <p>Parameters:</p> Name Type Description Default <code>axis_name</code> <code>str</code> <p>Name of the axis to jog (must be a key in <code>axes</code>).</p> required <code>direction</code> <code>str</code> <p><code>\"+\"</code> to move in the positive direction, <code>\"-\"</code> to move in the negative direction.</p> required <code>step_mm</code> <code>float</code> <p>Distance to step in millimetres.  Must be positive.</p> required <code>velocity</code> <code>float</code> <p>Jog speed in mm/s.  Defaults to <code>10.0</code>.</p> <code>10.0</code> <code>timeout</code> <code>int</code> <p>Maximum seconds to wait for the move to complete. When exceeded the axis backend issues a stop command and the move is abandoned.  Defaults to <code>30</code> seconds \u2014 long enough for any normal jog step, but ensures the axis will always halt rather than run indefinitely.</p> <code>15</code> <p>Returns:</p> Type Description <code>dict[str, float]</code> <p>Full gantry location dict after the move completes.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If direction is not <code>\"+\"</code> or <code>\"-\"</code>, or if step_mm is not positive.</p> <code>KeyError</code> <p>If axis_name is not registered with the gantry.</p> Source code in <code>src/applied_motion/cli/session.py</code> <pre><code>def jog(\n    self,\n    axis_name: str,\n    direction: str,\n    step_mm: float,\n    velocity: float = 10.0,\n    timeout: int = 15,\n) -&gt; dict[str, float]:\n    \"\"\"Step-move one axis by *step_mm* in *direction*.\n\n    Reads the current axis position, computes an absolute target, and\n    dispatches a single-axis move via\n    [`Gantry.move_to`][applied_motion.applied_motion.Gantry.move_to].\n\n    Args:\n        axis_name: Name of the axis to jog (must be a key in\n            ``axes``).\n        direction: ``\"+\"`` to move in the positive direction, ``\"-\"``\n            to move in the negative direction.\n        step_mm: Distance to step in millimetres.  Must be positive.\n        velocity: Jog speed in mm/s.  Defaults to ``10.0``.\n        timeout: Maximum seconds to wait for the move to complete.\n            When exceeded the axis backend issues a stop command and\n            the move is abandoned.  Defaults to ``30`` seconds \u2014\n            long enough for any normal jog step, but ensures the\n            axis will always halt rather than run indefinitely.\n\n    Returns:\n        Full gantry location dict after the move completes.\n\n    Raises:\n        ValueError: If *direction* is not ``\"+\"`` or ``\"-\"``, or if\n            *step_mm* is not positive.\n        KeyError: If *axis_name* is not registered with the gantry.\n    \"\"\"\n    if direction not in (\"+\", \"-\"):\n        raise ValueError(f\"direction must be '+' or '-', got {direction!r}\")\n    if step_mm &lt;= 0:\n        raise ValueError(f\"step_mm must be positive, got {step_mm!r}\")\n    if axis_name not in self.gantry.axes:\n        raise KeyError(f\"Axis {axis_name!r} is not registered with this gantry\")\n\n    axis = self.gantry.axes[axis_name]\n    current = axis.get_current_axis_position()\n    raw_delta = step_mm if direction == \"+\" else -step_mm\n    if raw_delta &gt; 0:\n        available = max(0.0, axis.max_position - current - _JOG_LIMIT_MARGIN)\n        delta = min(raw_delta, available)\n    else:\n        available = max(0.0, current - axis.min_position - _JOG_LIMIT_MARGIN)\n        delta = max(raw_delta, -available)\n\n    logger.info(\n        \"MotionSession.jog: axis=%s limits=[%.3f, %.3f] mm current=%.3f delta=%.3f vel=%.1f mm/s timeout=%ss\",\n        axis_name,\n        axis.min_position,\n        axis.max_position,\n        current,\n        delta,\n        velocity,\n        timeout,\n    )\n    if delta != raw_delta:\n        logger.warning(\n            \"MotionSession.jog: axis=%s delta clamped %.3f\\u2192%.3f mm (%s mm margin from SW limit)\",\n            axis_name,\n            raw_delta,\n            delta,\n            _JOG_LIMIT_MARGIN,\n        )\n    if abs(delta) &lt; 1e-3:\n        logger.info(\"MotionSession.jog: axis=%s at limit, skipping move\", axis_name)\n        return self.gantry.get_location()\n\n    axis.move(delta, velocity, position_type=\"relative\", timeout=timeout)\n    location = self.gantry.get_location()\n    logger.debug(\"MotionSession.jog: post-move location=%s\", location)\n    return location\n</code></pre>"},{"location":"autoapi/applied_motion/cli/#applied_motion.cli.MotionSession.capture","title":"capture","text":"<pre><code>capture(label: str) -&gt; dict[str, float]\n</code></pre> <p>Record the current gantry position under label.</p> <p>Calls <code>Gantry.get_location</code>, stores the result in <code>positions</code>, then invokes on_capture if one was provided.</p> <p>Parameters:</p> Name Type Description Default <code>label</code> <code>str</code> <p>Unique name for this position (e.g. <code>\"deck_a1\"</code>). Overwrites any previous entry with the same label.</p> required <p>Returns:</p> Type Description <code>dict[str, float]</code> <p>The recorded position dict <code>{axis_name: position_mm}</code>.</p> Source code in <code>src/applied_motion/cli/session.py</code> <pre><code>def capture(self, label: str) -&gt; dict[str, float]:\n    \"\"\"Record the current gantry position under *label*.\n\n    Calls [`Gantry.get_location`][applied_motion.applied_motion.Gantry.get_location], stores\n    the result in ``positions``, then invokes *on_capture* if one\n    was provided.\n\n    Args:\n        label: Unique name for this position (e.g. ``\"deck_a1\"``).\n            Overwrites any previous entry with the same label.\n\n    Returns:\n        The recorded position dict ``{axis_name: position_mm}``.\n\n    Raises:\n        Any exception raised by *on_capture* propagates unchanged.\n    \"\"\"\n    position = self.gantry.get_location()\n    self.positions[label] = position\n    logger.info(\"MotionSession.capture: label=%r position=%s\", label, position)\n    if self.on_capture is not None:\n        self.on_capture(label, position)\n    return position\n</code></pre>"},{"location":"autoapi/applied_motion/cli/#applied_motion.cli.MotionSession.save","title":"save","text":"<pre><code>save(path: Path | str) -&gt; None\n</code></pre> <p>Write all captured positions to a JSON file.</p> <p>The output format is a plain JSON object mapping label \u2192 <code>{axis_name: position_mm}</code>.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>Path | str</code> <p>Destination file path.  Parent directories must exist.</p> required Source code in <code>src/applied_motion/cli/session.py</code> <pre><code>def save(self, path: Path | str) -&gt; None:\n    \"\"\"Write all captured positions to a JSON file.\n\n    The output format is a plain JSON object mapping label \u2192\n    ``{axis_name: position_mm}``.\n\n    Args:\n        path: Destination file path.  Parent directories must exist.\n    \"\"\"\n    path = Path(path)\n    with path.open(\"w\") as fh:\n        json.dump(self.positions, fh, indent=2)\n    logger.info(\"MotionSession.save: wrote %d position(s) to %s\", len(self.positions), path)\n</code></pre>"},{"location":"autoapi/applied_motion/cli/#applied_motion.cli.MotionSession.load","title":"load","text":"<pre><code>load(path: Path | str) -&gt; None\n</code></pre> <p>Merge positions from a JSON file into <code>positions</code>.</p> <p>Existing labels are overwritten by entries from path; labels absent in path are preserved.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>Path | str</code> <p>Source JSON file previously written by <code>save</code>.</p> required <p>Raises:</p> Type Description <code>OSError</code> <p>If path cannot be opened.</p> <code>ValueError</code> <p>If the file contains invalid JSON.</p> Source code in <code>src/applied_motion/cli/session.py</code> <pre><code>def load(self, path: Path | str) -&gt; None:\n    \"\"\"Merge positions from a JSON file into ``positions``.\n\n    Existing labels are overwritten by entries from *path*; labels\n    absent in *path* are preserved.\n\n    Args:\n        path: Source JSON file previously written by [`save`][applied_motion.cli.session.MotionSession.save].\n\n    Raises:\n        OSError: If *path* cannot be opened.\n        ValueError: If the file contains invalid JSON.\n    \"\"\"\n    path = Path(path)\n    with path.open() as fh:\n        loaded: dict[str, dict[str, float]] = json.load(fh)\n    self.positions.update(loaded)\n    logger.info(\"MotionSession.load: read %d position(s) from %s\", len(loaded), path)\n</code></pre>"},{"location":"autoapi/applied_motion/cli/#applied_motion.cli.Command","title":"Command  <code>dataclass</code>","text":"<pre><code>Command(\n    name: str,\n    handler: CommandHandler,\n    usage: str = \"\",\n    help: str = \"\",\n    completions: Callable[[], list[str]] | None = None,\n)\n</code></pre> <p>A single named leaf command in a command group.</p>"},{"location":"autoapi/applied_motion/cli/#applied_motion.cli.CommandGroup","title":"CommandGroup","text":"<pre><code>CommandGroup(name: str, help: str = '')\n</code></pre> <p>A composable namespace of commands and nested child groups.</p> <p>Initialise an empty command group.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>The group's own name.</p> required <code>help</code> <code>str</code> <p>Short description of the group.</p> <code>''</code> Source code in <code>src/applied_motion/cli/compose/core.py</code> <pre><code>def __init__(self, name: str, help: str = \"\") -&gt; None:\n    \"\"\"Initialise an empty command group.\n\n    Args:\n        name: The group's own name.\n        help: Short description of the group.\n    \"\"\"\n    self.name = name\n    self.help = help\n    self.commands: dict[str, Command] = {}\n    self.children: dict[str, CommandGroup] = {}\n    logger.debug(\"CommandGroup created: name=%s\", name)\n</code></pre>"},{"location":"autoapi/applied_motion/cli/#applied_motion.cli.CommandGroup.add_command","title":"add_command","text":"<pre><code>add_command(command: Command) -&gt; Command\n</code></pre> <p>Register a leaf command on this group.</p> <p>Parameters:</p> Name Type Description Default <code>command</code> <code>Command</code> <p>The command to register.</p> required <p>Returns:</p> Type Description <code>Command</code> <p>The registered command.</p> Source code in <code>src/applied_motion/cli/compose/core.py</code> <pre><code>def add_command(self, command: Command) -&gt; Command:\n    \"\"\"Register a leaf command on this group.\n\n    Args:\n        command: The command to register.\n\n    Returns:\n        The registered command.\n    \"\"\"\n    self.commands[command.name.lower()] = command\n    return command\n</code></pre>"},{"location":"autoapi/applied_motion/cli/#applied_motion.cli.CommandGroup.add_child","title":"add_child","text":"<pre><code>add_child(\n    group: CommandGroup, name: str | None = None\n) -&gt; CommandGroup\n</code></pre> <p>Mount a child group under a name.</p> <p>Parameters:</p> Name Type Description Default <code>group</code> <code>CommandGroup</code> <p>The child group to mount.</p> required <code>name</code> <code>str | None</code> <p>Mount key. Defaults to the child group's name.</p> <code>None</code> <p>Returns:</p> Type Description <code>CommandGroup</code> <p>The mounted child group.</p> Source code in <code>src/applied_motion/cli/compose/core.py</code> <pre><code>def add_child(self, group: \"CommandGroup\", name: str | None = None) -&gt; \"CommandGroup\":\n    \"\"\"Mount a child group under a name.\n\n    Args:\n        group: The child group to mount.\n        name: Mount key. Defaults to the child group's name.\n\n    Returns:\n        The mounted child group.\n    \"\"\"\n    key = (name or group.name).lower()\n    self.children[key] = group\n    logger.debug(\"CommandGroup %s mounted child under %r\", self.name, key)\n    return group\n</code></pre>"},{"location":"autoapi/applied_motion/cli/#applied_motion.cli.CommandGroup.dispatch","title":"dispatch","text":"<pre><code>dispatch(tokens: Sequence[str]) -&gt; None\n</code></pre> <p>Route a token sequence to a child group or a local command.</p> <p>Parameters:</p> Name Type Description Default <code>tokens</code> <code>Sequence[str]</code> <p>The whitespace-split command line.</p> required <p>Raises:</p> Type Description <code>UnknownCommandError</code> <p>If no matching child or command is found.</p> Source code in <code>src/applied_motion/cli/compose/core.py</code> <pre><code>def dispatch(self, tokens: Sequence[str]) -&gt; None:\n    \"\"\"Route a token sequence to a child group or a local command.\n\n    Args:\n        tokens: The whitespace-split command line.\n\n    Raises:\n        UnknownCommandError: If no matching child or command is found.\n    \"\"\"\n    if not tokens:\n        return\n    head = tokens[0].lower()\n    rest = tokens[1:]\n    if head in self.children:\n        self.children[head].dispatch(rest)\n        return\n    if head in self.commands:\n        self.commands[head].handler(rest)\n        return\n    raise UnknownCommandError(f\"Unknown command: {tokens[0]!r}\")\n</code></pre>"},{"location":"autoapi/applied_motion/cli/#applied_motion.cli.CommandGroup.iter_paths","title":"iter_paths","text":"<pre><code>iter_paths(\n    prefix: Sequence[str] = (),\n) -&gt; Iterator[tuple[str, ...]]\n</code></pre> <p>Yield the fully-qualified path of every command in the tree.</p> <p>Parameters:</p> Name Type Description Default <code>prefix</code> <code>Sequence[str]</code> <p>Internal prefix accumulator.</p> <code>()</code> <p>Yields:</p> Type Description <code>tuple[str, ...]</code> <p>Tuples of namespace names ending in a command name.</p> Source code in <code>src/applied_motion/cli/compose/core.py</code> <pre><code>def iter_paths(self, prefix: Sequence[str] = ()) -&gt; Iterator[tuple[str, ...]]:\n    \"\"\"Yield the fully-qualified path of every command in the tree.\n\n    Args:\n        prefix: Internal prefix accumulator.\n\n    Yields:\n        Tuples of namespace names ending in a command name.\n    \"\"\"\n    for name in self.commands:\n        yield (*tuple(prefix), name)\n    for child_name, child in self.children.items():\n        yield from child.iter_paths((*tuple(prefix), child_name))\n</code></pre>"},{"location":"autoapi/applied_motion/cli/#applied_motion.cli.CommandGroup.format_help","title":"format_help","text":"<pre><code>format_help(indent: int = 0) -&gt; list[str]\n</code></pre> <p>Build indented help lines for this group and all descendants.</p> <p>Parameters:</p> Name Type Description Default <code>indent</code> <code>int</code> <p>Current indentation depth.</p> <code>0</code> <p>Returns:</p> Type Description <code>list[str]</code> <p>A list of formatted help lines.</p> Source code in <code>src/applied_motion/cli/compose/core.py</code> <pre><code>def format_help(self, indent: int = 0) -&gt; list[str]:\n    \"\"\"Build indented help lines for this group and all descendants.\n\n    Args:\n        indent: Current indentation depth.\n\n    Returns:\n        A list of formatted help lines.\n    \"\"\"\n    pad = \"  \" * indent\n    lines: list[str] = []\n    for cmd in self.commands.values():\n        usage = cmd.usage or cmd.name\n        suffix = f\"    {cmd.help}\" if cmd.help else \"\"\n        lines.append(f\"{pad}{usage}{suffix}\")\n    for child_name, child in self.children.items():\n        header = f\"{pad}{child_name}\"\n        if child.help:\n            header = f\"{header}    {child.help}\"\n        lines.append(header)\n        lines.extend(child.format_help(indent + 1))\n    return lines\n</code></pre>"},{"location":"autoapi/applied_motion/cli/#applied_motion.cli.UnknownCommandError","title":"UnknownCommandError","text":"<p>               Bases: <code>CommandError</code></p> <p>Raise when a token matches neither a child group nor a local command.</p>"},{"location":"autoapi/applied_motion/cli/#applied_motion.cli.UsageError","title":"UsageError","text":"<p>               Bases: <code>CommandError</code></p> <p>Raise when a command is invoked with missing or invalid arguments.</p>"},{"location":"autoapi/applied_motion/cli/#applied_motion.cli.build_standalone_motion_parser","title":"build_standalone_motion_parser","text":"<pre><code>build_standalone_motion_parser(\n    *,\n    prog: str = \"applied-motion\",\n    extensions: Sequence[MotionCliExtension] = (),\n) -&gt; argparse.ArgumentParser\n</code></pre> <p>Build standalone motion CLI parser.</p> <p>Parameters:</p> Name Type Description Default <code>prog</code> <code>str</code> <p>Program name shown in help output.</p> <code>'applied-motion'</code> <code>extensions</code> <code>Sequence[MotionCliExtension]</code> <p>Optional extension hooks for additional subcommands.</p> <code>()</code> <p>Returns:</p> Type Description <code>ArgumentParser</code> <p>Fully configured standalone parser.</p> Source code in <code>src/applied_motion/cli/cli.py</code> <pre><code>def build_standalone_motion_parser(\n    *,\n    prog: str = \"applied-motion\",\n    extensions: Sequence[MotionCliExtension] = (),\n) -&gt; argparse.ArgumentParser:\n    \"\"\"Build standalone motion CLI parser.\n\n    Args:\n        prog: Program name shown in help output.\n        extensions: Optional extension hooks for additional subcommands.\n\n    Returns:\n        Fully configured standalone parser.\n    \"\"\"\n    parser = argparse.ArgumentParser(\n        prog=prog,\n        description=\"Composable motion control CLI for teach-in and axis operations.\",\n    )\n    parser.add_argument(\n        \"--config\",\n        required=True,\n        type=Path,\n        metavar=\"PATH\",\n        help=\"Path to gantry JSON configuration file.\",\n    )\n    parser.add_argument(\n        \"--gantry-name\",\n        default=\"gantry_1\",\n        metavar=\"NAME\",\n        help=\"Gantry component name in config (default: gantry_1).\",\n    )\n    parser.add_argument(\n        \"--log-level\",\n        default=\"OFF\",\n        choices=LOG_LEVEL_CHOICES,\n        metavar=\"LEVEL\",\n        help=\"Python logging level (default: OFF).\",\n    )\n    subparsers = parser.add_subparsers(dest=\"motion_command\")\n    _add_motion_command_parsers(subparsers)\n    for extension in extensions:\n        extension(subparsers)\n    return parser\n</code></pre>"},{"location":"autoapi/applied_motion/cli/#applied_motion.cli.dispatch_motion_command","title":"dispatch_motion_command","text":"<pre><code>dispatch_motion_command(args: Namespace) -&gt; int\n</code></pre> <p>Dispatch parsed motion command namespace.</p> <p>Initialises the gantry once and passes it to the selected handler. Guarantees <code>gantry.close()</code> is called on exit regardless of outcome.</p> <p>Parameters:</p> Name Type Description Default <code>args</code> <code>Namespace</code> <p>Parsed argument namespace from a motion parser.</p> required <p>Returns:</p> Type Description <code>int</code> <p>Process exit code.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If no command handler is available in args.</p> Source code in <code>src/applied_motion/cli/cli.py</code> <pre><code>def dispatch_motion_command(args: argparse.Namespace) -&gt; int:\n    \"\"\"Dispatch parsed motion command namespace.\n\n    Initialises the gantry once and passes it to the selected handler.\n    Guarantees ``gantry.close()`` is called on exit regardless of outcome.\n\n    Args:\n        args: Parsed argument namespace from a motion parser.\n\n    Returns:\n        Process exit code.\n\n    Raises:\n        ValueError: If no command handler is available in *args*.\n    \"\"\"\n    if hasattr(args, \"log_level\"):\n        configure_logging(args.log_level)\n\n    handler = getattr(args, \"_handler\", None)\n    if handler is None:\n        raise ValueError(\"No motion command selected\")\n    gantry = _connect_gantry(args.config, args.gantry_name)\n    try:\n        return handler(args, gantry)\n    finally:\n        gantry.close()\n</code></pre>"},{"location":"autoapi/applied_motion/cli/#applied_motion.cli.register_motion_cli","title":"register_motion_cli","text":"<pre><code>register_motion_cli(\n    parent_subparsers: _SubParsersAction,\n    *,\n    command_name: str = \"motion\",\n    extensions: Sequence[MotionCliExtension] = (),\n) -&gt; argparse.ArgumentParser\n</code></pre> <p>Attach motion CLI subtree to parent parser.</p> <p>Designed for higher-level system CLIs that compose multiple domains (for example <code>motion</code> and <code>fluid</code>) side-by-side.</p> <p>Parameters:</p> Name Type Description Default <code>parent_subparsers</code> <code>_SubParsersAction</code> <p>Parent parser subcommand registry.</p> required <code>command_name</code> <code>str</code> <p>Name used for mounted motion command subtree.</p> <code>'motion'</code> <code>extensions</code> <code>Sequence[MotionCliExtension]</code> <p>Optional extension hooks that can add extra motion subcommands.</p> <code>()</code> <p>Returns:</p> Type Description <code>ArgumentParser</code> <p>Mounted motion command parser.</p> Source code in <code>src/applied_motion/cli/cli.py</code> <pre><code>def register_motion_cli(\n    parent_subparsers: argparse._SubParsersAction,\n    *,\n    command_name: str = \"motion\",\n    extensions: Sequence[MotionCliExtension] = (),\n) -&gt; argparse.ArgumentParser:\n    \"\"\"Attach motion CLI subtree to parent parser.\n\n    Designed for higher-level system CLIs that compose multiple domains\n    (for example ``motion`` and ``fluid``) side-by-side.\n\n    Args:\n        parent_subparsers: Parent parser subcommand registry.\n        command_name: Name used for mounted motion command subtree.\n        extensions: Optional extension hooks that can add extra motion\n            subcommands.\n\n    Returns:\n        Mounted motion command parser.\n    \"\"\"\n    parser = parent_subparsers.add_parser(command_name, help=\"Motion control commands\")\n    parser.add_argument(\n        \"--config\",\n        required=True,\n        type=Path,\n        metavar=\"PATH\",\n        help=\"Path to gantry JSON configuration file.\",\n    )\n    parser.add_argument(\n        \"--gantry-name\",\n        default=\"gantry_1\",\n        metavar=\"NAME\",\n        help=\"Gantry component name in config (default: gantry_1).\",\n    )\n    parser.add_argument(\n        \"--log-level\",\n        default=\"INHERIT\",\n        choices=INHERITED_LOG_LEVEL_CHOICES,\n        metavar=\"LEVEL\",\n        help=\"Python logging level (default: INHERIT from parent CLI, else OFF).\",\n    )\n    motion_subparsers = parser.add_subparsers(dest=\"motion_command\")\n    _add_motion_command_parsers(motion_subparsers)\n    for extension in extensions:\n        extension(motion_subparsers)\n    return parser\n</code></pre>"},{"location":"autoapi/applied_motion/cli/#applied_motion.cli.run_repl","title":"run_repl","text":"<pre><code>run_repl(session: MotionSession, gantry: Gantry) -&gt; int\n</code></pre> <p>Launch the interactive teach-in REPL for a connected gantry.</p> <p>Presents a prompt-toolkit REPL that accepts <code>jog</code>, <code>capture</code>, <code>where</code>, <code>home</code>, <code>teach pos</code>, <code>teach tray</code>, <code>list</code>, <code>save</code>, <code>load</code>, <code>help</code>, and <code>quit</code> commands.  Tab-completion and command history are provided automatically.</p> <p>Parameters:</p> Name Type Description Default <code>session</code> <code>MotionSession</code> <p>A <code>MotionSession</code> instance backed by a connected, homed gantry.  Captured positions accumulate in <code>session.positions</code>.</p> required <code>gantry</code> <code>Gantry</code> <p>The connected <code>Gantry</code> instance whose axes define the tab-completion candidates and receive motion commands.</p> required Source code in <code>src/applied_motion/cli/cli.py</code> <pre><code>def run_repl(session: MotionSession, gantry: Gantry) -&gt; int:  # noqa\n    \"\"\"Launch the interactive teach-in REPL for a connected gantry.\n\n    Presents a prompt-toolkit REPL that accepts ``jog``, ``capture``,\n    ``where``, ``home``, ``teach pos``, ``teach tray``, ``list``, ``save``,\n    ``load``, ``help``, and ``quit`` commands.  Tab-completion and command\n    history are provided automatically.\n\n    Args:\n        session: A [`MotionSession`][applied_motion.cli.session.MotionSession]\n            instance backed by a connected, homed gantry.  Captured\n            positions accumulate in ``session.positions``.\n        gantry: The connected [`Gantry`][applied_motion.applied_motion.Gantry]\n            instance whose axes define the tab-completion candidates and\n            receive motion commands.\n    \"\"\"\n    axis_names = list(gantry.axes.keys())\n    ps: PromptSession[str] = PromptSession(\n        history=InMemoryHistory(),\n        completer=_build_completer(axis_names),\n    )\n\n    console.print(_HELP_TEXT)\n\n    previous_disable = logging.root.manager.disable\n    logging.disable(logging.CRITICAL)\n    try:\n        while True:\n            try:\n                raw = ps.prompt(\"motion&gt; \").strip()\n            except (EOFError, KeyboardInterrupt):  # noqa\n                console.print(\"\\n[festo.muted]Exiting.[/]\")\n                return 130\n\n            if not raw:\n                continue\n\n            parts = raw.split()\n            cmd = parts[0].lower()\n\n            try:\n                if cmd in (\"quit\", \"exit\", \"q\"):\n                    console.print(\"[festo.ok]\u2713[/] Quitting program.\")\n                    break\n\n                elif cmd == \"help\":\n                    console.print(_HELP_TEXT)\n\n                elif cmd == \"where\":\n                    loc = gantry.get_location()\n                    console.print(_location_table(loc))\n\n                elif cmd == \"home\":\n                    gantry.home()\n                    console.print(\"[festo.ok]\u2713[/] All axes homed.\")\n\n                elif cmd == \"jog\":\n                    if len(parts) == 1:\n                        # No args \u2192 enter arrow-key jog TUI\n                        _run_jog_mode(session, gantry)\n                    elif len(parts) &lt; 4:\n                        console.print(f\"{format_badge('USAGE', 'warn')} [festo.brand]jog[/]\")\n                        console.print(f\"    {format_help_line('jog', 'Enter arrow-key jog mode')}\")\n                        console.print(\n                            f\"    {format_help_line('jog &lt;axis&gt; &lt;+/-&gt; &lt;step&gt; [vel]', 'Single step-move (mm, default vel=10 mm/s)')}\"\n                        )\n                    else:\n                        axis = parts[1].upper()\n                        direction = parts[2]\n                        step = float(parts[3])\n                        vel = float(parts[4]) if len(parts) &gt; 4 else 10.0\n                        try:\n                            loc = session.jog(axis, direction, step, vel)\n                            console.print(_location_table(loc))\n                        except (ValueError, KeyError) as exc:\n                            console.print(f\"[festo.err]\u2717[/] {exc}\")\n                        except Exception as exc:\n                            logger.exception(\n                                \"CLI command 'jog': failed axis=%s direction=%s step=%s vel=%s\",\n                                axis,\n                                direction,\n                                step,\n                                vel,\n                            )\n                            console.print(f\"[festo.err]\u2717[/] Move rejected by axis: {exc}\")\n\n                elif cmd == \"capture\":\n                    if len(parts) &lt; 2:\n                        console.print(f\"[festo.err]\u2717[/] Usage: {format_help_line('capture &lt;label&gt;')}\")\n                        continue\n                    label = parts[1]\n                    session.capture(label)\n                    console.print(f\"[festo.ok]\u2713[/] Captured [bold]{label!r}[/]\")\n\n                elif cmd == \"teach\":\n                    if not gantry.supports_teach():\n                        console.print(\n                            f\"{format_badge('TEACH DISABLED', 'warn')} [festo.muted]Modbus backend has no PLC teach command.[/]\\n\"\n                            \"    Use [festo.ok]capture[/] to save positions to JSON instead.\"\n                        )\n                    elif len(parts) &gt;= 3 and parts[1] == \"pos\":\n                        pos_id = int(parts[2])\n                        gantry.teach_pos(pos_id=pos_id)\n                        console.print(f\"[festo.ok]\u2713[/] TEACH_POS sent ([festo.ok]pos_id={pos_id}[/])\")\n                    elif len(parts) &gt;= 4 and parts[1] == \"tray\":\n                        tray_id, tray_pos = int(parts[2]), int(parts[3])\n                        gantry.teach_tray(tray_id=tray_id, tray_pos=tray_pos)\n                        console.print(\n                            f\"[festo.ok]\u2713[/] TEACH_TRAY sent ([festo.ok]tray_id={tray_id}[/], [festo.ok]tray_pos={tray_pos}[/])\"\n                        )\n                    else:\n                        console.print(\"[festo.err]\u2717[/] Usage:\")\n                        console.print(f\"    {format_help_line('teach pos &lt;pos_id&gt;')}\")\n                        console.print(f\"    {format_help_line('teach tray &lt;tray_id&gt; &lt;tray_pos&gt;')}\")\n\n                elif cmd == \"list\":\n                    if not session.positions:\n                        console.print(\"[festo.muted]No positions captured yet.[/]\")\n                    else:\n                        console.print(_positions_table(session.positions))\n\n                elif cmd == \"save\":\n                    if len(parts) &lt; 2:\n                        console.print(f\"[festo.err]\u2717[/] Usage: {format_help_line('save &lt;path&gt;')}\")\n                        continue\n                    session.save(parts[1])\n                    console.print(f\"[festo.ok]\u2713[/] {len(session.positions)} position(s) saved \u2192 [bold]{parts[1]}[/]\")\n\n                elif cmd == \"load\":\n                    if len(parts) &lt; 2:\n                        console.print(f\"[festo.err]\u2717[/] Usage: {format_help_line('load &lt;path&gt;')}\")\n                        continue\n                    before = len(session.positions)\n                    session.load(parts[1])\n                    added = len(session.positions) - before\n                    console.print(f\"[festo.ok]\u2713[/] {added} position(s) loaded from [bold]{parts[1]}[/]\")\n\n                elif cmd == \"loglevel\":\n                    console.print(f\"[festo.ok]\u2713[/] {set_runtime_log_level(parts[1:])}\")\n\n                else:\n                    console.print(f\"[festo.err]\u2717[/] Unknown command: [bold]{cmd!r}[/]  (type [festo.ok]help[/])\")\n\n            except (KeyError, ValueError, IndexError) as exc:\n                console.print(f\"[festo.err]\u2717[/] {exc}\")\n            except Exception as exc:\n                logger.exception(\"Unexpected error processing command %r\", raw)\n                console.print(f\"[festo.err]\u2717[/] Unexpected error: {exc}\")\n    finally:\n        logging.disable(previous_disable)\n\n    console.print(\"[festo.ok]\u2713[/] Quitting program repl successful.\")\n    return 1\n</code></pre>"},{"location":"autoapi/applied_motion/cli/cli/","title":"cli","text":""},{"location":"autoapi/applied_motion/cli/cli/#applied_motion.cli.cli","title":"cli","text":"<p>Interactive commissioning and motion REPL for gantry operation.</p> <p>Requires the <code>cli</code> optional-dependency extra::</p> <pre><code>pip install festo-dev-applied-motion[cli]\n</code></pre> <p>Launch via the installed entry point::</p> <pre><code>applied-motion --config gantry.json\n</code></pre> <p>Or directly::</p> <pre><code>python -m applied_motion.cli.cli --config gantry.json\n</code></pre>"},{"location":"autoapi/applied_motion/cli/cli/#applied_motion.cli.cli--commands","title":"Commands","text":"<p>The REPL accepts the following commands (tab-completion and command history are provided by <code>prompt_toolkit</code>):</p> <p>======================================  ====================================== Command                                 Effect ======================================  ====================================== <code>jog</code>                                 Enter arrow-key jog mode (see below) <code>jog &lt;axis&gt; &lt;+/-&gt; &lt;step&gt; [vel]</code>       Single step-move, then return to REPL <code>where</code>                               Print current axis positions <code>home</code>                                Home all axes <code>capture &lt;label&gt;</code>                     Record current position as label <code>teach pos &lt;pos_id&gt;</code>                  (FPosBAPI only) Send TEACH_POS to PLC # TODO: INclude tool id <code>teach tray &lt;tray_id&gt; &lt;tray_pos&gt;</code>     (FPosBAPI only) Send TEACH_TRAY to PLC <code>list</code>                                List all captured positions <code>save &lt;path&gt;</code>                         Write positions to a JSON file <code>load &lt;path&gt;</code>                         Merge positions from a JSON file <code>loglevel [OFF|DEBUG|INFO|WARNING|ERROR|CRITICAL]</code>  Show or change current log level <code>help</code>                                Print this command reference <code>quit</code>                                Exit the REPL ======================================  ======================================</p>"},{"location":"autoapi/applied_motion/cli/cli/#applied_motion.cli.cli--jog-mode-key-bindings","title":"Jog mode key bindings","text":"<p>=====================  ================================================ Key                    Action =====================  ================================================ \u2190 / \u2192                  Step axis[0] (typically X) negative / positive \u2191 / \u2193                  Step axis[1] (typically Y) positive / negative Page Up / Page Down    Step axis[2] (typically Z) positive / negative <code>+</code>                  Increase step size (cycles 0.1\u21920.5\u21921\u21925\u219210\u219250 mm) <code>-</code>                  Decrease step size <code>Tab</code>                Cycle PgUp/PgDn target to next depth axis (3+ axes) <code>Shift+Tab</code>          Cycle PgUp/PgDn target to previous depth axis <code>Esc</code> or <code>q</code>       Exit jog mode, return to REPL =====================  ================================================</p>"},{"location":"autoapi/applied_motion/cli/cli/#applied_motion.cli.cli.MotionCliExtension","title":"MotionCliExtension  <code>module-attribute</code>","text":"<pre><code>MotionCliExtension = Callable[[_SubParsersAction], None]\n</code></pre> <p>Extension hook signature for adding extra motion subcommands.</p> <p>Each extension receives motion command's subparser collection and may call <code>add_parser</code> to register additional nested commands.</p>"},{"location":"autoapi/applied_motion/cli/cli/#applied_motion.cli.cli.run_repl","title":"run_repl","text":"<pre><code>run_repl(session: MotionSession, gantry: Gantry) -&gt; int\n</code></pre> <p>Launch the interactive teach-in REPL for a connected gantry.</p> <p>Presents a prompt-toolkit REPL that accepts <code>jog</code>, <code>capture</code>, <code>where</code>, <code>home</code>, <code>teach pos</code>, <code>teach tray</code>, <code>list</code>, <code>save</code>, <code>load</code>, <code>help</code>, and <code>quit</code> commands.  Tab-completion and command history are provided automatically.</p> <p>Parameters:</p> Name Type Description Default <code>session</code> <code>MotionSession</code> <p>A <code>MotionSession</code> instance backed by a connected, homed gantry.  Captured positions accumulate in <code>session.positions</code>.</p> required <code>gantry</code> <code>Gantry</code> <p>The connected <code>Gantry</code> instance whose axes define the tab-completion candidates and receive motion commands.</p> required Source code in <code>src/applied_motion/cli/cli.py</code> <pre><code>def run_repl(session: MotionSession, gantry: Gantry) -&gt; int:  # noqa\n    \"\"\"Launch the interactive teach-in REPL for a connected gantry.\n\n    Presents a prompt-toolkit REPL that accepts ``jog``, ``capture``,\n    ``where``, ``home``, ``teach pos``, ``teach tray``, ``list``, ``save``,\n    ``load``, ``help``, and ``quit`` commands.  Tab-completion and command\n    history are provided automatically.\n\n    Args:\n        session: A [`MotionSession`][applied_motion.cli.session.MotionSession]\n            instance backed by a connected, homed gantry.  Captured\n            positions accumulate in ``session.positions``.\n        gantry: The connected [`Gantry`][applied_motion.applied_motion.Gantry]\n            instance whose axes define the tab-completion candidates and\n            receive motion commands.\n    \"\"\"\n    axis_names = list(gantry.axes.keys())\n    ps: PromptSession[str] = PromptSession(\n        history=InMemoryHistory(),\n        completer=_build_completer(axis_names),\n    )\n\n    console.print(_HELP_TEXT)\n\n    previous_disable = logging.root.manager.disable\n    logging.disable(logging.CRITICAL)\n    try:\n        while True:\n            try:\n                raw = ps.prompt(\"motion&gt; \").strip()\n            except (EOFError, KeyboardInterrupt):  # noqa\n                console.print(\"\\n[festo.muted]Exiting.[/]\")\n                return 130\n\n            if not raw:\n                continue\n\n            parts = raw.split()\n            cmd = parts[0].lower()\n\n            try:\n                if cmd in (\"quit\", \"exit\", \"q\"):\n                    console.print(\"[festo.ok]\u2713[/] Quitting program.\")\n                    break\n\n                elif cmd == \"help\":\n                    console.print(_HELP_TEXT)\n\n                elif cmd == \"where\":\n                    loc = gantry.get_location()\n                    console.print(_location_table(loc))\n\n                elif cmd == \"home\":\n                    gantry.home()\n                    console.print(\"[festo.ok]\u2713[/] All axes homed.\")\n\n                elif cmd == \"jog\":\n                    if len(parts) == 1:\n                        # No args \u2192 enter arrow-key jog TUI\n                        _run_jog_mode(session, gantry)\n                    elif len(parts) &lt; 4:\n                        console.print(f\"{format_badge('USAGE', 'warn')} [festo.brand]jog[/]\")\n                        console.print(f\"    {format_help_line('jog', 'Enter arrow-key jog mode')}\")\n                        console.print(\n                            f\"    {format_help_line('jog &lt;axis&gt; &lt;+/-&gt; &lt;step&gt; [vel]', 'Single step-move (mm, default vel=10 mm/s)')}\"\n                        )\n                    else:\n                        axis = parts[1].upper()\n                        direction = parts[2]\n                        step = float(parts[3])\n                        vel = float(parts[4]) if len(parts) &gt; 4 else 10.0\n                        try:\n                            loc = session.jog(axis, direction, step, vel)\n                            console.print(_location_table(loc))\n                        except (ValueError, KeyError) as exc:\n                            console.print(f\"[festo.err]\u2717[/] {exc}\")\n                        except Exception as exc:\n                            logger.exception(\n                                \"CLI command 'jog': failed axis=%s direction=%s step=%s vel=%s\",\n                                axis,\n                                direction,\n                                step,\n                                vel,\n                            )\n                            console.print(f\"[festo.err]\u2717[/] Move rejected by axis: {exc}\")\n\n                elif cmd == \"capture\":\n                    if len(parts) &lt; 2:\n                        console.print(f\"[festo.err]\u2717[/] Usage: {format_help_line('capture &lt;label&gt;')}\")\n                        continue\n                    label = parts[1]\n                    session.capture(label)\n                    console.print(f\"[festo.ok]\u2713[/] Captured [bold]{label!r}[/]\")\n\n                elif cmd == \"teach\":\n                    if not gantry.supports_teach():\n                        console.print(\n                            f\"{format_badge('TEACH DISABLED', 'warn')} [festo.muted]Modbus backend has no PLC teach command.[/]\\n\"\n                            \"    Use [festo.ok]capture[/] to save positions to JSON instead.\"\n                        )\n                    elif len(parts) &gt;= 3 and parts[1] == \"pos\":\n                        pos_id = int(parts[2])\n                        gantry.teach_pos(pos_id=pos_id)\n                        console.print(f\"[festo.ok]\u2713[/] TEACH_POS sent ([festo.ok]pos_id={pos_id}[/])\")\n                    elif len(parts) &gt;= 4 and parts[1] == \"tray\":\n                        tray_id, tray_pos = int(parts[2]), int(parts[3])\n                        gantry.teach_tray(tray_id=tray_id, tray_pos=tray_pos)\n                        console.print(\n                            f\"[festo.ok]\u2713[/] TEACH_TRAY sent ([festo.ok]tray_id={tray_id}[/], [festo.ok]tray_pos={tray_pos}[/])\"\n                        )\n                    else:\n                        console.print(\"[festo.err]\u2717[/] Usage:\")\n                        console.print(f\"    {format_help_line('teach pos &lt;pos_id&gt;')}\")\n                        console.print(f\"    {format_help_line('teach tray &lt;tray_id&gt; &lt;tray_pos&gt;')}\")\n\n                elif cmd == \"list\":\n                    if not session.positions:\n                        console.print(\"[festo.muted]No positions captured yet.[/]\")\n                    else:\n                        console.print(_positions_table(session.positions))\n\n                elif cmd == \"save\":\n                    if len(parts) &lt; 2:\n                        console.print(f\"[festo.err]\u2717[/] Usage: {format_help_line('save &lt;path&gt;')}\")\n                        continue\n                    session.save(parts[1])\n                    console.print(f\"[festo.ok]\u2713[/] {len(session.positions)} position(s) saved \u2192 [bold]{parts[1]}[/]\")\n\n                elif cmd == \"load\":\n                    if len(parts) &lt; 2:\n                        console.print(f\"[festo.err]\u2717[/] Usage: {format_help_line('load &lt;path&gt;')}\")\n                        continue\n                    before = len(session.positions)\n                    session.load(parts[1])\n                    added = len(session.positions) - before\n                    console.print(f\"[festo.ok]\u2713[/] {added} position(s) loaded from [bold]{parts[1]}[/]\")\n\n                elif cmd == \"loglevel\":\n                    console.print(f\"[festo.ok]\u2713[/] {set_runtime_log_level(parts[1:])}\")\n\n                else:\n                    console.print(f\"[festo.err]\u2717[/] Unknown command: [bold]{cmd!r}[/]  (type [festo.ok]help[/])\")\n\n            except (KeyError, ValueError, IndexError) as exc:\n                console.print(f\"[festo.err]\u2717[/] {exc}\")\n            except Exception as exc:\n                logger.exception(\"Unexpected error processing command %r\", raw)\n                console.print(f\"[festo.err]\u2717[/] Unexpected error: {exc}\")\n    finally:\n        logging.disable(previous_disable)\n\n    console.print(\"[festo.ok]\u2713[/] Quitting program repl successful.\")\n    return 1\n</code></pre>"},{"location":"autoapi/applied_motion/cli/cli/#applied_motion.cli.cli.register_motion_cli","title":"register_motion_cli","text":"<pre><code>register_motion_cli(\n    parent_subparsers: _SubParsersAction,\n    *,\n    command_name: str = \"motion\",\n    extensions: Sequence[MotionCliExtension] = (),\n) -&gt; argparse.ArgumentParser\n</code></pre> <p>Attach motion CLI subtree to parent parser.</p> <p>Designed for higher-level system CLIs that compose multiple domains (for example <code>motion</code> and <code>fluid</code>) side-by-side.</p> <p>Parameters:</p> Name Type Description Default <code>parent_subparsers</code> <code>_SubParsersAction</code> <p>Parent parser subcommand registry.</p> required <code>command_name</code> <code>str</code> <p>Name used for mounted motion command subtree.</p> <code>'motion'</code> <code>extensions</code> <code>Sequence[MotionCliExtension]</code> <p>Optional extension hooks that can add extra motion subcommands.</p> <code>()</code> <p>Returns:</p> Type Description <code>ArgumentParser</code> <p>Mounted motion command parser.</p> Source code in <code>src/applied_motion/cli/cli.py</code> <pre><code>def register_motion_cli(\n    parent_subparsers: argparse._SubParsersAction,\n    *,\n    command_name: str = \"motion\",\n    extensions: Sequence[MotionCliExtension] = (),\n) -&gt; argparse.ArgumentParser:\n    \"\"\"Attach motion CLI subtree to parent parser.\n\n    Designed for higher-level system CLIs that compose multiple domains\n    (for example ``motion`` and ``fluid``) side-by-side.\n\n    Args:\n        parent_subparsers: Parent parser subcommand registry.\n        command_name: Name used for mounted motion command subtree.\n        extensions: Optional extension hooks that can add extra motion\n            subcommands.\n\n    Returns:\n        Mounted motion command parser.\n    \"\"\"\n    parser = parent_subparsers.add_parser(command_name, help=\"Motion control commands\")\n    parser.add_argument(\n        \"--config\",\n        required=True,\n        type=Path,\n        metavar=\"PATH\",\n        help=\"Path to gantry JSON configuration file.\",\n    )\n    parser.add_argument(\n        \"--gantry-name\",\n        default=\"gantry_1\",\n        metavar=\"NAME\",\n        help=\"Gantry component name in config (default: gantry_1).\",\n    )\n    parser.add_argument(\n        \"--log-level\",\n        default=\"INHERIT\",\n        choices=INHERITED_LOG_LEVEL_CHOICES,\n        metavar=\"LEVEL\",\n        help=\"Python logging level (default: INHERIT from parent CLI, else OFF).\",\n    )\n    motion_subparsers = parser.add_subparsers(dest=\"motion_command\")\n    _add_motion_command_parsers(motion_subparsers)\n    for extension in extensions:\n        extension(motion_subparsers)\n    return parser\n</code></pre>"},{"location":"autoapi/applied_motion/cli/cli/#applied_motion.cli.cli.build_standalone_motion_parser","title":"build_standalone_motion_parser","text":"<pre><code>build_standalone_motion_parser(\n    *,\n    prog: str = \"applied-motion\",\n    extensions: Sequence[MotionCliExtension] = (),\n) -&gt; argparse.ArgumentParser\n</code></pre> <p>Build standalone motion CLI parser.</p> <p>Parameters:</p> Name Type Description Default <code>prog</code> <code>str</code> <p>Program name shown in help output.</p> <code>'applied-motion'</code> <code>extensions</code> <code>Sequence[MotionCliExtension]</code> <p>Optional extension hooks for additional subcommands.</p> <code>()</code> <p>Returns:</p> Type Description <code>ArgumentParser</code> <p>Fully configured standalone parser.</p> Source code in <code>src/applied_motion/cli/cli.py</code> <pre><code>def build_standalone_motion_parser(\n    *,\n    prog: str = \"applied-motion\",\n    extensions: Sequence[MotionCliExtension] = (),\n) -&gt; argparse.ArgumentParser:\n    \"\"\"Build standalone motion CLI parser.\n\n    Args:\n        prog: Program name shown in help output.\n        extensions: Optional extension hooks for additional subcommands.\n\n    Returns:\n        Fully configured standalone parser.\n    \"\"\"\n    parser = argparse.ArgumentParser(\n        prog=prog,\n        description=\"Composable motion control CLI for teach-in and axis operations.\",\n    )\n    parser.add_argument(\n        \"--config\",\n        required=True,\n        type=Path,\n        metavar=\"PATH\",\n        help=\"Path to gantry JSON configuration file.\",\n    )\n    parser.add_argument(\n        \"--gantry-name\",\n        default=\"gantry_1\",\n        metavar=\"NAME\",\n        help=\"Gantry component name in config (default: gantry_1).\",\n    )\n    parser.add_argument(\n        \"--log-level\",\n        default=\"OFF\",\n        choices=LOG_LEVEL_CHOICES,\n        metavar=\"LEVEL\",\n        help=\"Python logging level (default: OFF).\",\n    )\n    subparsers = parser.add_subparsers(dest=\"motion_command\")\n    _add_motion_command_parsers(subparsers)\n    for extension in extensions:\n        extension(subparsers)\n    return parser\n</code></pre>"},{"location":"autoapi/applied_motion/cli/cli/#applied_motion.cli.cli.dispatch_motion_command","title":"dispatch_motion_command","text":"<pre><code>dispatch_motion_command(args: Namespace) -&gt; int\n</code></pre> <p>Dispatch parsed motion command namespace.</p> <p>Initialises the gantry once and passes it to the selected handler. Guarantees <code>gantry.close()</code> is called on exit regardless of outcome.</p> <p>Parameters:</p> Name Type Description Default <code>args</code> <code>Namespace</code> <p>Parsed argument namespace from a motion parser.</p> required <p>Returns:</p> Type Description <code>int</code> <p>Process exit code.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If no command handler is available in args.</p> Source code in <code>src/applied_motion/cli/cli.py</code> <pre><code>def dispatch_motion_command(args: argparse.Namespace) -&gt; int:\n    \"\"\"Dispatch parsed motion command namespace.\n\n    Initialises the gantry once and passes it to the selected handler.\n    Guarantees ``gantry.close()`` is called on exit regardless of outcome.\n\n    Args:\n        args: Parsed argument namespace from a motion parser.\n\n    Returns:\n        Process exit code.\n\n    Raises:\n        ValueError: If no command handler is available in *args*.\n    \"\"\"\n    if hasattr(args, \"log_level\"):\n        configure_logging(args.log_level)\n\n    handler = getattr(args, \"_handler\", None)\n    if handler is None:\n        raise ValueError(\"No motion command selected\")\n    gantry = _connect_gantry(args.config, args.gantry_name)\n    try:\n        return handler(args, gantry)\n    finally:\n        gantry.close()\n</code></pre>"},{"location":"autoapi/applied_motion/cli/cli/#applied_motion.cli.cli.main","title":"main","text":"<pre><code>main(argv: list[str] | None = None) -&gt; None\n</code></pre> <p>Entry point for <code>applied-motion</code> standalone CLI command.</p> <p>Parameters:</p> Name Type Description Default <code>argv</code> <code>list[str] | None</code> <p>Optional argv override used by tests.</p> <code>None</code> Source code in <code>src/applied_motion/cli/cli.py</code> <pre><code>def main(argv: list[str] | None = None) -&gt; None:\n    \"\"\"Entry point for ``applied-motion`` standalone CLI command.\n\n    Args:\n        argv: Optional argv override used by tests.\n    \"\"\"\n    parser = build_standalone_motion_parser()\n    args = parser.parse_args(argv)\n    if args.motion_command is None:\n        args._handler = _run_shell\n    exit_code = 1\n    try:\n        console.print(f\"[festo.muted]Loading config:[/] {args.config}\")\n        exit_code = dispatch_motion_command(args)\n    except KeyboardInterrupt:\n        console.print(\"\\n[festo.muted]Interrupted.[/]\")\n        sys.exit(130)\n    except Exception as exc:\n        logger.debug(\"CLI fatal error\", exc_info=True)\n        console.print(f\"[festo.err]\u2717[/] {exc}\")\n        sys.exit(1)\n\n    if exit_code:\n        tone = \"warn\" if exit_code == 130 else \"err\"\n        console.print(f\"{format_badge('EXIT', tone)} [festo.muted]code {exit_code}[/]\")\n        console.print(\"Ctrl+c to end shell\")\n        sys.exit(exit_code)\n</code></pre>"},{"location":"autoapi/applied_motion/cli/formatting/","title":"formatting","text":""},{"location":"autoapi/applied_motion/cli/formatting/#applied_motion.cli.formatting","title":"formatting","text":"<p>Reusable formatting helpers for Festo-styled CLI output.</p> <p>This module centralises Rich-markup snippets used by command help and usage messages so style choices stay consistent across multiple REPL entry points.</p>"},{"location":"autoapi/applied_motion/cli/formatting/#applied_motion.cli.formatting.format_usage","title":"format_usage","text":"<pre><code>format_usage(usage: str) -&gt; str\n</code></pre> <p>Return a usage string with Festo style tags applied token-by-token.</p> <p>Parameters:</p> Name Type Description Default <code>usage</code> <code>str</code> <p>Plain usage text such as <code>\"jog &lt;axis&gt; &lt;+/-&gt; &lt;step&gt; [vel]\"</code>.</p> required <p>Returns:</p> Type Description <code>str</code> <p>Rich-markup usage string where command tokens use <code>festo.ok</code>, required</p> <code>str</code> <p>arguments use <code>festo.value</code>, and optional arguments use <code>festo.muted</code>.</p> Source code in <code>src/applied_motion/cli/formatting.py</code> <pre><code>def format_usage(usage: str) -&gt; str:\n    \"\"\"Return a usage string with Festo style tags applied token-by-token.\n\n    Args:\n        usage: Plain usage text such as ``\"jog &lt;axis&gt; &lt;+/-&gt; &lt;step&gt; [vel]\"``.\n\n    Returns:\n        Rich-markup usage string where command tokens use ``festo.ok``, required\n        arguments use ``festo.value``, and optional arguments use ``festo.muted``.\n    \"\"\"\n    styled_tokens: list[str] = []\n    for token in usage.split():\n        clean = token[1:] if token.startswith(\"\\\\\") else token\n        if clean.startswith(\"&lt;\") and clean.endswith(\"&gt;\"):\n            styled_tokens.append(f\"[festo.value]{token}[/]\")\n        elif clean.startswith(\"[\") and clean.endswith(\"]\"):\n            styled_tokens.append(f\"[festo.muted]{token}[/]\")\n        else:\n            styled_tokens.append(f\"[festo.ok]{token}[/]\")\n    return \" \".join(styled_tokens)\n</code></pre>"},{"location":"autoapi/applied_motion/cli/formatting/#applied_motion.cli.formatting.format_help_line","title":"format_help_line","text":"<pre><code>format_help_line(\n    usage: str, description: str = \"\", indent: int = 0\n) -&gt; str\n</code></pre> <p>Return a single styled help line.</p> <p>Parameters:</p> Name Type Description Default <code>usage</code> <code>str</code> <p>Command usage text.</p> required <code>description</code> <code>str</code> <p>Optional description shown to the right of usage.</p> <code>''</code> <code>indent</code> <code>int</code> <p>Two-space indentation depth.</p> <code>0</code> <p>Returns:</p> Type Description <code>str</code> <p>Styled single-line help entry.</p> Source code in <code>src/applied_motion/cli/formatting.py</code> <pre><code>def format_help_line(usage: str, description: str = \"\", indent: int = 0) -&gt; str:\n    \"\"\"Return a single styled help line.\n\n    Args:\n        usage: Command usage text.\n        description: Optional description shown to the right of usage.\n        indent: Two-space indentation depth.\n\n    Returns:\n        Styled single-line help entry.\n    \"\"\"\n    pad = \"  \" * indent\n    rendered_usage = format_usage(usage)\n    if description:\n        return f\"{pad}{rendered_usage}    [festo.muted]{description}[/]\"\n    return f\"{pad}{rendered_usage}\"\n</code></pre>"},{"location":"autoapi/applied_motion/cli/formatting/#applied_motion.cli.formatting.format_group_header","title":"format_group_header","text":"<pre><code>format_group_header(\n    name: str, description: str = \"\", indent: int = 0\n) -&gt; str\n</code></pre> <p>Return a styled namespace/group header line.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Group name (namespace token).</p> required <code>description</code> <code>str</code> <p>Optional group description.</p> <code>''</code> <code>indent</code> <code>int</code> <p>Two-space indentation depth.</p> <code>0</code> <p>Returns:</p> Type Description <code>str</code> <p>Styled single-line group header.</p> Source code in <code>src/applied_motion/cli/formatting.py</code> <pre><code>def format_group_header(name: str, description: str = \"\", indent: int = 0) -&gt; str:\n    \"\"\"Return a styled namespace/group header line.\n\n    Args:\n        name: Group name (namespace token).\n        description: Optional group description.\n        indent: Two-space indentation depth.\n\n    Returns:\n        Styled single-line group header.\n    \"\"\"\n    pad = \"  \" * indent\n    rendered_name = f\"[festo.brand]{name}[/]\"\n    if description:\n        return f\"{pad}{rendered_name}    [festo.muted]{description}[/]\"\n    return f\"{pad}{rendered_name}\"\n</code></pre>"},{"location":"autoapi/applied_motion/cli/formatting/#applied_motion.cli.formatting.format_badge","title":"format_badge","text":"<pre><code>format_badge(label: str, tone: str = 'info') -&gt; str\n</code></pre> <p>Return a compact styled badge label.</p> <p>Parameters:</p> Name Type Description Default <code>label</code> <code>str</code> <p>Badge text to render.</p> required <code>tone</code> <code>str</code> <p>Badge tone name. Supported values are <code>\"ok\"</code>, <code>\"warn\"</code>, <code>\"err\"</code>, <code>\"muted\"</code>, and <code>\"info\"</code>.</p> <code>'info'</code> <p>Returns:</p> Type Description <code>str</code> <p>Rich-markup badge string.</p> Source code in <code>src/applied_motion/cli/formatting.py</code> <pre><code>def format_badge(label: str, tone: str = \"info\") -&gt; str:\n    \"\"\"Return a compact styled badge label.\n\n    Args:\n        label: Badge text to render.\n        tone: Badge tone name. Supported values are ``\"ok\"``, ``\"warn\"``,\n            ``\"err\"``, ``\"muted\"``, and ``\"info\"``.\n\n    Returns:\n        Rich-markup badge string.\n    \"\"\"\n    style_map = {\n        \"ok\": \"festo.ok\",\n        \"warn\": \"festo.warn\",\n        \"err\": \"festo.err\",\n        \"muted\": \"festo.muted\",\n        \"info\": \"festo.info\",\n    }\n    style = style_map.get(tone, \"festo.info\")\n    return f\"[{style}]\u25cf {label}[/]\"\n</code></pre>"},{"location":"autoapi/applied_motion/cli/formatting/#applied_motion.cli.formatting.format_bool","title":"format_bool","text":"<pre><code>format_bool(value: bool | None) -&gt; str\n</code></pre> <p>Return styled yes/no/unknown marker for booleans.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>bool | None</code> <p>Boolean-like value.</p> required <p>Returns:</p> Type Description <code>str</code> <p>Styled marker string.</p> Source code in <code>src/applied_motion/cli/formatting.py</code> <pre><code>def format_bool(value: bool | None) -&gt; str:\n    \"\"\"Return styled yes/no/unknown marker for booleans.\n\n    Args:\n        value: Boolean-like value.\n\n    Returns:\n        Styled marker string.\n    \"\"\"\n    if value is True:\n        return format_badge(\"YES\", \"ok\")\n    if value is False:\n        return format_badge(\"NO\", \"err\")\n    return format_badge(\"N/A\", \"muted\")\n</code></pre>"},{"location":"autoapi/applied_motion/cli/formatting/#applied_motion.cli.formatting.build_help_block","title":"build_help_block","text":"<pre><code>build_help_block(\n    entries: Sequence[tuple[str, str]],\n    *,\n    title: str = \"Commands\",\n    footer: Sequence[str] = (),\n) -&gt; str\n</code></pre> <p>Build a complete styled help block from usage-description pairs.</p> <p>Parameters:</p> Name Type Description Default <code>entries</code> <code>Sequence[tuple[str, str]]</code> <p>Ordered <code>(usage, description)</code> pairs.</p> required <code>title</code> <code>str</code> <p>Section heading.</p> <code>'Commands'</code> <code>footer</code> <code>Sequence[str]</code> <p>Additional already-styled lines to append.</p> <code>()</code> <p>Returns:</p> Type Description <code>str</code> <p>Newline-joined Rich-markup block.</p> Source code in <code>src/applied_motion/cli/formatting.py</code> <pre><code>def build_help_block(\n    entries: Sequence[tuple[str, str]],\n    *,\n    title: str = \"Commands\",\n    footer: Sequence[str] = (),\n) -&gt; str:\n    \"\"\"Build a complete styled help block from usage-description pairs.\n\n    Args:\n        entries: Ordered ``(usage, description)`` pairs.\n        title: Section heading.\n        footer: Additional already-styled lines to append.\n\n    Returns:\n        Newline-joined Rich-markup block.\n    \"\"\"\n    lines = [f\"[festo.brand]{title}[/]\"]\n    lines.extend(format_help_line(usage, description) for usage, description in entries)\n    lines.extend(footer)\n    return \"\\n\".join(lines)\n</code></pre>"},{"location":"autoapi/applied_motion/cli/logging_utils/","title":"logging_utils","text":""},{"location":"autoapi/applied_motion/cli/logging_utils/#applied_motion.cli.logging_utils","title":"logging_utils","text":"<p>Runtime logging helpers for applied-motion CLI entry points.</p> <p>These helpers support two related use cases:</p> <ul> <li>configuring logging once at process startup for standalone and composed CLIs,</li> <li>changing the active root log level while an interactive CLI is already   running.</li> </ul>"},{"location":"autoapi/applied_motion/cli/logging_utils/#applied_motion.cli.logging_utils.current_log_level_name","title":"current_log_level_name","text":"<pre><code>current_log_level_name() -&gt; str\n</code></pre> <p>Return current effective root log level name.</p> <p>Returns:</p> Type Description <code>str</code> <p>Effective root log level name, or <code>\"OFF\"</code> when global logging is</p> <code>str</code> <p>disabled.</p> Source code in <code>src/applied_motion/cli/logging_utils.py</code> <pre><code>def current_log_level_name() -&gt; str:\n    \"\"\"Return current effective root log level name.\n\n    Returns:\n        Effective root log level name, or ``\"OFF\"`` when global logging is\n        disabled.\n    \"\"\"\n    if logging.root.manager.disable &gt;= logging.CRITICAL:\n        return \"OFF\"\n\n    level_name = logging.getLevelName(logging.getLogger().getEffectiveLevel())\n    return level_name if isinstance(level_name, str) else str(level_name)\n</code></pre>"},{"location":"autoapi/applied_motion/cli/logging_utils/#applied_motion.cli.logging_utils.configure_logging","title":"configure_logging","text":"<pre><code>configure_logging(log_level: str | None) -&gt; str\n</code></pre> <p>Configure or inherit process logging.</p> <p>Parameters:</p> Name Type Description Default <code>log_level</code> <code>str | None</code> <p>Requested log level name. <code>None</code> or <code>\"INHERIT\"</code> keeps current logging configuration unchanged. <code>\"OFF\"</code> disables all logging.</p> required <p>Returns:</p> Type Description <code>str</code> <p>Active log level name after configuration completes.</p> Source code in <code>src/applied_motion/cli/logging_utils.py</code> <pre><code>def configure_logging(log_level: str | None) -&gt; str:\n    \"\"\"Configure or inherit process logging.\n\n    Args:\n        log_level: Requested log level name. ``None`` or ``\"INHERIT\"`` keeps\n            current logging configuration unchanged. ``\"OFF\"`` disables all\n            logging.\n\n    Returns:\n        Active log level name after configuration completes.\n    \"\"\"\n    if log_level is None:\n        _configure_pymodbus_logger()\n        return current_log_level_name()\n\n    resolved_log_level = log_level.upper()\n    if resolved_log_level == \"INHERIT\":\n        if not _has_parent_logging_configuration():\n            logging.disable(logging.CRITICAL)\n            _configure_pymodbus_logger()\n            return \"OFF\"\n        _configure_pymodbus_logger()\n        return current_log_level_name()\n    if resolved_log_level == \"OFF\":\n        logging.disable(logging.CRITICAL)\n        _configure_pymodbus_logger()\n        return \"OFF\"\n\n    logging.disable(logging.NOTSET)\n    level_value = getattr(logging, resolved_log_level, logging.WARNING)\n    logging.basicConfig(level=level_value, format=_LOG_FORMAT, force=True, stream=sys.stdout)\n    root_logger = logging.getLogger()\n    root_logger.setLevel(level_value)\n    for handler in root_logger.handlers:\n        handler.setLevel(level_value)\n    _configure_pymodbus_logger()\n    return resolved_log_level\n</code></pre>"},{"location":"autoapi/applied_motion/cli/logging_utils/#applied_motion.cli.logging_utils.set_runtime_log_level","title":"set_runtime_log_level","text":"<pre><code>set_runtime_log_level(args: Sequence[str]) -&gt; str\n</code></pre> <p>Report or update log level for running interactive CLI.</p> <p>Parameters:</p> Name Type Description Default <code>args</code> <code>Sequence[str]</code> <p>Tokens following <code>loglevel</code> command.</p> required <p>Returns:</p> Type Description <code>str</code> <p>Human-readable status message.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If invalid argument count or unsupported level supplied.</p> Source code in <code>src/applied_motion/cli/logging_utils.py</code> <pre><code>def set_runtime_log_level(args: Sequence[str]) -&gt; str:\n    \"\"\"Report or update log level for running interactive CLI.\n\n    Args:\n        args: Tokens following ``loglevel`` command.\n\n    Returns:\n        Human-readable status message.\n\n    Raises:\n        ValueError: If invalid argument count or unsupported level supplied.\n    \"\"\"\n    if not args:\n        return f\"Current log level: {current_log_level_name()}\"\n    if len(args) != 1:\n        raise ValueError(f\"Usage: loglevel [{'|'.join(LOG_LEVEL_CHOICES)}]\")\n\n    requested_level = args[0].upper()\n    if requested_level not in LOG_LEVEL_CHOICES:\n        raise ValueError(f\"Unsupported log level {args[0]!r}. Choose from {', '.join(LOG_LEVEL_CHOICES)}\")\n\n    active_level = configure_logging(requested_level)\n    return f\"Log level set to {active_level}\"\n</code></pre>"},{"location":"autoapi/applied_motion/cli/session/","title":"session","text":""},{"location":"autoapi/applied_motion/cli/session/#applied_motion.cli.session","title":"session","text":"<p>Gantry commissioning and motion session \u2014 position recording and jogging.</p> <p>This module is backend-agnostic.  It does not depend on <code>prompt_toolkit</code> or <code>rich</code> and can be imported and tested without the <code>teach</code> extra installed, as long as <code>applied_motion.cli</code> (the package guard) is not triggered first.</p> <p>The <code>MotionSession</code> class deliberately avoids calling any private attributes of <code>Gantry</code>. PLC-specific actions (<code>TEACH_POS</code>, <code>TEACH_TRAY</code>) are delegated to the caller via the <code>on_capture</code> hook, keeping this class backend-agnostic and independently unit-testable.</p>"},{"location":"autoapi/applied_motion/cli/session/#applied_motion.cli.session.OnCaptureHook","title":"OnCaptureHook  <code>module-attribute</code>","text":"<pre><code>OnCaptureHook = Callable[[str, dict[str, float]], None]\n</code></pre> <p>Type alias for the optional capture hook.</p> <p>Signature: <code>on_capture(label: str, position: dict[str, float]) -&gt; None</code></p> <p>Called after every successful <code>MotionSession.capture</code> with the position label and the recorded position dict.  Typical uses:</p> <ul> <li>Send <code>TEACH_POS</code> / <code>TEACH_TRAY</code> to the CECC-X PLC via   <code>FPosBAPIClient</code>.</li> <li>Emit an event to a higher-level orchestration layer.</li> <li>Write a running log to a remote database.</li> </ul>"},{"location":"autoapi/applied_motion/cli/session/#applied_motion.cli.session.MotionSession","title":"MotionSession","text":"<pre><code>MotionSession(\n    gantry: Gantry, on_capture: OnCaptureHook | None = None\n)\n</code></pre> <p>Records gantry positions interactively or programmatically.</p> <p>Provides step-jog and position capture on top of a connected <code>Gantry</code>.  Captured positions are kept in <code>positions</code> and can be persisted to and from JSON via <code>save</code> / <code>load</code>.</p> <p>This class has no knowledge of the gantry backend (Modbus vs FPosBAPI) and no dependency on <code>prompt_toolkit</code> or <code>rich</code>.  Backend-specific post-capture actions (e.g. sending <code>TEACH_POS</code> to the PLC) are performed by the caller through the on_capture hook.</p> <p>Parameters:</p> Name Type Description Default <code>gantry</code> <code>Gantry</code> <p>Connected and homed <code>Gantry</code> instance.</p> required <code>on_capture</code> <code>OnCaptureHook | None</code> <p>Optional hook called immediately after each successful <code>capture</code>.  Receives the position label and position dict. Exceptions raised by the hook propagate to the caller.</p> <code>None</code> <p>Attributes:</p> Name Type Description <code>positions</code> <code>dict[str, dict[str, float]]</code> <p>Ordered dict mapping label \u2192 <code>{axis_name: position_mm}</code>. Populated by <code>capture</code> and <code>load</code>.</p> <p>Example::</p> <pre><code># FPosBAPI: wire a hook that commits positions to the PLC\ndef plc_hook(label, pos):\n    gantry.teach_pos(pos_id=label_to_id[label])\n\nsession = MotionSession(gantry, on_capture=plc_hook)\nsession.jog(\"X\", \"+\", 5.0)\nsession.capture(\"deck_a1\")\nsession.save(\"deck_layout.json\")\n</code></pre> Source code in <code>src/applied_motion/cli/session.py</code> <pre><code>def __init__(  # noqa\n    self,\n    gantry: Gantry,\n    on_capture: OnCaptureHook | None = None,\n) -&gt; None:\n    self.gantry = gantry\n    self.on_capture = on_capture\n    self.positions: dict[str, dict[str, float]] = {}\n    logger.debug(\n        \"MotionSession: created for gantry=%r hook=%s\",\n        gantry,\n        getattr(on_capture, \"__name__\", repr(on_capture)) if on_capture is not None else \"None\",\n    )\n</code></pre>"},{"location":"autoapi/applied_motion/cli/session/#applied_motion.cli.session.MotionSession.jog","title":"jog","text":"<pre><code>jog(\n    axis_name: str,\n    direction: str,\n    step_mm: float,\n    velocity: float = 10.0,\n    timeout: int = 15,\n) -&gt; dict[str, float]\n</code></pre> <p>Step-move one axis by step_mm in direction.</p> <p>Reads the current axis position, computes an absolute target, and dispatches a single-axis move via <code>Gantry.move_to</code>.</p> <p>Parameters:</p> Name Type Description Default <code>axis_name</code> <code>str</code> <p>Name of the axis to jog (must be a key in <code>axes</code>).</p> required <code>direction</code> <code>str</code> <p><code>\"+\"</code> to move in the positive direction, <code>\"-\"</code> to move in the negative direction.</p> required <code>step_mm</code> <code>float</code> <p>Distance to step in millimetres.  Must be positive.</p> required <code>velocity</code> <code>float</code> <p>Jog speed in mm/s.  Defaults to <code>10.0</code>.</p> <code>10.0</code> <code>timeout</code> <code>int</code> <p>Maximum seconds to wait for the move to complete. When exceeded the axis backend issues a stop command and the move is abandoned.  Defaults to <code>30</code> seconds \u2014 long enough for any normal jog step, but ensures the axis will always halt rather than run indefinitely.</p> <code>15</code> <p>Returns:</p> Type Description <code>dict[str, float]</code> <p>Full gantry location dict after the move completes.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If direction is not <code>\"+\"</code> or <code>\"-\"</code>, or if step_mm is not positive.</p> <code>KeyError</code> <p>If axis_name is not registered with the gantry.</p> Source code in <code>src/applied_motion/cli/session.py</code> <pre><code>def jog(\n    self,\n    axis_name: str,\n    direction: str,\n    step_mm: float,\n    velocity: float = 10.0,\n    timeout: int = 15,\n) -&gt; dict[str, float]:\n    \"\"\"Step-move one axis by *step_mm* in *direction*.\n\n    Reads the current axis position, computes an absolute target, and\n    dispatches a single-axis move via\n    [`Gantry.move_to`][applied_motion.applied_motion.Gantry.move_to].\n\n    Args:\n        axis_name: Name of the axis to jog (must be a key in\n            ``axes``).\n        direction: ``\"+\"`` to move in the positive direction, ``\"-\"``\n            to move in the negative direction.\n        step_mm: Distance to step in millimetres.  Must be positive.\n        velocity: Jog speed in mm/s.  Defaults to ``10.0``.\n        timeout: Maximum seconds to wait for the move to complete.\n            When exceeded the axis backend issues a stop command and\n            the move is abandoned.  Defaults to ``30`` seconds \u2014\n            long enough for any normal jog step, but ensures the\n            axis will always halt rather than run indefinitely.\n\n    Returns:\n        Full gantry location dict after the move completes.\n\n    Raises:\n        ValueError: If *direction* is not ``\"+\"`` or ``\"-\"``, or if\n            *step_mm* is not positive.\n        KeyError: If *axis_name* is not registered with the gantry.\n    \"\"\"\n    if direction not in (\"+\", \"-\"):\n        raise ValueError(f\"direction must be '+' or '-', got {direction!r}\")\n    if step_mm &lt;= 0:\n        raise ValueError(f\"step_mm must be positive, got {step_mm!r}\")\n    if axis_name not in self.gantry.axes:\n        raise KeyError(f\"Axis {axis_name!r} is not registered with this gantry\")\n\n    axis = self.gantry.axes[axis_name]\n    current = axis.get_current_axis_position()\n    raw_delta = step_mm if direction == \"+\" else -step_mm\n    if raw_delta &gt; 0:\n        available = max(0.0, axis.max_position - current - _JOG_LIMIT_MARGIN)\n        delta = min(raw_delta, available)\n    else:\n        available = max(0.0, current - axis.min_position - _JOG_LIMIT_MARGIN)\n        delta = max(raw_delta, -available)\n\n    logger.info(\n        \"MotionSession.jog: axis=%s limits=[%.3f, %.3f] mm current=%.3f delta=%.3f vel=%.1f mm/s timeout=%ss\",\n        axis_name,\n        axis.min_position,\n        axis.max_position,\n        current,\n        delta,\n        velocity,\n        timeout,\n    )\n    if delta != raw_delta:\n        logger.warning(\n            \"MotionSession.jog: axis=%s delta clamped %.3f\\u2192%.3f mm (%s mm margin from SW limit)\",\n            axis_name,\n            raw_delta,\n            delta,\n            _JOG_LIMIT_MARGIN,\n        )\n    if abs(delta) &lt; 1e-3:\n        logger.info(\"MotionSession.jog: axis=%s at limit, skipping move\", axis_name)\n        return self.gantry.get_location()\n\n    axis.move(delta, velocity, position_type=\"relative\", timeout=timeout)\n    location = self.gantry.get_location()\n    logger.debug(\"MotionSession.jog: post-move location=%s\", location)\n    return location\n</code></pre>"},{"location":"autoapi/applied_motion/cli/session/#applied_motion.cli.session.MotionSession.capture","title":"capture","text":"<pre><code>capture(label: str) -&gt; dict[str, float]\n</code></pre> <p>Record the current gantry position under label.</p> <p>Calls <code>Gantry.get_location</code>, stores the result in <code>positions</code>, then invokes on_capture if one was provided.</p> <p>Parameters:</p> Name Type Description Default <code>label</code> <code>str</code> <p>Unique name for this position (e.g. <code>\"deck_a1\"</code>). Overwrites any previous entry with the same label.</p> required <p>Returns:</p> Type Description <code>dict[str, float]</code> <p>The recorded position dict <code>{axis_name: position_mm}</code>.</p> Source code in <code>src/applied_motion/cli/session.py</code> <pre><code>def capture(self, label: str) -&gt; dict[str, float]:\n    \"\"\"Record the current gantry position under *label*.\n\n    Calls [`Gantry.get_location`][applied_motion.applied_motion.Gantry.get_location], stores\n    the result in ``positions``, then invokes *on_capture* if one\n    was provided.\n\n    Args:\n        label: Unique name for this position (e.g. ``\"deck_a1\"``).\n            Overwrites any previous entry with the same label.\n\n    Returns:\n        The recorded position dict ``{axis_name: position_mm}``.\n\n    Raises:\n        Any exception raised by *on_capture* propagates unchanged.\n    \"\"\"\n    position = self.gantry.get_location()\n    self.positions[label] = position\n    logger.info(\"MotionSession.capture: label=%r position=%s\", label, position)\n    if self.on_capture is not None:\n        self.on_capture(label, position)\n    return position\n</code></pre>"},{"location":"autoapi/applied_motion/cli/session/#applied_motion.cli.session.MotionSession.save","title":"save","text":"<pre><code>save(path: Path | str) -&gt; None\n</code></pre> <p>Write all captured positions to a JSON file.</p> <p>The output format is a plain JSON object mapping label \u2192 <code>{axis_name: position_mm}</code>.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>Path | str</code> <p>Destination file path.  Parent directories must exist.</p> required Source code in <code>src/applied_motion/cli/session.py</code> <pre><code>def save(self, path: Path | str) -&gt; None:\n    \"\"\"Write all captured positions to a JSON file.\n\n    The output format is a plain JSON object mapping label \u2192\n    ``{axis_name: position_mm}``.\n\n    Args:\n        path: Destination file path.  Parent directories must exist.\n    \"\"\"\n    path = Path(path)\n    with path.open(\"w\") as fh:\n        json.dump(self.positions, fh, indent=2)\n    logger.info(\"MotionSession.save: wrote %d position(s) to %s\", len(self.positions), path)\n</code></pre>"},{"location":"autoapi/applied_motion/cli/session/#applied_motion.cli.session.MotionSession.load","title":"load","text":"<pre><code>load(path: Path | str) -&gt; None\n</code></pre> <p>Merge positions from a JSON file into <code>positions</code>.</p> <p>Existing labels are overwritten by entries from path; labels absent in path are preserved.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>Path | str</code> <p>Source JSON file previously written by <code>save</code>.</p> required <p>Raises:</p> Type Description <code>OSError</code> <p>If path cannot be opened.</p> <code>ValueError</code> <p>If the file contains invalid JSON.</p> Source code in <code>src/applied_motion/cli/session.py</code> <pre><code>def load(self, path: Path | str) -&gt; None:\n    \"\"\"Merge positions from a JSON file into ``positions``.\n\n    Existing labels are overwritten by entries from *path*; labels\n    absent in *path* are preserved.\n\n    Args:\n        path: Source JSON file previously written by [`save`][applied_motion.cli.session.MotionSession.save].\n\n    Raises:\n        OSError: If *path* cannot be opened.\n        ValueError: If the file contains invalid JSON.\n    \"\"\"\n    path = Path(path)\n    with path.open() as fh:\n        loaded: dict[str, dict[str, float]] = json.load(fh)\n    self.positions.update(loaded)\n    logger.info(\"MotionSession.load: read %d position(s) from %s\", len(loaded), path)\n</code></pre>"},{"location":"autoapi/applied_motion/cli/theme/","title":"theme","text":""},{"location":"autoapi/applied_motion/cli/theme/#applied_motion.cli.theme","title":"theme","text":"<p>Festo-branded Rich theme and console factory.</p> <p>Palette sourced from the Festo corporate design system (<code>docs/css/extra.css</code> across all Festo Python packages).  Proportions follow the Festo corporate colour-proportion guideline (dominant white space \u2192 black text \u2192 grey tones \u2192 light blue highlight \u2192 Festo blue call-to-action accent):</p> <p>=========================  =========  ============================================== Name                       Hex        Reference =========================  =========  ============================================== <code>FESTO_BLACK_ATERUL</code>     #000000    Font / body text <code>FESTO_CHARCOAL</code>         #333333    Dark backgrounds, strong text <code>FESTO_GRAY_1</code>           #717D86    Pantone 431 C \u2014 darkest grey <code>FESTO_GRAY_2</code>           #949EA6    Pantone 430 C <code>FESTO_GRAY_CANUL</code>       #B6BEC6    Pantone 429 C \u2014 borders, muted labels <code>FESTO_GRAY_4</code>           #D0D6DC    Pantone 428 C <code>FESTO_GRAY_SUCANUL</code>     #E5E8EB    Pantone 427 C \u2014 large areas, table values <code>FESTO_BLUE_CAERUL</code>      #0091DC    HKS 47 K / PMS Process Blue \u2014 call-to-action <code>FESTO_BLUE_2</code>           #48B9EB    Pantone 298 C <code>FESTO_BLUE_3</code>           #92D5F6    Pantone 297 C <code>FESTO_BLUE_SUCAERUL</code>    #C8E6FA    Pantone 290 C \u2014 highlight / subtle accents <code>FESTO_BLUE_5</code>           #DEF0FC    Lightest blue tint =========================  =========  ==============================================</p> <p>Import this module in any CLI that needs Festo-consistent output::</p> <pre><code>from applied_motion.cli.theme import festo_console, FESTO_BLUE_CAERUL\n\nconsole = festo_console()\nconsole.print(\"[festo.brand]Motion Control CLI[/]\")\nconsole.print(\"[festo.ok]\u2713[/] Axes homed.\")\n</code></pre>"},{"location":"autoapi/applied_motion/cli/theme/#applied_motion.cli.theme--named-styles","title":"Named styles","text":"<p><code>festo.brand</code>     Bold Festo blue \u2014 section headers, product names. <code>festo.ok</code>     Festo blue \u2014 success marks, command names in help text, inline values. <code>festo.muted</code>     Mid-grey \u2014 secondary labels, dimmed hints. <code>festo.value</code>     Light grey \u2014 numeric values in tables.</p>"},{"location":"autoapi/applied_motion/cli/theme/#applied_motion.cli.theme.festo_console","title":"festo_console","text":"<pre><code>festo_console() -&gt; Console\n</code></pre> <p>Return a Rich :class:<code>~rich.console.Console</code> using the Festo brand theme.</p> <p>All :func:<code>~rich.console.Console.print</code> calls on the returned console recognise <code>[festo.brand]</code>, <code>[festo.ok]</code>, <code>[festo.muted]</code>, and <code>[festo.value]</code> as styled markup tags in addition to standard Rich tags.</p> <p>Returns:</p> Type Description <code>Console</code> <p>Themed console instance ready for CLI output.</p> Source code in <code>src/applied_motion/cli/theme.py</code> <pre><code>def festo_console() -&gt; Console:\n    \"\"\"Return a Rich :class:`~rich.console.Console` using the Festo brand theme.\n\n    All :func:`~rich.console.Console.print` calls on the returned console\n    recognise ``[festo.brand]``, ``[festo.ok]``, ``[festo.muted]``, and\n    ``[festo.value]`` as styled markup tags in addition to standard Rich tags.\n\n    Returns:\n        Themed console instance ready for CLI output.\n    \"\"\"\n    return Console(theme=FESTO_THEME)\n</code></pre>"},{"location":"autoapi/applied_motion/cli/compose/","title":"compose","text":""},{"location":"autoapi/applied_motion/cli/compose/#applied_motion.cli.compose","title":"compose","text":"<p>Composable command-registry framework shared by CLI packages.</p>"},{"location":"autoapi/applied_motion/cli/compose/#applied_motion.cli.compose.Command","title":"Command  <code>dataclass</code>","text":"<pre><code>Command(\n    name: str,\n    handler: CommandHandler,\n    usage: str = \"\",\n    help: str = \"\",\n    completions: Callable[[], list[str]] | None = None,\n)\n</code></pre> <p>A single named leaf command in a command group.</p>"},{"location":"autoapi/applied_motion/cli/compose/#applied_motion.cli.compose.CommandError","title":"CommandError","text":"<p>               Bases: <code>Exception</code></p> <p>Base error for command dispatch or execution failures.</p>"},{"location":"autoapi/applied_motion/cli/compose/#applied_motion.cli.compose.CommandGroup","title":"CommandGroup","text":"<pre><code>CommandGroup(name: str, help: str = '')\n</code></pre> <p>A composable namespace of commands and nested child groups.</p> <p>Initialise an empty command group.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>The group's own name.</p> required <code>help</code> <code>str</code> <p>Short description of the group.</p> <code>''</code> Source code in <code>src/applied_motion/cli/compose/core.py</code> <pre><code>def __init__(self, name: str, help: str = \"\") -&gt; None:\n    \"\"\"Initialise an empty command group.\n\n    Args:\n        name: The group's own name.\n        help: Short description of the group.\n    \"\"\"\n    self.name = name\n    self.help = help\n    self.commands: dict[str, Command] = {}\n    self.children: dict[str, CommandGroup] = {}\n    logger.debug(\"CommandGroup created: name=%s\", name)\n</code></pre>"},{"location":"autoapi/applied_motion/cli/compose/#applied_motion.cli.compose.CommandGroup.add_command","title":"add_command","text":"<pre><code>add_command(command: Command) -&gt; Command\n</code></pre> <p>Register a leaf command on this group.</p> <p>Parameters:</p> Name Type Description Default <code>command</code> <code>Command</code> <p>The command to register.</p> required <p>Returns:</p> Type Description <code>Command</code> <p>The registered command.</p> Source code in <code>src/applied_motion/cli/compose/core.py</code> <pre><code>def add_command(self, command: Command) -&gt; Command:\n    \"\"\"Register a leaf command on this group.\n\n    Args:\n        command: The command to register.\n\n    Returns:\n        The registered command.\n    \"\"\"\n    self.commands[command.name.lower()] = command\n    return command\n</code></pre>"},{"location":"autoapi/applied_motion/cli/compose/#applied_motion.cli.compose.CommandGroup.add_child","title":"add_child","text":"<pre><code>add_child(\n    group: CommandGroup, name: str | None = None\n) -&gt; CommandGroup\n</code></pre> <p>Mount a child group under a name.</p> <p>Parameters:</p> Name Type Description Default <code>group</code> <code>CommandGroup</code> <p>The child group to mount.</p> required <code>name</code> <code>str | None</code> <p>Mount key. Defaults to the child group's name.</p> <code>None</code> <p>Returns:</p> Type Description <code>CommandGroup</code> <p>The mounted child group.</p> Source code in <code>src/applied_motion/cli/compose/core.py</code> <pre><code>def add_child(self, group: \"CommandGroup\", name: str | None = None) -&gt; \"CommandGroup\":\n    \"\"\"Mount a child group under a name.\n\n    Args:\n        group: The child group to mount.\n        name: Mount key. Defaults to the child group's name.\n\n    Returns:\n        The mounted child group.\n    \"\"\"\n    key = (name or group.name).lower()\n    self.children[key] = group\n    logger.debug(\"CommandGroup %s mounted child under %r\", self.name, key)\n    return group\n</code></pre>"},{"location":"autoapi/applied_motion/cli/compose/#applied_motion.cli.compose.CommandGroup.dispatch","title":"dispatch","text":"<pre><code>dispatch(tokens: Sequence[str]) -&gt; None\n</code></pre> <p>Route a token sequence to a child group or a local command.</p> <p>Parameters:</p> Name Type Description Default <code>tokens</code> <code>Sequence[str]</code> <p>The whitespace-split command line.</p> required <p>Raises:</p> Type Description <code>UnknownCommandError</code> <p>If no matching child or command is found.</p> Source code in <code>src/applied_motion/cli/compose/core.py</code> <pre><code>def dispatch(self, tokens: Sequence[str]) -&gt; None:\n    \"\"\"Route a token sequence to a child group or a local command.\n\n    Args:\n        tokens: The whitespace-split command line.\n\n    Raises:\n        UnknownCommandError: If no matching child or command is found.\n    \"\"\"\n    if not tokens:\n        return\n    head = tokens[0].lower()\n    rest = tokens[1:]\n    if head in self.children:\n        self.children[head].dispatch(rest)\n        return\n    if head in self.commands:\n        self.commands[head].handler(rest)\n        return\n    raise UnknownCommandError(f\"Unknown command: {tokens[0]!r}\")\n</code></pre>"},{"location":"autoapi/applied_motion/cli/compose/#applied_motion.cli.compose.CommandGroup.iter_paths","title":"iter_paths","text":"<pre><code>iter_paths(\n    prefix: Sequence[str] = (),\n) -&gt; Iterator[tuple[str, ...]]\n</code></pre> <p>Yield the fully-qualified path of every command in the tree.</p> <p>Parameters:</p> Name Type Description Default <code>prefix</code> <code>Sequence[str]</code> <p>Internal prefix accumulator.</p> <code>()</code> <p>Yields:</p> Type Description <code>tuple[str, ...]</code> <p>Tuples of namespace names ending in a command name.</p> Source code in <code>src/applied_motion/cli/compose/core.py</code> <pre><code>def iter_paths(self, prefix: Sequence[str] = ()) -&gt; Iterator[tuple[str, ...]]:\n    \"\"\"Yield the fully-qualified path of every command in the tree.\n\n    Args:\n        prefix: Internal prefix accumulator.\n\n    Yields:\n        Tuples of namespace names ending in a command name.\n    \"\"\"\n    for name in self.commands:\n        yield (*tuple(prefix), name)\n    for child_name, child in self.children.items():\n        yield from child.iter_paths((*tuple(prefix), child_name))\n</code></pre>"},{"location":"autoapi/applied_motion/cli/compose/#applied_motion.cli.compose.CommandGroup.format_help","title":"format_help","text":"<pre><code>format_help(indent: int = 0) -&gt; list[str]\n</code></pre> <p>Build indented help lines for this group and all descendants.</p> <p>Parameters:</p> Name Type Description Default <code>indent</code> <code>int</code> <p>Current indentation depth.</p> <code>0</code> <p>Returns:</p> Type Description <code>list[str]</code> <p>A list of formatted help lines.</p> Source code in <code>src/applied_motion/cli/compose/core.py</code> <pre><code>def format_help(self, indent: int = 0) -&gt; list[str]:\n    \"\"\"Build indented help lines for this group and all descendants.\n\n    Args:\n        indent: Current indentation depth.\n\n    Returns:\n        A list of formatted help lines.\n    \"\"\"\n    pad = \"  \" * indent\n    lines: list[str] = []\n    for cmd in self.commands.values():\n        usage = cmd.usage or cmd.name\n        suffix = f\"    {cmd.help}\" if cmd.help else \"\"\n        lines.append(f\"{pad}{usage}{suffix}\")\n    for child_name, child in self.children.items():\n        header = f\"{pad}{child_name}\"\n        if child.help:\n            header = f\"{header}    {child.help}\"\n        lines.append(header)\n        lines.extend(child.format_help(indent + 1))\n    return lines\n</code></pre>"},{"location":"autoapi/applied_motion/cli/compose/#applied_motion.cli.compose.UnknownCommandError","title":"UnknownCommandError","text":"<p>               Bases: <code>CommandError</code></p> <p>Raise when a token matches neither a child group nor a local command.</p>"},{"location":"autoapi/applied_motion/cli/compose/#applied_motion.cli.compose.UsageError","title":"UsageError","text":"<p>               Bases: <code>CommandError</code></p> <p>Raise when a command is invoked with missing or invalid arguments.</p>"},{"location":"autoapi/applied_motion/cli/compose/core/","title":"core","text":""},{"location":"autoapi/applied_motion/cli/compose/core/#applied_motion.cli.compose.core","title":"core","text":"<p>Composable, transportable command-registry core for interactive CLIs.</p>"},{"location":"autoapi/applied_motion/cli/compose/core/#applied_motion.cli.compose.core.CommandError","title":"CommandError","text":"<p>               Bases: <code>Exception</code></p> <p>Base error for command dispatch or execution failures.</p>"},{"location":"autoapi/applied_motion/cli/compose/core/#applied_motion.cli.compose.core.UsageError","title":"UsageError","text":"<p>               Bases: <code>CommandError</code></p> <p>Raise when a command is invoked with missing or invalid arguments.</p>"},{"location":"autoapi/applied_motion/cli/compose/core/#applied_motion.cli.compose.core.UnknownCommandError","title":"UnknownCommandError","text":"<p>               Bases: <code>CommandError</code></p> <p>Raise when a token matches neither a child group nor a local command.</p>"},{"location":"autoapi/applied_motion/cli/compose/core/#applied_motion.cli.compose.core.Command","title":"Command  <code>dataclass</code>","text":"<pre><code>Command(\n    name: str,\n    handler: CommandHandler,\n    usage: str = \"\",\n    help: str = \"\",\n    completions: Callable[[], list[str]] | None = None,\n)\n</code></pre> <p>A single named leaf command in a command group.</p>"},{"location":"autoapi/applied_motion/cli/compose/core/#applied_motion.cli.compose.core.CommandGroup","title":"CommandGroup","text":"<pre><code>CommandGroup(name: str, help: str = '')\n</code></pre> <p>A composable namespace of commands and nested child groups.</p> <p>Initialise an empty command group.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>The group's own name.</p> required <code>help</code> <code>str</code> <p>Short description of the group.</p> <code>''</code> Source code in <code>src/applied_motion/cli/compose/core.py</code> <pre><code>def __init__(self, name: str, help: str = \"\") -&gt; None:\n    \"\"\"Initialise an empty command group.\n\n    Args:\n        name: The group's own name.\n        help: Short description of the group.\n    \"\"\"\n    self.name = name\n    self.help = help\n    self.commands: dict[str, Command] = {}\n    self.children: dict[str, CommandGroup] = {}\n    logger.debug(\"CommandGroup created: name=%s\", name)\n</code></pre>"},{"location":"autoapi/applied_motion/cli/compose/core/#applied_motion.cli.compose.core.CommandGroup.add_command","title":"add_command","text":"<pre><code>add_command(command: Command) -&gt; Command\n</code></pre> <p>Register a leaf command on this group.</p> <p>Parameters:</p> Name Type Description Default <code>command</code> <code>Command</code> <p>The command to register.</p> required <p>Returns:</p> Type Description <code>Command</code> <p>The registered command.</p> Source code in <code>src/applied_motion/cli/compose/core.py</code> <pre><code>def add_command(self, command: Command) -&gt; Command:\n    \"\"\"Register a leaf command on this group.\n\n    Args:\n        command: The command to register.\n\n    Returns:\n        The registered command.\n    \"\"\"\n    self.commands[command.name.lower()] = command\n    return command\n</code></pre>"},{"location":"autoapi/applied_motion/cli/compose/core/#applied_motion.cli.compose.core.CommandGroup.add_child","title":"add_child","text":"<pre><code>add_child(\n    group: CommandGroup, name: str | None = None\n) -&gt; CommandGroup\n</code></pre> <p>Mount a child group under a name.</p> <p>Parameters:</p> Name Type Description Default <code>group</code> <code>CommandGroup</code> <p>The child group to mount.</p> required <code>name</code> <code>str | None</code> <p>Mount key. Defaults to the child group's name.</p> <code>None</code> <p>Returns:</p> Type Description <code>CommandGroup</code> <p>The mounted child group.</p> Source code in <code>src/applied_motion/cli/compose/core.py</code> <pre><code>def add_child(self, group: \"CommandGroup\", name: str | None = None) -&gt; \"CommandGroup\":\n    \"\"\"Mount a child group under a name.\n\n    Args:\n        group: The child group to mount.\n        name: Mount key. Defaults to the child group's name.\n\n    Returns:\n        The mounted child group.\n    \"\"\"\n    key = (name or group.name).lower()\n    self.children[key] = group\n    logger.debug(\"CommandGroup %s mounted child under %r\", self.name, key)\n    return group\n</code></pre>"},{"location":"autoapi/applied_motion/cli/compose/core/#applied_motion.cli.compose.core.CommandGroup.dispatch","title":"dispatch","text":"<pre><code>dispatch(tokens: Sequence[str]) -&gt; None\n</code></pre> <p>Route a token sequence to a child group or a local command.</p> <p>Parameters:</p> Name Type Description Default <code>tokens</code> <code>Sequence[str]</code> <p>The whitespace-split command line.</p> required <p>Raises:</p> Type Description <code>UnknownCommandError</code> <p>If no matching child or command is found.</p> Source code in <code>src/applied_motion/cli/compose/core.py</code> <pre><code>def dispatch(self, tokens: Sequence[str]) -&gt; None:\n    \"\"\"Route a token sequence to a child group or a local command.\n\n    Args:\n        tokens: The whitespace-split command line.\n\n    Raises:\n        UnknownCommandError: If no matching child or command is found.\n    \"\"\"\n    if not tokens:\n        return\n    head = tokens[0].lower()\n    rest = tokens[1:]\n    if head in self.children:\n        self.children[head].dispatch(rest)\n        return\n    if head in self.commands:\n        self.commands[head].handler(rest)\n        return\n    raise UnknownCommandError(f\"Unknown command: {tokens[0]!r}\")\n</code></pre>"},{"location":"autoapi/applied_motion/cli/compose/core/#applied_motion.cli.compose.core.CommandGroup.iter_paths","title":"iter_paths","text":"<pre><code>iter_paths(\n    prefix: Sequence[str] = (),\n) -&gt; Iterator[tuple[str, ...]]\n</code></pre> <p>Yield the fully-qualified path of every command in the tree.</p> <p>Parameters:</p> Name Type Description Default <code>prefix</code> <code>Sequence[str]</code> <p>Internal prefix accumulator.</p> <code>()</code> <p>Yields:</p> Type Description <code>tuple[str, ...]</code> <p>Tuples of namespace names ending in a command name.</p> Source code in <code>src/applied_motion/cli/compose/core.py</code> <pre><code>def iter_paths(self, prefix: Sequence[str] = ()) -&gt; Iterator[tuple[str, ...]]:\n    \"\"\"Yield the fully-qualified path of every command in the tree.\n\n    Args:\n        prefix: Internal prefix accumulator.\n\n    Yields:\n        Tuples of namespace names ending in a command name.\n    \"\"\"\n    for name in self.commands:\n        yield (*tuple(prefix), name)\n    for child_name, child in self.children.items():\n        yield from child.iter_paths((*tuple(prefix), child_name))\n</code></pre>"},{"location":"autoapi/applied_motion/cli/compose/core/#applied_motion.cli.compose.core.CommandGroup.format_help","title":"format_help","text":"<pre><code>format_help(indent: int = 0) -&gt; list[str]\n</code></pre> <p>Build indented help lines for this group and all descendants.</p> <p>Parameters:</p> Name Type Description Default <code>indent</code> <code>int</code> <p>Current indentation depth.</p> <code>0</code> <p>Returns:</p> Type Description <code>list[str]</code> <p>A list of formatted help lines.</p> Source code in <code>src/applied_motion/cli/compose/core.py</code> <pre><code>def format_help(self, indent: int = 0) -&gt; list[str]:\n    \"\"\"Build indented help lines for this group and all descendants.\n\n    Args:\n        indent: Current indentation depth.\n\n    Returns:\n        A list of formatted help lines.\n    \"\"\"\n    pad = \"  \" * indent\n    lines: list[str] = []\n    for cmd in self.commands.values():\n        usage = cmd.usage or cmd.name\n        suffix = f\"    {cmd.help}\" if cmd.help else \"\"\n        lines.append(f\"{pad}{usage}{suffix}\")\n    for child_name, child in self.children.items():\n        header = f\"{pad}{child_name}\"\n        if child.help:\n            header = f\"{header}    {child.help}\"\n        lines.append(header)\n        lines.extend(child.format_help(indent + 1))\n    return lines\n</code></pre>"},{"location":"autoapi/applied_motion/cli/compose/repl/","title":"repl","text":""},{"location":"autoapi/applied_motion/cli/compose/repl/#applied_motion.cli.compose.repl","title":"repl","text":"<p>Generic prompt_toolkit REPL driver for command-group trees.</p>"},{"location":"autoapi/applied_motion/cli/compose/repl/#applied_motion.cli.compose.repl.NamespaceCompleter","title":"NamespaceCompleter","text":"<pre><code>NamespaceCompleter(root: CommandGroup)\n</code></pre> <p>               Bases: <code>Completer</code></p> <p>Hierarchical tab-completer for a command-group tree.</p> <p>Initialise the completer for a command tree.</p> <p>Parameters:</p> Name Type Description Default <code>root</code> <code>CommandGroup</code> <p>The root command-group to complete against.</p> required Source code in <code>src/applied_motion/cli/compose/repl.py</code> <pre><code>def __init__(self, root: CommandGroup) -&gt; None:\n    \"\"\"Initialise the completer for a command tree.\n\n    Args:\n        root: The root command-group to complete against.\n    \"\"\"\n    self.root = root\n</code></pre>"},{"location":"autoapi/applied_motion/cli/compose/repl/#applied_motion.cli.compose.repl.NamespaceCompleter.get_completions","title":"get_completions","text":"<pre><code>get_completions(document, complete_event)\n</code></pre> <p>Yield completion candidates for the current input.</p> <p>Parameters:</p> Name Type Description Default <code>document</code> <p>The current input document.</p> required <code>complete_event</code> <p>Completion event object.</p> required Source code in <code>src/applied_motion/cli/compose/repl.py</code> <pre><code>def get_completions(self, document, complete_event):  # noqa: ANN001, ANN201\n    \"\"\"Yield completion candidates for the current input.\n\n    Args:\n        document: The current input document.\n        complete_event: Completion event object.\n    \"\"\"\n    text = document.text_before_cursor\n    tokens = text.split()\n    at_word_boundary = text == \"\" or text.endswith(\" \")\n    consumed = tokens if at_word_boundary else tokens[:-1]\n    partial = \"\" if at_word_boundary else tokens[-1].lower()\n\n    group = self.root\n    for token in consumed:\n        key = token.lower()\n        if key in group.children:\n            group = group.children[key]\n        else:\n            return\n\n    options = list(group.children.keys()) + list(group.commands.keys())\n    if group is self.root:\n        options = options + _RESERVED_CMDS\n    for option in options:\n        if option.startswith(partial):\n            yield Completion(option, start_position=-len(partial))\n</code></pre>"},{"location":"autoapi/applied_motion/cli/compose/repl/#applied_motion.cli.compose.repl.render_help","title":"render_help","text":"<pre><code>render_help(root: CommandGroup) -&gt; str\n</code></pre> <p>Render an aggregated help listing for a command tree.</p> Source code in <code>src/applied_motion/cli/compose/repl.py</code> <pre><code>def render_help(root: CommandGroup) -&gt; str:\n    \"\"\"Render an aggregated help listing for a command tree.\"\"\"\n    lines = [\"[festo.brand]Commands[/]\"]\n\n    def _append(group: CommandGroup, indent: int) -&gt; None:\n        for cmd in group.commands.values():\n            lines.append(format_help_line(cmd.usage or cmd.name, cmd.help, indent=indent))\n        for child_name, child in group.children.items():\n            lines.append(format_group_header(child_name, child.help, indent=indent))\n            _append(child, indent + 1)\n\n    _append(root, 0)\n    lines.append(format_help_line(\"help\", \"Show this reference\"))\n    lines.append(format_help_line(\"quit\", \"Exit\"))\n    return \"\\n\".join(lines)\n</code></pre>"},{"location":"autoapi/applied_motion/cli/compose/repl/#applied_motion.cli.compose.repl.run_repl","title":"run_repl","text":"<pre><code>run_repl(\n    root: CommandGroup,\n    prompt: str = \"&gt; \",\n    console: Console | None = None,\n    intro: bool = True,\n) -&gt; None\n</code></pre> <p>Run an interactive REPL over a command tree.</p> Source code in <code>src/applied_motion/cli/compose/repl.py</code> <pre><code>def run_repl(  # noqa: C901\n    root: CommandGroup,\n    prompt: str = \"&gt; \",\n    console: Console | None = None,\n    intro: bool = True,\n) -&gt; None:\n    \"\"\"Run an interactive REPL over a command tree.\"\"\"\n    console = console or festo_console()\n    session: PromptSession[str] = PromptSession(history=InMemoryHistory(), completer=NamespaceCompleter(root))\n\n    if intro:\n        console.print(render_help(root))\n\n    with patch_stdout(raw=True):\n        while True:\n            try:\n                raw = session.prompt(prompt).strip()\n            except (EOFError, KeyboardInterrupt):\n                console.print(\"\\n[festo.muted]Exiting.[/]\")\n                break\n\n            if not raw:\n                continue\n\n            tokens = raw.split()\n            head = tokens[0].lower()\n\n            if head in (\"quit\", \"exit\"):\n                break\n            if head == \"help\":\n                console.print(render_help(root))\n                continue\n\n            try:\n                root.dispatch(tokens)\n            except UsageError as exc:\n                console.print(f\"[festo.err]\u2717[/] {exc}\")\n            except UnknownCommandError as exc:\n                console.print(f\"[festo.err]\u2717[/] {exc}  (type [festo.ok]help[/])\")\n            except NotImplementedError as exc:\n                console.print(f\"[festo.warn]![/] Not supported: {exc}\")\n            except AttributeError as exc:\n                console.print(f\"[festo.warn]![/] Not available: {exc}\")\n            except (KeyError, ValueError, IndexError, RuntimeError, CommandError) as exc:\n                console.print(f\"[festo.err]\u2717[/] {exc}\")\n            except Exception as exc:  # noqa: BLE001\n                logger.exception(\"Unexpected error processing command %r\", raw)\n                console.print(f\"[festo.err]\u2717[/] Unexpected error: {exc}\")\n</code></pre>"},{"location":"examples/edcon/","title":"festo-edcon / Modbus Examples","text":"<p>Copy-paste oriented examples for direct-drive setups using <code>EdconAxis</code> and <code>Gantry</code>.</p>"},{"location":"examples/edcon/#1-connect-via-configuration-file","title":"1. Connect via configuration file","text":"connect_from_config.py<pre><code>from pathlib import Path\n\nfrom applied_motion import Gantry\n\ngantry = Gantry.from_config(Path(\"modbus-gantry-config.json\"))\nprint(repr(gantry))\n</code></pre> <p>Config example:</p> modbus-gantry-config.json<pre><code>{\n    \"spec_version\": \"3.0\",\n    \"component_config\": {\n        \"metadata\": {},\n        \"components\": {\n            \"gantry_1\": {\n                \"backend\": \"modbus\",\n                \"axes\": {\n                    \"X\": {\"name\": \"X\", \"ip\": \"192.168.0.100\"},\n                    \"Y\": {\"name\": \"Y\", \"ip\": \"192.168.0.101\"},\n                    \"Z\": {\"name\": \"Z\", \"ip\": \"192.168.0.102\"}\n                },\n                \"axis_order\": [\"X\", \"Y\", \"Z\"],\n                \"concurrent_axes\": [\"X\", \"Y\"]\n            }\n        }\n    }\n}\n</code></pre>"},{"location":"examples/edcon/#2-construct-manually-without-config","title":"2. Construct manually (without config)","text":"connect_manual.py<pre><code>from applied_motion.backends.edcon_axis import EdconAxis\nfrom applied_motion.applied_motion import Gantry\n\nx = EdconAxis(name=\"X\", ip=\"192.168.0.100\")\ny = EdconAxis(name=\"Y\", ip=\"192.168.0.101\")\nz = EdconAxis(name=\"Z\", ip=\"192.168.0.102\")\n\ngantry = Gantry(axes={\"X\": x, \"Y\": y, \"Z\": z})\n</code></pre>"},{"location":"examples/edcon/#3-home-then-move-sequentially","title":"3. Home then move sequentially","text":"sequential_move.py<pre><code>from collections import deque\nfrom pathlib import Path\n\nfrom applied_motion import Gantry\n\ngantry = Gantry.from_config(Path(\"modbus-gantry-config.json\"))\ngantry.home()\n\nmovements = deque([\n    {\"X\": {\"position\": 120.0, \"velocity\": 60.0}},\n    {\"Y\": {\"position\":  80.0, \"velocity\": 50.0}},\n    {\"Z\": {\"position\":  15.0, \"velocity\": 25.0}},\n])\n\ngantry.move_to(movements)\n</code></pre>"},{"location":"examples/edcon/#4-relative-move","title":"4. Relative move","text":"relative_move.py<pre><code>from collections import deque\nfrom pathlib import Path\n\nfrom applied_motion import Gantry\n\ngantry = Gantry.from_config(Path(\"modbus-gantry-config.json\"))\ngantry.home()\n\ngantry.move_to(deque([\n    {\"X\": {\"position\": 10.0, \"velocity\": 30.0, \"position_type\": \"relative\"}},\n]))\n</code></pre>"},{"location":"examples/edcon/#5-run-one-full-batch-concurrently","title":"5. Run one full batch concurrently","text":"concurrent_batch.py<pre><code>from collections import deque\nfrom pathlib import Path\n\nfrom applied_motion import Gantry\n\ngantry = Gantry.from_config(Path(\"modbus-gantry-config.json\"))\ngantry.home()\n\nmovements = deque([\n    {\"X\": {\"position\": 180.0, \"velocity\": 80.0}},\n    {\"Y\": {\"position\":  40.0, \"velocity\": 80.0}},\n])\n\ngantry.move_to(movements, concurrent=True)\n</code></pre>"},{"location":"examples/edcon/#6-read-status-and-position","title":"6. Read status and position","text":"status_and_location.py<pre><code>from pathlib import Path\nfrom pprint import pprint\n\nfrom applied_motion import Gantry\n\ngantry = Gantry.from_config(Path(\"modbus-gantry-config.json\"))\n\nprint(gantry.get_location())\npprint(gantry.get_status())\n</code></pre>"},{"location":"examples/edcon/#7-error-handling","title":"7. Error handling","text":"error_handling.py<pre><code>from collections import deque\nfrom pathlib import Path\n\nfrom applied_motion import Gantry\nfrom applied_motion.applied_motion import MovementError\n\ngantry = Gantry.from_config(Path(\"modbus-gantry-config.json\"))\n\ntry:\n    gantry.move_to(deque([\n        {\"X\": {\"position\": 999999.0, \"velocity\": 5.0}},\n    ]))\nexcept MovementError as exc:\n    print(f\"Movement failed: {exc}\")\n</code></pre>"},{"location":"examples/fposbapi/","title":"FPosBAPI Examples","text":"<p>Copy-paste ready examples for every common workflow with the FPosBAPI backend. All examples assume a CECC-X PLC is reachable at <code>192.168.10.25:1234</code>.</p>"},{"location":"examples/fposbapi/#1-connect-via-configuration-file","title":"1. Connect via Configuration File","text":"<p>Config-driven approach \u2014 pass JSON config file to <code>Gantry.from_config</code>:</p> connect_from_config.py<pre><code>from pathlib import Path\nfrom collections import deque\nfrom applied_motion import Gantry\n\n# Point at your JSON config file\nCONFIG_PATH = Path(\"gantry-config.json\")\n\ngantry = Gantry.from_config(CONFIG_PATH)\n\nprint(repr(gantry))\n# Gantry(['X', 'Y', 'Z'])\n</code></pre> <p>The config file (<code>gantry-config.json</code>):</p> gantry-config.json<pre><code>{\n    \"spec_version\": \"3.0\",\n    \"system_config\": {\"metadata\": {}},\n    \"component_config\": {\n        \"metadata\": {},\n        \"components\": {\n            \"gantry_1\": {\n                \"backend\": \"fposbapi\",\n                \"interface\": {\n                    \"type\": \"tcp/ip\",\n                    \"ip\": \"192.168.10.25\",\n                    \"port\": 1234\n                },\n                \"axes\": {\n                    \"X\": {\"name\": \"X\", \"index\": 1},\n                    \"Y\": {\"name\": \"Y\", \"index\": 2},\n                    \"Z\": {\"name\": \"Z\", \"index\": 3}\n                },\n                \"axis_order\": [\"X\", \"Y\", \"Z\"],\n                \"concurrent_axes\": null\n            }\n        }\n    }\n}\n</code></pre> <p>If you have multiple gantries in one config file, pass the component name as the second argument:</p> <pre><code>gantry = Gantry.from_config(Path(\"gantry-config.json\"), name=\"gantry_2\")\n</code></pre>"},{"location":"examples/fposbapi/#2-connect-manually-without-a-config-file","title":"2. Connect Manually (without a config file)","text":"<p>For quick scripts or interactive sessions you can build the stack by hand:</p> connect_manual.py<pre><code>from applied_motion.backends.fposbapi_client import FPosBAPIClient\nfrom applied_motion.backends.fposbapi_axis import FPosBAxis\nfrom applied_motion.backends.gantry_backend import FPosBAPIGantryBackend\nfrom applied_motion.applied_motion import Gantry\n\n# One shared TCP connection for the whole gantry\nclient = FPosBAPIClient(ip=\"192.168.10.25\", port=1234, timeout=10.0)\n\n# Axis index matches CoDeSys program: 1=X, 2=Y, 3=Z\nx = FPosBAxis(name=\"X\", index=1, client=client)\ny = FPosBAxis(name=\"Y\", index=2, client=client)\nz = FPosBAxis(name=\"Z\", index=3, client=client)\n\ngantry = Gantry(\n    axes={\"X\": x, \"Y\": y, \"Z\": z},\n    concurrent_axes=None,\n    _backend=FPosBAPIGantryBackend(client),\n)\n</code></pre>"},{"location":"examples/fposbapi/#3-homing","title":"3. Homing","text":"<p>Always home the gantry before commanding absolute moves. <code>Gantry.home()</code> sends a single <code>HOME</code> command to the PLC, which references all axes simultaneously:</p> homing.py<pre><code>from pathlib import Path\nfrom applied_motion import Gantry\n\ngantry = Gantry.from_config(Path(\"gantry-config.json\"))\n\nprint(\"Homing all axes \u2026\")\ngantry.home()\nprint(\"Homing complete.\")\n</code></pre> <p>You can also check whether homing has already been completed via an individual axis proxy:</p> <pre><code>from applied_motion.backends.fposbapi_axis import FPosBAxis\n\nalready_homed = x.is_homed()\nif not already_homed:\n    gantry.home()\n</code></pre>"},{"location":"examples/fposbapi/#4-moving-to-an-absolute-position","title":"4. Moving to an Absolute Position","text":"<p>Pass a <code>deque</code> of movement dicts to <code>Gantry.move_to</code>. Each dict maps one axis name to its target <code>position</code> (mm) and <code>velocity</code> (mm/s):</p> absolute_move.py<pre><code>from pathlib import Path\nfrom collections import deque\nfrom applied_motion import Gantry\n\ngantry = Gantry.from_config(Path(\"gantry-config.json\"))\ngantry.home()\n\n# Move X \u2192 150 mm, then Y \u2192 75 mm, then Z \u2192 20 mm (sequentially)\nmovements = deque([\n    {\"X\": {\"position\": 150.0, \"velocity\": 80.0}},\n    {\"Y\": {\"position\":  75.0, \"velocity\": 60.0}},\n    {\"Z\": {\"position\":  20.0, \"velocity\": 40.0}},\n])\ngantry.move_to(movements)\n</code></pre>"},{"location":"examples/fposbapi/#5-relative-move","title":"5. Relative Move","text":"<p>Pass <code>position_type=\"relative\"</code> to displace an axis from its current position:</p> relative_move.py<pre><code>from pathlib import Path\nfrom collections import deque\nfrom applied_motion import Gantry\n\ngantry = Gantry.from_config(Path(\"gantry-config.json\"))\ngantry.home()\n\n# Advance X by 10 mm from wherever it currently is\ngantry.move_to(deque([\n    {\"X\": {\"position\": 10.0, \"velocity\": 30.0, \"position_type\": \"relative\"}},\n]))\n</code></pre>"},{"location":"examples/fposbapi/#6-concurrent-axis-moves","title":"6. Concurrent Axis Moves","text":"<p>Set <code>concurrent=True</code> on <code>move_to</code> to dispatch all movements in the queue simultaneously in parallel threads:</p> concurrent_move.py<pre><code>from pathlib import Path\nfrom collections import deque\nfrom applied_motion import Gantry\n\ngantry = Gantry.from_config(Path(\"gantry-config.json\"))\ngantry.home()\n\n# X and Y move at the same time\nmovements = deque([\n    {\"X\": {\"position\": 200.0, \"velocity\": 100.0}},\n    {\"Y\": {\"position\":  50.0, \"velocity\":  80.0}},\n])\ngantry.move_to(movements, concurrent=True)\n</code></pre> <p>Velocity note: <code>SET_PAR 103</code> (global speed) is written immediately before each <code>MOVE_AXIS</code> command.  With <code>concurrent=True</code> these two commands are not atomic \u2014 interleaved <code>SET_PAR</code> writes may affect the speed of the other axis.  Use concurrent moves only when per-move velocity precision is not required.</p> <p>If your config defines <code>concurrent_axes</code>, then with <code>concurrent=False</code> (default) the gantry still batches those configured axes together.</p>"},{"location":"examples/fposbapi/#7-reading-current-positions","title":"7. Reading Current Positions","text":"<p><code>Gantry.get_location()</code> queries every axis and returns a dict of positions in mm:</p> read_positions.py<pre><code>from pathlib import Path\nfrom applied_motion import Gantry\n\ngantry = Gantry.from_config(Path(\"gantry-config.json\"))\n\nlocation = gantry.get_location()\nprint(location)\n# {'X': 150.0, 'Y': 75.0, 'Z': 20.0}\n\n# Or query a single axis directly\nx_pos = gantry.axes[\"X\"].get_current_axis_position()\nprint(f\"X axis: {x_pos} mm\")\n</code></pre>"},{"location":"examples/fposbapi/#8-sending-a-raw-fposbapi-command","title":"8. Sending a Raw FPosBAPI Command","text":"<p>For commands not yet wrapped by a higher-level method, call <code>FPosBAPIClient.send_command</code> directly:</p> raw_command.py<pre><code>from applied_motion.backends.fposbapi_client import FPosBAPIClient\n\nclient = FPosBAPIClient(ip=\"192.168.10.25\", port=1234)\n\n# Enable the motion controller\nclient.send_command(\"ENABLE\")\n\n# Read a PLC parameter (parameter 103 = global speed)\nlines = client.send_command(\"GET_PAR\", 103)\nprint(lines)\n# ['1, GET_PAR, 103, 80.0, 0, NULL, SUCCESS']\n\n# Set a PLC parameter\nclient.send_command(\"SET_PAR\", 103, 120.0)\n</code></pre>"},{"location":"examples/fposbapi/#9-error-handling","title":"9. Error Handling","text":""},{"location":"examples/fposbapi/#fposbapi-protocol-errors","title":"FPosBAPI protocol errors","text":"<p><code>FPosBAPIClientError</code> is raised when the PLC returns a non-<code>SUCCESS</code> status or the connection is lost:</p> error_handling.py<pre><code>from pathlib import Path\nfrom collections import deque\nfrom applied_motion import Gantry\nfrom applied_motion.backends.fposbapi_client import FPosBAPIClientError\nfrom applied_motion.applied_motion import MovementError\n\ngantry = Gantry.from_config(Path(\"gantry-config.json\"))\ngantry.home()\n\ntry:\n    gantry.move_to(deque([\n        {\"X\": {\"position\": 9999.0, \"velocity\": 80.0}},  # out of range\n    ]))\nexcept FPosBAPIClientError as exc:\n    print(f\"PLC rejected the command: {exc}\")\nexcept MovementError as exc:\n    print(f\"Gantry-level move failure: {exc}\")\n</code></pre>"},{"location":"examples/fposbapi/#connection-failure-at-startup","title":"Connection failure at startup","text":"<p>If the TCP connection cannot be established, <code>FPosBAPIClient.__init__</code> raises <code>OSError</code>. <code>Gantry.from_config</code> propagates this after closing the socket cleanly:</p> <pre><code>from pathlib import Path\nfrom applied_motion import Gantry\n\ntry:\n    gantry = Gantry.from_config(Path(\"gantry-config.json\"))\nexcept OSError as exc:\n    print(f\"Cannot connect to CECC-X: {exc}\")\n</code></pre>"},{"location":"examples/fposbapi/#10-enabling-logging","title":"10. Enabling Logging","text":"<p><code>festo-dev-applied-motion</code> uses Python's standard <code>logging</code> module throughout. Enable <code>DEBUG</code> output to trace every frame exchanged with the PLC:</p> logging_setup.py<pre><code>import logging\nfrom pathlib import Path\nfrom applied_motion import Gantry\n\nlogging.basicConfig(\n    level=logging.DEBUG,\n    format=\"%(asctime)s [%(levelname)s] %(name)s: %(message)s\",\n)\n\ngantry = Gantry.from_config(Path(\"gantry-config.json\"))\ngantry.home()\n</code></pre> <p>Sample output:</p> <pre><code>2026-05-11 10:00:01,234 [INFO] applied_motion.backends.fposbapi_client: FPosBAPIClient connected to 192.168.10.25:1234\n2026-05-11 10:00:01,235 [DEBUG] applied_motion.backends.fposbapi_client: FPosBAPIClient &lt;- 1, ENABLE\n2026-05-11 10:00:01,312 [INFO] applied_motion.gantry: Gantry.home: sending HOME via FPosBAPI client\n2026-05-11 10:00:01,313 [DEBUG] applied_motion.backends.fposbapi_client: FPosBAPIClient &lt;- 2, HOME\n</code></pre> <p>Use <code>logging.INFO</code> in production to keep only state-change events.</p>"},{"location":"examples/fposbapi/#11-read-a-full-status-snapshot","title":"11. Read a Full Status Snapshot","text":"<p>Use <code>get_status()</code> when you want both axis and controller health in one call:</p> status_snapshot.py<pre><code>from pathlib import Path\nfrom pprint import pprint\n\nfrom applied_motion import Gantry\n\ngantry = Gantry.from_config(Path(\"gantry-config.json\"))\npprint(gantry.get_status())\n</code></pre>"},{"location":"examples/fposbapi/#12-teach-plc-positions-and-trays","title":"12. Teach PLC Positions and Trays","text":"<p>When running against an FPosBAPI backend, you can call gantry-level teach helpers:</p> teach_examples.py<pre><code>from pathlib import Path\n\nfrom applied_motion import Gantry\n\ngantry = Gantry.from_config(Path(\"gantry-config.json\"))\ngantry.home()\n\nif gantry.supports_teach():\n    gantry.teach_pos(1)\n    gantry.teach_tray(tray_id=1, tray_pos=1)\n</code></pre>"},{"location":"examples/fposbapi/#13-discover-supported-controller-commands","title":"13. Discover Supported Controller Commands","text":"<p>Different PLC builds may expose different command sets:</p> list_commands.py<pre><code>from pathlib import Path\n\nfrom applied_motion import Gantry\n\ngantry = Gantry.from_config(Path(\"gantry-config.json\"))\nprint(gantry.list_commands())\n</code></pre>"},{"location":"getting-started/","title":"Getting Started","text":"<p><code>festo-dev-applied-motion</code> gives you one Python API (<code>Gantry</code>) across both supported backend types:</p> Backend Hardware Transport Modbus / festo-edcon (<code>EdconAxis</code>) CMMT / CMMT-ST drives Modbus TCP per axis FPosBAPI (<code>FPosBAxis</code>) CECC-X PLC with CoDeSys FPosBAPI server Shared TCP ASCII protocol <p>Use this page for the quickest path to a running configuration, then jump into the backend-specific guide.</p>"},{"location":"getting-started/#hardware-requirements-fposbapi","title":"Hardware requirements (FPosBAPI)","text":"<ul> <li>CECC-X PLC with the FPosBAPI CoDeSys application deployed and running.</li> <li>The PLC listens on TCP port 1234 by default.</li> <li>The controlling PC must be on the same Ethernet subnet as the PLC.</li> </ul>"},{"location":"getting-started/#installation","title":"Installation","text":""},{"location":"getting-started/#from-a-package-registry","title":"From a package registry","text":"<pre><code>uv add festo-dev-applied-motion\n</code></pre>"},{"location":"getting-started/#from-the-git-repository","title":"From the Git repository","text":"<pre><code>uv pip install git+https://github.com/Festo-se/festo-dev-applied-motion.git\n</code></pre>"},{"location":"getting-started/#editable-install-from-source","title":"Editable install from source","text":"<pre><code>git clone https://github.com/Festo-se/festo-dev-applied-motion.git\ncd festo-dev-applied-motion\nuv pip install -e .\n</code></pre>"},{"location":"getting-started/#quick-start","title":"Quick start","text":"<p>One direct path to a working gantry session is <code>Gantry.from_config(...)</code>. Pass it a JSON configuration dict (or path to a JSON file) describing your hardware:</p> <pre><code>from pathlib import Path\nfrom collections import deque\nfrom applied_motion import Gantry\n\n# Load from a JSON file on disk\ngantry = Gantry.from_config(Path(\"my-gantry-config.json\"))\n\n# Home all axes before commanding motion\ngantry.home()\n\n# Move the X axis to 150 mm at 80 mm/s\ngantry.move_to(deque([{\"X\": {\"position\": 150.0, \"velocity\": 80.0}}]))\n\n# Read back all axis positions\nprint(gantry.get_location())\n# \u2192 {'X': 150.0, 'Y': 0.0, 'Z': 0.0}\n</code></pre> <p>See FPosBAPI Examples for complete examples.</p>"},{"location":"getting-started/#quick-start-modbus-festo-edcon","title":"Quick start (Modbus / festo-edcon)","text":"<p>For direct-drive setups, configure each axis with <code>name</code> and <code>ip</code>, and set backend to <code>\"modbus\"</code>.</p> <p>Critical prerequisite (Edcon / Modbus): Each physical axis must be commissioned in Festo Automation Suite first (network settings, drive readiness, and motion commissioning). If axes are not commissioned in Festo Automation Suite, Python commands can connect but motion calls are likely to fail or behave unpredictably.</p> my-modbus-gantry-config.json<pre><code>{\n    \"spec_version\": \"3.0\",\n    \"component_config\": {\n        \"metadata\": {},\n        \"components\": {\n            \"gantry_1\": {\n                \"backend\": \"modbus\",\n                \"axes\": {\n                    \"X\": {\"name\": \"X\", \"ip\": \"192.168.0.100\"},\n                    \"Y\": {\"name\": \"Y\", \"ip\": \"192.168.0.101\"},\n                    \"Z\": {\"name\": \"Z\", \"ip\": \"192.168.0.102\"}\n                },\n                \"axis_order\": [\"X\", \"Y\", \"Z\"],\n                \"concurrent_axes\": [\"X\", \"Y\"]\n            }\n        }\n    }\n}\n</code></pre> <pre><code>from collections import deque\nfrom pathlib import Path\n\nfrom applied_motion import Gantry\n\ngantry = Gantry.from_config(Path(\"my-modbus-gantry-config.json\"))\ngantry.home()\ngantry.move_to(deque([{\"X\": {\"position\": 120.0, \"velocity\": 60.0}}]))\nprint(gantry.get_location())\n</code></pre> <p>See festo-edcon Examples for more patterns.</p>"},{"location":"getting-started/#configuration-file-format","title":"Configuration file format","text":"<p>A minimal three-axis FPosBAPI config looks like this:</p> my-gantry-config.json<pre><code>{\n    \"spec_version\": \"3.0\",\n    \"component_config\": {\n        \"metadata\": {},\n        \"components\": {\n            \"gantry_1\": {\n                \"backend\": \"fposbapi\",\n                \"interface\": {\n                    \"type\": \"tcp/ip\",\n                    \"ip\": \"192.168.10.25\",\n                    \"port\": 1234\n                },\n                \"axes\": {\n                    \"X\": {\"name\": \"X\", \"index\": 1},\n                    \"Y\": {\"name\": \"Y\", \"index\": 2},\n                    \"Z\": {\"name\": \"Z\", \"index\": 3}\n                },\n                \"axis_order\": [\"X\", \"Y\", \"Z\"],\n                \"concurrent_axes\": null\n            }\n        }\n    }\n}\n</code></pre> <p>Replace <code>192.168.10.25</code> with the actual IP address of your CECC-X PLC.  The <code>index</code> values must match the axis numbering configured in the CoDeSys program (1 = X, 2 = Y, 3 = Z by default).</p>"},{"location":"getting-started/#component_config-fields-that-matter-most","title":"<code>component_config</code> fields that matter most","text":"<p>For production-style config files, <code>Gantry.from_config(...)</code> reads under:</p> <ul> <li><code>component_config.components.&lt;gantry_name&gt;</code></li> </ul> <p>Most startup issues come from a small set of fields. Validate these first.</p>"},{"location":"getting-started/#1-gantry-identity-and-backend-selection","title":"1) Gantry identity and backend selection","text":"<p>Inside <code>component_config.components</code>, choose one gantry object (usually <code>gantry_1</code>) with:</p> <ul> <li><code>backend</code>: must be <code>\"modbus\"</code> or <code>\"fposbapi\"</code></li> <li><code>axes</code>: per-axis map (shape depends on backend)</li> <li>Optional orchestration fields: <code>axis_order</code>, <code>concurrent_axes</code></li> </ul> <p>Example:</p> <pre><code>\"component_config\": {\n    \"components\": {\n        \"gantry_1\": {\n            \"backend\": \"fposbapi\",\n            \"axes\": {\n                \"X\": {\"name\": \"X\", \"index\": 1},\n                \"Y\": {\"name\": \"Y\", \"index\": 2},\n                \"Z\": {\"name\": \"Z\", \"index\": 3}\n            },\n            \"axis_order\": [\"X\", \"Y\", \"Z\"],\n            \"concurrent_axes\": null\n        }\n    }\n}\n</code></pre>"},{"location":"getting-started/#2-interface-fposbapi-only","title":"2) <code>interface</code> (FPosBAPI only)","text":"<p>For <code>backend: \"fposbapi\"</code>, <code>interface</code> identifies CECC-X endpoint:</p> <ul> <li><code>interface.type</code>: use <code>\"tcp/ip\"</code></li> <li><code>interface.ip</code>: CECC-X PLC IPv4 address</li> <li><code>interface.port</code>: FPosBAPI server port (typically <code>1234</code>)</li> </ul> <p>How to identify values on your system:</p> <ul> <li><code>ip</code>: read PLC Ethernet IP from your PLC engineering/deployment setup.</li> <li><code>port</code>: use configured FPosBAPI server port in CoDeSys project/runtime (default <code>1234</code> if unchanged).</li> </ul>"},{"location":"getting-started/#3-axes-mapping","title":"3) <code>axes</code> mapping","text":""},{"location":"getting-started/#for-fposbapi","title":"For FPosBAPI","text":"<p>Each axis entry requires:</p> <ul> <li><code>name</code>: logical axis label used in Python movement dicts (for example <code>\"X\"</code>)</li> <li><code>index</code>: 1-based PLC axis index used by FPosBAPI program</li> </ul> <p>Example:</p> <pre><code>\"axes\": {\n    \"X\": {\"name\": \"X\", \"index\": 1},\n    \"Y\": {\"name\": \"Y\", \"index\": 2},\n    \"Z\": {\"name\": \"Z\", \"index\": 3}\n}\n</code></pre> <p>How to identify <code>index</code> values:</p> <ul> <li>Open CoDeSys project for CECC-X.</li> <li>Check axis numbering used by FPosBAPI server logic.</li> <li>Copy that exact numbering into config. Do not assume physical wiring order equals PLC index order.</li> </ul>"},{"location":"getting-started/#for-modbus-edcon","title":"For Modbus / Edcon","text":"<p>Each axis entry requires:</p> <ul> <li><code>name</code>: logical axis label used in Python movement dicts</li> <li><code>ip</code>: drive IP for that axis</li> </ul> <p>Example:</p> <pre><code>\"axes\": {\n    \"X\": {\"name\": \"X\", \"ip\": \"192.168.0.100\"},\n    \"Y\": {\"name\": \"Y\", \"ip\": \"192.168.0.101\"},\n    \"Z\": {\"name\": \"Z\", \"ip\": \"192.168.0.102\"}\n}\n</code></pre> <p>How to identify <code>ip</code> values:</p> <ul> <li>Read each drive IP from Festo Automation Suite after commissioning.</li> <li>Confirm host machine can reach each drive IP on same subnet.</li> </ul> <p>Commissioning reminder (must-do): Edcon axis communication assumes drives were fully commissioned in Festo Automation Suite. If not commissioned, expect at worst damage components and at best failed homing/moves even if network ping succeeds.</p>"},{"location":"getting-started/#4-axis_order-and-concurrent_axes","title":"4) <code>axis_order</code> and <code>concurrent_axes</code>","text":"<ul> <li><code>axis_order</code>: creation/execution order for axes.<ul> <li>Keep list aligned with keys in <code>axes</code>.</li> </ul> </li> <li><code>concurrent_axes</code>: optional list of axis names allowed to run in same movement batch when <code>concurrent=False</code>.<ul> <li>Use <code>null</code> to disable backend-driven grouping.</li> </ul> </li> </ul> <p>Example:</p> <pre><code>\"axis_order\": [\"X\", \"Y\", \"Z\"],\n\"concurrent_axes\": [\"X\", \"Y\"]\n</code></pre>"},{"location":"getting-started/#5-build-your-own-config-from-plant-data","title":"5) Build your own config from plant data","text":"<p>Use this process to fill values safely:</p> <ol> <li>Pick backend (<code>\"modbus\"</code> direct-drive or <code>\"fposbapi\"</code> CECC-X PLC).</li> <li>Create axis inventory table from real hardware:<ul> <li>logical name (<code>X</code>, <code>Y</code>, <code>Z</code>...)</li> <li>device IP (Modbus) or PLC axis index (FPosBAPI)</li> </ul> </li> <li>Enter <code>axes</code> from that table.</li> <li>Set <code>axis_order</code> to your intended deterministic move ordering.</li> <li>Add <code>concurrent_axes</code> only for axis pairs validated as safe to run together.</li> </ol> <p>Practical inventory example:</p> <ul> <li>Physical drives in cabinet:<ul> <li>CMMT-X at <code>192.168.0.100</code></li> <li>CMMT-Y at <code>192.168.0.101</code></li> <li>CMMT-Z at <code>192.168.0.102</code></li> </ul> </li> <li>Resulting Modbus <code>axes</code> map:<ul> <li><code>X</code> uses <code>192.168.0.100</code></li> <li><code>Y</code> uses <code>192.168.0.101</code></li> <li><code>Z</code> uses <code>192.168.0.102</code></li> </ul> </li> </ul> <p>For FPosBAPI, equivalent mapping comes from CoDeSys axis index assignment rather than per-axis drive IPs.</p>"},{"location":"getting-started/#next-steps","title":"Next steps","text":"<ul> <li>FPosBAPI User Guide \u2014 protocol and PLC-backed architecture details.</li> <li>festo-edcon User Guide \u2014 direct-drive architecture and motion behavior.</li> <li>FPosBAPI Examples \u2014 copy-paste oriented FPosBAPI workflows.</li> <li>festo-edcon Examples \u2014 copy-paste oriented Modbus/festo-edcon workflows.</li> <li>API Reference \u2014 auto-generated class/method reference (shown in docs navigation after build).</li> </ul>"},{"location":"user-guide/cli-composition-spec/","title":"CLI Composition Specification (Hierarchical + Dependency Injection)","text":""},{"location":"user-guide/cli-composition-spec/#status","title":"Status","text":"<p>Draft for implementation in <code>applied_motion</code> and adopters that mount <code>applied_motion</code> as a subsidiary CLI.  Intention is for the composable CLI with Festo theming to become its own module.</p>"},{"location":"user-guide/cli-composition-spec/#problem-statement","title":"Problem Statement","text":"<p>Current cross-package CLI inclusion pattern often hardcodes a specific import target at call time, for example:</p> <ul> <li>Parent CLI command handler imports concrete child REPL function directly.</li> <li>Parent CLI command handler constructs child session directly.</li> <li>Parent CLI command wiring is coupled to one child package path.</li> </ul> <p>This is workable but fragile for long-term composition:</p> <ol> <li>Import path change in child package breaks parent command.</li> <li>Parent cannot switch child target REPL without code edit.</li> <li>Tests require patching module internals instead of injecting doubles.</li> <li>Parent command cannot express multiple child CLIs cleanly.</li> </ol>"},{"location":"user-guide/cli-composition-spec/#goal","title":"Goal","text":"<p>Define a stable composition contract so parent CLI can include subsidiary CLI/REPL via dependency injection, not direct import coupling.</p>"},{"location":"user-guide/cli-composition-spec/#non-goals","title":"Non-Goals","text":"<ul> <li>Replacing existing <code>CommandGroup</code> tree model in <code>applied_motion.cli.compose.core</code>.</li> <li>Rewriting standalone <code>applied-motion</code> command parser behavior.</li> <li>Forcing one DI framework. Plain Python callables remain enough.</li> </ul>"},{"location":"user-guide/cli-composition-spec/#scope","title":"Scope","text":"<p>This spec covers hierarchical composition cases like:</p> <ul> <li>Parent system CLI includes motion subtree.</li> <li>Domain CLI (for example fluid control) exposes command that launches motion REPL.</li> <li>Multi-level composition where root CLI mounts several child groups and optional sub-REPL launchers.</li> </ul>"},{"location":"user-guide/cli-composition-spec/#terms","title":"Terms","text":"<ul> <li>Root CLI: top-level command process.</li> <li>Parent group: command group that mounts child groups or launch commands.</li> <li>Child group: mounted <code>CommandGroup</code> from subsidiary package.</li> <li>Sub-REPL launcher: callable that starts interactive subsidiary loop.</li> <li>Launch context: typed object carrying dependencies needed by launcher.</li> </ul>"},{"location":"user-guide/cli-composition-spec/#existing-building-blocks-in-applied_motion","title":"Existing Building Blocks in <code>applied_motion</code>","text":"<p><code>applied_motion</code> already provides composable primitives:</p> <ul> <li><code>Command</code> and <code>CommandGroup</code> in <code>applied_motion.cli.compose.core</code>.</li> <li>Generic tree REPL driver in <code>applied_motion.cli.compose.repl.run_repl</code>.</li> <li>Argument-parser composition via <code>register_motion_cli(...)</code> in <code>applied_motion.cli.cli</code>.</li> </ul> <p>This spec extends usage guidance and contracts around these primitives.</p>"},{"location":"user-guide/cli-composition-spec/#required-design-principles","title":"Required Design Principles","text":"<ol> <li> <p>No hardcoded child imports in parent command handlers    Parent receives launcher dependency from constructor/factory arguments.</p> </li> <li> <p>Typed callable contracts    Launchers expose explicit input/output signatures.</p> </li> <li> <p>Late binding at composition root    Root wiring code decides concrete launcher implementation.</p> </li> <li> <p>Safe defaults    If launcher missing, command either:</p> </li> <li>hidden from registration, or</li> <li> <p>present but returns clear <code>UsageError</code> / <code>NotImplementedError</code>.</p> </li> <li> <p>Transportable command groups    Child groups remain mountable under arbitrary namespace (<code>gantry</code>, <code>motion</code>, etc.).</p> </li> </ol>"},{"location":"user-guide/cli-composition-spec/#contract-parameterizable-sub-repl-launcher","title":"Contract: Parameterizable Sub-REPL Launcher","text":""},{"location":"user-guide/cli-composition-spec/#type-contract","title":"Type contract","text":"<p>Define a protocol (or equivalent callable alias) in composition-facing API:</p> <pre><code>from typing import Protocol\n\n\nclass SubReplLauncher(Protocol):\n    def __call__(self, launch_context: object) -&gt; int | None: ...\n</code></pre> <p>Project may specialize <code>launch_context</code> type instead of <code>object</code> where shared model exists.</p>"},{"location":"user-guide/cli-composition-spec/#parent-registration-contract","title":"Parent registration contract","text":"<p>Any parent builder that adds command like <code>applied-motion</code> must accept launcher as parameter:</p> <pre><code>def build_group(\n    session: ParentSession,\n    *,\n    applied_motion_launcher: SubReplLauncher | None = None,\n) -&gt; CommandGroup:\n    ...\n</code></pre> <p>Behavior:</p> <ul> <li>When launcher exists, command calls launcher with constructed context.</li> <li>When launcher is <code>None</code>, command omitted or explicitly unavailable.</li> </ul>"},{"location":"user-guide/cli-composition-spec/#child-package-adapter-contract","title":"Child package adapter contract","text":"<p>Concrete adapter lives near integration boundary, not inside generic command handlers. Adapter owns child imports.</p> <pre><code>def default_applied_motion_launcher(context: MotionLaunchContext) -&gt; int | None:\n    from applied_motion.cli.cli import run_repl\n    from applied_motion.cli.session import MotionSession\n\n    return run_repl(MotionSession(context.gantry), context.gantry)\n</code></pre> <p>This confines import fragility to one adapter function.</p>"},{"location":"user-guide/cli-composition-spec/#reference-composition-pattern","title":"Reference Composition Pattern","text":""},{"location":"user-guide/cli-composition-spec/#1-define-launch-context-model","title":"1) Define launch context model","text":"<p>Context includes only dependencies child launcher needs (for example <code>gantry</code>, logger, feature flags).</p>"},{"location":"user-guide/cli-composition-spec/#2-inject-launcher-into-parent-group-builder","title":"2) Inject launcher into parent group builder","text":"<p>Wiring happens once at top-level CLI assembly.</p>"},{"location":"user-guide/cli-composition-spec/#3-add-leaf-command-that-delegates-to-launcher","title":"3) Add leaf command that delegates to launcher","text":"<p>Command handler validates prerequisites, builds context, calls injected launcher.</p>"},{"location":"user-guide/cli-composition-spec/#4-mount-parent-group-under-root-as-usual","title":"4) Mount parent group under root as usual","text":"<p>Uses existing <code>CommandGroup.add_child(...)</code> semantics.</p>"},{"location":"user-guide/cli-composition-spec/#error-and-exit-code-semantics","title":"Error and Exit-Code Semantics","text":"<ul> <li>Launcher should return <code>int | None</code>; <code>None</code> treated as success (<code>0</code>) by parent if parent tracks exit status.</li> <li>Parent command must convert missing prerequisites into <code>UsageError</code> with actionable message.</li> <li>Unexpected launcher exceptions bubble to existing root error boundary and logging policy.</li> </ul>"},{"location":"user-guide/cli-composition-spec/#test-requirements","title":"Test Requirements","text":"<p>For each parent command exposing injected sub-REPL:</p> <ol> <li>Delegation test: injected launcher called once with expected context.</li> <li>Unavailable test: when launcher <code>None</code>, command absent or deterministic error.</li> <li>Prerequisite test: missing dependency (for example gantry) raises <code>UsageError</code>.</li> <li>Import-isolation test: command module import does not require optional child CLI extras.</li> </ol> <p>For adapter function:</p> <ol> <li>Adapter smoke test: imports child module and calls child REPL function with expected objects.</li> </ol>"},{"location":"user-guide/cli-composition-spec/#migration-plan","title":"Migration Plan","text":""},{"location":"user-guide/cli-composition-spec/#phase-1-non-breaking","title":"Phase 1 (non-breaking)","text":"<ul> <li>Add optional launcher parameter to parent builders.</li> <li>Keep current direct launch behavior as default adapter wired at composition root.</li> </ul>"},{"location":"user-guide/cli-composition-spec/#phase-2","title":"Phase 2","text":"<ul> <li>Move direct import logic out of command handler into adapter function.</li> <li>Update tests to inject fake launcher.</li> </ul>"},{"location":"user-guide/cli-composition-spec/#phase-3","title":"Phase 3","text":"<ul> <li>Expose reusable helper utilities for multi-child composition (optional).</li> </ul>"},{"location":"user-guide/cli-composition-spec/#acceptance-criteria","title":"Acceptance Criteria","text":"<p>Implementation is compliant when all are true:</p> <ol> <li>Parent command handler has no direct import of child REPL module.</li> <li>Child REPL target can be swapped in tests and production via injected callable.</li> <li>Parent still composes with existing <code>CommandGroup</code> and <code>run_repl</code> flow.</li> <li>Existing user-facing command names/usage unchanged unless explicitly versioned.</li> <li>Optional child dependency missing does not break parent module import.</li> </ol>"},{"location":"user-guide/cli-composition-spec/#example-hierarchical-topology","title":"Example Hierarchical Topology","text":"<pre><code>root\n\u251c\u2500\u2500 fluid\n\u2502   \u251c\u2500\u2500 status\n\u2502   \u251c\u2500\u2500 dispense\n\u2502   \u2514\u2500\u2500 applied-motion   # delegates via injected launcher\n\u2514\u2500\u2500 motion               # optionally mounted full motion subtree\n</code></pre> <p>Both paths can coexist:</p> <ul> <li>Full <code>motion</code> subtree for direct command execution.</li> <li>Shortcut launch command under another domain group.</li> </ul> <p>Each path still resolves concrete behavior through injected adapter, not hardcoded import in leaf handler.</p>"},{"location":"user-guide/cli-composition-spec/#security-and-operational-notes","title":"Security and Operational Notes","text":"<ul> <li>Launch context should contain only required runtime objects.</li> <li>Do not pass raw credentials in context unless child launcher truly requires them.</li> <li>Keep adapter imports local to adapter call if optional dependencies are heavyweight or extra-gated.</li> </ul>"},{"location":"user-guide/cli-composition-spec/#future-extensions","title":"Future Extensions","text":"<ul> <li>Named launcher registry (<code>dict[str, SubReplLauncher]</code>) for multiple subsidiary REPL targets.</li> <li>Feature-flagged inclusion/exclusion of child launch commands.</li> <li>Common typed context package for cross-repo CLI composition consistency.</li> </ul>"},{"location":"user-guide/cli/","title":"CLI Reference","text":"<p><code>festo-dev-applied-motion</code> ships an interactive commissioning CLI for jogging axes and recording positions.</p>"},{"location":"user-guide/cli/#installation","title":"Installation","text":"<p>Requires the <code>cli</code> extra:</p> <pre><code>uv add festo-dev-applied-motion[cli]\n</code></pre>"},{"location":"user-guide/cli/#entry-point","title":"Entry point","text":"<pre><code>applied-motion --config gantry.json [subcommand]\n</code></pre> <p>If no subcommand is given, the interactive motion shell starts automatically.</p>"},{"location":"user-guide/cli/#global-flags","title":"Global flags","text":"Flag Default Description <code>--config PATH</code> (required) Path to gantry JSON config <code>--gantry-name NAME</code> <code>gantry_1</code> Gantry component key in config <code>--log-level LEVEL</code> <code>WARNING</code> Python logging threshold"},{"location":"user-guide/cli/#subcommands","title":"Subcommands","text":"Subcommand Description <code>shell</code> Interactive motion REPL (default) <code>where</code> Print current axis positions once <code>home</code> Home all axes once <code>status</code> Print gantry status snapshot <code>jog</code> Single non-interactive jog step <code>jog-tui</code> Arrow-key interactive jog TUI (no REPL) <code>teach-pos</code> Send <code>TEACH_POS</code> to PLC (FPosBAPI only) <code>teach-tray</code> Send <code>TEACH_TRAY</code> to PLC (FPosBAPI only)"},{"location":"user-guide/cli/#jog","title":"Jog","text":"<p>Jogging moves one axis by a fixed step distance. Two modes are available.</p>"},{"location":"user-guide/cli/#non-interactive-jog-step","title":"Non-interactive jog step","text":"<pre><code>applied-motion --config gantry.json jog &lt;axis&gt; &lt;direction&gt; &lt;step&gt; [--velocity V] [--timeout T]\n</code></pre> Argument Description <code>axis</code> Axis name (e.g. <code>X</code>, <code>Y</code>, <code>Z</code>) <code>direction</code> <code>+</code> (positive) or <code>-</code> (negative) <code>step</code> Distance in mm (must be positive) <code>--velocity</code> Speed in mm/s (default: <code>10.0</code>) <code>--timeout</code> Move timeout in seconds (default: <code>30</code>) <p>Prints the full gantry position table after the move completes, then exits.</p> <p>Examples:</p> <pre><code># Move X axis +5 mm at default velocity\napplied-motion --config gantry.json jog X + 5\n\n# Move Z axis -10 mm at 25 mm/s with 60 s timeout\napplied-motion --config gantry.json jog Z - 10 --velocity 25 --timeout 60\n</code></pre>"},{"location":"user-guide/cli/#arrow-key-jog-tui","title":"Arrow-key jog TUI","text":"<pre><code>applied-motion --config gantry.json jog-tui\n</code></pre> <p>Connects to the gantry and opens a full-screen TUI. No motion REPL involved. Press <code>Esc</code> or <code>q</code> to exit.</p>"},{"location":"user-guide/cli/#key-bindings","title":"Key bindings","text":"Key Action <code>\u2190</code> / <code>\u2192</code> Step axis[0] (typically X) negative / positive <code>\u2191</code> / <code>\u2193</code> Step axis[1] (typically Y) positive / negative <code>Page Up</code> / <code>Page Down</code> Step depth axis (Z or active depth axis) positive / negative <code>+</code> Increase step size (cycles 0.1 \u2192 0.5 \u2192 1 \u2192 5 \u2192 10 \u2192 25 \u2192 50 mm) <code>-</code> Decrease step size <code>Tab</code> Cycle Page Up/Page Down target to next depth axis (3+ axes) <code>Shift+Tab</code> Cycle to previous depth axis <code>Esc</code> or <code>q</code> Exit jog TUI <p>The TUI displays current position for all axes, the active step size, and the last operation status. When three or more axes are configured, <code>Tab</code> cycles which axis responds to Page Up/Page Down \u2014 the active axis is marked with <code>\u25c0</code>.</p>"},{"location":"user-guide/cli/#jog-inside-the-motion-shell","title":"Jog inside the motion shell","text":"<p>After launching <code>shell</code>, the <code>jog</code> command is also available at the REPL prompt:</p> <pre><code>motion&gt; jog                          # enter arrow-key jog mode\nmotion&gt; jog X + 5                    # single step, 5 mm at default velocity\nmotion&gt; jog X + 5 25                 # single step, 5 mm at 25 mm/s\n</code></pre>"},{"location":"user-guide/cli/#interactive-motion-shell","title":"Interactive motion shell","text":"<pre><code>applied-motion --config gantry.json shell\n</code></pre>"},{"location":"user-guide/cli/#repl-commands","title":"REPL commands","text":"Command Description <code>jog</code> Enter arrow-key jog mode <code>jog &lt;axis&gt; &lt;+/-&gt; &lt;step&gt; [vel]</code> Single step-move <code>where</code> Print current axis positions <code>home</code> Home all axes <code>capture &lt;label&gt;</code> Record current position as label <code>teach pos &lt;pos_id&gt;</code> Send <code>TEACH_POS</code> to PLC (FPosBAPI only) <code>teach tray &lt;tray_id&gt; &lt;tray_pos&gt;</code> Send <code>TEACH_TRAY</code> to PLC (FPosBAPI only) <code>list</code> List all captured positions <code>save &lt;path&gt;</code> Write positions to a JSON file <code>load &lt;path&gt;</code> Merge positions from a JSON file <code>help</code> Print command reference <code>quit</code> Exit <p>Tab-completion and command history are available at the prompt.</p>"},{"location":"user-guide/cli/#typical-commissioning-workflow","title":"Typical commissioning workflow","text":"<ol> <li> <p>Start the shell and home the gantry:    <pre><code>motion&gt; home\n</code></pre></p> </li> <li> <p>Jog to a target position using arrow-key mode or single-step commands:    <pre><code>motion&gt; jog\n# ... move with arrow keys, press Esc when done\n</code></pre></p> </li> <li> <p>Capture the position under a label:    <pre><code>motion&gt; capture deck_a1\n</code></pre></p> </li> <li> <p>Repeat for each position. Review with <code>list</code>.</p> </li> <li> <p>Save to JSON:    <pre><code>motion&gt; save deck_layout.json\n</code></pre></p> </li> </ol> <p>For FPosBAPI backends, use <code>teach pos &lt;id&gt;</code> after each capture to commit the position directly to the PLC slot before saving.</p>"},{"location":"user-guide/cli/#programmatic-use","title":"Programmatic use","text":"<p><code>MotionSession</code> is available without the <code>cli</code> extra and has no dependency on <code>prompt_toolkit</code> or <code>rich</code>:</p> <pre><code>from applied_motion import Gantry\nfrom applied_motion.cli import MotionSession\n\nwith Gantry.from_config(\"gantry.json\") as gantry:\n    gantry.home()\n    session = MotionSession(gantry)\n    session.jog(\"X\", \"+\", 5.0)           # step 5 mm\n    session.capture(\"deck_a1\")\n    session.save(\"deck_layout.json\")\n</code></pre> <p>The <code>on_capture</code> hook fires after every <code>capture</code> call \u2014 use it to commit positions to the PLC:</p> <pre><code>def plc_hook(label: str, pos: dict[str, float]) -&gt; None:\n    gantry.teach_pos(pos_id=label_to_id[label])\n\nsession = MotionSession(gantry, on_capture=plc_hook)\n</code></pre> <p><code>MotionSession.jog</code> returns the full location dict after the move:</p> <pre><code>location = session.jog(\"Z\", \"-\", 10.0, velocity=25.0, timeout=60)\nprint(location)  # {'X': 150.0, 'Y': 0.0, 'Z': -10.0}\n</code></pre>"},{"location":"user-guide/edcon/","title":"festo-edcon / Modbus Backend","text":"<p>The festo-edcon backend connects Python directly to each Festo drive using Modbus TCP. Each logical axis is represented by an <code>EdconAxis</code>, and the gantry coordinates those axis objects through <code>Gantry</code>.</p>"},{"location":"user-guide/edcon/#architecture","title":"Architecture","text":"<pre><code>\u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n\u2502                 Gantry                \u2502\n\u2502  axes: {X: EdconAxis, Y: EdconAxis\u2026}  \u2502\n\u2502  _backend: ModbusGantryBackend        \u2502\n\u2502                                       \u2502\n\u2502   \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510   \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510         \u2502\n\u2502   \u2502EdconAxis \u2502   \u2502EdconAxis \u2502  ...    \u2502\n\u2502   \u2502 name=\"X\" \u2502   \u2502 name=\"Y\" \u2502         \u2502\n\u2502   \u2502 ip=.100  \u2502   \u2502 ip=.101  \u2502         \u2502\n\u2502   \u2514\u2500\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2500\u2500\u2518   \u2514\u2500\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2500\u2500\u2518         \u2502\n\u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n         \u2502              \u2502\n     Modbus TCP     Modbus TCP\n         \u2502              \u2502\n      CMMT-X         CMMT-Y\n</code></pre> <p>Unlike FPosBAPI, there is no shared PLC socket. Each axis owns its own transport.</p>"},{"location":"user-guide/edcon/#axis-construction","title":"Axis Construction","text":"<p>You can construct axes directly:</p> <pre><code>from applied_motion.backends.edcon_axis import EdconAxis\n\nx_axis = EdconAxis(name=\"X\", ip=\"192.168.0.100\")\ny_axis = EdconAxis(name=\"Y\", ip=\"192.168.0.101\")\n</code></pre> <p>Or, create through config:</p> <pre><code>from pathlib import Path\nfrom applied_motion import Gantry\n\ngantry = Gantry.from_config(Path(\"modbus-gantry-config.json\"))\n</code></pre>"},{"location":"user-guide/edcon/#config-shape-modbus","title":"Config Shape (Modbus)","text":"<pre><code>{\n    \"backend\": \"modbus\",\n    \"axes\": {\n        \"X\": {\"name\": \"X\", \"ip\": \"192.168.0.100\"},\n        \"Y\": {\"name\": \"Y\", \"ip\": \"192.168.0.101\"},\n        \"Z\": {\"name\": \"Z\", \"ip\": \"192.168.0.102\"}\n    },\n    \"axis_order\": [\"X\", \"Y\", \"Z\"],\n    \"concurrent_axes\": [\"X\", \"Y\"]\n}\n</code></pre> <p>Supported fields:</p> Key Required Description <code>backend</code> No* Defaults to <code>\"modbus\"</code> when omitted. <code>axes</code> Yes Axis map with <code>name</code> + <code>ip</code> per axis. <code>axis_order</code> No Execution/creation order (defaults to <code>axes</code> insertion order). <code>concurrent_axes</code> No Optional subset of axis names that may be batched together. <p>*Omitting <code>backend</code> still selects Modbus.</p>"},{"location":"user-guide/edcon/#motion-behavior","title":"Motion Behavior","text":""},{"location":"user-guide/edcon/#home","title":"<code>home()</code>","text":"<p>For Modbus backend, <code>Gantry.home()</code> calls <code>home()</code> on each axis sequentially.</p>"},{"location":"user-guide/edcon/#move_tomovements-timeoutnone-concurrentfalse","title":"<code>move_to(movements, timeout=None, concurrent=False)</code>","text":"<ul> <li><code>concurrent=True</code> dispatches all valid moves in one parallel batch.</li> <li><code>concurrent=False</code> (default) still uses <code>concurrent_axes</code> grouping when configured.</li> </ul> <p>Each movement is a single-axis dict:</p> <pre><code>{\"X\": {\"position\": 120.0, \"velocity\": 60.0}}\n</code></pre> <p>All user-facing position values are in mm.</p>"},{"location":"user-guide/edcon/#status-and-diagnostics","title":"Status and Diagnostics","text":"<p>Use <code>get_status()</code> for per-axis health plus summary booleans:</p> <pre><code>status = gantry.get_status()\nprint(status[\"summary\"][\"healthy\"])\nprint(status[\"axes\"][\"X\"])\n</code></pre> <p>For Modbus backend, <code>controller</code> fields in status are <code>None</code> (no shared PLC controller API).</p>"},{"location":"user-guide/edcon/#error-handling","title":"Error Handling","text":"<p>Axis-level move failures surface as <code>MovementError</code> from <code>Gantry.move_to</code>. Malformed movement entries and unknown axis names are skipped and logged.</p> <pre><code>from collections import deque\nfrom applied_motion.applied_motion import MovementError\n\ntry:\n    gantry.move_to(deque([\n        {\"X\": {\"position\": 999999.0, \"velocity\": 50.0}},\n    ]))\nexcept MovementError as exc:\n    print(f\"Move failed: {exc}\")\n</code></pre>"},{"location":"user-guide/fposbapi/","title":"FPosBAPI Backend","text":"<p>The FPosBAPI backend connects Python directly to a CECC-X PLC running the FPosBAPI CoDeSys server application. A single persistent TCP socket carries all motion commands for every axis of the gantry, serialised by an internal threading lock so concurrent Python threads cannot interleave frames.</p>"},{"location":"user-guide/fposbapi/#architecture","title":"Architecture","text":"<pre><code>\u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n\u2502           Gantry                \u2502\n\u2502  axes: {X: FPosBAxis, \u2026}        \u2502\n\u2502  _backend: FPosBAPIGantryBackend\u2502\n\u2502                                 \u2502\n\u2502  \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510 \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n\u2502  \u2502FPosBAxis \u2502 \u2502FPosBAPIGantryBackend \u2502\n\u2502  \u2502  name=\"X\"\u2502 \u2502  ip / port   \u2502  \u2502\n\u2502  \u2502  index=1 \u2502 \u2502  \u2514\u2500client\u2500\u2500\u2500\u2500\u2510     \u2502\n\u2502  \u2502          \u2502\u2500\u2502               \u2502     \u2502\n\u2502  \u2502FPosBAxis \u2502 \u2502   FPosBAPIClient    \u2502\n\u2502  \u2502  name=\"Y\"\u2502 \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n\u2502  \u2502  index=2 \u2502         \u2502         \u2502\n\u2502  \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518         \u2502 TCP     \u2502\n\u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500 \u2518\n                         \u2502\n              CECC-X PLC (CoDeSys)\n              FPosBAPI server :1234\n</code></pre> <p>Three classes work together:</p> Class Responsibility <code>FPosBAPIClient</code> Opens and maintains the TCP socket; formats/parses ASCII frames; thread-safe via <code>threading.Lock</code>. <code>FPosBAxis</code> Represents one logical axis; translates <code>move()</code>, <code>home()</code>, and <code>get_current_axis_position()</code> into FPosBAPI commands. <code>FPosBAPIGantryBackend</code> Owns the shared <code>FPosBAPIClient</code> and backend-specific behavior (<code>home</code>, diagnostics, teach operations). <code>Gantry</code> Owns axis mapping and delegates backend behavior to <code>FPosBAPIGantryBackend</code>; dispatches movement queues."},{"location":"user-guide/fposbapi/#wire-protocol","title":"Wire Protocol","text":"<p>The server speaks a line-oriented ASCII protocol over TCP.</p> <p>Request format</p> <pre><code>MSG_ID, COMMAND[, PARAM, ...]\\r\\n\n</code></pre> <p>Response format</p> <pre><code>MSG_ID, COMMAND[, ECHO_PARAMS..., RETURN_VALS...], ERR_ID, ERR_TYPE, ERR_MSG\\r\\n\n</code></pre> <p>On success the last three comma-delimited fields are always <code>0, NULL, SUCCESS</code>. On error they carry a non-zero error id, an error type string, and a message string.</p> <p>Example exchange for a move command:</p> <pre><code>\u2192 1, SET_PAR, 103, 80.0\\r\\n\n\u2190 1, SET_PAR, 103, 80.0, 0, NULL, SUCCESS\\r\\n\n\u2192 2, MOVE_AXIS, 1, 0, 150.0\\r\\n\n\u2190 2, MOVE_AXIS, 1, 0, 150.0, 0, NULL, SUCCESS\\r\\n\n</code></pre>"},{"location":"user-guide/fposbapi/#fposbapiclient","title":"FPosBAPIClient","text":"<p><code>FPosBAPIClient</code> is instantiated once per gantry and shared across all axes. You normally let <code>Gantry.from_config</code> create it; but for special use cases you can build it directly:</p> <pre><code>from applied_motion.backends.fposbapi_client import FPosBAPIClient\n\nclient = FPosBAPIClient(ip=\"192.168.10.25\", port=1234, timeout=10.0)\n</code></pre> Parameter Type Default Description <code>ip</code> <code>str</code> \u2014 IPv4 address of the CECC-X PLC. <code>port</code> <code>int</code> <code>1234</code> TCP port the FPosBAPI server is listening on. <code>timeout</code> <code>float \\| None</code> <code>None</code> Socket timeout in seconds; <code>None</code> = blocking, no timeout."},{"location":"user-guide/fposbapi/#sending-raw-commands","title":"Sending raw commands","text":"<pre><code># Send a command with no parameters\nlines = client.send_command(\"ENABLE\")\n\n# Send a command with parameters\nlines = client.send_command(\"SET_PAR\", 103, 80.0)\n\n# Send a move command (axis_index=1, relative=0, position_mm=150.0)\nlines = client.send_command(\"MOVE_AXIS\", 1, 0, 150.0)\n</code></pre> <p><code>send_command</code> returns a list of all response lines received before the empty frame terminator. It raises <code>FPosBAPIClientError</code> if the server reports an error or the connection is lost.</p>"},{"location":"user-guide/fposbapi/#reconnection-behaviour","title":"Reconnection behaviour","text":"<p>If the TCP connection drops mid-session, <code>FPosBAPIClient</code> performs exactly one automatic reconnect attempt before raising <code>FPosBAPIClientError</code>. TCP keepalive is enabled on the socket (15 s idle time, 5 s interval) to detect silent link failures early.</p>"},{"location":"user-guide/fposbapi/#command-wrappers-available-today","title":"Command wrappers available today","text":"<p><code>FPosBAPIClient</code> provides typed wrappers around many PLC commands in addition to <code>send_command</code>, including:</p> <ul> <li>Motion: <code>enable</code>, <code>disable</code>, <code>home</code>, <code>move_pos</code>, <code>move_loc</code>, <code>halt</code>, <code>resume</code>, <code>abort</code></li> <li>Teaching: <code>teach_pos</code>, <code>teach_tray</code>, <code>read_pos</code>, <code>write_pos</code>, <code>read_tray</code>, <code>write_tray</code></li> <li>Diagnostics: <code>sys_status</code>, <code>is_error</code>, <code>fpb_error</code>, <code>read_err</code>, <code>err_log</code>, <code>com_log</code></li> <li>Parameters / I/O: <code>get_par</code>, <code>set_par</code>, <code>get_io</code>, <code>set_io</code></li> </ul>"},{"location":"user-guide/fposbapi/#fposbaxis","title":"FPosBAxis","text":"<p><code>FPosBAxis</code> represents a single logical axis of the gantry. It stores a 1-based axis index that must match the axis numbering in the CoDeSys program:</p> Index Axis <code>1</code> X <code>2</code> Y <code>3</code> Z"},{"location":"user-guide/fposbapi/#construction-manual","title":"Construction (manual)","text":"<pre><code>from applied_motion.backends.fposbapi_client import FPosBAPIClient\nfrom applied_motion.backends.fposbapi_axis import FPosBAxis\n\nclient = FPosBAPIClient(ip=\"192.168.10.25\")\nx_axis = FPosBAxis(name=\"X\", index=1, client=client)\ny_axis = FPosBAxis(name=\"Y\", index=2, client=client)\nz_axis = FPosBAxis(name=\"Z\", index=3, client=client)\n</code></pre>"},{"location":"user-guide/fposbapi/#moving-an-axis","title":"Moving an axis","text":"<pre><code># Absolute move: go to 200 mm at 100 mm/s\nx_axis.move(position=200.0, velocity=100.0)\n\n# Relative move: advance 10 mm from the current position\nx_axis.move(position=10.0, velocity=50.0, position_type=\"relative\")\n</code></pre> <p>Note on velocity: <code>move()</code> writes the velocity to PLC parameter 103 (global speed) immediately before the <code>MOVE_AXIS</code> command.  When axes move concurrently the two commands are not atomic \u2014 use sequential moves when per-move velocity accuracy matters.</p>"},{"location":"user-guide/fposbapi/#reading-the-current-position","title":"Reading the current position","text":"<pre><code>pos_mm = x_axis.get_current_axis_position()\nprint(f\"X is at {pos_mm} mm\")\n</code></pre>"},{"location":"user-guide/fposbapi/#homing","title":"Homing","text":"<p>Homing via a single axis proxy sends the <code>HOME</code> command to the PLC, which always homes all axes simultaneously.  Prefer <code>Gantry.home()</code> to avoid issuing the command more than once.</p> <pre><code>x_axis.home()  # homes X, Y, and Z together\n</code></pre>"},{"location":"user-guide/fposbapi/#drive-ready-state-and-homed-state","title":"Drive-ready state and homed state","text":"<p><code>FPosBAxis.ready_for_motion()</code> queries <code>IS_ENBL</code>, and <code>FPosBAxis.is_homed()</code> queries <code>IS_HOME</code>.</p> <pre><code>if not x_axis.ready_for_motion():\n    raise RuntimeError(\"Drives are not enabled\")\n\nif not x_axis.is_homed():\n    x_axis.home()\n</code></pre>"},{"location":"user-guide/fposbapi/#gantry-fposbapi-mode","title":"Gantry (FPosBAPI mode)","text":""},{"location":"user-guide/fposbapi/#from-a-configuration-file","title":"From a configuration file","text":"<pre><code>from pathlib import Path\nfrom applied_motion import Gantry\n\ngantry = Gantry.from_config(Path(\"my-gantry-config.json\"))\n</code></pre> <p><code>from_config</code> reads the <code>backend</code> key, creates one <code>FPosBAPIClient</code>, sends an initial <code>ENABLE</code> command to verify the connection, then wraps each axis entry in an <code>FPosBAxis</code> proxy.</p>"},{"location":"user-guide/fposbapi/#supported-config-keys","title":"Supported config keys","text":"<pre><code>{\n    \"backend\": \"fposbapi\",\n    \"interface\": {\n        \"type\": \"tcp/ip\",\n        \"ip\": \"192.168.10.25\",\n        \"port\": 1234\n    },\n    \"axes\": {\n        \"X\": {\"name\": \"X\", \"index\": 1},\n        \"Y\": {\"name\": \"Y\", \"index\": 2},\n        \"Z\": {\"name\": \"Z\", \"index\": 3}\n    },\n    \"axis_order\": [\"X\", \"Y\", \"Z\"],\n    \"concurrent_axes\": null\n}\n</code></pre> Key Required Description <code>backend</code> Yes Must be <code>\"fposbapi\"</code> to select this backend. <code>interface.ip</code> Yes IPv4 address of the CECC-X PLC. <code>interface.port</code> No TCP port (default <code>1234</code>). <code>axes</code> Yes Dict of axis name \u2192 <code>{name, index}</code>. <code>axis_order</code> No Ordered list of axis names; defaults to dict insertion order. <code>concurrent_axes</code> No List of axis names allowed to move simultaneously, or <code>null</code>."},{"location":"user-guide/fposbapi/#full-config-file-layout-spec-version-30","title":"Full config file layout (spec version 3.0)","text":"<p><code>from_config</code> also accepts the full Festo component config schema used in production:</p> gantry-config.json<pre><code>{\n    \"spec_version\": \"3.0\",\n    \"system_config\": {\"metadata\": {}},\n    \"component_config\": {\n        \"metadata\": {},\n        \"components\": {\n            \"gantry_1\": {\n                \"backend\": \"fposbapi\",\n                \"interface\": {\n                    \"type\": \"tcp/ip\",\n                    \"ip\": \"192.168.10.25\",\n                    \"port\": 1234\n                },\n                \"axes\": {\n                    \"X\": {\"name\": \"X\", \"index\": 1},\n                    \"Y\": {\"name\": \"Y\", \"index\": 2},\n                    \"Z\": {\"name\": \"Z\", \"index\": 3}\n                },\n                \"axis_order\": [\"X\", \"Y\", \"Z\"],\n                \"concurrent_axes\": null\n            }\n        }\n    }\n}\n</code></pre> <p>When the top-level <code>component_config</code> key is present, <code>from_config</code> scopes automatically to <code>component_config.components.&lt;name&gt;</code> (default <code>\"gantry_1\"</code>).</p>"},{"location":"user-guide/fposbapi/#gantry-api-reference","title":"Gantry API Reference","text":""},{"location":"user-guide/fposbapi/#gantryhome","title":"<code>Gantry.home()</code>","text":"<p>Sends a single <code>HOME</code> command via the shared <code>FPosBAPIClient</code>.  All axes home simultaneously on the PLC side.</p> <pre><code>gantry.home()\n</code></pre>"},{"location":"user-guide/fposbapi/#gantrymove_tomovements-timeoutnone-concurrentfalse","title":"<code>Gantry.move_to(movements, timeout=None, concurrent=False)</code>","text":"<p>Dispatches a queue of movement dicts to the gantry axes.</p> <pre><code>from collections import deque\n\nmovements = deque([\n    {\"X\": {\"position\": 150.0, \"velocity\": 80.0}},\n    {\"Y\": {\"position\": 75.0,  \"velocity\": 60.0}},\n    {\"Z\": {\"position\": 20.0,  \"velocity\": 40.0}},\n])\ngantry.move_to(movements)\n</code></pre> <p>Each dict maps one axis name to its kinematic parameters.</p> <code>move_to</code> parameter Description <code>movements</code> <code>deque</code> of <code>{axis_name: {position, velocity}}</code> dicts. <code>timeout</code> Per-move time limit in seconds, or <code>None</code>. <code>concurrent</code> <code>True</code> to dispatch all valid movements in one parallel batch. <p>When <code>concurrent=False</code>, the gantry still uses <code>concurrent_axes</code> grouping from config:</p> <ul> <li>axes listed in <code>concurrent_axes</code> may run in the same batch,</li> <li>axes not in that set are dispatched as individual batches.</li> </ul>"},{"location":"user-guide/fposbapi/#gantryget_location","title":"<code>Gantry.get_location()</code>","text":"<p>Returns a dict of axis name \u2192 current position (mm) by querying each axis.</p> <pre><code>location = gantry.get_location()\n# {'X': 150.0, 'Y': 75.0, 'Z': 20.0}\n</code></pre>"},{"location":"user-guide/fposbapi/#gantryget_status","title":"<code>Gantry.get_status()</code>","text":"<p>Returns backend metadata, per-axis states, and controller diagnostics.</p> <pre><code>status = gantry.get_status()\nprint(status[\"backend\"])\nprint(status[\"summary\"][\"healthy\"])\nprint(status[\"controller\"])  # sys_status/is_error/fpb_error/read_err/error\n</code></pre>"},{"location":"user-guide/fposbapi/#teaching-helpers-and-command-discovery","title":"Teaching helpers and command discovery","text":"<p>For FPosBAPI backends:</p> <pre><code>if gantry.supports_teach():\n    gantry.teach_pos(1)\n    gantry.teach_tray(tray_id=1, tray_pos=1)\n\nprint(gantry.list_commands())\n</code></pre>"},{"location":"user-guide/fposbapi/#error-handling","title":"Error Handling","text":"<p>All FPosBAPI errors surface as <code>FPosBAPIClientError</code>.</p> <pre><code>from applied_motion.backends.fposbapi_client import FPosBAPIClientError\n\ntry:\n    gantry.move_to(deque([{\"X\": {\"position\": 9999.0, \"velocity\": 80.0}}]))\nexcept FPosBAPIClientError as exc:\n    print(f\"PLC rejected the command: {exc}\")\n</code></pre> <p>Motion failures at the <code>Gantry</code> level raise <code>MovementError</code>, which wraps the underlying exception.</p> <pre><code>from applied_motion.applied_motion import MovementError\n\ntry:\n    gantry.move_to(movements)\nexcept MovementError as exc:\n    print(f\"Gantry move failed: {exc}\")\n</code></pre>"}]}